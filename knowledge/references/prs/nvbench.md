# NVBench PR Knowledge Notes

Repository: <https://github.com/NVIDIA/nvbench>

This page is the production-PR layer for kernel-knowledge. It favors merged PRs that changed kernels, dispatch, JIT/runtime integration, tuning policy, tests, benchmarks, or profiling evidence. Release, CI-only, formatting, pure version-bump, and non-target-backend PRs are filtered out.

## Repository Source Scan

## Coverage Summary

| Category | CUDA-kernel PRs |
| --- | ---: |
| Memory / Primitives | 2 |
| Compiler / Runtime | 2 |
| Benchmark / Test Evidence | 58 |

## Pull Request Case Notes

### Memory / Primitives

Use this section for: Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract.
NCU first look: DRAM throughput, L2 hit rate, memory sectors, shared-memory conflicts, and synchronization overhead.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#56](https://github.com/NVIDIA/nvbench/pull/56) Reduce the width of pow2 axes in markdown tables. | 2021-12-20 | memory_primitives, benchmark_test | Before: code block After: code block | benchmark: `nvbench/markdown_printer.cu` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |
| [#353](https://github.com/NVIDIA/nvbench/pull/353) Correct cuda cccl examples per change in api | 2026-05-06 | memory_primitives, benchmark_test | Closes 344 This PR updates examples of benchmarking of `cuda.compute` and `cuda.coop` using `cuda.bench`. | wrapper: `python/examples/cuda_compute_segmented_reduce.py`, `python/examples/cuda_coop_block_reduce.py` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |

### Compiler / Runtime

Use this section for: Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests.
NCU first look: Generated kernel shape, launch overhead, occupancy, register pressure, and compile-time-selected schedule.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#331](https://github.com/NVIDIA/nvbench/pull/331) Update python examples | 2026-04-02 | compiler_runtime, gemm_quant, memory_primitives | Update Python examples: - replace deprecated namespaces (remove `experemental`) - replace use of `cccl.parallel` and `cccl.cooperative` to `cuda.compute` and `cuda.coop` - account for changes in `cuda.compute` API - remove `auto_throughput.py` since Python API does not expose CUPTI support (311) - t... | kernel: `python/examples/cute_dsl_sgemm.py`, `python/examples/cutlass_gemm.py`<br>wrapper: `python/examples/axes.py`, `python/examples/cpu_activity.py`, `python/examples/cuda_compute_segmented_reduce.py`, `python/examples/cuda_coop_block_reduce.py`<br>other: `python/examples/requirements.txt` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#269](https://github.com/NVIDIA/nvbench/pull/269) remove pynvjitlink references in examples | 2025-09-17 | compiler_runtime, memory_primitives | Towards https://github.com/rapidsai/build-planning/issues/210 Based on the information from this comment on the pynvjitlink repo and this section of the numba-cuda docs, it looks like both the linker and LTO are already configured as desired in the newer `numba-cuda` versions. So it should be fine t... | wrapper: `python/examples/cccl_cooperative_block_reduce.py`<br>other: `python/examples/requirements.txt` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |

### Benchmark / Test Evidence

Use this section for: Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code.
NCU first look: Use the PR's benchmark/profile command as the first replay target.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#70](https://github.com/NVIDIA/nvbench/pull/70) Python / JSON updates | 2022-01-13 | benchmark_test, scheduler_autotune | This branch adds a number of new features to the JSON output and python tools. - (Breaking) Refactor `nvbench::summary` API and usage. - (Breaking) Changed layout of JSON output to be more consistent and easier to parse. - Add new `--jsonbin <file>` option prints regular JSON output along with suppl... | benchmark: `cmake/NVBenchConfigTarget.cmake`, `nvbench/config.cuh.in`, `nvbench/csv_printer.cu`, `nvbench/detail/measure_cold.cu`<br>test: `scripts/test_cmp.json`, `scripts/test_ref.json`<br>other: `requirements.txt` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#347](https://github.com/NVIDIA/nvbench/pull/347) Implement decorators to register benchmarks add axis and options | 2026-05-14 | benchmark_test | Closes 351 Add decorators for registering a benchmark, adding axis and options. `cuda.bench.register(fn)` continues returning Benchmark, and supports legacy use. New signature added: `cuda.bench.register()` which returns a decorator. code block This pattern supplements earlier pattern of code block... | kernel: `python/examples/cute_dsl_sgemm.py`<br>benchmark: `python/cuda/bench/__init__.py`, `python/cuda/bench/__init__.pyi`, `python/cuda/bench/_decorators.py`, `python/examples/pytorch_bench.py`<br>wrapper: `python/examples/auto_throughput.py`, `python/examples/axes.py`, `python/examples/cpu_activity.py`, `python/examples/cuda_compute_segmented_reduce.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#29](https://github.com/NVIDIA/nvbench/pull/29) CUPTI support | 2021-12-18 | benchmark_test | Initial CUPTI support enables collection of the following metrics: - L1 hit rates - L2 hit rates - DRAM throughput - Store efficiency - Load efficiency This addresses the following issue. I haven't enabled CUPTI by default because there'll be a crash on Windows systems if the CUPTI lib64 directory i... | benchmark: `cmake/NVBenchCUPTI.cmake`, `cmake/NVBenchDependencies.cmake`, `cmake/NVBenchDependentDlls.cmake`, `cmake/NVBenchExports.cmake`<br>test: `testing/cmake/test_export/CMakeLists.txt`<br>other: `.gitignore`, `CMakeLists.txt`, `cmake/config.cuh.in`, `examples/CMakeLists.txt` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#53](https://github.com/NVIDIA/nvbench/pull/53) Enable extra warning flags | 2021-12-20 | benchmark_test | - /W4 on MSVC - -Wall -Wextra + others on gcc/clang - New NVBench_ENABLE_WERROR option to toggle "warnings as errors" - Mark the nlohmann_json library as IMPORTED to switch to system includes - Rename nvbench_main -> nvbench.main to follow target name conventions - Explicitly suppress some cudafe wa... | benchmark: `cmake/NVBenchConfigTarget.cmake`, `cmake/NVBenchDependencies.cmake`, `cmake/NVBenchExports.cmake`, `nvbench/CMakeLists.txt`<br>test: `testing/range.cu`<br>other: `CMakeLists.txt`, `examples/CMakeLists.txt`, `examples/auto_throughput.cu` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#237](https://github.com/NVIDIA/nvbench/pull/237) Python API for NVBench | 2025-08-06 | benchmark_test | Closes gh-228 The prototype is based on pybind11 to minimize boiler-plate code needed to deal with move-only semantics of many nvbench classes. The code is based on earlier prototype written by @gevtushenko | kernel: `python/examples/cutlass_gemm.py`<br>benchmark: `python/cuda/bench/__init__.py`, `python/cuda/bench/__init__.pyi`, `python/cuda/bench/py.typed`, `python/examples/pytorch_bench.py`<br>test: `python/test/run_1.py`, `python/test/stubs.py`<br>wrapper: `python/examples/auto_throughput.py`, `python/examples/axes.py`, `python/examples/cccl_cooperative_block_reduce.py`, `python/examples/cccl_parallel_segmented_reduce.py`<br>docs: `python/README.md`, `python/src/.BUILD_LOCALLY.md`<br>other: `python/.gitignore`, `python/CMakeLists.txt`, `python/examples/requirements.txt`, `python/pyproject.toml` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#330](https://github.com/NVIDIA/nvbench/pull/330) Mark NVBench headers as SYSTEM for consuming targets + FIX | 2026-03-23 | benchmark_test | Fixes 30. This PR supersedes 267. It builds on 267 and fixes build breakage with CTK 12.9 and older versions of GCC observed in 267. | benchmark: `nvbench/CMakeLists.txt`, `nvbench/axes_metadata.cuh`, `nvbench/axis_base.cuh`, `nvbench/benchmark.cuh` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#96](https://github.com/NVIDIA/nvbench/pull/96) Fix formatting | 2022-08-22 | benchmark_test | This PR changes the line limit to 100 and also uses newer policy for `SortIncludes`. I've auto formatted the source base with clang-format 14.0. The sources seems to benefit from larger line limit. | benchmark: `nvbench/axes_metadata.cuh`, `nvbench/axes_metadata.cxx`, `nvbench/axis_base.cuh`, `nvbench/axis_base.cxx`<br>other: `.clang-format`, `.git-blame-ignore-revs` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#64](https://github.com/NVIDIA/nvbench/pull/64) New convergence check | 2021-12-22 | benchmark_test, memory_primitives | Fixes 61. Adds a new convergence check that avoids the issue of short / noisy tests consistently running to timeout. See commit messages and comments in `measure_cold.cu` for details. This change alone reduces the runtime of the Thrust+CUB benchmarks from **30 hours** (!!) to 5 hours while producing... | benchmark: `nvbench/detail/measure_cold.cu`, `nvbench/detail/measure_cold.cuh`, `nvbench/detail/ring_buffer.cuh`, `nvbench/detail/statistics.cuh`<br>test: `testing/CMakeLists.txt`, `testing/device/CMakeLists.txt`, `testing/ring_buffer.cu`<br>other: `CMakeLists.txt` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#151](https://github.com/NVIDIA/nvbench/pull/151) Entropy-based stopping criterion | 2024-01-12 | benchmark_test | Closes https://github.com/NVIDIA/nvbench/issues/150 and https://github.com/NVIDIA/nvbench/issues/147. This PR adds new command line option `--stopping-criterion <criterion>` with two predefined criteria `stdrel` and `entropy` along with API for customization of the stopping criterion. The `nvbench/e... | benchmark: `nvbench/CMakeLists.txt`, `nvbench/benchmark_base.cuh`, `nvbench/benchmark_base.cxx`, `nvbench/criterion_manager.cuh`<br>test: `testing/CMakeLists.txt`, `testing/criterion_manager.cu`, `testing/criterion_params.cu`, `testing/entropy_criterion.cu`<br>docs: `docs/cli_help.md`<br>other: `.clangd`, `examples/CMakeLists.txt`, `examples/custom_criterion.cu` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#76](https://github.com/NVIDIA/nvbench/pull/76) Add implicit stream benchmarking support | 2022-02-11 | benchmark_test | Closes https://github.com/NVIDIA/nvbench/issues/13 This PR adds support to benchmark functions that do not expose stream parameters. | benchmark: `docs/benchmarks.md`, `nvbench/cuda_stream.cuh`, `nvbench/detail/measure_cold.cu`, `nvbench/detail/measure_cupti.cu`<br>test: `testing/state.cu`<br>docs: `README.md`<br>other: `examples/CMakeLists.txt`, `examples/stream.cu` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#79](https://github.com/NVIDIA/nvbench/pull/79) Fix a bug in config count unit test: count number of devices as well | 2022-02-18 | benchmark_test, scheduler_autotune | This PR fixes a minor bug when testing `benchmark::get_config_count()`: the number of devices should be taken into consideration as well. | benchmark: `testing/benchmark.cu` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#339](https://github.com/NVIDIA/nvbench/pull/339) Implement CLI option to control warmups for cold measurements | 2026-05-12 | benchmark_test | Closes 338 Closes 340 <strike>CLI option `--warmup-runs` implemented and documented.</strike> > [!Note] > **UPDATE**: CLI options added `--cold-warmup-runs <counts>` and `--cold-max-warmup-walltime <seconds>`. The warm-up counts is enforced to always be positive. This is necessary to ensure that JIT... | benchmark: `nvbench/benchmark_base.cuh`, `nvbench/benchmark_base.cxx`, `nvbench/detail/measure_cold.cu`, `nvbench/detail/measure_cold.cuh`<br>test: `testing/option_parser.cu`, `testing/state_generator.cu`<br>docs: `docs/cli_help.md` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#42](https://github.com/NVIDIA/nvbench/pull/42) Port to rapids-cmake | 2021-10-26 | benchmark_test, scheduler_autotune | - Add export sets - Add install rules - Remove manual CPM import, port to rapids_cpm_*, etc - Organize CMake code into cmake/*.cmake files. - NVBench is now a shared library. - Add version.cuh, git_revision.cuh headers - Add `--version` option to benchmark executables | benchmark: `cmake/NVBenchConfigTarget.cmake`, `cmake/NVBenchDependencies.cmake`, `cmake/NVBenchExports.cmake`, `cmake/NVBenchInstallRules.cmake`<br>test: `testing/CMakeLists.txt`, `testing/cmake/CMakeLists.txt`, `testing/cmake/test_export/CMakeLists.txt`<br>other: `CMakeLists.txt`, `cmake/modules/CPM.cmake`, `examples/CMakeLists.txt` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#356](https://github.com/NVIDIA/nvbench/pull/356) Provide BenchmarkResult class for parsing JSON output of NVBench-instrumented benchmarks | 2026-05-13 | benchmark_test | Updated description This PR add `cuda.bench.results.BenchmarkResult` class that provides `from_json(fn, metadata = None)` and `empty(metadata = None)` class methods to create result from existing JSON file, or to create empty class in case a process collecting benchmark data returned abnormally. PR... | benchmark: `ci/test_cuda_bench_inner.sh`, `python/cuda/bench/__init__.py`, `python/cuda/bench/__init__.pyi`, `python/cuda/bench/results/__init__.py`<br>other: `python/examples/requirements.txt`, `python/pyproject.toml` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#223](https://github.com/NVIDIA/nvbench/pull/223) Remove min-time/max-noise API. | 2025-05-08 | benchmark_test | These are now owned by the stdrel stopping criterion, and should not be exposed directly in the benchmark/state/etc APIs. There's a small chance this may affect users that are calling `NVBENCH_BENCH(...).set_min_time(...)` or `NVBENCH_BENCH(...).set_max_noise(...)`. These can be updated to `NVBENCH_... | benchmark: `nvbench/benchmark_base.cuh`, `nvbench/detail/measure_hot.cu`, `nvbench/detail/stdrel_criterion.cxx`, `nvbench/json_printer.cu`<br>test: `testing/criterion_params.cu`, `testing/option_parser.cu`, `testing/state_generator.cu` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#85](https://github.com/NVIDIA/nvbench/pull/85) Add --disable-blocking-kernel and --profile options. | 2022-04-26 | benchmark_test | Closes 45. Adds a mode that forces a benchmark not to use the `blocking_kernel`, simplifying profiling usecases. This can be enabled by any of the following methods: - Passing `--disable-blocking-kernel` on the command line - `NVBENCH_CREATE(...).set_no_block(true)` when declaring a benchmark - `sta... | benchmark: `nvbench/benchmark_base.cuh`, `nvbench/blocking_kernel.cu`, `nvbench/detail/measure_cold.cu`, `nvbench/detail/measure_cold.cuh`<br>docs: `docs/cli_help.md` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#102](https://github.com/NVIDIA/nvbench/pull/102) fmt::memory_buffer is no longer an iterator. | 2022-11-08 | benchmark_test | This change retains compatibility with older versions of fmt while enabling usage with newer versions. I did not update the version in `cmake/NVBenchDependencies.cmake`, but I can do so if a version bump is desired. I did find that with the newer library the `fmt` target did not reliably exist, so I... | benchmark: `cmake/NVBenchDependencies.cmake`, `nvbench/csv_printer.cu`, `nvbench/cupti_profiler.cxx`, `nvbench/markdown_printer.cu`<br>test: `testing/axes_metadata.cu`, `testing/create.cu`, `testing/option_parser.cu`, `testing/runner.cu` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#206](https://github.com/NVIDIA/nvbench/pull/206) Discard measurements while GPU is throttling | 2025-04-14 | benchmark_test | Fixes https://github.com/NVIDIA/nvbench/issues/193 This PR adds a few options: 1. `--throttle-threshold 0.8` (80%) - sets percentage of peak frequency that's considered throttling. If SM frequency drops below this value, warning is issued. 2. `--throttle-recovery-delay 5.0` - waits for 5 seconds to... | benchmark: `nvbench/CMakeLists.txt`, `nvbench/benchmark_base.cuh`, `nvbench/benchmark_base.cxx`, `nvbench/detail/gpu_frequency.cuh`<br>docs: `docs/cli_help.md` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#207](https://github.com/NVIDIA/nvbench/pull/207) Throttling followup | 2025-04-18 | benchmark_test, memory_primitives | - Show SM clock info in summaries example. - Add new hidden summary with percent sm clock scaling, - Fix sm clock logging to record current clock rate instead of default clock rate. - Reduce memory usage of clock rate logging. - Replace references to `peak_sm_clock` with `default_sm_clock`. - The ac... | benchmark: `nvbench/benchmark_base.cuh`, `nvbench/detail/gpu_frequency.cuh`, `nvbench/detail/gpu_frequency.cxx`, `nvbench/detail/measure_cold.cu`<br>docs: `docs/cli_help.md`<br>other: `examples/summaries.cu` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#326](https://github.com/NVIDIA/nvbench/pull/326) Fix GCC16 sfinae incomplete warnings  | 2026-03-24 | benchmark_test | Closes 325. Resolves `-Wsfinae-incomplete` warning when compiling with GCC 16.0. - Replace use of `std::reference_wrapper<const nvbench::benchmark_base>` in `nvbench::state` with `const nvbench::benchmark_base*` enabling for `benchmark_base` type to be incomplete. - Use PImpl pattern to move constru... | benchmark: `nvbench/benchmark_base.cuh`, `nvbench/benchmark_base.cxx`, `nvbench/detail/measure_cold.cu`, `nvbench/detail/measure_cpu_only.cxx` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#54](https://github.com/NVIDIA/nvbench/pull/54) Print progress in markdown log. | 2021-12-20 | benchmark_test | code block I also cleaned up some virtual interfaces while implementing this: - nvbench::benchmark doesn't add state, no need to override the destructor. - nvbench::printer_base's virtual API should support decoration, not just overriding. Making the virtual API protected instead of private allows d... | benchmark: `nvbench/benchmark.cuh`, `nvbench/benchmark_base.cuh`, `nvbench/benchmark_base.cxx`, `nvbench/csv_printer.cuh` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#32](https://github.com/NVIDIA/nvbench/pull/32) Add --run-once option. | 2021-10-07 | benchmark_test | Fixes 10. Adds a mode that forces a benchmark to only run once, simplifying profiling usecases. This can be enabled by any of the following methods: * Passing `--run-once` on the command line * `NVBENCH_CREATE(...).set_run_once(true)` when declaring a benchmark * `state.set_run_once(true)` from with... | benchmark: `nvbench/benchmark_base.cuh`, `nvbench/detail/measure_cold.cu`, `nvbench/detail/measure_cold.cuh`, `nvbench/detail/state_exec.cuh`<br>docs: `docs/cli_help.md` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#63](https://github.com/NVIDIA/nvbench/pull/63) Fix progress display for inactive type axis values. | 2021-12-22 | benchmark_test | When type axis values were disabled they were still counted towards a benchmark's total number of configs. | benchmark: `nvbench/benchmark_base.cxx`, `nvbench/option_parser.cu`, `nvbench/type_axis.cuh`, `nvbench/type_axis.cxx` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#113](https://github.com/NVIDIA/nvbench/pull/113) Fixes cudaErrorInvalidValue when running on nvbench-created cuda stream | 2025-04-30 | benchmark_test | This PR fixes a minor issue that may occur when `nvbench` is run on multiple GPUs without a user-provided cuda stream. The issue The error that I observed in this case looked like: code block When run with `memcheck` I would see: code block The Problem It seems that nvbench is creating all the nvben... | benchmark: `nvbench/cuda_stream.cuh`, `nvbench/detail/measure_cold.cu`, `nvbench/detail/measure_cupti.cu`, `nvbench/detail/measure_hot.cu`<br>test: `testing/CMakeLists.txt`, `testing/cuda_stream.cu`, `testing/state.cu` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#174](https://github.com/NVIDIA/nvbench/pull/174) Restrict stopping criterion parameter usage in command line | 2025-04-30 | benchmark_test | Now the option parser throws an exception if any parameters don't match corresponding stopping criterions. This PR addresses issue 153 . | benchmark: `nvbench/benchmark_base.cuh`, `nvbench/benchmark_base.cxx`, `nvbench/criterion_manager.cuh`, `nvbench/criterion_manager.cxx`<br>test: `testing/option_parser.cu`<br>docs: `docs/cli_help.md`<br>other: `examples/CMakeLists.txt` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#302](https://github.com/NVIDIA/nvbench/pull/302) Build multi architecture cuda wheels | 2026-01-29 | benchmark_test | This PR adds logic to build nvbench for multiple architectures. It also moves the scripts to `python` in order to package them with the wheel. This means that installing pynvbench, you can do code block as it will be added to the path. | benchmark: `ci/build_pynvbench_wheel_for_cuda.sh`, `nvbench/json_printer.cu`, `python/scripts/nvbench_compare.py`, `python/scripts/nvbench_histogram.py`<br>test: `python/scripts/test_cmp.json`, `python/scripts/test_ref.json`<br>wrapper: `python/scripts/__init__.py`<br>other: `python/pyproject.toml` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#309](https://github.com/NVIDIA/nvbench/pull/309) Support --no-batched CLI option | 2026-02-02 | benchmark_test | Closes 272 The option sets `m_skip_batched` added boolean member in `nvbench::benchmark_base` class. Methods `bool get_skip_batched()` and `void set_skip_batched(bool)` are added. The member variable `m_skip_batched` is also added to `nvbench::state` class. Similarly named methods are added. CLI hel... | benchmark: `nvbench/benchmark_base.cuh`, `nvbench/detail/state_exec.cuh`, `nvbench/option_parser.cu`, `nvbench/option_parser.cuh`<br>docs: `docs/cli_help.md` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#289](https://github.com/NVIDIA/nvbench/pull/289) Add multi-cuda wheel build | 2026-01-28 | benchmark_test | This PR introduces "multi-CUDA" wheels builds, exactly similar to the ones we build for CCCL, as described here. Instead of shipping multiple packages (`pynvbench-cu12`, `pynvbench-cu13`); we ship a single one (`pynvbench`) that contains both CUDA 12 and 13 binaries (bindings). The resulting wheel s... | benchmark: `ci/build_pynvbench_wheel.sh`, `ci/build_pynvbench_wheel_for_cuda.sh`, `ci/build_pynvbench_wheel_inner.sh`, `ci/test_pynvbench.sh`<br>other: `.github/workflows/build-and-test-python-wheels.yml`, `.github/workflows/build-wheels.yml`, `.gitignore`, `ci/build_multi_cuda_wheel.sh` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#173](https://github.com/NVIDIA/nvbench/pull/173) Added cudaGetLastError() calls to reset benchmarking kernel errors (issue 88). | 2024-05-31 | benchmark_test | Created the NVBENCH_CUDA_CALL_RESET_ERROR() macro and use it to wrap cudaStreamSynchronize() calls. | benchmark: `nvbench/cuda_call.cuh`<br>test: `testing/CMakeLists.txt`, `testing/reset_error.cu` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#290](https://github.com/NVIDIA/nvbench/pull/290) Make python nvbench benchmarks interruptible | 2026-01-23 | benchmark_test | This PR adds `try/catch` around invocation of Python function that defines benchmark function. If execution of benchmark for some input parameters resulted in Python exception being raised, it gets propagated as `nvbench::stop_iteration_loop` exception, also added in this PR. The `nvbench::runner` w... | benchmark: `nvbench/benchmark.cuh`, `nvbench/benchmark_base.cuh`, `nvbench/runner.cuh`, `python/src/py_nvbench.cpp`<br>other: `python/CMakeLists.txt` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#327](https://github.com/NVIDIA/nvbench/pull/327) Reworked cupti_profiler to use Host + Range Profiler APIs end-to-end | 2026-03-23 | benchmark_test | NVPW_* API has been deprecated since CTK 13.0. Followed advice in compilation message to replace NVPW_* API with CUPTI Profiler Host API. `libnvbench.so` no longer links to `nvperf_host` directly, only to `libcupti`. NVBench uses the **CUPTI Host API** to build a config image from metric names, and... | benchmark: `cmake/NVBenchCUPTI.cmake`, `nvbench/CMakeLists.txt`, `nvbench/cupti_profiler.cuh`, `nvbench/cupti_profiler_host.cuh` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#346](https://github.com/NVIDIA/nvbench/pull/346) Correct Python API signature of State.get_axis_values_as_strings | 2026-05-04 | benchmark_test | The C++ API has default Boolean argument color, but Python API declared no arguments. Closes 345 Also two additional drive-by fixes to files in `"test/"` folder, including replacing use of deprecated `cuda.core.experimental` with `cuda.core`. | benchmark: `python/cuda/bench/__init__.pyi`, `python/src/py_nvbench.cpp`<br>test: `python/test/run_1.py`, `python/test/stubs.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#130](https://github.com/NVIDIA/nvbench/pull/130) Introduce machine-readable version of --list | 2023-04-10 | benchmark_test | CUB tuning infrastructure needs a machine-readable format for `--list`. This PR introduces `--jsonlist` option and fixes a compilation issues for a few examples. | benchmark: `nvbench/json_printer.cu`, `nvbench/json_printer.cuh`, `nvbench/option_parser.cu`, `nvbench/option_parser.cuh`<br>other: `examples/exec_tag_sync.cu`, `examples/exec_tag_timer.cu` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#157](https://github.com/NVIDIA/nvbench/pull/157) Set `CUDA_MODULE_LOADING=EAGER` before `main`. | 2024-04-06 | benchmark_test | Fixes 136 | benchmark: `nvbench/benchmark_base.cuh`, `nvbench/benchmark_manager.cuh`, `nvbench/benchmark_manager.cxx`, `nvbench/main.cuh`<br>other: `examples/CMakeLists.txt` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#254](https://github.com/NVIDIA/nvbench/pull/254) Remove cli run-once and disable-blocking-kernel options | 2025-07-28 | benchmark_test | Closes gh-253 Removes support for `--run-once` and `--disable-blocking-kernel` from `nvbench::option_parser`. - Removed `nvbench::option_parser::enable_run_once()` and `nvbench::option_parser::disable_blocking_kernel()`. - Added `nvbench::option_parser::enable_profile()` that calls `bench.set_run_on... | benchmark: `nvbench/blocking_kernel.cu`, `nvbench/option_parser.cu`, `nvbench/option_parser.cuh`<br>test: `testing/CMakeLists.txt`<br>docs: `docs/cli_help.md` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#305](https://github.com/NVIDIA/nvbench/pull/305) Replace all occurences of pynvbench with cuda-bench | 2026-01-29 | benchmark_test | Replace all occurences of pynvbench with cuda-bench | benchmark: `ci/build_cuda_bench_wheel_for_cuda.sh`, `ci/test_cuda_bench.sh`, `ci/test_cuda_bench_inner.sh`, `python/cuda/bench/__init__.py`<br>other: `.github/workflows/build-and-test-python-wheels.yml`, `.github/workflows/publish-wheels.yml`, `ci/build_multi_cuda_wheel.sh`, `python/pyproject.toml` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#337](https://github.com/NVIDIA/nvbench/pull/337) Port changes to the package from #323 | 2026-04-22 | benchmark_test | This PR ports changes to the cuda.bench package sources from 323 - Fixed relative text alignment in docstrings to fix autodoc warnigns - Renamed cuda.bench.test_cpp_exception and cuda.bench.test_py_exception functions to start with underscore, signaling that these functions are internal and should n... | benchmark: `python/cuda/bench/__init__.py`, `python/src/py_nvbench.cpp`, `python/test/test_cuda_bench.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#14](https://github.com/NVIDIA/nvbench/pull/14) Improve compare output | 2021-06-22 | benchmark_test | This PR makes some changes to the JSON schema used for nvbench output and adds a new script to generate comparisons between multiple benchmarks. It builds on work by @allisonvacanti, who has the original version of this script in her fork. | benchmark: `nvbench/json_printer.cu`, `scripts/nvbench_compare.py`<br>test: `scripts/test_cmp.json`, `scripts/test_ref.json`<br>other: `requirements.txt` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#202](https://github.com/NVIDIA/nvbench/pull/202) Misc fixes | 2025-04-04 | benchmark_test | Whitespace, docs, typos, etc. | benchmark: `nvbench/benchmark_base.cuh`, `nvbench/cuda_stream.cuh`, `nvbench/detail/measure_cold.cu`, `nvbench/option_parser.cu` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#214](https://github.com/NVIDIA/nvbench/pull/214) Fix throttle setter return values and update customization example | 2025-04-24 | benchmark_test | This PR fixes a bug where the benchmark throttle setters were incorrectly returning void instead of the updated benchmark settings. It also updates the example to demonstrate how to customize the throttle threshold. | benchmark: `nvbench/benchmark_base.cuh` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#311](https://github.com/NVIDIA/nvbench/pull/311) Remove cupti from cuda-bench dependencies | 2026-02-03 | benchmark_test | We currently include an option in NVBench that enables collecting cupti metrics. Following some internal discussion, we decided to remove this option as it is meant for profiling and is orthogonal to NVBench's goal which is to benchmark kernels. | benchmark: `python/cuda/bench/__init__.py`, `python/cuda/bench/__init__.pyi`, `python/src/py_nvbench.cpp`<br>wrapper: `python/examples/auto_throughput.py`<br>other: `python/CMakeLists.txt`, `python/pyproject.toml` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#25](https://github.com/NVIDIA/nvbench/pull/25) Workaround CUDA 11.0 ICE on centos7 | 2021-07-15 | benchmark_test | Workaround CUDA 11.0 ICE on centos7 | benchmark: `nvbench/CMakeLists.txt`, `nvbench/axes_metadata.cuh`, `nvbench/detail/l2flush.cuh`, `nvbench/detail/statistics.cuh` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#31](https://github.com/NVIDIA/nvbench/pull/31) Make benchmark names more unique. | 2021-10-07 | benchmark_test | Fixes 28, in which multiple benchmark TUs are linked together that use NVBENCH_CREATE on the same line in multiple files, leading to symbol name collisions. | benchmark: `nvbench/create.cuh` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#234](https://github.com/NVIDIA/nvbench/pull/234) Allow kernel_generator to be stateful | 2025-06-29 | benchmark_test | In python kernel generator is a user-defined callable. We need to capture Python object of that callable in kernel generator provided for each benchmark. To this end, nvbench::benchmark has been modified to have member of kernel_generator type (must be copy-constructable). Constructor acquires an op... | benchmark: `nvbench/benchmark.cuh`, `nvbench/runner.cuh` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#277](https://github.com/NVIDIA/nvbench/pull/277) Profile only the kernels involved in the benchmark | 2025-10-21 | benchmark_test | Profile only the kernels involved in the benchmark | benchmark: `nvbench/detail/measure_cold.cuh` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#286](https://github.com/NVIDIA/nvbench/pull/286) entropy criterion optimizations | 2025-12-06 | benchmark_test | **Overview** This PR significantly optimizes the entropy criterion algorithm by reducing its computational complexity from O(n) to O(1) per update. The new approach leverages incremental updates and efficient data structures to avoid repeated full traversals, resulting in substantial performance imp... | benchmark: `nvbench/detail/entropy_criterion.cuh`, `nvbench/detail/entropy_criterion.cxx`, `nvbench/detail/online_linear_regression.cuh` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#322](https://github.com/NVIDIA/nvbench/pull/322) Save frequencies when bulk-saving of times is enabled | 2026-02-27 | benchmark_test | Closes 318 This PR: - applies changes started by @gevtushenko to collect bulk values of frequency estimates and enable saving them alongside bulk of time-stamps - the same batched `writeout_data` utility is used to save duration and frequency data faster | benchmark: `nvbench/detail/measure_cold.cu`, `nvbench/detail/measure_cold.cuh`, `nvbench/json_printer.cu`, `nvbench/json_printer.cuh` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#252](https://github.com/NVIDIA/nvbench/pull/252) Run once disables blocking kernel | 2025-07-28 | benchmark_test | Per 240 and 242, the first launcher run should not use blocking kernel to avoid possibility of a deadlock caused by module loading. For normal runs, the first call is the warm-up call. The 241 disabled use of blocking-kernel is warm-up call of measure_cold. This PR disables its used in warm-up call... | benchmark: `nvbench/detail/measure_cold.cuh`, `nvbench/detail/measure_hot.cuh`, `nvbench/main.cuh` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#257](https://github.com/NVIDIA/nvbench/pull/257) nvbench::state::set_stopping_criterion now also sets criterion params | 2025-08-01 | benchmark_test | Closes 255 This change closes gh-255 by aligning the implementation of ``nvbench::state::set_stopping_criterion`` with that of ``nvbench::benchmark_base::set_stopping_criterion``. | benchmark: `nvbench/state.cuh`, `nvbench/state.cxx` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#211](https://github.com/NVIDIA/nvbench/pull/211) Fetch clock rates using cudaDeviceGetAttribute. | 2025-04-14 | benchmark_test | Fetch clock rates using cudaDeviceGetAttribute. | benchmark: `nvbench/device_info.cu`, `nvbench/device_info.cuh` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#275](https://github.com/NVIDIA/nvbench/pull/275) Add an benchmark example parametrized by typename and integral constant. | 2025-10-07 | benchmark_test | Add a variation of copy_type_sweep kernel, where block size is controlled via integral constant passed as template parameter. Closes 274 | other: `examples/axes.cu` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#350](https://github.com/NVIDIA/nvbench/pull/350) Make nvbench::main exported cmake target map to static library | 2026-05-13 | benchmark_test | Previously, it mapped to main.cu.o object file. Now it corresponds to static library `libnvbench_main.a` which is archive file with `main.cu.o` object in it. This closes 349 The content of NVBench install prefix now is code block by CodeRabbit * **Chores** * Optimized library packaging and distribut... | benchmark: `nvbench/CMakeLists.txt`, `nvbench/main.cu` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#90](https://github.com/NVIDIA/nvbench/pull/90) Plot comparison results | 2024-11-13 | benchmark_test | This PR adds visualization to the comparison script. Here's an example for: code block When script is executed, one can specify the axis along which the visualization should be made: code block !image During visualization, reference is plotted as dashed line and the noise is illustrated as an error... | benchmark: `scripts/nvbench_compare.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#146](https://github.com/NVIDIA/nvbench/pull/146) No batch tag | 2023-11-02 | benchmark_test | NVBench always collects batch measurements: \| NumElements \| DataSize \| Samples \| CPU Time \| Noise \| GPU Time \| Noise \| Elem/s \| GlobalMem BW \| BWUtil \| Samples \| Batch GPU \| \|-------------\|------------\|---------\|------------\|--------\|------------\|-------\|---------\|--------------\|--------\|---------\|-... | benchmark: `nvbench/exec_tag.cuh` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#233](https://github.com/NVIDIA/nvbench/pull/233) Set underlying type for enum class exec_tag to uint16_t | 2025-06-29 | benchmark_test | Set underlying type for `enum class exec_tag` to `::cuda::std::uint16_t`, rather than the 32-bit default integral type. This change reduces size of exec_tag instance from 4 bytes to 2 bytes, it also makes it more explicit what underlying type exec_tag is using. | benchmark: `nvbench/exec_tag.cuh` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#241](https://github.com/NVIDIA/nvbench/pull/241) Do not use blocking kernel in warmup run of measure_cold | 2025-07-04 | benchmark_test | Implements suggestion https://github.com/NVIDIA/nvbench/issues/240 Adding this change of top of changes in 237 fixes the deadlock reported in 240. | benchmark: `nvbench/detail/measure_cold.cuh` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#246](https://github.com/NVIDIA/nvbench/pull/246) Avoid cuda/std types in host compiler headers | 2025-07-17 | benchmark_test | Fixes: 245 | benchmark: `nvbench/exec_tag.cuh` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#328](https://github.com/NVIDIA/nvbench/pull/328) Set type-axes name to ItemsPerThread to replace auto-generated T | 2026-03-20 | benchmark_test | In `"examples/auto_throughput.cu"` use `set_type_axes_names` benchmark method to give meaningful name to type axes of benchmark `throughput_bench`. The output is not meaningful: code block Previously, `ItemsPerThread` column was non-descriptively named `T` . | other: `examples/auto_throughput.cu` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |

## Per-PR Ledger Fields

When using an idea from this page, add one row to `artifacts/source-idea-ledger.md` with:

| Field | Value to record |
| --- | --- |
| Source key | `<repo>#<pr-number>` |
| Code evidence | Kernel, wrapper, benchmark, and test paths opened from the PR diff |
| Hypothesis | The concrete optimization idea derived from the PR |
| First experiment | Candidate version and benchmark shape set |
| Result | Correctness, geomean, best/worst cases, and NCU digest path |
| Do-not-reread key | Same as source key unless a single PR yields multiple independent ideas |

## How To Use This Page

- During the initial knowledge pass, read the category matching the target kernel and copy PR URL, changed paths, and hypothesis into the source idea ledger.
- During plateau expansion, choose PRs not already present in ledger do-not-reread keys; inspect the diff, linked issue, changed tests, and benchmark files before using the idea.
- Treat PR code as baseline/prior art unless the task and license allow copying or adapting it. When copied, record exact PR, commit, files, notice, and first delta.

# GPU MODE KernelBot PR Knowledge Notes

Repository: <https://github.com/gpu-mode/kernelbot>

This page is the production-PR layer for kernel-knowledge. It favors merged PRs that changed kernels, dispatch, JIT/runtime integration, tuning policy, tests, benchmarks, or profiling evidence. Release, CI-only, formatting, pure version-bump, and non-target-backend PRs are filtered out.

## Repository Source Scan

## Coverage Summary

| Category | CUDA-kernel PRs |
| --- | ---: |
| GEMM / Quantization | 1 |
| Attention / KV / Decode | 2 |
| Memory / Primitives | 1 |
| Scheduler / Autotune | 1 |
| Architecture / Pipeline | 24 |
| Compiler / Runtime | 4 |
| Benchmark / Test Evidence | 34 |
| Other Kernel Cases | 18 |

## Pull Request Case Notes

### GEMM / Quantization

Use this section for: Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate.
NCU first look: Tensor pipe %, DRAM/L2 bytes, active cycles, register pressure, and scale-load traffic.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#351](https://github.com/gpu-mode/kernelbot/pull/351) fix example submission names | 2025-09-09 | gemm_quant, attention_kv, memory_primitives, arch_pipeline, compiler_runtime | Description This fixes all the example leaderboard names: Some were not using the -dev variant, some were not using the right leaderboard name at all. Since during experimentation the bot automatically creates a `-dev` leaderboard, I figured it would be easiest to also use the `-dev` variant here so... | kernel: `examples/vectoradd_py/submission_triton.py`<br>wrapper: `examples/gather/submission.py`, `examples/gather/wrong.py`, `examples/matmul_py/submission.py`, `examples/softmax_py/submission.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |

### Attention / KV / Decode

Use this section for: Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization.
NCU first look: Long scoreboard, L2/DRAM traffic, global-load sectors, tensor pipe %, and shared-memory bank conflicts.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#145](https://github.com/gpu-mode/kernelbot/pull/145) Task definitions as json strings | 2025-02-03 | attention_kv, arch_pipeline | Store generic task definitions as json string in the database (abusing the existing reference_code field for now). This allows us to easily define multi-file tasks, have different eval scripts for different tasks (even though its possible, in general we should avoid that, I think) Currently, I've co... | wrapper: `examples/eval.py`, `examples/identity_cuda/task.h`, `examples/identity_cuda/utils.h`, `examples/identity_py/eval.py`<br>other: `examples/eval.cu`, `examples/identity_cuda/eval.cu`, `examples/identity_cuda/reference.cuh`, `examples/identity_cuda/submission.cu` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#453](https://github.com/gpu-mode/kernelbot/pull/453) Add flash-linear-attention and causal-conv1d to Helion Docker image | 2026-03-03 | attention_kv | These are dependencies needed by the gated_deltanet and causal_conv kernel benchmarks. | other: `docker/helion.Dockerfile` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |

### Memory / Primitives

Use this section for: Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract.
NCU first look: DRAM throughput, L2 hit rate, memory sectors, shared-memory conflicts, and synchronization overhead.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#355](https://github.com/gpu-mode/kernelbot/pull/355) examples: profiling | 2025-09-10 | memory_primitives | Description This PR adds profiling info gathering to the example `eval.py`. The following things are added: * Profiling implemented for distributed runs in general * Currently, each process gathers results via `torch.profiler.profile` as with the single-threaded case. The resulting events are passed... | wrapper: `examples/eval.py` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |

### Scheduler / Autotune

Use this section for: Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing.
NCU first look: SM occupancy, waves/SM, active cycles, tail effects, and load imbalance.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#357](https://github.com/gpu-mode/kernelbot/pull/357) Temporary fix, send only split log | 2025-09-14 | scheduler_autotune | This is a temporary fix jsut so I can deploy the problem, we should probably remove the functionality to create threads anyway as they aren't particularly used. | kernel: `src/kernelbot/cogs/admin_cog.py` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |

### Architecture / Pipeline

Use this section for: Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages.
NCU first look: Tensor pipe %, memory pipe utilization, barrier stalls, wait groups, and occupancy.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#316](https://github.com/gpu-mode/kernelbot/pull/316) Libkernelbot | 2025-07-23 | arch_pipeline, benchmark_test | Going off https://github.com/gpu-mode/discord-cluster-manager/pull/308 | kernel: `src/kernelbot/api/__init__.py`, `src/kernelbot/api/api_utils.py`, `src/kernelbot/api/main.py`, `src/kernelbot/cogs/__init__.py`<br>wrapper: `examples/identity_py/submission.py`, `scripts/ci_test_cuda.py`, `scripts/ci_test_python.py`, `scripts/local-test.py`<br>other: `.github/workflows/amd_workflow.yml`, `.github/workflows/nvidia_workflow.yml`, `.github/workflows/runner_ci.yml`, `pyproject.toml` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#130](https://github.com/gpu-mode/kernelbot/pull/130) Unifying github and modal runners | 2025-01-15 | arch_pipeline, benchmark_test | this attempts to unify the code paths for github and modal, by reusing the same running scripts and discord output generation. This is a major revision to how github runners are handled. Instead of individual files, we now submit all files packed up in a json object. Additionally, the python environ... | wrapper: `.github/workflows/runner.py`, `examples/identity_py/reference.py`, `scripts/ci_test_cuda.py`, `scripts/ci_test_python.py`<br>other: `.github/workflows/amd_workflow.yml`, `.github/workflows/nvidia_workflow.yml` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#299](https://github.com/gpu-mode/kernelbot/pull/299) [ez] update references so they run locally | 2025-06-14 | arch_pipeline, gemm_quant, attention_kv | update the references in the examples cause the return types are out dated. This is p much tested in testing https://github.com/gpu-mode/discord-cluster-manager/pull/299 | wrapper: `examples/identity_py/reference.py`, `examples/matmul_py/reference.py`, `examples/softmax_py/reference.py`, `examples/vectoradd_py/reference.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#131](https://github.com/gpu-mode/kernelbot/pull/131) update pytorch script runner to use pipe | 2025-01-14 | arch_pipeline, benchmark_test | Updates the python interface to match the recent cuda changes. Turns out `run_cuda_program` actually doesn't do anything cuda specific, so we can instead reuse this piece of code. In the current state, standalone python (and also cuda, I think) scripts are somewhat broken, as they never use the spec... | wrapper: `scripts/ci_test_cuda.py`, `scripts/ci_test_python.py`, `src/discord-cluster-manager/cogs/verify_run_cog.py`, `src/discord-cluster-manager/consts.py`<br>other: `.github/workflows/cuda_test.yml`, `.github/workflows/runner_ci.yml`, `src/discord-cluster-manager/eval.cu` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#303](https://github.com/gpu-mode/kernelbot/pull/303) remove SCRIPT run mode | 2025-06-24 | arch_pipeline, benchmark_test | As discussed; we have never actually used the SCRIPT mode in the last 6 months or so; no reason to keep the complexity around for that. If we really need it, we can reimplement. Right now, this removes the entire SubmitCog, and a fair bit of branching in other code areas. | wrapper: `examples/eval.py`, `scripts/ci_test_cuda.py`, `src/discord-cluster-manager/api/main.py`, `src/discord-cluster-manager/bot.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#300](https://github.com/gpu-mode/kernelbot/pull/300) Disentangling discord | 2025-06-18 | arch_pipeline | In preparation for further cleanups, this PR tries to disentangle our files so there is less (direct and indirect) importing of discord happening. * db is not independent from env * db helper typedefs are now in db file (not 100% sure about that one, but imo a better place than "utils") * instead of... | wrapper: `src/discord-cluster-manager/api/main.py`, `src/discord-cluster-manager/api/utils.py`, `src/discord-cluster-manager/bot.py`, `src/discord-cluster-manager/cogs/admin_cog.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#38](https://github.com/gpu-mode/kernelbot/pull/38) More informative execution logs/telemetry | 2024-12-03 | arch_pipeline, benchmark_test | This PR adds telemetry logs for the following metrics: * File size * How long a given job took to get queued * How long to execute/run **Screenshots:** \| !Screenshot 1 \| !Screenshot 2 \| \| --- \| --- \| - [x] Modal - [ ] GitHub Actions > [!NOTE] > For Modal, it is not possible to programmatically fetch... | wrapper: `scripts/modal-test.py`, `src/discord-cluster-manager/cogs/modal_cog.py`, `src/discord-cluster-manager/modal_runner.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#104](https://github.com/gpu-mode/kernelbot/pull/104) Example Kernels for the Leaderboard  | 2025-01-09 | arch_pipeline, attention_kv | Description Examples of CUDA, Python, and TK leaderboards. Feel free to add more! Checklist Before submitting this PR, ensure the following steps have been completed: - [x] Run the slash command `/verifyruns` on your own server. - Run the cluster bot on your server: code block - Start training runs... | wrapper: `examples/softmax_py/reference.py`, `examples/softmax_py/submission.py`<br>other: `examples/simple_cuda/reference.cu`, `examples/simple_cuda/submission.cu`, `examples/thunderkittens_example/reference.cu`, `examples/thunderkittens_example/submission.cu` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#97](https://github.com/gpu-mode/kernelbot/pull/97) Add Modal support for Leaderboard (Python + CUDA) | 2025-01-06 | arch_pipeline | Description Adds Python and CUDA support for Modal runners on the leaderboard. This will be especially useful for reducing overhead that the GitHub runners have, and provides a wider variety of devices to use. Also, this PR is in preparation for CUTLASS and TK support for the leaderboard. <img width... | wrapper: `src/discord-cluster-manager/bot.py`, `src/discord-cluster-manager/cogs/github_cog.py`, `src/discord-cluster-manager/cogs/leaderboard_cog.py`, `src/discord-cluster-manager/cogs/misc_cog.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#171](https://github.com/gpu-mode/kernelbot/pull/171) Database update to record submissions | 2025-02-21 | arch_pipeline, benchmark_test | This is a first attempt at reworking our database to enable detailed recording of submissions. It split submissions into 3 tables: 1) code files. Here we store the actually submitted code. We have a hash column that allows us to quickly detect if the same code is submitted again, and in this case we... | wrapper: `src/discord-cluster-manager/cogs/admin_cog.py`, `src/discord-cluster-manager/cogs/leaderboard_cog.py`, `src/discord-cluster-manager/leaderboard_db.py`, `src/discord-cluster-manager/migrations/20250221_01_GA8ro-submission-collection.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#99](https://github.com/gpu-mode/kernelbot/pull/99) Finish up Modal support + Example of ThunderKittens working | 2025-01-07 | arch_pipeline | Description This is a relatively large PR with a lot of finishing changes for modal support and ThunderKittens (TK) support. Namely, 1. We can compile (only tested on just importing and building) ThunderKittens kernel on Modal. More extensive testing coming. 2. The UI for Modal actually works now, a... | wrapper: `src/discord-cluster-manager/cogs/leaderboard_cog.py`, `src/discord-cluster-manager/cogs/modal_cog.py`, `src/discord-cluster-manager/cogs/verify_run_cog.py`, `src/discord-cluster-manager/consts.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#110](https://github.com/gpu-mode/kernelbot/pull/110) Consistent naming between CUDA and Python submissions | 2025-01-10 | arch_pipeline | Description CUDA submissions now expect the same function names (basically signatures) as Python submissions, so reference --> ref_kernel submission --> custom_kernel Also, examples are changed to reflect this change (I mainly did this because we don't have any interesting active CUDA leaderboards r... | wrapper: `examples/identity_py/reference.py`, `examples/identity_py/submission.py`, `src/discord-cluster-manager/cogs/github_cog.py`, `src/discord-cluster-manager/cogs/modal_cog.py`<br>docs: `docs/docs/submitting-your-first-kernel/cuda-submissions.md`<br>other: `examples/identity_cuda/reference.cuh`, `examples/identity_cuda/submission.cuh`, `examples/thunderkittens_example/reference.cuh`, `examples/thunderkittens_example/submission.cuh` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#201](https://github.com/gpu-mode/kernelbot/pull/201) stricter gpu scheduling for a100s and h100s | 2025-03-03 | arch_pipeline, benchmark_test | Description The `app.function` parameter `gpu` will map work onto more than one GPU type when provided the strings `"A100"` and `"H100"`, which makes benchmarking noisier. This PR changes those strings to `"A100-80GB"` and `"H100!"` (`!` meaning "strict") so that work is mapped onto only one GPU typ... | wrapper: `src/discord-cluster-manager/modal_runner_archs.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#207](https://github.com/gpu-mode/kernelbot/pull/207) Record system information | 2025-03-15 | arch_pipeline | Description An attempt to record more information about the system we're running on. If we'd done that from the beginning, we would've been able to identify the submissions that were affected by our unspecific modal runner selection. This implementation imports pytorch to get the GPU name from pytho... | wrapper: `src/discord-cluster-manager/cogs/github_cog.py`, `src/discord-cluster-manager/cogs/leaderboard_cog.py`, `src/discord-cluster-manager/leaderboard_db.py`, `src/discord-cluster-manager/migrations/20250304_01_DzORz-collect-system-information-for-each-run.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#63](https://github.com/gpu-mode/kernelbot/pull/63) Refactor Leaderboard Submission Eval Code. | 2024-12-21 | arch_pipeline | Description The old leaderboard submission eval code expects a `metric()` function from the reference code specified by the leaderboard creator. Instead, we only expect the leaderboard creator to have 2 functions: the reference kernel itself, and an input data generator. We now handle checking imple... | wrapper: `src/discord-cluster-manager/cogs/leaderboard_cog.py`, `src/discord-cluster-manager/leaderboard_eval.py`<br>other: `.github/workflows/nvidia_workflow.yml` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#304](https://github.com/gpu-mode/kernelbot/pull/304) bunch of smaller improvements | 2025-06-24 | arch_pipeline | Some more cleanups and smaller improvements. * Handle KernelBotErrors in API * make db functions raise errors instead of returning none to simplify code * one less layer of function calling in discord submission flow * API now respects when bot is stopped | wrapper: `src/discord-cluster-manager/api/api_utils.py`, `src/discord-cluster-manager/api/main.py`, `src/discord-cluster-manager/cogs/admin_cog.py`, `src/discord-cluster-manager/cogs/leaderboard_cog.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#35](https://github.com/gpu-mode/kernelbot/pull/35) Feat: support cuda on modal | 2024-11-23 | arch_pipeline | Description Enables running a CUDA script on Modal provider, when provided with file that ends with `.cu` Checklist Before submitting this PR, ensure the following steps have been completed: - [x] Run the smoke test on your own server. | wrapper: `src/discord-cluster-manager/cogs/modal_cog.py`, `src/discord-cluster-manager/modal_runner.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#84](https://github.com/gpu-mode/kernelbot/pull/84) Feat/cuda lb | 2024-12-27 | arch_pipeline | Description Cuda leaderboards cc @alexzhang13 Checklist Before submitting this PR, ensure the following steps have been completed: - [x] Run the slash command `/verifyruns` on your own server. - Run the cluster bot on your server: code block - Start training runs with the slash command `/verifyruns`... | wrapper: `src/discord-cluster-manager/cogs/github_cog.py`, `src/discord-cluster-manager/leaderboard_eval.py`<br>other: `.github/workflows/nvidia_workflow.yml` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#115](https://github.com/gpu-mode/kernelbot/pull/115) Add ninja and setuptools to allow for inlining CUDA for GH runners | 2025-01-10 | arch_pipeline | Description Add ninja and setuptools to allow for inlining CUDA for GH runners. Also I changed a really minor naming issue for the GH runner. Checklist Before submitting this PR, ensure the following steps have been completed: - [x] Run the slash command `/verifyruns` on your own server. - Run the c... | wrapper: `src/discord-cluster-manager/cogs/leaderboard_cog.py`<br>other: `.github/workflows/nvidia_workflow.yml` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#348](https://github.com/gpu-mode/kernelbot/pull/348) Fix: remove tzinfo | 2025-08-31 | arch_pipeline | Description Please provide a brief summary of the changes in this pull request. Checklist Before submitting this PR, ensure the following steps have been completed: - [ ] Run the slash command `/verifyruns` on your own server. - Run the cluster bot on your server: code block - Start training runs wi... | kernel: `src/libkernelbot/leaderboard_db.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#361](https://github.com/gpu-mode/kernelbot/pull/361) fix | 2025-09-26 | arch_pipeline | Description Please provide a brief summary of the changes in this pull request. Checklist Before submitting this PR, ensure the following steps have been completed: - [ ] Run the slash command `/verifyruns` on your own server. - Run the cluster bot on your server: code block - Start training runs wi... | kernel: `src/kernelbot/cogs/admin_cog.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#306](https://github.com/gpu-mode/kernelbot/pull/306) update and pin modal images | 2025-06-24 | arch_pipeline | An attempt to provide fixed versions and update our dependencies. I tried to include a few more of the nvidia python libraries, but it's a mess (e.g., `nvmath-python' conflicts with 'cuda-core`); probably cutlass-dsl is the most important one, though, which seems to work. | wrapper: `src/discord-cluster-manager/modal_runner.py`, `src/discord-cluster-manager/modal_runner_archs.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#118](https://github.com/gpu-mode/kernelbot/pull/118) Change timing analysis for CUDA / Python eval scripts. | 2025-01-11 | arch_pipeline | Description Tested locally on both Python and CUDA leaderboards, only noticeable difference is slowdown in average times (which makes sense, we were overlapping kernel launches before). Checklist Before submitting this PR, ensure the following steps have been completed: - [x] Run the slash command `... | wrapper: `src/discord-cluster-manager/leaderboard_eval.py`<br>docs: `docs/docs/creating-a-leaderboard/cuda-creations.md`, `docs/docs/creating-a-leaderboard/python-creations.md` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#236](https://github.com/gpu-mode/kernelbot/pull/236) KernelBotError error message | 2025-04-15 | arch_pipeline | Specifically intercept uncaught KernelBotError messages and print a useful message. | wrapper: `src/discord-cluster-manager/utils.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |

### Compiler / Runtime

Use this section for: Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests.
NCU first look: Generated kernel shape, launch overhead, occupancy, register pressure, and compile-time-selected schedule.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#461](https://github.com/gpu-mode/kernelbot/pull/461) Add nvtriton (Triton TileIR backend) to helion Dockerfile | 2026-03-11 | compiler_runtime, arch_pipeline | - Install nvtriton (Triton with TileIR compiler backend) into the helion Docker image - The nvtriton wheel replaces the upstream `triton==3.6.0` that ships with PyTorch cu130 (same package name `triton`, drop-in compatible) - Sets `ENABLE_TILE=1` to activate the TileIR backend by default What is nvt... | other: `docker/helion.Dockerfile` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#112](https://github.com/gpu-mode/kernelbot/pull/112) Vectors Add - Pytorch CUDA inline and Triton example | 2025-01-10 | compiler_runtime | Description Implemented two sample of vectors add: - Pytorch CUDA online - Triton Checklist Before submitting this PR, ensure the following steps have been completed: - [ X] Run the slash command `/verifyruns` on your own server. - Run the cluster bot on your server: code block - Start training runs... | kernel: `examples/vectoradd_triton/reference.py`, `examples/vectoradd_triton/submission.py`<br>wrapper: `examples/vectoradd_cuda_inline/reference.py`, `examples/vectoradd_cuda_inline/submission.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#371](https://github.com/gpu-mode/kernelbot/pull/371) Feat: add cutedsl to templates | 2025-11-11 | compiler_runtime | Feat: add cutedsl to templates | kernel: `src/kernelbot/cogs/leaderboard_cog.py`, `src/libkernelbot/task.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#441](https://github.com/gpu-mode/kernelbot/pull/441) Add CUTLASS v4.3.5 C++ headers to Modal runner image | 2026-02-09 | compiler_runtime | Install CUTLASS C++ headers to /opt/cutlass so users can include <cutlass/...> and include <cute/...> in their submissions. Also adds a test script to validate the setup before deploying, and documents how to add new C++ deps. | wrapper: `src/runners/modal_runner.py`<br>other: `.github/workflows/claude-code-review.yml`, `.github/workflows/claude.yml` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |

### Benchmark / Test Evidence

Use this section for: Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code.
NCU first look: Use the PR's benchmark/profile command as the first replay target.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#335](https://github.com/gpu-mode/kernelbot/pull/335) Multi-GPU | 2025-08-27 | benchmark_test, memory_primitives | Implement multi-gpu tasks: design: run_eval still calls only a single instance of eval.py, but if the task is multi-gpu, eval.py will create a pool of multiple worker processes, and call the user op on each with a different rank argument. in the current code, input generation and testing is also han... | kernel: `src/libkernelbot/consts.py`, `src/libkernelbot/run_eval.py`, `src/libkernelbot/task.py`<br>test: `tests/conftest.py`, `tests/test_backend.py`, `tests/test_modal.py`, `tests/test_task.py`<br>wrapper: `examples/eval.py`, `examples/gather/reference.py`, `examples/gather/submission.py`, `examples/gather/task.py`<br>other: `examples/gather/task.yml` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#146](https://github.com/gpu-mode/kernelbot/pull/146) WIP: Tests and Benchmarks | 2025-02-08 | benchmark_test | Massive refactoring of our cuda eval template: Now supports individual test cases and benchmarks, with corresponding commands. At this point, the code is absolutely unpolished, but it serves to indicate what I'd like to achieve. Still missing: * [ ] python support * [ ] test/benchmark timeouts * [ ]... | kernel: `examples/vectoradd_py/submission_triton.py`<br>wrapper: `examples/eval.py`, `examples/identity_cuda/task.h`, `examples/identity_py/reference.py`, `examples/identity_py/task.py`<br>other: `examples/eval.cu`, `examples/identity_cuda/reference.cuh`, `examples/identity_cuda/submission.cu`, `examples/identity_cuda/task.yml` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#132](https://github.com/gpu-mode/kernelbot/pull/132) Allow arbitrary sets of files to be used in python and cuda runner code | 2025-01-15 | benchmark_test, arch_pipeline | For cuda, we specify a set of source files (to compile and link) and header files (just create them), for python a dict of python files and which of these should be executed as the main file. This will go nicely together with the changes in 130 , and enable us to, e.g., useful utilities as common fi... | wrapper: `.github/workflows/runner.py`, `scripts/ci_test_cuda.py`, `scripts/ci_test_python.py`, `scripts/local-test.py`<br>docs: `docs/docs/creating-a-leaderboard/cuda-creations.md`, `docs/docs/creating-a-leaderboard/python-creations.md`<br>other: `src/discord-cluster-manager/eval.cu` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#169](https://github.com/gpu-mode/kernelbot/pull/169) Update of result structures and eval runs | 2025-02-21 | benchmark_test, arch_pipeline | Update evalulation runs: * Attach a potential compile result to _each_ run (we might want to compile tests without `-DNDEBUG`, for example; not part of this PR, though) * record start and end time of runs * have task specific timeouts for test, benchmark, and ranked runs, with reasonable defaults (i... | wrapper: `.github/workflows/runner.py`, `scripts/ci_test_cuda.py`, `scripts/ci_test_python.py`, `src/discord-cluster-manager/cogs/github_cog.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#334](https://github.com/gpu-mode/kernelbot/pull/334) Add submission api for web | 2025-08-28 | benchmark_test | Description Add an API serve for website gpumode, the api will return sub_id and sub_job_status_id immediately after the job is enqueue with background manager, then pull the data periodically for the update. Adding queue mechnism to accept run jobs from api request /submission/ and runs the backend... | kernel: `src/kernelbot/api/api_utils.py`, `src/kernelbot/api/main.py`, `src/kernelbot/main.py`, `src/libkernelbot/backend.py`<br>test: `tests/conftest.py`, `tests/test_background_submission_manager.py`, `tests/test_leaderboard_db.py`, `tests/test_submission.py`<br>wrapper: `src/migrations/20250822_01_UtXzl-website-submission.py`<br>other: `.github/workflows/lint.yml`, `pyproject.toml`, `requirements.txt` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#470](https://github.com/gpu-mode/kernelbot/pull/470) Feat: rate limiting | 2026-03-17 | benchmark_test | Feat: rate limiting | kernel: `src/kernelbot/api/api_utils.py`, `src/kernelbot/api/main.py`, `src/kernelbot/cogs/leaderboard_cog.py`, `src/libkernelbot/backend.py`<br>test: `tests/conftest.py`, `tests/test_admin_api.py`, `tests/test_leaderboard_db.py`<br>wrapper: `src/migrations/20260317_01_rate-limits.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#152](https://github.com/gpu-mode/kernelbot/pull/152) Make run_cuda_script accept more compile flags arguments | 2025-02-01 | benchmark_test, arch_pipeline | Description * Adds additional arguments for cuda compiles that allow specifying link libraries, defines, and generic flags. * Additional verification and unit tests. The additional checks that supplied include directories actually exist required to move the modal-specific thunderkittens include into... | wrapper: `scripts/ci_test_cuda.py`, `scripts/ci_test_python.py`, `src/discord-cluster-manager/cogs/modal_cog.py`, `src/discord-cluster-manager/consts.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#468](https://github.com/gpu-mode/kernelbot/pull/468) Update runs | 2026-03-14 | benchmark_test | Update runs | kernel: `src/kernelbot/api/api_utils.py`, `src/kernelbot/api/main.py`, `src/kernelbot/cogs/admin_cog.py`, `src/kernelbot/cogs/leaderboard_cog.py`<br>test: `tests/conftest.py`, `tests/test_admin_api.py`, `tests/test_leaderboard_db.py`<br>wrapper: `src/migrations/20260311_01_closed-leaderboard-visibility.py`, `src/migrations/20260313_01_invite-multi-leaderboard.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#120](https://github.com/gpu-mode/kernelbot/pull/120) factored out run_cuda_eval, so it can be used independently of modal | 2025-01-11 | benchmark_test, arch_pipeline | split out `run_cuda_script` to make it independent of model; in particular, this allows you to run simple checks locally (if you have a working cuda installation), as the `scripts/local-test.py` script illustrates (cwd is expected to be the repository root). | wrapper: `scripts/local-test.py`, `src/discord-cluster-manager/modal_runner.py`, `src/discord-cluster-manager/modal_runner_archs.py`, `src/discord-cluster-manager/run_eval.py`<br>other: `.github/workflows/cuda_test.yml` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#125](https://github.com/gpu-mode/kernelbot/pull/125) Small changes to eval.cu | 2025-01-13 | benchmark_test, arch_pipeline | Collection of small changes to the eval.cu code: * make sure to check return codes of cuda api calls * correctly exit if there is an evaluation mismatch during the timing code * better debugging for identity example * std::move instead of copy, so we don't measure the function-call copy overhead * e... | wrapper: `scripts/ci_test_cuda.py`, `src/discord-cluster-manager/run_eval.py`<br>other: `.github/workflows/cuda_test.yml`, `examples/identity_cuda/reference.cuh`, `src/discord-cluster-manager/eval.cu` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#126](https://github.com/gpu-mode/kernelbot/pull/126) Using pipe to transfer data from runner to bot | 2025-01-13 | benchmark_test, arch_pipeline | This PR changes the mechanism by which the eval.cu script reports back to its caller. Instead of writing to stdout, we use a dedicated pipe as our communication channel. This is cleaner, IMO, as it leaves the standard io channels for the user (e.g., for debugging), and somewhat safer (simple `std::c... | wrapper: `scripts/ci_test_cuda.py`, `src/discord-cluster-manager/cogs/verify_run_cog.py`, `src/discord-cluster-manager/run_eval.py`<br>other: `src/discord-cluster-manager/eval.cu` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#322](https://github.com/gpu-mode/kernelbot/pull/322) Backend tests | 2025-08-25 | benchmark_test | Built atop the database tests, and using the same principles. The tests aren't very fancy, they'll mostly detect if we cause unintended changes to our output I think. | kernel: `src/kernelbot/cogs/leaderboard_cog.py`, `src/kernelbot/env.py`, `src/libkernelbot/backend.py`, `src/libkernelbot/leaderboard_db.py`<br>test: `.github/workflows/testing.yml`, `unit-tests/conftest.py`, `unit-tests/test_backend.py`, `unit-tests/test_leaderboard_db.py`<br>other: `.github/workflows/coverage.yml`, `pyproject.toml` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#368](https://github.com/gpu-mode/kernelbot/pull/368) [WIP] Ncu profile | 2025-11-10 | benchmark_test | [WIP] Ncu profile | kernel: `src/kernelbot/api/api_utils.py`, `src/kernelbot/discord_reporter.py`, `src/kernelbot/discord_utils.py`, `src/libkernelbot/launchers/github.py`<br>test: `tests/test_report.py`<br>wrapper: `examples/eval.py`, `scripts/ci_test_cuda.py`, `scripts/ci_test_python.py`, `src/runners/modal_runner.py`<br>other: `.github/workflows/nvidia-arc-health.yml`, `.github/workflows/nvidia_workflow.yml` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#333](https://github.com/gpu-mode/kernelbot/pull/333) Modal tests | 2025-08-26 | benchmark_test | Basic, discord-free testing that modal submissions work. We probably want to add some example scripts that use the additional libraries that we install (triton, tinygrad, cuda-python), to verify those work correctly, too, but that should be a pretty straightforward extension. | kernel: `src/libkernelbot/launchers/modal.py`<br>test: `.github/workflows/testing.yml`, `tests/conftest.py`, `tests/test_backend.py`, `tests/test_leaderboard_db.py`<br>wrapper: `examples/vectoradd_py/submission_cuda_inline.py`, `src/runners/modal_runner.py`<br>other: `pyproject.toml` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#408](https://github.com/gpu-mode/kernelbot/pull/408) Add API-only mode and admin CLI support | 2026-02-01 | benchmark_test | This PR needs to be reviewed and tested in conjunction with https://github.com/gpu-mode/popcorn-cli/pull/29 It's now possible to pass an `--api-only` flag to start the FastAPI server without Discord credentials. Which means admins are now determined by a hardcoded token, locally you can set this to... | kernel: `src/kernelbot/api/main.py`, `src/kernelbot/cogs/admin_cog.py`, `src/kernelbot/env.py`, `src/kernelbot/main.py`<br>test: `SKILLS/test_bot.md`, `tests/test_admin_api.py`<br>docs: `CLAUDE.md` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#459](https://github.com/gpu-mode/kernelbot/pull/459) Add Hugging Face dataset export flow for competition submissions | 2026-03-13 | benchmark_test | Add Hugging Face dataset export flow for competition submissions This PR adds a Hugging Face export path for competition submissions. The goal is to make it easy to keep a private, continuously updated dataset of live competition submissions, while also giving admins a clean way to publish final com... | kernel: `src/kernelbot/api/main.py`, `src/kernelbot/cogs/admin_cog.py`, `src/kernelbot/env.py`, `src/libkernelbot/hf_export.py`<br>test: `tests/test_admin_api.py`, `tests/test_hf_export.py`<br>other: `pyproject.toml`, `src/libkernelbot/sql/get_hf_export_rows.sql`, `uv.lock` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#471](https://github.com/gpu-mode/kernelbot/pull/471) Add user ban system for blocking submissions | 2026-03-18 | benchmark_test | - Adds `is_banned` boolean column to `user_info` table (new yoyo migration) - Ban check in `prepare_submission()` — the single chokepoint that blocks all 3 entry points (Discord, CLI API, Web API) - Discord admin commands: `/admin ban` and `/admin unban` - API admin endpoints: `POST /admin/ban/{user... | kernel: `src/kernelbot/api/main.py`, `src/kernelbot/cogs/admin_cog.py`, `src/libkernelbot/leaderboard_db.py`, `src/libkernelbot/submission.py`<br>test: `tests/test_submission.py`<br>wrapper: `src/migrations/20260318_01_ban-user.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#279](https://github.com/gpu-mode/kernelbot/pull/279) pytorch-level profiler | 2025-05-13 | benchmark_test, arch_pipeline | Enables running the user's code under torch.profiler to get the duration of individual kernel calls (though not anything useful about what's happening inside a kernel). formatting in discord is pretty bad, though, because the tables are too wide and get word-wrapped. The PR also contains some unrela... | wrapper: `examples/eval.py`, `src/discord-cluster-manager/cogs/leaderboard_cog.py`, `src/discord-cluster-manager/report.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#378](https://github.com/gpu-mode/kernelbot/pull/378) Add hostname to sysinfo | 2025-12-14 | benchmark_test | basically what it says, can help us isolate faulty gpus/nodes | kernel: `src/libkernelbot/report.py`, `src/libkernelbot/run_eval.py`, `src/libkernelbot/submission.py`<br>test: `tests/test_backend.py`, `tests/test_report.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#445](https://github.com/gpu-mode/kernelbot/pull/445) Add optional leaderboard_name filter to admin show-stats | 2026-02-11 | benchmark_test | Support filtering stats by a specific leaderboard in both the Discord command and the API endpoint. Defaults to all leaderboards when omitted. | kernel: `src/kernelbot/api/main.py`, `src/kernelbot/cogs/admin_cog.py`, `src/libkernelbot/leaderboard_db.py`<br>test: `tests/test_admin_api.py`, `tests/test_leaderboard_db.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#473](https://github.com/gpu-mode/kernelbot/pull/473) Kernelguard pre-queue check addition | 2026-04-02 | benchmark_test | Integrates KernelGuard as a submission pre-check gate that detects and rejects exploit submissions before GPU execution. The check runs at the API boundary, before queue enqueue on the async path and before GPU dispatch on the sync path, so blocked payloads never consume worker resources. Flagged su... | kernel: `src/kernelbot/api/api_utils.py`, `src/kernelbot/api/main.py`, `src/libkernelbot/backend.py`, `src/libkernelbot/background_submission_manager.py`<br>test: `tests/test_backend.py`, `tests/test_background_submission_manager.py`, `tests/test_kernelguard.py`<br>other: `pyproject.toml`, `uv.lock` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#319](https://github.com/gpu-mode/kernelbot/pull/319) Adding unit tests | 2025-07-27 | benchmark_test | This PR adds a bunch of unit tests for libkernelbot. While doing so, slight improvements (error checking) were made to submission.py. This doesn't cover all the files in libkernelbot, but `consts.py` and `db_types.py` just define dataclasses/enums so there's not much to test there, and `leaderboard_... | kernel: `src/libkernelbot/submission.py`, `src/libkernelbot/task.py`, `src/libkernelbot/utils.py`<br>test: `unit-tests/test_report.py`, `unit-tests/test_submission.py`, `unit-tests/test_task.py`, `unit-tests/test_utils.py`<br>other: `.github/workflows/runner_ci.yml` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#354](https://github.com/gpu-mode/kernelbot/pull/354) Profiling Infrastructure | 2025-09-10 | benchmark_test | Description This PR adds some infrastructure for dealing with profiling data. Basically, the idea is that any data which is placed in the `profile_data/` directory in the github runner is exported (when `EvalResult.profile_result` is set) to the user via discord. As discord attachments could not han... | kernel: `src/kernelbot/discord_reporter.py`, `src/libkernelbot/launchers/github.py`, `src/libkernelbot/report.py`, `src/libkernelbot/run_eval.py`<br>test: `tests/test_backend.py`, `tests/test_report.py`<br>wrapper: `scripts/ci_test_cuda.py`, `scripts/ci_test_python.py`<br>other: `.github/workflows/amd_workflow.yml`, `.github/workflows/nvidia_workflow.yml` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#412](https://github.com/gpu-mode/kernelbot/pull/412) Add user submission management API endpoints | 2026-02-06 | benchmark_test | Should be reviewed in conjunction with https://github.com/gpu-mode/popcorn-cli/pull/32 - Add get_user_submissions() DB method to query user's submissions - Add GET /user/submissions endpoint to list user's submissions - Add GET /user/submissions/{id} endpoint to view submission with code - Add DELET... | kernel: `src/kernelbot/api/main.py`, `src/libkernelbot/leaderboard_db.py`, `src/libkernelbot/problem_sync.py`<br>test: `tests/test_leaderboard_db.py`<br>docs: `CLAUDE.md` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#436](https://github.com/gpu-mode/kernelbot/pull/436) Add per-user submission rate limit (1 per hour) | 2026-02-07 | benchmark_test | Enforce a global per-user rate limit of 1 submission per hour across all leaderboards and modes. The 429 response suggests using the NVIDIA runner instead of Modal for faster iteration. | kernel: `src/kernelbot/api/api_utils.py`, `src/libkernelbot/leaderboard_db.py`<br>test: `tests/test_admin_api.py`, `tests/test_leaderboard_db.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#474](https://github.com/gpu-mode/kernelbot/pull/474) Add admin bulk submission delete endpoint | 2026-04-01 | benchmark_test | - add an admin API to delete all submissions for a given leaderboard ID and username - delete dependent submission job status rows and runs before deleting submissions - add API and DB tests covering successful deletion and missing leaderboard handling Validation - uv run --extra dev pytest tests/te... | kernel: `src/kernelbot/api/main.py`, `src/libkernelbot/leaderboard_db.py`<br>test: `tests/test_admin_api.py`, `tests/test_leaderboard_db.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#321](https://github.com/gpu-mode/kernelbot/pull/321) enable coverage reports | 2025-07-28 | benchmark_test | Enables generation of coverage reports and extends some testing so that the targeted files get >90% coverage. | kernel: `src/libkernelbot/report.py`, `src/libkernelbot/submission.py`, `src/libkernelbot/task.py`<br>test: `unit-tests/test_report.py`, `unit-tests/test_submission.py`, `unit-tests/test_task.py`, `unit-tests/test_utils.py`<br>other: `.github/workflows/coverage.yml`, `.github/workflows/runner_ci.yml`, `.github/workflows/unit_tests.yml`, `pyproject.toml` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#345](https://github.com/gpu-mode/kernelbot/pull/345) Forward fix the web api pr | 2025-08-28 | benchmark_test | Description Forward fix the web api pr dependency issue 1. for unittest use monkeypatch to gracefully introduce dummy env var 2. add dummy report for manager. Checklist Before submitting this PR, ensure the following steps have been completed: - [ ] Run the slash command `/verifyruns` on your own se... | kernel: `src/libkernelbot/background_submission_manager.py`<br>test: `tests/conftest.py`, `tests/test_validate_user_header.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#477](https://github.com/gpu-mode/kernelbot/pull/477) [codex] add admin leaderboard submission listing | 2026-04-07 | benchmark_test | What changed Adds an admin-only API endpoint to list all submissions for a leaderboard with stable metadata needed for manual cleanup and ID-by-ID deletion. - adds `GET /admin/leaderboards/{leaderboard_name}/submissions` - adds `LeaderboardDB.get_leaderboard_submission_history(...)` - adds admin API... | kernel: `src/kernelbot/api/main.py`, `src/libkernelbot/leaderboard_db.py`<br>test: `tests/test_admin_api.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#232](https://github.com/gpu-mode/kernelbot/pull/232) Hardening | 2025-04-08 | benchmark_test | Prevent us from accepting several classes of cheat solutions: * solutions that modify the input data * solutions that attempt to write to our file descriptor * solutions that try to hack our rng * solutions that try to cache the benchmark result Preventing directly messing with the interpreter state... | wrapper: `examples/eval.py`, `examples/identity_py/cheat-fd.py`, `examples/identity_py/cheat-input.py`, `examples/identity_py/cheat-rng.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#320](https://github.com/gpu-mode/kernelbot/pull/320) Database tests | 2025-08-09 | benchmark_test | Uses docker-compose to start up a container with the db, and run the yoyo migrations, so we can access a real database for our testing. All of this is wrapped inside a pytest fixture, so for local testing you can just run pytest as normally. However, this means that pytest now takes a few seconds to... | kernel: `src/libkernelbot/leaderboard_db.py`<br>test: `unit-tests/test_leaderboard_db.py`<br>wrapper: `src/migrations/20250728_01_Q3jso-fix-code-table.py`<br>other: `docker-compose.test.yml`, `pyproject.toml` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#360](https://github.com/gpu-mode/kernelbot/pull/360) Enhance GPU health check with distributed testing | 2025-09-24 | benchmark_test | Increased timeout for health check job and added distributed health check step to verify GPU availability and initialization. Description Please provide a brief summary of the changes in this pull request. Checklist Before submitting this PR, ensure the following steps have been completed: - [ ] Run... | kernel: `src/libkernelbot/launchers/github.py`<br>test: `scripts/test_distributed.py`, `tests/test_github.py`<br>other: `.github/workflows/amd-health.yml` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#340](https://github.com/gpu-mode/kernelbot/pull/340) github tests | 2025-09-16 | benchmark_test | Adds github-runner runs to our CI setup. | kernel: `src/libkernelbot/launchers/github.py`<br>test: `.github/workflows/testing.yml`, `tests/test_github.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#390](https://github.com/gpu-mode/kernelbot/pull/390) Change runner environment for CUDA jobs | 2026-01-07 | benchmark_test | Temporarily moving our CI to B200 machines since H100 machines are no longer available | wrapper: `scripts/ci_test_cuda.py`<br>other: `.github/workflows/runner_ci.yml` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |

### Other Kernel Cases

Use this section for: Use the PR as grounded prior art; inspect diff, linked tests, and benchmark evidence before applying the idea.
NCU first look: Choose metrics based on the changed kernel family after opening the diff.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#374](https://github.com/gpu-mode/kernelbot/pull/374) Feat disallow streams | 2025-11-27 | kernel_other | Feat disallow streams | kernel: `src/kernelbot/api/api_utils.py`, `src/kernelbot/cogs/leaderboard_cog.py`, `src/libkernelbot/backend.py`, `src/libkernelbot/report.py` | Use the PR as grounded prior art; inspect diff, linked tests, and benchmark evidence before applying the idea. |
| [#346](https://github.com/gpu-mode/kernelbot/pull/346) Feat: add mi300x8 | 2025-08-28 | kernel_other | Adds runners for MI300x8 | kernel: `src/libkernelbot/consts.py`, `src/libkernelbot/launchers/github.py`, `src/libkernelbot/task.py` | Use the PR as grounded prior art; inspect diff, linked tests, and benchmark evidence before applying the idea. |
| [#317](https://github.com/gpu-mode/kernelbot/pull/317) Smaller refactorings | 2025-07-26 | kernel_other | A bunch of changes I made when looking into milestones, but which are independent of the actual milestone implementation. | kernel: `src/libkernelbot/backend.py`, `src/libkernelbot/db_types.py`, `src/libkernelbot/leaderboard_db.py`, `src/libkernelbot/submission.py`<br>wrapper: `examples/eval.py`, `examples/utils.py` | Use the PR as grounded prior art; inspect diff, linked tests, and benchmark evidence before applying the idea. |
| [#373](https://github.com/gpu-mode/kernelbot/pull/373) Feat: add stream warning | 2025-11-27 | kernel_other | Feat: add stream warning | kernel: `src/libkernelbot/backend.py`, `src/libkernelbot/report.py` | Use the PR as grounded prior art; inspect diff, linked tests, and benchmark evidence before applying the idea. |
| [#376](https://github.com/gpu-mode/kernelbot/pull/376) Fix deadline | 2025-11-30 | kernel_other | Fix deadline | kernel: `src/libkernelbot/submission.py` | Use the PR as grounded prior art; inspect diff, linked tests, and benchmark evidence before applying the idea. |
| [#377](https://github.com/gpu-mode/kernelbot/pull/377) Fix deadline check | 2025-11-30 | kernel_other | Fix deadline check | kernel: `src/libkernelbot/submission.py` | Use the PR as grounded prior art; inspect diff, linked tests, and benchmark evidence before applying the idea. |
| [#381](https://github.com/gpu-mode/kernelbot/pull/381) Feat: add longer polling | 2025-12-17 | kernel_other | Feat: add longer polling | kernel: `src/libkernelbot/launchers/github.py` | Use the PR as grounded prior art; inspect diff, linked tests, and benchmark evidence before applying the idea. |
| [#385](https://github.com/gpu-mode/kernelbot/pull/385) Feat: round robin keys | 2025-12-20 | kernel_other | Feat: round robin keys | kernel: `src/libkernelbot/launchers/github.py` | Use the PR as grounded prior art; inspect diff, linked tests, and benchmark evidence before applying the idea. |
| [#394](https://github.com/gpu-mode/kernelbot/pull/394) leaderboard.submission add status columns | 2026-01-12 | kernel_other | add status column for submission table this is mainly used to define the status of the submission, right now we only have two conditions: - ACTIVE: this is default value when a submission record is created, active submission is considered valid submission for rankings in competitions - INACTIVE: thi... | wrapper: `src/migrations/20260108_01_gzSm3-add-submission-status.py` | Use the PR as grounded prior art; inspect diff, linked tests, and benchmark evidence before applying the idea. |
| [#403](https://github.com/gpu-mode/kernelbot/pull/403) Fix: too long body | 2026-01-25 | kernel_other | Fix: too long body | kernel: `src/kernelbot/discord_utils.py` | Use the PR as grounded prior art; inspect diff, linked tests, and benchmark evidence before applying the idea. |
| [#446](https://github.com/gpu-mode/kernelbot/pull/446) Add /health endpoint to API | 2026-02-12 | kernel_other | Add /health endpoint to API | kernel: `src/kernelbot/api/main.py` | Use the PR as grounded prior art; inspect diff, linked tests, and benchmark evidence before applying the idea. |
| [#451](https://github.com/gpu-mode/kernelbot/pull/451) add index for query leaderboard ranking | 2026-02-26 | kernel_other | Overview add index for fast leaderboard query for active ranking. This should works well with query https://github.com/gpu-mode/kernelboard/blob/main/kernelboard/api/leaderboard_summaries.pyL63 partial idx for runs Notice we have partial idx for runs, it still works for additional condition Adding M... | wrapper: `src/migrations/20260226_01_WgYAV-queryindex.py` | Use the PR as grounded prior art; inspect diff, linked tests, and benchmark evidence before applying the idea. |
| [#455](https://github.com/gpu-mode/kernelbot/pull/455) Mi355 | 2026-03-04 | kernel_other | Mi355 | kernel: `src/libkernelbot/consts.py`, `src/libkernelbot/launchers/github.py`<br>other: `.github/workflows/amd_workflow.yml`, `.github/workflows/publish_amd_docker.yml`, `docker/amd-docker.Dockerfile` | Use the PR as grounded prior art; inspect diff, linked tests, and benchmark evidence before applying the idea. |
| [#325](https://github.com/gpu-mode/kernelbot/pull/325) leaderboard submit should show active leaderboards | 2025-08-09 | kernel_other | leaderboard submit should show active leaderboards | kernel: `src/libkernelbot/leaderboard_db.py` | Use the PR as grounded prior art; inspect diff, linked tests, and benchmark evidence before applying the idea. |
| [#383](https://github.com/gpu-mode/kernelbot/pull/383) Patch create_dispatch | 2025-12-19 | kernel_other | github decided to return 200 instead of 204 as success now, lib isn't updated | kernel: `src/libkernelbot/launchers/github.py` | Use the PR as grounded prior art; inspect diff, linked tests, and benchmark evidence before applying the idea. |
| [#384](https://github.com/gpu-mode/kernelbot/pull/384) Patch create dispatch | 2025-12-19 | kernel_other | Patch create dispatch | kernel: `src/libkernelbot/launchers/github.py` | Use the PR as grounded prior art; inspect diff, linked tests, and benchmark evidence before applying the idea. |
| [#396](https://github.com/gpu-mode/kernelbot/pull/396) dont block profiling on cli | 2026-01-13 | kernel_other | dont block profiling on cli | kernel: `src/kernelbot/api/api_utils.py` | Use the PR as grounded prior art; inspect diff, linked tests, and benchmark evidence before applying the idea. |
| [#469](https://github.com/gpu-mode/kernelbot/pull/469) Update hf export data timings | 2026-03-16 | kernel_other | Update hf export data timings because of frequent deployments task is missed each day with 24 hours loops | kernel: `src/kernelbot/cogs/admin_cog.py` | Use the PR as grounded prior art; inspect diff, linked tests, and benchmark evidence before applying the idea. |

## Per-PR Ledger Fields

When using an idea from this page, add one row to `artifacts/source-idea-ledger.md` with:

| Field | Value to record |
| --- | --- |
| Source key | `<repo>#<pr-number>` |
| Code evidence | Kernel, wrapper, benchmark, and test paths opened from the PR diff |
| Hypothesis | The concrete optimization idea derived from the PR |
| First experiment | Candidate version and benchmark shape set |
| Result | Correctness, geomean, best/worst cases, and NCU digest path |
| Do-not-reread key | Same as source key unless a single PR yields multiple independent ideas |

## How To Use This Page

- During the initial knowledge pass, read the category matching the target kernel and copy PR URL, changed paths, and hypothesis into the source idea ledger.
- During plateau expansion, choose PRs not already present in ledger do-not-reread keys; inspect the diff, linked issue, changed tests, and benchmark files before using the idea.
- Treat PR code as baseline/prior art unless the task and license allow copying or adapting it. When copied, record exact PR, commit, files, notice, and first delta.

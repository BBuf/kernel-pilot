# FlashAttention PR Knowledge Notes

Repository: <https://github.com/Dao-AILab/flash-attention>

This page is the production-PR layer for kernel-knowledge. It favors merged PRs that changed kernels, dispatch, JIT/runtime integration, tuning policy, tests, benchmarks, or profiling evidence. Release, CI-only, formatting, pure version-bump, and non-target-backend PRs are filtered out.

## Repository Source Scan

Read these source regions before opening individual PR diffs:

- `csrc/flash_attn/src`
- `csrc/flash_attn/src/flash_fwd_kernel.h`
- `flash_attn/cute`
- `hopper`
- `benchmarks/benchmark_attn.py`
- `benchmarks/bench_sm90.py`

## Coverage Summary

| Category | CUDA-kernel PRs |
| --- | ---: |
| Attention / KV / Decode | 70 |
| Memory / Primitives | 4 |
| Scheduler / Autotune | 16 |
| Architecture / Pipeline | 47 |
| Compiler / Runtime | 40 |
| Benchmark / Test Evidence | 17 |

## Pull Request Case Notes

### Attention / KV / Decode

Use this section for: Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization.
NCU first look: Long scoreboard, L2/DRAM traffic, global-load sectors, tensor pipe %, and shared-memory bank conflicts.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#2441](https://github.com/Dao-AILab/flash-attention/pull/2441) [Cute,Sm100,Fwd] add MLA 64/512 with topk sparsity for MQA 128 heads | 2026-04-15 | attention_kv, arch_pipeline, benchmark_test, moe_routing, scheduler_autotune | Initial saturating decode perf numbers, deepseek 64/512 shape and topk length = 2048. code block Now added: 1. persistent kernel for perf ✅ 2. expose through interface ✅ 3. handle seqlen_q > seqlen_k when causal (kernel currently hangs) ✅ Tests added in `test_flash_attn_mla_absorbed` and `test_flash... | kernel: `flash_attn/cute/cute_dsl_utils.py`, `flash_attn/cute/flash_fwd_mla_sm100.py`, `flash_attn/cute/interface.py`, `flash_attn/cute/mask.py`<br>benchmark: `benchmarks/benchmark_attn.py`, `flash_attn/cute/bench_utils.py`<br>test: `flash_attn/cute/testing.py`, `tests/cute/test_flash_attn.py`<br>other: `.pre-commit-config.yaml` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1236](https://github.com/Dao-AILab/flash-attention/pull/1236) FA3 kvcache + split kv + gqa parallelization | 2024-10-15 | attention_kv, arch_pipeline, scheduler_autotune, benchmark_test | This PR adds split KV ("Flash decoding") and GQA parallelization improvements for FA3. Some essential parts of the KV cache API are added as well, including the `cache_seqlens` and `cache_batch_idx` arguments. Up to 15x improvement over FA2 measured on my H100 PCIe in exceptional cases, e.g. code bl... | benchmark: `hopper/benchmark_flash_attention_fp8.py`, `hopper/benchmark_split_kv.py`<br>wrapper: `hopper/block_info.h`, `hopper/combine.h`, `hopper/epilogue_fwd_sm90_tma.hpp`, `hopper/flash.h`<br>other: `hopper/flash_fwd_hdim128_bf16_gqa16_sm90.cu`, `hopper/flash_fwd_hdim128_bf16_gqa2_sm90.cu`, `hopper/flash_fwd_hdim128_bf16_gqa32_sm90.cu`, `hopper/flash_fwd_hdim128_bf16_gqa4_sm90.cu` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2043](https://github.com/Dao-AILab/flash-attention/pull/2043) [Cute,Fwd] Extend score_mod to variable sequence length | 2025-12-15 | attention_kv, arch_pipeline, compiler_runtime, scheduler_autotune, benchmark_test | This PR extends the flexattention-style `score_mod` support from 1840 to the variable sequence length setting. The following cases are now covered: - When `q` and/or `k` are varlen in any combination (TF, FT, TT, FF) - PackGQA for varlen on SM90 only This does incur a slight complexity overhead in d... | kernel: `flash_attn/cute/flash_fwd.py`, `flash_attn/cute/flash_fwd_sm100.py`, `flash_attn/cute/interface.py`, `flash_attn/cute/seqlen_info.py`<br>test: `tests/cute/score_mod_definitions.py`, `tests/cute/test_flash_attn.py`, `tests/cute/test_score_mod.py`, `tests/cute/test_score_mod_varlen.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2412](https://github.com/Dao-AILab/flash-attention/pull/2412) Feat([FA4][CUTE DSL]) Add head_dim=256 support (forward + backward) | 2026-04-23 | attention_kv, compiler_runtime, scheduler_autotune, benchmark_test | This PR adds `head_dim=256` support to the FA4 FlashAttention implementation built with the CUTLASS CUTE DSL. What’s included - A dedicated kernel path specialized for `head_dim=256` - Forward and backward support - No API changes for existing head dimensions Motivation - `head_dim=256` is common in... | kernel: `flash_attn/cute/flash_bwd_preprocess.py`, `flash_attn/cute/interface.py`, `flash_attn/cute/mask.py`, `flash_attn/cute/sm100_hd256_2cta_fmha_backward.py`<br>test: `tests/cute/test_flash_attn.py`, `tests/cute/test_flash_attn_varlen.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1173](https://github.com/Dao-AILab/flash-attention/pull/1173) FA3 FP8 qkv descales + restore max offset for h128 causal + added sync for producer WG | 2024-08-25 | attention_kv, arch_pipeline, benchmark_test, gemm_quant | This PR adds qkv descales for the fp8 kernel, added warpgroup sync in producer in do_transpose_V lambda, and slightly altered schedule for h128 causal fp8 that allows for using max offset to fill fp8 range before 2nd gemm with minimal perf penalty (<10 tflops measured on my H100 PCIe, log attached).... | kernel: `hopper/flash_fwd_kernel.h`, `hopper/kernel_traits.h`<br>benchmark: `hopper/benchmark_flash_attention_fp8.py`<br>test: `hopper/test_flash_attn.py`<br>wrapper: `hopper/flash.h`, `hopper/flash_api.cpp`, `hopper/flash_attn_interface.py`, `hopper/flash_fwd_launch_template.h` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2253](https://github.com/Dao-AILab/flash-attention/pull/2253) Deterministic backward for blocksparse impl | 2026-05-04 | attention_kv, memory_primitives, arch_pipeline, compiler_runtime, benchmark_test | Stacked PRs: * 2512 * __->__2253 --- --- --- Deterministic backward for blocksparse impl PyTorch PR: https://github.com/pytorch/pytorch/pull/174813 This make it possible to run bwd with blocksparse tensors w/ determinism. At a very high level, the current implementation needs two things 1. For every... | kernel: `flash_attn/cute/block_info.py`, `flash_attn/cute/block_sparse_utils.py`, `flash_attn/cute/block_sparsity.py`, `flash_attn/cute/cache_utils.py`<br>test: `tests/cute/test_mask_mod.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2186](https://github.com/Dao-AILab/flash-attention/pull/2186) [Cute,Fwd,Sm100] support irregular qhead / kvhead ratios | 2026-03-20 | attention_kv, arch_pipeline, compiler_runtime, benchmark_test | This PR enables the Pack GQA optimization for attention head group sizes which don't divide `m_block_size` by using cp.async to load Q. Previously, `pack_gqa` would be set to false in the SM100 interface whenever `nheads / nheads_kv` does not divide `128`. This offers significant speedup for autoreg... | kernel: `flash_attn/cute/flash_fwd_sm100.py`, `flash_attn/cute/interface.py`<br>test: `tests/cute/test_flash_attn_fast.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1840](https://github.com/Dao-AILab/flash-attention/pull/1840) Refactors to enable FlexAttention | 2025-10-08 | attention_kv, arch_pipeline, benchmark_test, compiler_runtime | This currently only enables score_mods with inline global loads. For a majority of score_mods I have found that unless you are indexing along the kv_index dim the slow down due to global loads is too high compared to the additional math inlined. KV_indexing however is a major performance hit ~700 Tf... | kernel: `flash_attn/cute/flash_fwd.py`, `flash_attn/cute/flash_fwd_sm100.py`, `flash_attn/cute/interface.py`, `flash_attn/cute/softmax.py`<br>test: `tests/cute/test_score_mod.py`<br>other: `.gitignore` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2020](https://github.com/Dao-AILab/flash-attention/pull/2020) add fastdivmod for oob reads in mask_mods | 2025-11-21 | attention_kv, arch_pipeline, compiler_runtime, benchmark_test | Follow up PR , replays same logic as score_mod If I manually set this code block <img width="941" height="155" alt="Screenshot 2025-11-18 at 7 51 33 PM" src="https://github.com/user-attachments/assets/724501ee-35d5-4844-9238-622ba4ae110a" /> With this PR I get: <img width="951" height="596" alt="Scr... | kernel: `flash_attn/cute/block_sparse_utils.py`, `flash_attn/cute/flash_fwd.py`, `flash_attn/cute/flash_fwd_sm100.py`, `flash_attn/cute/mask.py`<br>test: `tests/cute/test_mask_mod.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2098](https://github.com/Dao-AILab/flash-attention/pull/2098) Add pack-gqa fwd support for sparse impl w/ broadcasted H dim | 2026-01-04 | attention_kv, arch_pipeline, compiler_runtime, benchmark_test | See title. Since we would need to iterate over the union of sparse blocks if we didnt broadcast on H dim, we only support this case in fwd. | kernel: `flash_attn/cute/block_sparse_utils.py`, `flash_attn/cute/flash_fwd_sm100.py`, `flash_attn/cute/interface.py`, `flash_attn/cute/mask.py`<br>test: `tests/cute/test_mask_mod.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2488](https://github.com/Dao-AILab/flash-attention/pull/2488) [hd256] Improve forward kernel with exp2 FMA emulation (3% to 9% performance gain) | 2026-04-28 | attention_kv, arch_pipeline, benchmark_test, scheduler_autotune, compiler_runtime | Change Replace a fraction of hardware `exp2` (SFU) instructions with a polynomial FMA emulation (`ex2_emulation_2`) in the softmax P-tile computation. The key insight: SM100's SFU throughput is a bottleneck for hdim=256 due to the large tile size. By substituting 3 out of every 4 `exp2` calls (`ex2_... | kernel: `flash_attn/cute/flash_fwd_sm100.py`, `flash_attn/cute/sm100_hd256_2cta_fmha_forward.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2338](https://github.com/Dao-AILab/flash-attention/pull/2338) [Fwd,SM100,CuTe] Fix split KV OOM with diff headdim + fix SM100 kwarg mismatch | 2026-03-12 | attention_kv, arch_pipeline, compiler_runtime, scheduler_autotune | Using split KV with diff headdim (e.g. 192/128 for DeepSeek MLA prefill) exceeds SMEM due to float32 partial output doubling the O buffer size. This PR reduces tile size or disables splitting in that case, with a heuristic to ensure optimal performance. Also fixes a bug introduced in 99d0148f where... | kernel: `flash_attn/cute/flash_fwd_sm100.py`, `flash_attn/cute/interface.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2527](https://github.com/Dao-AILab/flash-attention/pull/2527) Fix test_flash_attn_fast varlen call after qv positional insert | 2026-04-30 | attention_kv, benchmark_test, scheduler_autotune, compiler_runtime | - `flash_attn_varlen_func` now takes `qv` as its 4th positional parameter, which shifted the existing positional call in `tests/cute/test_flash_attn_fast.py` so `cu_seqlens_q/k` and `max_seqlen_q/k` got bound to the wrong slots. - This caused 132 `AttributeError: 'int' object has no attribute 'shape... | test: `tests/cute/test_flash_attn_fast.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1999](https://github.com/Dao-AILab/flash-attention/pull/1999) [Cute,Fwd,Sm100] Support paged attention | 2025-11-14 | attention_kv, arch_pipeline, compiler_runtime | This feature aims to support arbitrary page sizes in the FA4 forward kernel. The current implementation adapts the implementation of paged attention from `paged_kv.h`. Optimizations not yet adapted: - Using thread 0 copy atom to resolve some values at compile-time - Sharded pointer computation + war... | kernel: `flash_attn/cute/flash_fwd_sm100.py`, `flash_attn/cute/interface.py`, `flash_attn/cute/mask.py`, `flash_attn/cute/paged_kv.py`<br>test: `tests/cute/test_flash_attn.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2411](https://github.com/Dao-AILab/flash-attention/pull/2411) Various bug fixes / enable subtile > 2 | 2026-03-30 | attention_kv, arch_pipeline, compiler_runtime, benchmark_test | Various bug fixes / enable subtile > 2 1. For small headdim we were expanding up to block_m 192. For the default block-q of 128. Update to take the block_sparse q size for both fwd and bwd. 2. added `get_sparse_q_block_size` this ideally just returns None if no sparsity or the set size from input. I... | kernel: `flash_attn/cute/block_sparse_utils.py`, `flash_attn/cute/block_sparsity.py`, `flash_attn/cute/flash_bwd_sm90.py`, `flash_attn/cute/interface.py`<br>test: `tests/cute/test_mask_mod.py`, `tests/cute/test_score_mod.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2489](https://github.com/Dao-AILab/flash-attention/pull/2489) [hd256] Add TMA paged KV support to SM100 2CTA forward kernel | 2026-05-01 | attention_kv, arch_pipeline, scheduler_autotune | Change Add paged KV support to the SM100 hd256 2CTA forward kernel. The paged path reuses the dense TMA load path. Logical KV blocks are remapped to physical page indices through the page table at load time, so each page maps to exactly one TMA tile. **Constraint:** `page_size` must equal `tile_n =... | kernel: `flash_attn/cute/interface.py`, `flash_attn/cute/paged_kv.py`, `flash_attn/cute/sm100_hd256_2cta_fmha_forward.py`<br>test: `tests/cute/test_flash_attn.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2333](https://github.com/Dao-AILab/flash-attention/pull/2333) Add SM120 varlen attention support | 2026-03-13 | attention_kv, scheduler_autotune, arch_pipeline, compiler_runtime | - Add variable-length (`flash_attn_varlen_func`) support for SM120 (Blackwell GeForce / DGX Spark) - Fix backward tile size mismatch in `SeqlenInfoQK.create` that caused incorrect dQ gradients - Fix forward crash from OOB `cu_seqlens` reads by wasted varlen scheduler grid tiles Depends on 2329 (forw... | kernel: `flash_attn/cute/flash_fwd.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1268](https://github.com/Dao-AILab/flash-attention/pull/1268) Paged Attention support for FA3 | 2024-11-10 | attention_kv, arch_pipeline | Adding support for Paged Attention / block table to the Flash-Attention 3 Kernel. Limits: - ~~Not yet integrated with 1236 which modifes the same code (and more).~~ - No fp8 support yet - as that support also seems missing in the non-paged varlen version so far. Test Plan: code block **Update** - Re... | test: `hopper/test_flash_attn.py`<br>wrapper: `hopper/copy_paged_sm90_tma.hpp`, `hopper/flash.h`, `hopper/flash_api.cpp`, `hopper/flash_attn_interface.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2239](https://github.com/Dao-AILab/flash-attention/pull/2239) Add `FLASH_ATTENTION_TRITON_AMD_CONFIG_JSON` env var support | 2026-02-07 | attention_kv, compiler_runtime, scheduler_autotune | Add an env var for configuring `attn_fwd` triton.Config. This allows setting of more optimal config without having to enable autotuning, which has issues. Motivation With autotuning disabled the current default configs are sub-optimal, at least for my gfx1100 but probably for other cards too. I get... | kernel: `flash_attn/flash_attn_triton_amd/fwd_prefill.py`, `flash_attn/flash_attn_triton_amd/utils.py`<br>docs: `README.md` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2303](https://github.com/Dao-AILab/flash-attention/pull/2303) [Cute,Fwd,Sm100] fix paged kv | 2026-03-03 | attention_kv, arch_pipeline, compiler_runtime | Fix following bugs for paged kv 1. invalid reg alloc for cpasync paged kv (reduced correction regs for this) 2. tx count assert check 3. 2cta disabled for cpasync paged kv 4. separate tma warp and load warps thread cooperative groups | kernel: `flash_attn/cute/flash_fwd_sm100.py`, `flash_attn/cute/interface.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2360](https://github.com/Dao-AILab/flash-attention/pull/2360) [Fwd,Sm90] Add paged KV attention support (tma and cp.async) | 2026-03-18 | attention_kv, arch_pipeline | Add paged KV cache support for SM90 forward via two paths: - TMA path (page_size == n_block_size): fast, uses TMA with page index indirection - cp.async path (page_size != n_block_size): general, uses PagedKVManager with PipelineAsync Also fixes hardcoded `128` → `tile_n` in compile key for paged KV... | kernel: `flash_attn/cute/block_sparse_utils.py`, `flash_attn/cute/flash_fwd_sm90.py`, `flash_attn/cute/interface.py`, `flash_attn/cute/paged_kv.py`<br>test: `tests/cute/test_flash_attn.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2104](https://github.com/Dao-AILab/flash-attention/pull/2104) [Cute,Fwd,Sm100] distributed offset calculation for paged KV | 2026-01-15 | attention_kv, arch_pipeline, compiler_runtime | This PR implements a separate sharding scheme for calculating KV row addresses for SM100 forward, following the implementation in FA3. The load is broken into two stages now: 1. Each of the load warp threads independently calculates a disjoint set of row addresses. 2. A group of coalesced load threa... | kernel: `flash_attn/cute/flash_fwd_sm100.py`, `flash_attn/cute/paged_kv.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2368](https://github.com/Dao-AILab/flash-attention/pull/2368) [Cute] fix: FA4 paged attention kv load for DeepSeek (192,128) on SM100 | 2026-03-18 | attention_kv, arch_pipeline | 2367 Bugs to fix Bug 1: `uneven_kv_smem offset` in non-TMA paged path. - flash_fwd_sm100.py line 260: The 3-stage kv_stage upgrade (kv_stage = 3) was gated on self.use_tma_KV, so the non-TMA paged path stays at kv_stage=2 and avoids the uneven_kv_smem offset logic that cp.async can't handle due to a... | kernel: `flash_attn/cute/flash_fwd_sm100.py`<br>test: `tests/cute/test_flash_attn.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2496](https://github.com/Dao-AILab/flash-attention/pull/2496) [CuTe,Flex] Add score_mod_bwd param to flash_attn_varlen_func | 2026-04-26 | attention_kv, compiler_runtime, scheduler_autotune | Fixes error introduced in 2485. cc @drisspg | kernel: `flash_attn/cute/interface.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2506](https://github.com/Dao-AILab/flash-attention/pull/2506) [CuTe, Flex] simplify blocksparse interface in flash_attn_func | 2026-04-27 | attention_kv, compiler_runtime, benchmark_test | cc @drisspg | kernel: `flash_attn/cute/interface.py`<br>test: `tests/cute/test_mask_mod.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1924](https://github.com/Dao-AILab/flash-attention/pull/1924) Remove self refs in softmax for-loop | 2025-10-13 | attention_kv, arch_pipeline, compiler_runtime | Attempted running flash attn cute on H100s with the following package versions: code block It appears the CuTE jit compiler isn't able to handle `self` in the `online_softmax` for-loop. Not very familiar with CuTE, but it seems like refactoring all references to `self` outside the for-loop in softma... | kernel: `flash_attn/cute/softmax.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2174](https://github.com/Dao-AILab/flash-attention/pull/2174) [Cute] update row_max before safe overwrite for online_softmax | 2026-01-13 | attention_kv, arch_pipeline, compiler_runtime | This fixes the issue pointed out in 2082 for online softmax as used for the sm90 kernel. Note that the corresponding logic for sm100 was already working. | kernel: `flash_attn/cute/softmax.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2229](https://github.com/Dao-AILab/flash-attention/pull/2229) Fix shared-memory race | 2026-02-04 | attention_kv, arch_pipeline, compiler_runtime | Stacked PRs: * 2227 * 2218 * 2216 * __->__2229 --- --- --- Fix shared-memory race | kernel: `flash_attn/cute/block_sparse_utils.py`, `flash_attn/cute/flash_fwd_sm100.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2328](https://github.com/Dao-AILab/flash-attention/pull/2328) fix: duplicate softmax_scale param | 2026-03-11 | attention_kv, arch_pipeline, compiler_runtime | Small regression from PR 2297 Caught this while trying FA4 on H100. Keeping the Optional one following other interface APIs. | kernel: `flash_attn/cute/flash_fwd.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#970](https://github.com/Dao-AILab/flash-attention/pull/970) Support unpadded LSE layout | 2024-06-27 | attention_kv, benchmark_test | Closes https://github.com/Dao-AILab/flash-attention/issues/725 For variable seqlen case, keep LSE in `(H, total_q_seqlen)` format instead of current `(B, H, max_q_seqlen)`. This helps to save memory when variance of sequence length within the batch is large, e.g. during long sequence training. Based... | kernel: `csrc/flash_attn/flash_api.cpp`, `csrc/flash_attn/src/flash.h`, `csrc/flash_attn/src/flash_bwd_kernel.h`, `csrc/flash_attn/src/flash_bwd_preprocess_kernel.h`<br>test: `tests/test_flash_attn.py`<br>wrapper: `flash_attn/flash_attn_interface.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2227](https://github.com/Dao-AILab/flash-attention/pull/2227) Nicer headdim error message | 2026-03-09 | attention_kv, compiler_runtime, benchmark_test | Stacked PRs: * 2218 * 2309 * __->__2227 --- --- --- Nicer headdim error message | kernel: `flash_attn/cute/interface.py`<br>test: `tests/cute/test_flash_attn.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2414](https://github.com/Dao-AILab/flash-attention/pull/2414) fix noisy logger | 2026-04-01 | attention_kv, memory_primitives, compiler_runtime | fix noisy logger | kernel: `flash_attn/cute/cache_utils.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2417](https://github.com/Dao-AILab/flash-attention/pull/2417) Allow compact block sparse index tensors | 2026-04-01 | attention_kv, compiler_runtime, benchmark_test | Relax validation in block_sparsity.py to allow idx.shape[3] <= expected_n_blocks instead of requiring exact equality. FA4 only accesses indices 0..cnt-1 per query tile, so the index tensor's last dimension does not need to be as large as ceil(seqlen_k / block_size_n). This enables memory-efficient c... | kernel: `flash_attn/cute/block_sparsity.py`<br>test: `tests/cute/test_mask_mod.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2433](https://github.com/Dao-AILab/flash-attention/pull/2433) Fix: disable 2-CTA backward mode when block_sparse_tensors is used | 2026-04-03 | attention_kv, compiler_runtime, arch_pipeline | The SM100 2-CTA backward kernel does not properly handle block_sparse_tensors. When block sparsity is combined with 2-CTA mode, the kernel hits an assertion: 'AssertionError: 2-CTA mode does not support block sparsity' This fix adds block_sparse_tensors to the disable_2cta condition in the backward... | kernel: `flash_attn/cute/interface.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2108](https://github.com/Dao-AILab/flash-attention/pull/2108) [NVIDIA] Enable Jetson Thor FA4 | 2025-12-29 | attention_kv, arch_pipeline, compiler_runtime | Jetson AGX Thor comes from gb200, it has tcgen05 and TMEM. Functionally, sm110a is identical to sm100a with the small exception that it does not support stochastic rounding after poring over PTX ISA docs, in case I can save anyone else the same time, comment from: https://github.com/Dao-AILab/flash-... | kernel: `flash_attn/cute/interface.py`<br>other: `csrc/cutlass` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1331](https://github.com/Dao-AILab/flash-attention/pull/1331) FA3 paged attention: Readiness for Cutlass 3.6 / default value for block_table | 2024-11-12 | attention_kv, arch_pipeline | Cutlass 3.6 made some incompatible API changes to the SM90_TMA_LOAD ops and structs which now all have additonal cache_hint arguments. Since the paged attention code for FA3 derives from that code ( specifically the file copy_paged_sm90_tma.hpp), it needs to be adapted. These changes aims for compat... | kernel: `hopper/copy_paged_sm90_tma_cutlass35.hpp`, `hopper/copy_paged_sm90_tma_cutlass36.hpp`<br>wrapper: `hopper/copy_paged_sm90_tma.hpp`, `hopper/flash_attn_interface.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2513](https://github.com/Dao-AILab/flash-attention/pull/2513) SM90 FA4 QuACK 0.4 Compatibility | 2026-04-28 | attention_kv, arch_pipeline, compiler_runtime | Dao-AILab/quack@0488ad5 `[Copy] Infer arch automatically in get_smem_store_atom` removed the `\arch` parameter from `get_smem_store_C` ~Backwards compatible with QuACK<0.4~ Edit: Backwards compatibility requested replaced with version bump | kernel: `flash_attn/cute/flash_bwd_sm90.py`<br>docs: `CLAUDE.md`<br>other: `flash_attn/cute/pyproject.toml` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1025](https://github.com/Dao-AILab/flash-attention/pull/1025) [WIP] First draft for softcapping. | 2024-07-08 | attention_kv, benchmark_test | Quick&dirty implementation (but seems functional). Fixes 1016 | kernel: `csrc/flash_attn/flash_api.cpp`, `csrc/flash_attn/src/flash.h`, `csrc/flash_attn/src/flash_fwd_kernel.h`, `csrc/flash_attn/src/flash_fwd_launch_template.h`<br>test: `tests/test_flash_attn.py`<br>wrapper: `flash_attn/flash_attn_interface.py`, `setup.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2483](https://github.com/Dao-AILab/flash-attention/pull/2483) [Tests,MLA] Close coverage gaps in test_flash_attn_mla_absorbed | 2026-04-27 | attention_kv, benchmark_test | Motivated by https://github.com/Dao-AILab/flash-attention/pull/2441 Expands the two MLA-absorbed tests with a few low-risk cases and fixes a stale `nheads` parametrize. The added coverage stays within what the MLA-absorbed kernel already supports per the guards in `interface.py:633-648`. Existing sk... | test: `tests/cute/test_flash_attn.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1945](https://github.com/Dao-AILab/flash-attention/pull/1945) Blackwell FlashAttention-BWD (v1.0) | 2025-10-19 | attention_kv, arch_pipeline | - Initial version of the Blackwell (sm100) BWD kernel for FlashAttention with swapAB. - Post processing kernel updated for SM100. - FA-4 BWD final version will follow soon Performance - ~1.1x speed up vs cuDNN SM100 backward for seqlen=8K; b=2; num_heads=16; head_dim=128 Changes - Added code block -... | kernel: `flash_attn/cute/flash_bwd_postprocess.py`, `flash_attn/cute/flash_bwd_sm100.py`, `flash_attn/cute/mask.py`, `flash_attn/cute/named_barrier.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2302](https://github.com/Dao-AILab/flash-attention/pull/2302) Enable hdim=96 bwd | 2026-03-03 | attention_kv, arch_pipeline, compiler_runtime | was almost there already, just needed minor epilogue changes | kernel: `flash_attn/cute/flash_bwd_sm100.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2505](https://github.com/Dao-AILab/flash-attention/pull/2505) Fix: pass `stream` to SM100 MLA kernel | 2026-04-27 | attention_kv, arch_pipeline | `FlashAttentionMLAForwardSm100.__call__()` does not pass `stream` through to `self.kernel().launch()`, so the operation is carried out on the default CUDA stream, leading to potential races with PyTorch ops. Simple 1-line fix. | kernel: `flash_attn/cute/flash_fwd_mla_sm100.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1442](https://github.com/Dao-AILab/flash-attention/pull/1442) Replace c10::optional with std::optional in flash_attn | 2025-01-14 | attention_kv | Remove c10::optional. c10::optional was introduced before c++17 is widely adopted. Now even PyTorch itself doesn't have any c10::optional usage anymore. Locally build with "python setup.py install" Built successfully, and showed the following things code block | kernel: `csrc/flash_attn/flash_api.cpp`, `csrc/flash_attn_ck/flash_api.cpp`, `csrc/flash_attn_ck/mha_bwd.cpp`, `csrc/flash_attn_ck/mha_fwd.cpp`<br>wrapper: `hopper/flash_api.cpp` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2282](https://github.com/Dao-AILab/flash-attention/pull/2282) Add FA4 publishing strategy | 2026-03-03 | attention_kv, compiler_runtime | Add FA4 publishing strategy Let me describe my tentative plan; we have allocated two pypi namespaces: `flash-attn-cute`, `flash-attn-4`. The current plan I went with is install via `flash-attn-4`. Reason why I chose this: With the recent release of fa3: https://download.pytorch.org/whl/flash-attn-3/... | kernel: `flash_attn/cute/__init__.py`<br>wrapper: `setup.py`<br>docs: `.github/workflows/README.md`, `README.md`, `flash_attn/cute/README.md`<br>other: `.github/workflows/publish-fa4.yml`, `flash_attn/cute/MANIFEST.in`, `flash_attn/cute/pyproject.toml` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1033](https://github.com/Dao-AILab/flash-attention/pull/1033) backwards for softcapping | 2024-07-22 | attention_kv, benchmark_test | backwards pass for softcapping dtanh is stored right after softcapping is applied, which is then later multiplied to `dS` | kernel: `csrc/flash_attn/src/flash_bwd_kernel.h`, `csrc/flash_attn/src/flash_bwd_launch_template.h`, `csrc/flash_attn/src/flash_fwd_kernel.h`, `csrc/flash_attn/src/utils.h`<br>test: `tests/test_flash_attn.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2063](https://github.com/Dao-AILab/flash-attention/pull/2063) Fix use-after-free in FA3 deterministic mode. | 2025-12-12 | attention_kv, arch_pipeline | The pytorch caching allocator actually saves us here, but if you turn it off, then compute-sanitizer will detect this. A simple reproducer is below. Run it with compute_sanitizer and without the caching allocator. e.g like this: PYTORCH_NO_CUDA_MEMORY_CACHING=1 compute-sanitizer python reproduce_bug... | wrapper: `hopper/flash_api.cpp`, `hopper/flash_api_stable.cpp` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2156](https://github.com/Dao-AILab/flash-attention/pull/2156) Improve flash.cute paged_kv cpasync | 2026-01-12 | attention_kv, compiler_runtime | Improve flash.cute paged_kv cpasync | kernel: `flash_attn/cute/paged_kv.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2301](https://github.com/Dao-AILab/flash-attention/pull/2301) Fix GQA crash in cute FLASH backend: init load_Q before conditional | 2026-03-03 | attention_kv, compiler_runtime | Fix for 2300. When GQA is used with `pack_gqa=True` and `tile_m % qhead_per_kvhead != 0`, `use_tma_Q` is `False` so `load_Q` is never assigned inside the `if const_expr(self.use_tma_Q)` block. But `load_Q` is referenced unconditionally at line 1822 in the block sparsity path, causing: code block Fix... | kernel: `flash_attn/cute/flash_fwd.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1180](https://github.com/Dao-AILab/flash-attention/pull/1180) Add ArchTag to pre/postprocess bwd kernels | 2024-08-28 | attention_kv, arch_pipeline | Certain versions of CUTLASS are having compiler errors related to the additional backwards kernels not having an `ArchTag` defined. When you launch the kernel it will check this type for feature compatibility. I'm compiling with a different CUTLASS version, but when FA updates its version errors wil... | kernel: `hopper/flash_bwd_postprocess_kernel.h`, `hopper/flash_bwd_preprocess_kernel.h`<br>wrapper: `hopper/flash_bwd_launch_template.h` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1856](https://github.com/Dao-AILab/flash-attention/pull/1856) [BugFix] fix flash_fwd.FlashAttentionForwardSm80  bugs | 2025-09-02 | attention_kv, compiler_runtime | @tridao softcap should only be referenced when its not none, currently the logic is reversed and will result in an error | kernel: `flash_attn/cute/flash_fwd.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1904](https://github.com/Dao-AILab/flash-attention/pull/1904) C++11 fix warnings | 2025-09-25 | attention_kv, arch_pipeline | The errors in hopper/flash_api.cpp are C++11 narrowing warnings (treated as errors in strict builds) when initializing at::cuda::CUDAGuard with a non-constant char cast to c10::DeviceIndex (signed char). cc @ko3n1g @tridao | wrapper: `hopper/flash_api.cpp`, `hopper/flash_api_stable.cpp`<br>other: `csrc/cutlass` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1957](https://github.com/Dao-AILab/flash-attention/pull/1957) Fix FA3 segfault with custom CUDA streams in ABI stable build | 2025-10-24 | attention_kv, arch_pipeline | Commit https://github.com/Dao-AILab/flash-attention/commit/b3846b05 introduced ABI-stable FA3 by switching to `flash_api_stable.cpp`, but it broke custom CUDA stream support, causing segmentation faults. The stable API implementation incorrectly retrieved the CUDA stream: https://github.com/Dao-AILa... | wrapper: `hopper/flash_api_stable.cpp` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2448](https://github.com/Dao-AILab/flash-attention/pull/2448) expose num_splits for FA2 and add option for kernel blocksize alignment | 2026-04-16 | attention_kv, scheduler_autotune | expose num_splits as a parameter in FA2. if user sets num_splits=1 we can assume they want a numerical match between paged KV vs non-paged. so when num_splits=1, align the split KV kernel (used for paging) block sizes to align with the standard kernel block sizes. | kernel: `csrc/flash_attn/flash_api.cpp`, `csrc/flash_attn/src/flash_fwd_launch_template.h` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1139](https://github.com/Dao-AILab/flash-attention/pull/1139) Add custom ops for compatibility with PT Compile | 2024-09-18 | attention_kv, benchmark_test | This PR adds basic support for torch.compile() for the non-varlen variants of Flash Attention. This essentially allows for models that use the `flash_attn_qkvpacked_func`, `flash_attn_kvpacked_func`, and `flash_attn_func` to compile without graph breaks. I can add unit tests if it makes sense. I'll... | kernel: `csrc/flash_attn/flash_api.cpp`<br>wrapper: `flash_attn/flash_attn_interface.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2327](https://github.com/Dao-AILab/flash-attention/pull/2327) Add Logging helper | 2026-03-11 | attention_kv, compiler_runtime | Stacked PRs: * 2326 * 2218 * __->__2327 --- --- --- Add Logging helper For provenance, with this: code block Prints code block | kernel: `flash_attn/cute/interface.py`, `flash_attn/cute/logging.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2388](https://github.com/Dao-AILab/flash-attention/pull/2388) fix: use LSE accum strides from params instead of hardcoded ones | 2026-03-25 | attention_kv, arch_pipeline | In the Split-KV path, the forward kernel computes LSE accumulator addresses using hardcoded strides instead of the stride values provided in the params structure. The combine kernel already uses the explicit strides from params, so this creates an inconsistency between the two kernels. As a result,... | wrapper: `hopper/flash_fwd_launch_template.h` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2251](https://github.com/Dao-AILab/flash-attention/pull/2251) [Cute] Handle window_size=(-1, -1) for non-local attention | 2026-02-23 | attention_kv, compiler_runtime | In flash attention cute, we use (None, None) to handle not using local attention. This PR tries to treat (-1, -1) as the same to avoid some tricky debugging situation. | kernel: `flash_attn/cute/interface.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2273](https://github.com/Dao-AILab/flash-attention/pull/2273) Correct Cutlass Error Handling | 2026-02-25 | attention_kv, arch_pipeline | Hello! There are some wrong handling of cutlass calls - currently, there are handle by `cudaGetLastError`, but this is not always correct - sometimes such calls can return cutlass errors like `cutlass::Status::kInternal` even without actual calls to cuda kernels. This MR provides fix for that. P.S.... | wrapper: `hopper/cuda_check.h`, `hopper/flash_bwd_launch_template.h`, `hopper/flash_fwd_combine_launch_template.h`, `hopper/flash_fwd_launch_template.h` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2545](https://github.com/Dao-AILab/flash-attention/pull/2545) [FA4][hd256] Fix layout of non-contiguous qkv in backward kernel | 2026-05-07 | attention_kv | This PR fix backward bug when declaring non-contiguous qkv on head_dim=256. All Unit tests passed. | kernel: `flash_attn/cute/mask.py`, `flash_attn/cute/sm100_hd256_2cta_fmha_backward_dkdvkernel.py`, `flash_attn/cute/sm100_hd256_2cta_fmha_backward_dqkernel.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1674](https://github.com/Dao-AILab/flash-attention/pull/1674) [BE] use more minimal torch headers for hopper/flash_api.cpp | 2025-05-22 | attention_kv, arch_pipeline | Tiny change to narrow the libtorch APIs used, found during my exploration work for designing a minimal set of stable torch ABIs. narrowed `<torch/nn/functional.h>` to `<torch/nn/functional/padding.h>` narrowed `<ATen/cuda/CUDAContext.h>` to `<ATen/cuda/CUDAContextLight.h>` After 1662 we should also... | wrapper: `hopper/flash_api.cpp` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2223](https://github.com/Dao-AILab/flash-attention/pull/2223) [FA3] Mark current main version as v3.0.0 stable | 2026-02-05 | attention_kv, arch_pipeline | A collaboration between Flash-Attention, PyTorch and xFormers is trying to provide pre-built wheels for FA3 across as many platforms/environments as possible (e.g., ARM, Windows, CUDA 13, ...). To simplify the installation workflow, it would help to tag these packages as stable, but the current main... | wrapper: `hopper/__init__.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1952](https://github.com/Dao-AILab/flash-attention/pull/1952) [NVIDIA] cutlass v4.3.0 | 2025-10-21 | attention_kv, compiler_runtime | @drisspg it should be fixed the hopper test fail cc @tridao | other: `csrc/cutlass`, `flash_attn/cute/pyproject.toml` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2337](https://github.com/Dao-AILab/flash-attention/pull/2337) BUG: SeqlenInfo.create has a tile parameter that defaults to 128 | 2026-03-12 | attention_kv, compiler_runtime | Details Line 216 flash_bwd_preprocess.py code block SeqlenInfo.create has a tile parameter that defaults to 128: code block This tile is used to compute offset_padded: seqlen_info.py Line 35-26 code block But the preprocess kernel's actual tile size is self.tile_m, which for SM90 with head_dim <= 64... | kernel: `flash_attn/cute/flash_bwd_preprocess.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2459](https://github.com/Dao-AILab/flash-attention/pull/2459) Handle linter for flash mla file | 2026-04-15 | attention_kv | I would prefer to exclude `flash_fwd_mla_sm100.py` from ruff format checks to preserve table formatting readability. Do you have a preference @tridao ? | kernel: `flash_attn/cute/flash_fwd_mla_sm100.py`<br>other: `.pre-commit-config.yaml` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1363](https://github.com/Dao-AILab/flash-attention/pull/1363) flash_bwd_kernel.h: add maybe_unused annotation to suppress compile warnings | 2024-12-02 | attention_kv | dtanh is not used when Is_softcap=false. Resulting warnings are quite noisy during compile. | kernel: `csrc/flash_attn/src/flash_bwd_kernel.h` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2260](https://github.com/Dao-AILab/flash-attention/pull/2260) [FA2] Fix int32 overflow | 2026-02-15 | attention_kv | Fix int32 overflow TLDR still possible to overflow since multiplication is done in `int` before index_t cast No appreciable perf delta ( shouldn't be since we are fixing a cast) Small repro: code block But didnt add since its a lil big in memory and feels like a one off <img width="1103" height="953... | kernel: `csrc/flash_attn/src/flash_bwd_kernel.h`, `csrc/flash_attn/src/flash_bwd_preprocess_kernel.h` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2295](https://github.com/Dao-AILab/flash-attention/pull/2295) [clang build] Fix clang parse error of missing 'typename' prior to dependent typename occurs because `LLVM/Clang` is strictly adhering to C++ standards | 2026-03-02 | attention_kv | The error missing 'typename' prior to dependent type name occurs because LLVM/Clang is strictly adhering to C++ standards. It requires the typename keyword when referring to a subtype of a template (like Kernel_traits::TiledMmaSdP), whereas NVIDIA's nvcc is often more "forgiving" and lets it slide.... | kernel: `csrc/flash_attn/src/flash_bwd_kernel.h` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2142](https://github.com/Dao-AILab/flash-attention/pull/2142) Update CUTLASS to fix undefined symbol : cuDriverGetVersion | 2026-01-07 | attention_kv | Pull https://github.com/NVIDIA/cutlass/pull/2928 to fix https://github.com/Dao-AILab/flash-attention/pull/2116 @tridao @drisspg @johnnynunez | other: `csrc/cutlass` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#976](https://github.com/Dao-AILab/flash-attention/pull/976) Fixing argument checking when using `seqlenq_ngroups_swapped`. | 2024-07-01 | attention_kv | When user send `out` as a parameter of the function `seqlenq_ngroups_swapped` with parameters that trigger, the CHECK_SHAPE is incorrect (since q shape is modified.) | kernel: `csrc/flash_attn/flash_api.cpp` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1427](https://github.com/Dao-AILab/flash-attention/pull/1427) Generalize cuda version checks for A100 and above | 2025-01-09 | attention_kv | These are the code changes required for FlashAttention to be compatible with upcoming blackwell release. I have also generalized the device cc checks to include everything 80 and above in `.cpp` file. I will create a separate PR with the correct `sm_*` compilation target for blackwell. | kernel: `csrc/flash_attn/flash_api.cpp` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |

### Memory / Primitives

Use this section for: Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract.
NCU first look: DRAM throughput, L2 hit rate, memory sectors, shared-memory conflicts, and synchronization overhead.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#2423](https://github.com/Dao-AILab/flash-attention/pull/2423) Fix some bugs of CI | 2026-04-02 | memory_primitives, attention_kv, compiler_runtime, benchmark_test | 1. Fix double blank line in cache_utils.py (ruff format) | kernel: `flash_attn/cute/cache_utils.py`<br>other: `.github/actions/gpu-test/action.yml`, `.github/workflows/ci.yml` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |
| [#2509](https://github.com/Dao-AILab/flash-attention/pull/2509) Add cache utils logging test | 2026-04-28 | memory_primitives, benchmark_test, compiler_runtime | Stacked PRs: * 2512 * 2253 * __->__2509 --- --- --- Add cache utils logging test Simple regression test for this noisy neighbor | test: `tests/cute/test_cache_utils.py` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |
| [#1719](https://github.com/Dao-AILab/flash-attention/pull/1719) fix: add tile shape to copy op template args | 2025-06-16 | memory_primitives, norm_elementwise, arch_pipeline | With the introduction of CUTLASS 4.0, `sm90_get_smem_store_op_for_accumulator` added `EpilogueTile_MN` as a new template arg, which is missing from `CopyOpR2S` here. This is mentioned on issue 1718 . Here's the error thrown during instantiation: code block This fix adds the correct epilogue tile sha... | wrapper: `hopper/epilogue_fwd.hpp` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |
| [#2079](https://github.com/Dao-AILab/flash-attention/pull/2079) Reduce Chance of Build OOM | 2026-01-21 | memory_primitives, attention_kv | Fix: https://github.com/Dao-AILab/flash-attention/issues/2051 When the previous auto `MAX_JOBS` code was written long ago, it was observed that 4 total nvcc threads (MAJ_JOBS=1, NVCC_THREADS=4) consumed peak ~9GB of memory during compile stage or around ~2.25GB per thread. However, my latest test wi... | wrapper: `setup.py` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |

### Scheduler / Autotune

Use this section for: Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing.
NCU first look: SM occupancy, waves/SM, active cycles, tail effects, and load imbalance.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#2150](https://github.com/Dao-AILab/flash-attention/pull/2150) [Cute, Bwd, Sm100] Add varlen for sm100 bwd | 2026-01-09 | scheduler_autotune, benchmark_test, arch_pipeline, compiler_runtime, attention_kv | We add varlen to sm100 backward pass and expose this capability through the `flash_attn_varlen_func` API. Head-to-head benchmark against non-varlen sm100 backward for equal sequence lengths shows minimal overhead: code block To fix an alignment issue with loading padded LSE in the backward kernel, w... | kernel: `flash_attn/cute/cute_dsl_utils.py`, `flash_attn/cute/flash_bwd_postprocess.py`, `flash_attn/cute/flash_bwd_preprocess.py`, `flash_attn/cute/flash_bwd_sm100.py`<br>benchmark: `benchmarks/benchmark_attn.py`<br>test: `flash_attn/cute/testing.py`, `tests/cute/test_flash_attn.py`, `tests/cute/test_flash_attn_race_condition.py`, `tests/cute/test_flash_attn_varlen.py` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#1823](https://github.com/Dao-AILab/flash-attention/pull/1823) Add sorting and head swizzle to varlen scheduler | 2025-08-22 | scheduler_autotune, memory_primitives, attention_kv, arch_pipeline | This PR adds inter-batch sort and intra-batch LPT scheduling for varlen FA3 workloads. On the backend, sorting is enabled when not local, and head swizzle is enabled when causal or local (same as for dynamic tile scheduler). The option to sort/swizzle or not is not accessible from the frontend to pr... | kernel: `hopper/flash_fwd_combine_kernel.h`, `hopper/tile_scheduler.hpp`, `hopper/tile_size.h`<br>test: `hopper/test_flash_attn.py`<br>wrapper: `hopper/flash.h`, `hopper/flash_api.cpp`, `hopper/flash_attn_interface.py`, `hopper/flash_fwd_combine_launch_template.h`<br>other: `hopper/flash_prepare_scheduler.cu` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#2347](https://github.com/Dao-AILab/flash-attention/pull/2347) Support 2CTA for sliding window hdim 192 | 2026-03-19 | scheduler_autotune, attention_kv, arch_pipeline, compiler_runtime, benchmark_test | Closes 2286 Keep 2CTA enabled for SM100 backward sliding window when `head_dim = 192`, which avoids the `Must use 2CTA for hdim 192` assertion. Validated on B300 with the given repro. | kernel: `flash_attn/cute/flash_bwd_sm100.py`, `flash_attn/cute/interface.py`<br>test: `tests/cute/test_flash_attn.py`, `tests/cute/test_flash_attn_race_condition.py` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#1934](https://github.com/Dao-AILab/flash-attention/pull/1934) feat: Adding varlen support to cute-dsl sm80 bwd | 2025-10-13 | scheduler_autotune, compiler_runtime, attention_kv, benchmark_test | Added varlen support for cute-dsl SM80 bwd. Split this into 3 commits: 1. Turning R2P trick off so that mask works for bwd (at least until https://github.com/Dao-AILab/flash-attention/issues/1915 is resolved) 2. Pre-indenting to make final commit more readable: Switching to using a tile scheduler re... | kernel: `flash_attn/cute/flash_bwd.py`, `flash_attn/cute/flash_bwd_postprocess.py`, `flash_attn/cute/flash_bwd_preprocess.py`, `flash_attn/cute/interface.py`<br>test: `tests/cute/test_flash_attn_varlen.py` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#2515](https://github.com/Dao-AILab/flash-attention/pull/2515) Fix ZeroDivisionError in num_splits_heuristic for empty Q workloads | 2026-05-13 | scheduler_autotune, attention_kv, compiler_runtime, benchmark_test | `num_splits_heuristic` in `flash_attn/cute/interface.py` divides `num_SMs` by `total_mblocks`, which collapses to 0 when `seqlen_q == 0` or `batch_size == 0`. The existing `seqlen_k == 0` early-exit in `_flash_attn_fwd` does not cover these cases, so empty-Q shapes (e.g. CUDA graph padding, empty mi... | kernel: `flash_attn/cute/interface.py`<br>test: `tests/cute/test_flash_attn.py` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#2346](https://github.com/Dao-AILab/flash-attention/pull/2346) Add to varlen | 2026-03-30 | scheduler_autotune, attention_kv, compiler_runtime, benchmark_test | Stacked PRs: * __->__2346 * 2218 --- --- --- Add to varlen <img width="892" height="450" alt="image" src="https://github.com/user-attachments/assets/321a90bf-d6a9-4082-81ac-9114eab3e665" /> | kernel: `flash_attn/cute/tile_scheduler.py`<br>test: `tests/cute/test_clc_fuzz.py` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#2455](https://github.com/Dao-AILab/flash-attention/pull/2455) Add CLC scheduler heuristic | 2026-04-16 | scheduler_autotune, benchmark_test | CLC Scheduler Heuristic Human note: I had Pi summarize all the things we ran. We pair-wrote compile + run scripts around the benchmark helper from Inductor / transformer-nuggets that flushes L2 and returns sample statistics, then bootstrapped a paired 95% confidence interval around `speedup_on_vs_of... | kernel: `flash_attn/cute/flash_fwd_sm100.py`, `flash_attn/cute/interface.py`<br>benchmark: `benchmarks/clc_bench.py`, `benchmarks/configs/clc.yaml`<br>test: `tests/cute/test_clc_fuzz.py`<br>docs: `AGENTS.md`, `CLAUDE.md`<br>other: `.gitignore` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#2550](https://github.com/Dao-AILab/flash-attention/pull/2550) fix varlen w/ paging split kv bug | 2026-05-12 | scheduler_autotune, attention_kv, benchmark_test | Paged KV always runs with split KV kernel because mha_varlen_fwd calls run_mha_fwd(..., force_split_kernel = paged KV) but because set_params_splitkv was never called before 2448, num_splits stayed 0 → the splitting heuristic never ran so even though the split KV kernel always runs with paged KV, it... | kernel: `csrc/flash_attn/flash_api.cpp`<br>test: `tests/test_flash_attn.py` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#2461](https://github.com/Dao-AILab/flash-attention/pull/2461) Disable 2CTA fwd non-causal on CUDA 12 to work around codegen regression | 2026-04-15 | scheduler_autotune, attention_kv, compiler_runtime, benchmark_test | - CUDA 12.9 has a codegen regression that causes ~18% slowdown for 2CTA forward non-causal (hdim=128: ~1280 vs ~1542 TFLOPS) - Auto-disable 2CTA when CUDA 12.9 is detected via `torch.version.cuda` - Users on CUDA 13.x are unaffected — 2CTA stays enabled and performs as expected - The manual `FA_DISA... | kernel: `flash_attn/cute/utils.py` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#2274](https://github.com/Dao-AILab/flash-attention/pull/2274) guard use_2cta_instrs on sm90 | 2026-02-25 | scheduler_autotune, arch_pipeline, attention_kv, compiler_runtime | guard use_2cta_instrs on sm90 | kernel: `flash_attn/cute/interface.py` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#2471](https://github.com/Dao-AILab/flash-attention/pull/2471) Add [STATIC] tile trace for CLC-off work distribution | 2026-04-27 | scheduler_autotune, attention_kv, compiler_runtime | - Adds a `[STATIC] query sm=... cta=... (m_blk=...,h=...,b=...,s=...) valid=...` trace line to `SingleTileLPTScheduler.get_current_work()` on the non-CLC path - Matches the existing `[CLC] query ...` format so `parse_clc_log.py` can produce work-distribution histograms for both CLC-on and CLC-off mo... | kernel: `flash_attn/cute/tile_scheduler.py` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#2508](https://github.com/Dao-AILab/flash-attention/pull/2508) Fix clc scheduling request bug | 2026-04-27 | scheduler_autotune, attention_kv, compiler_runtime | Stacked PRs: * 2253 * 2509 * __->__2508 --- --- --- Fix clc scheduling request bug We had a bug where cache key and compilation was not using the correct scheduler based off of heurisitc | kernel: `flash_attn/cute/interface.py` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#2556](https://github.com/Dao-AILab/flash-attention/pull/2556) split out varlen batch search into utils | 2026-05-14 | scheduler_autotune, attention_kv, compiler_runtime | cc @jayhshah | kernel: `flash_attn/cute/compute_block_sparsity.py`, `flash_attn/cute/utils.py` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#2218](https://github.com/Dao-AILab/flash-attention/pull/2218) [Ai-assisted] CLC work stealing | 2026-03-28 | scheduler_autotune | Stacked PRs: * 2346 * __->__2218 --- --- --- CLC work stealing Not for land yet; the scheduling leaking abstraction all over the place is bad, im going to find a better way to encapsulate that. I had to help alot on this one even though claude and codex did most of the setup. Example work steal "tra... | kernel: `flash_attn/cute/block_sparsity.py`, `flash_attn/cute/cache_utils.py`, `flash_attn/cute/flash_fwd_sm100.py`, `flash_attn/cute/interface.py`<br>test: `tests/cute/conftest.py`, `tests/cute/test_clc_fuzz.py` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#2320](https://github.com/Dao-AILab/flash-attention/pull/2320) Change 2cta opt in to have min seqlen > 2*m_block_size | 2026-03-09 | scheduler_autotune, attention_kv, compiler_runtime | Stacked PRs: * 2218 * __->__2320 --- --- --- Change 2cta opt in to have min seqlen > 2*m_block_size | kernel: `flash_attn/cute/interface.py` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#2476](https://github.com/Dao-AILab/flash-attention/pull/2476) Fix: pass num_splits through varlen_fwd Python wrapper (fixes #2448 regression) | 2026-04-20 | scheduler_autotune, attention_kv | PR 2448 (`b322ae2`) added a trailing `int num_splits = 0` parameter to `mha_varlen_fwd` in `csrc/flash_attn/flash_api.cpp`, but didn't update the Python wrapper nor add `py::arg("num_splits") = 0` to the pybind11 binding. Because pybind11 doesn't honour C++ default values without an explicit `py::ar... | wrapper: `flash_attn/flash_attn_interface.py` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |

### Architecture / Pipeline

Use this section for: Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages.
NCU first look: Tensor pipe %, memory pipe utilization, barrier stalls, wait groups, and occupancy.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#1075](https://github.com/Dao-AILab/flash-attention/pull/1075) Changes For FP8  | 2024-07-23 | arch_pipeline, benchmark_test, gemm_quant, attention_kv, norm_elementwise | This PR contains the changes required for FP8 for persistent + ping-pong schedule, with pre-transpose of V in python. The following are the experimental results from PCIe based H100: causal=False, headdim=128, batch_size=32, seqlen=512 Pytorch fwd: 52.28 TFLOPs/s, 1.3145040992336967 ms, Flash3 fwd:... | kernel: `hopper/kernel_traits.h`<br>benchmark: `hopper/benchmark_flash_attention.py`, `hopper/benchmark_flash_attention_fp8.py`<br>test: `hopper/test_flash_attn.py`<br>wrapper: `hopper/epilogue_fwd_sm90_tma.hpp`, `hopper/flash_api.cpp`, `hopper/flash_attn_interface.py`, `hopper/flash_fwd_launch_template.h`<br>other: `hopper/flash_fwd_hdim128_fp8_sm90.cu`, `hopper/flash_fwd_hdim256_fp8_sm90.cu`, `hopper/flash_fwd_hdim64_fp8_sm90.cu` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1940](https://github.com/Dao-AILab/flash-attention/pull/1940) [Cute,Fwd,Sm100] Implement SplitKV | 2025-11-05 | arch_pipeline, scheduler_autotune, attention_kv, compiler_runtime, benchmark_test | This PR implements split KV for FA4 for compute capability 10. Supports split KV for all of the following features: - Varlen - Learnable sink - Causal / sliding window attention - Head dim 64, 96, 128 - GQA (1) Implements a simple num splits heuristic to determine number of splits when kernel receiv... | kernel: `flash_attn/cute/block_info.py`, `flash_attn/cute/flash_bwd.py`, `flash_attn/cute/flash_bwd_postprocess.py`, `flash_attn/cute/flash_bwd_preprocess.py`<br>test: `tests/cute/test_flash_attn.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2109](https://github.com/Dao-AILab/flash-attention/pull/2109) [Cute,Fwd,Sm100] fp8 e4m3 and e5m2 support | 2026-04-17 | arch_pipeline, attention_kv, gemm_quant, compiler_runtime, benchmark_test | Adds FP8 (E4M3/E5M2) support to Flash Attention 4 on SM100 (Blackwell GPUs). This brings FP8 inference acceleration to the CuTe-DSL implementation with full correctness validation. What's New FP8 Forward Pass - **Supported types**: FP8 E4M3fn and E5M2 for Q/K/V inputs - **Output format**: BF16 - **D... | kernel: `flash_attn/cute/blackwell_helpers.py`, `flash_attn/cute/block_sparse_utils.py`, `flash_attn/cute/cute_dsl_utils.py`, `flash_attn/cute/flash_fwd_sm100.py`<br>benchmark: `flash_attn/cute/benchmark_flash_attention_fp8.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1893](https://github.com/Dao-AILab/flash-attention/pull/1893) Improve causal backward determinism perf with SPT schedule | 2025-09-17 | arch_pipeline, benchmark_test, attention_kv, scheduler_autotune, norm_elementwise | We improve the performance of deterministic mode for the FA3 backward kernel with causal masking by using a "shortest-processing time first" (SPT) schedule, traversing nblocks from right to left and visiting an mblock in nblock decreasing order for reduce add in the dQ computation. Typical performan... | kernel: `hopper/tile_scheduler.hpp`<br>test: `hopper/test_flash_attn_bwd_determinism.py`<br>wrapper: `hopper/epilogue_bwd.hpp`, `hopper/flash_api.cpp`, `hopper/flash_api_stable.cpp`, `hopper/flash_bwd_launch_template.h` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2270](https://github.com/Dao-AILab/flash-attention/pull/2270) [Cute,Sm100,Bwd] Add hdim 192 hdimv 128 backward for sm100 | 2026-02-25 | arch_pipeline, compiler_runtime, scheduler_autotune, benchmark_test, attention_kv | Tested against all combinations of the following: - MHA or MQA/GQA - causal True or False - deterministic True or False. 10000 iterations for bitwise equality with determinism - varlen: cu_seqlens_{q,k} x seqused_{q,k} + maybe zero_lengths_{q,k} Speed looks good for a first version: code block Local... | kernel: `flash_attn/cute/flash_bwd_postprocess.py`, `flash_attn/cute/flash_bwd_preprocess.py`, `flash_attn/cute/flash_bwd_sm100.py`, `flash_attn/cute/interface.py`<br>test: `tests/cute/test_flash_attn.py`, `tests/cute/test_flash_attn_race_condition.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2033](https://github.com/Dao-AILab/flash-attention/pull/2033) [Cute,Bwd,Sm100] enable deterministic mode for sm100 bwd and fix race conditions | 2025-11-25 | arch_pipeline, compiler_runtime, attention_kv, scheduler_autotune, benchmark_test | We enable deterministic mode for sm100 bwd kernel and fix outstanding race conditions. PR includes: 1. Sync compute warpgroup prior to releasing LSE (for tmem store fence) and release Psum + producer commit on dS (for tmem store fence and async smem fence); both were causing errors. 2. Sync compute... | kernel: `flash_attn/cute/flash_bwd_sm100.py`, `flash_attn/cute/interface.py`, `flash_attn/cute/tile_scheduler.py`, `flash_attn/cute/utils.py`<br>test: `tests/cute/test_flash_attn_race_condition.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2167](https://github.com/Dao-AILab/flash-attention/pull/2167) [Cute,Bwd,Sm100] fix seqused in varlen bwd | 2026-01-10 | arch_pipeline, compiler_runtime, scheduler_autotune, attention_kv, benchmark_test | We fix errors related to using seqused_{q,k} instead of cu_seqlens_{q,k} in the sm100 backward kernel and enable additional tests for this in `test_flash_attn_varlen_output`. Note that we currently don't store zero to the unused region for seqused_{q,k}, so the tests fill zero for this region. | kernel: `flash_attn/cute/flash_bwd_sm100.py`, `flash_attn/cute/interface.py`<br>test: `tests/cute/test_flash_attn.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1361](https://github.com/Dao-AILab/flash-attention/pull/1361) Fix FA3 Varlen Performance regression | 2024-12-02 | arch_pipeline, attention_kv, benchmark_test, scheduler_autotune, memory_primitives | This fixes an issue in the varlen Flash Attention 3 Varlen Kernel, leading ptxas to serialize wgmma instructions, obviously having a drastic performance impact that was hidden by the fact that varlen performance is still better in the degraded version than in the non-varlen case With this fix, ptxas... | kernel: `hopper/copy_paged_sm90_tma_cutlass35.hpp`, `hopper/copy_paged_sm90_tma_cutlass36.hpp`<br>wrapper: `hopper/mainloop_fwd_sm90_tma_gmma_ws.hpp` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1985](https://github.com/Dao-AILab/flash-attention/pull/1985) [Cute] Block sparse support Sm100 | 2025-11-18 | arch_pipeline, compiler_runtime, attention_kv, benchmark_test | - Implement block-sparse attention in flash_fwd_sm100.py - Update interface.py to handle SM100 block size calculations (2x multiplier for m_block_size since 1 CTA handles 2*tile_m rows) - Add mask_mod parameter support in mask.py for block-sparse masking - Add SM100 test fixtures and tile size handl... | kernel: `flash_attn/cute/block_sparse_utils.py`, `flash_attn/cute/compute_block_sparsity.py`, `flash_attn/cute/flash_bwd_sm100.py`, `flash_attn/cute/flash_fwd_sm100.py`<br>test: `tests/cute/test_mask_mod.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1233](https://github.com/Dao-AILab/flash-attention/pull/1233) Add local attention in Hopper FAv3 | 2024-09-20 | arch_pipeline, attention_kv, benchmark_test | PR Description ATT, add local_window support in Hopper FAv3 and unittests. * Local masking is checked in each mainloop iteration. It's possible to further optimize it by only checking local masking in the first m and last n iterations, and leaving iterations in-between unchecked. This PR doesn't do... | kernel: `hopper/flash_bwd_kernel.h`, `hopper/flash_fwd_kernel.h`, `hopper/tile_scheduler.hpp`<br>test: `hopper/test_flash_attn.py`<br>wrapper: `hopper/flash.h`, `hopper/flash_api.cpp`, `hopper/flash_attn_interface.py`, `hopper/flash_bwd_launch_template.h` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2046](https://github.com/Dao-AILab/flash-attention/pull/2046) [Cute,Bwd,Sm100] Add local for sm100 bwd | 2025-12-06 | arch_pipeline, compiler_runtime, attention_kv, benchmark_test | This PR adds support for local attention to sm100 bwd. We have tested for all valid permutations of `(window_size_left, window_size_right)` with `window_size_{left, right}` negative, positive or `None`, in particular removing any restrictions related to completely masked out rows or columns. | kernel: `flash_attn/cute/block_info.py`, `flash_attn/cute/flash_bwd_sm100.py`, `flash_attn/cute/interface.py`, `flash_attn/cute/mask.py`<br>test: `flash_attn/cute/testing.py`, `tests/cute/test_flash_attn.py`, `tests/cute/test_flash_attn_race_condition.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2202](https://github.com/Dao-AILab/flash-attention/pull/2202) BWD sm100 2cta | 2026-02-20 | arch_pipeline, scheduler_autotune, attention_kv, memory_primitives, compiler_runtime | Added support for 2CTA BWD on sm100. Updated the post-processing kernel to support 2CTA as well. | kernel: `flash_attn/cute/blackwell_helpers.py`, `flash_attn/cute/copy_utils.py`, `flash_attn/cute/flash_bwd_postprocess.py`, `flash_attn/cute/flash_bwd_sm100.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1604](https://github.com/Dao-AILab/flash-attention/pull/1604) Support hdimQK != hdimV backward | 2025-04-24 | arch_pipeline, benchmark_test, attention_kv, norm_elementwise | Recently, there are cases where ~~hdimQK >= hdimV~~ hdimQK != hdimV, such as DeepSeek (1487). So we worked on a simple trick (rounding hdimQK and hdimV to the same value) to support that configuration in the backward kernel as well. Of course, it would be best to tile K and V with different rounding... | test: `hopper/test_flash_attn.py`<br>wrapper: `hopper/epilogue_bwd.hpp`, `hopper/flash_api.cpp`, `hopper/flash_attn_interface.py`, `hopper/flash_bwd_launch_template.h` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2085](https://github.com/Dao-AILab/flash-attention/pull/2085) Add blocksparse support for bwd on blackwell | 2025-12-18 | arch_pipeline, attention_kv, compiler_runtime, benchmark_test | Some design decisions; * Due to softmax pipelining in the fwd we are using a block_sparse size of 2 * tile_m and by default (at least in flex in core) we produce the bwds sparse info based off of the forward info. I added subtiling with a default of 2. So that we iterate 2 for all loaded q_indices.... | kernel: `flash_attn/cute/block_sparse_utils.py`, `flash_attn/cute/block_sparsity.py`, `flash_attn/cute/flash_bwd_sm100.py`, `flash_attn/cute/interface.py`<br>test: `tests/cute/test_mask_mod.py`, `tests/cute/test_score_mod.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2497](https://github.com/Dao-AILab/flash-attention/pull/2497) [FA4][hd256] Backward TMA bulk-store epilogue + LSE/dpsum coalesce | 2026-05-01 | arch_pipeline, attention_kv, norm_elementwise, scheduler_autotune, compiler_runtime | Two commits targeting the `sm100_hd256_2cta_fmha_backward` kernels on Blackwell (B200): **Commit 1 — `1748c9a`: Coalesce LSE/dpsum per-K-iter loads in dkdv** - Replaces scattered per-thread indexing (`thread_idx * N + i`) with warp-coalesced indexing (`thread_idx + i * threads_per_warp`) for LSE and... | kernel: `flash_attn/cute/sm100_hd256_2cta_fmha_backward_dkdvkernel.py`, `flash_attn/cute/sm100_hd256_2cta_fmha_backward_dqkernel.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2070](https://github.com/Dao-AILab/flash-attention/pull/2070) Add score-mod bwd support  | 2025-12-16 | arch_pipeline, attention_kv, benchmark_test, compiler_runtime | Adds the code paths to generate correct grads when fwd uses score_mod function. For some score_mods (score**2 case) we will need to have S live beyond when we would typically free and reuse tmem for P. For now I am keeping S around in rmem. I verified on toy that we are good on register usage, but r... | kernel: `flash_attn/cute/flash_bwd_sm100.py`, `flash_attn/cute/interface.py`, `flash_attn/cute/softmax.py`<br>test: `tests/cute/test_score_mod.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2365](https://github.com/Dao-AILab/flash-attention/pull/2365) [Sm90] Fix test_mask_mod and bwd block-sparse kwarg mismatch | 2026-03-17 | arch_pipeline, benchmark_test, attention_kv, compiler_runtime | - **test_mask_mod.py:** Update `_flash_attn_fwd` calls to use `tile_mn=(m, n)` instead of removed `m_block_size`/`n_block_size` params (broken since `99d0148`). Fixes 1,714 failures. - **flash_bwd_sm90.py:** Fix kwarg `num_wg_mma` → `num_mma_warp_groups` in `dQaccum_store_block_sparse_bwd_sm90` call... | kernel: `flash_attn/cute/flash_bwd_sm90.py`<br>test: `tests/cute/test_mask_mod.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1182](https://github.com/Dao-AILab/flash-attention/pull/1182) Add seqused_q in fwd / bwd and seqused_k in bwd in hopper FA. | 2024-09-16 | arch_pipeline, attention_kv | PR description Add seqused_q for Q and O in hopper FA fwd / bwd kernels; Add seqused_k for K and V in hopper FA bwd kernel; Update APIs defined in flash_attn_interface.py and flash_api.cpp: `mha_varlen_fwd` and `mha_varlen_bwd` API changes in flash_api.cpp are not backward compatible; Add unittests... | kernel: `flash_attn/flash_blocksparse_attention.py`, `hopper/flash_bwd_postprocess_kernel.h`, `hopper/flash_bwd_preprocess_kernel.h`<br>test: `hopper/test_flash_attn.py`, `tests/test_flash_attn.py`, `tests/test_rotary.py`, `tests/test_util.py`<br>wrapper: `flash_attn/bert_padding.py`, `flash_attn/models/bert.py`, `hopper/epilogue_bwd_sm90_tma.hpp`, `hopper/flash.h` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2510](https://github.com/Dao-AILab/flash-attention/pull/2510) [Cute,Bwd,Sm90] Fix determinism for GQA, port Sm100 approach in | 2026-04-30 | arch_pipeline, compiler_runtime, attention_kv, benchmark_test | [Cute,Bwd,Sm90] Fix determinism for GQA, port Sm100 approach in | kernel: `flash_attn/cute/flash_bwd_sm90.py`<br>test: `tests/cute/test_flash_attn_race_condition.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2137](https://github.com/Dao-AILab/flash-attention/pull/2137) score-mod backward SM90 | 2026-01-10 | arch_pipeline, attention_kv, compiler_runtime, benchmark_test | Stacked PRs: * 2158 * 2145 * 2146 * 2143 * __->__2137 * 2136 --- --- --- score-mod backward SM90 stack-pr keeps deleting my damn summary, sigh FWD: <img width="12505" height="5996" alt="image" src="https://github.com/user-attachments/assets/39f60280-8c39-4a04-840b-f2faf81859a0" /> BWD: <img width="1... | kernel: `flash_attn/cute/block_sparsity.py`, `flash_attn/cute/flash_bwd_sm90.py`, `flash_attn/cute/interface.py`, `flash_attn/cute/mask.py`<br>test: `tests/cute/test_mask_mod.py`, `tests/cute/test_score_mod.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2276](https://github.com/Dao-AILab/flash-attention/pull/2276) [Flex, Sm100] fix mask mod bugs | 2026-02-26 | arch_pipeline, attention_kv, compiler_runtime, benchmark_test | CC @tridao | kernel: `flash_attn/cute/block_sparse_utils.py`, `flash_attn/cute/mask.py`<br>test: `tests/cute/test_mask_mod.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2158](https://github.com/Dao-AILab/flash-attention/pull/2158) [CUTE][SM90] GQA backward non deterministic | 2026-01-10 | arch_pipeline, compiler_runtime, attention_kv, benchmark_test | Stacked PRs: * __->__2158 * 2145 * 2146 * 2143 * 2137 * 2136 --- --- --- [CUTE][SM90] GQA backward non deterministic <img width="1539" height="545" alt="image" src="https://github.com/user-attachments/assets/e1c03488-bca6-4bf0-93ff-b9efe62e5abb" /> <img width="1456" height="503" alt="image" src="htt... | kernel: `flash_attn/cute/flash_bwd_sm90.py`<br>test: `tests/cute/test_flash_attn.py`, `tests/cute/test_mask_mod.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2194](https://github.com/Dao-AILab/flash-attention/pull/2194) [Cute, SM90] fix fwd varlen Cute implementation bug for H100 | 2026-01-20 | arch_pipeline, compiler_runtime, scheduler_autotune, attention_kv | fixes 2192 This is a great explanation for what is the issue | kernel: `flash_attn/cute/flash_bwd.py`, `flash_attn/cute/flash_bwd_sm90.py`, `flash_attn/cute/flash_fwd.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2280](https://github.com/Dao-AILab/flash-attention/pull/2280) [Cute,Sm100,Bwd] Fix and enable 2CTA path for hdim 128 backward | 2026-02-28 | arch_pipeline, compiler_runtime, scheduler_autotune, attention_kv | Latest gain is about ~110 tflops on causal=False, ~90 for causal=True. code block | kernel: `flash_attn/cute/flash_bwd_sm100.py`, `flash_attn/cute/interface.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1868](https://github.com/Dao-AILab/flash-attention/pull/1868) flash-attn-cute bwd sm90 | 2025-09-13 | arch_pipeline, attention_kv, compiler_runtime | FlashAttention backward for SM90 in CuTe-DSL. Changes * Main kernel `flash_attn/cute/flash_bwd_sm90.py` * Minor updates `block_info.py`, `named_barrier.py` * Postprocessing kernel for SM90 `flash_bwd_postprocess.py`, * Inline PTX func added for TMA cpasync bulk reduce atomic add for dQaccum `hopper_... | kernel: `flash_attn/cute/block_info.py`, `flash_attn/cute/flash_bwd_postprocess.py`, `flash_attn/cute/flash_bwd_sm90.py`, `flash_attn/cute/hopper_helpers.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1993](https://github.com/Dao-AILab/flash-attention/pull/1993) [Cute,Fwd,Sm100] Support `q_stage=1` for inference | 2026-01-08 | arch_pipeline, compiler_runtime, attention_kv | This PR allows the SM100 forward kernel to be compiled for only a single q tile. We decide whether to use one or two q stages based on the max q sequence length in `cute/interface.py`. Extra warps freed up from the `softmax1` group are either set as empty warps or repurposed for `cp.async` loading.... | kernel: `flash_attn/cute/block_sparsity.py`, `flash_attn/cute/flash_fwd_sm100.py`, `flash_attn/cute/interface.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2330](https://github.com/Dao-AILab/flash-attention/pull/2330) [Bwd,Sm120] Add SM120 backward pass support | 2026-03-12 | arch_pipeline, attention_kv, compiler_runtime | Add backward pass support for SM120 (Blackwell GeForce / DGX Spark). SM120 uses SM80-era MMA instructions (`mma.sync.aligned.m16n8k16`) with 99 KB shared memory (vs 163 KB on SM80). - `FlashAttentionBackwardSm120` subclass overrides `can_implement()` with SM120 SMEM capacity (99 KB) - SM120 backward... | kernel: `flash_attn/cute/flash_bwd_postprocess.py`, `flash_attn/cute/flash_bwd_sm120.py`, `flash_attn/cute/interface.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2549](https://github.com/Dao-AILab/flash-attention/pull/2549) [Cute,Bwd,Sm100] fix incorrect calculation of n_block global max for bwd deterministic | 2026-05-08 | arch_pipeline, compiler_runtime, attention_kv | PR https://github.com/Dao-AILab/flash-attention/pull/2253 refactored the bwd dQ deterministic code by introducing a new `get_n_block_max_for_m_block` method, however this took in an incorrect calculation of `n_block_global_max` by using the cta rather than the cluster n tile (thus doubling the value... | kernel: `flash_attn/cute/block_info.py`, `flash_attn/cute/flash_bwd_sm100.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2235](https://github.com/Dao-AILab/flash-attention/pull/2235) [Cute,Fwd,Sm100] hdim 192 smem fix | 2026-02-05 | arch_pipeline, compiler_runtime, attention_kv | Fixes issue with decode hdim 192 in attn forward where we erroneously had set kv_stage = 4 in an earlier commit. | kernel: `flash_attn/cute/flash_fwd_sm100.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2563](https://github.com/Dao-AILab/flash-attention/pull/2563) [Cute, flex, sm90] fix sm90 flex | 2026-05-13 | arch_pipeline, compiler_runtime, attention_kv | pass the seqlen para for `produce_block_sparse_loads` in sm90 fwd. Break by https://github.com/Dao-AILab/flash-attention/pull/2224 A simple bug reproduce script on sm90: code block And I wonder why such a bug was not detected by the CI ?? | kernel: `flash_attn/cute/flash_fwd_sm90.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2209](https://github.com/Dao-AILab/flash-attention/pull/2209) [Flex][SM100] Replay expand fix on sm100 | 2026-01-26 | arch_pipeline, attention_kv, compiler_runtime | [Flex][SM100] Replay expand fix on sm100 | kernel: `flash_attn/cute/flash_bwd_sm100.py`, `flash_attn/cute/flash_fwd_sm100.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2014](https://github.com/Dao-AILab/flash-attention/pull/2014) [Cute,Sm100,Fwd] use correction warps for epi when not using TMA | 2025-11-19 | arch_pipeline, compiler_runtime | This PR switches the sm100 fwd epilogue over to using the 4 correction warps when not using TMA store (e.g., when query seqlen is variable), instead of adding the empty warp. This also enables Paged KV in general with varlen since we eliminate the contention issue between producer and epilogue warps... | kernel: `flash_attn/cute/block_sparse_utils.py`, `flash_attn/cute/flash_fwd_sm100.py`, `flash_attn/cute/interface.py`<br>test: `tests/cute/test_flash_attn.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2026](https://github.com/Dao-AILab/flash-attention/pull/2026) [Cute,Fwd,Sm100] don't pass mask_fn to softmax_step generically | 2025-11-22 | arch_pipeline, attention_kv, compiler_runtime | Fixes perf regression from masking on every iteration: with fix: code block without fix: code block | kernel: `flash_attn/cute/flash_fwd_sm100.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2292](https://github.com/Dao-AILab/flash-attention/pull/2292) Fix sm100 fwd missing tSrQs init regression | 2026-02-28 | arch_pipeline, attention_kv, compiler_runtime | Stacked PRs: * __->__2292 * 2291 --- --- --- Fix sm100 fwd missing tSrQs init regression Main is broken with me, looks this was removed on accident | kernel: `flash_attn/cute/flash_fwd_sm100.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2293](https://github.com/Dao-AILab/flash-attention/pull/2293) Fix sm100 fwd missing tSrQs init regression | 2026-03-01 | arch_pipeline, attention_kv, compiler_runtime | Dupe of https://github.com/Dao-AILab/flash-attention/pull/2292 | kernel: `flash_attn/cute/flash_fwd_sm100.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2136](https://github.com/Dao-AILab/flash-attention/pull/2136) block-sparse backward SM90 | 2026-01-10 | arch_pipeline, attention_kv, compiler_runtime | Stacked PRs: * 2158 * 2145 * 2146 * 2143 * 2137 * __->__2136 --- --- --- block-sparse backward SM90 | kernel: `flash_attn/cute/block_sparse_utils.py`, `flash_attn/cute/flash_bwd_sm90.py`, `flash_attn/cute/interface.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2345](https://github.com/Dao-AILab/flash-attention/pull/2345) fix the create_ragged_tensor_for_tma issue | 2026-03-14 | arch_pipeline, attention_kv, compiler_runtime | 2344 The quack-kernels 0.2.10 doesn't have the create_ragged_tensor_for_tma issue function in quack.utils. | other: `flash_attn/cute/pyproject.toml` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2309](https://github.com/Dao-AILab/flash-attention/pull/2309) [Fwd,Sm100] Extract named barriers | 2026-03-09 | arch_pipeline, attention_kv, compiler_runtime | Stacked PRs: * 2218 * __->__2309 * 2227 --- --- --- [Fwd,Sm100] Extract named barriers | kernel: `flash_attn/cute/flash_fwd_sm100.py`, `flash_attn/cute/named_barrier.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2504](https://github.com/Dao-AILab/flash-attention/pull/2504) [SM100] Guard gO None in empty-tile correction | 2026-04-27 | arch_pipeline, attention_kv, compiler_runtime | [SM100][BlockSparse] Guard `gO` indexing with `None` check in empty-tile correction Symptom `cute.compile` raises a Python `TypeError` at trace time, the kernel never launches. Triggers when **all** of the following hold: 1. SM100 (GB200). 2. FlexAttention with a BlockMask (i.e. `use_block_sparsity=... | kernel: `flash_attn/cute/block_sparse_utils.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2315](https://github.com/Dao-AILab/flash-attention/pull/2315) [Cute, SM90, bwd] Wire seqused_q/k through backward pass | 2026-03-12 | arch_pipeline, compiler_runtime, attention_kv | Enable \seqused_q/k\ support in SM90 backward pass by wiring the parameters through to \SeqlenInfoCls\. Relates to 2163 Root Cause \FlashAttentionBackwardSm90.__call__()\ accepts \mSeqUsedQ\ and \mSeqUsedK\ parameters, but \SeqlenInfoCls\ creation hardcodes them to \None\: \\\python Before (flash_bw... | Open PR diff and inspect changed kernel/test/benchmark paths. | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2329](https://github.com/Dao-AILab/flash-attention/pull/2329) SM120 forward pass (Blackwell GeForce / DGX Spark) | 2026-03-12 | arch_pipeline, attention_kv | - Add `FlashAttentionForwardSm120`, a thin subclass of `FlashAttentionForwardSm80` that overrides `can_implement()` with SM120's 99 KB SMEM capacity check - Wire SM120 dispatch into `interface.py` with SM120-tuned tile sizes and SM80-compatible argument ordering - Fix an SM80 causal softmax bug in `... | kernel: `flash_attn/cute/flash_fwd.py`, `flash_attn/cute/flash_fwd_sm120.py`, `flash_attn/cute/interface.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2332](https://github.com/Dao-AILab/flash-attention/pull/2332) [cutlass] Allow compilation of cutlass FA3 for sm100 via enable_sm90 | 2026-03-12 | arch_pipeline, attention_kv | Rename enable_sm90_or_later to enable_sm90 and restrict the arch guard from `__CUDA_ARCH__ == 900` to `__CUDA_ARCH__ == 900`. FA3's sm90 kernels use CUTLASS GMMA (wgmma) instructions that are gated behind CUTE_ARCH_MMA_SM90A_ENABLED, which requires `__CUDA_ARCH__ == 900` (exact match). On sm100 (Bla... | wrapper: `hopper/flash_bwd_launch_template.h`, `hopper/flash_fwd_launch_template.h`, `hopper/utils.h` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1860](https://github.com/Dao-AILab/flash-attention/pull/1860) [BUG] CUDA 13: make FA3 compatible with CUDA 13 Builds | 2025-09-04 | arch_pipeline, attention_kv | @tridao Fix CUDA barrier init crash when num_consumers < NumThreadsPerWarpGroup Previously, integer division caused num_consumer_warpgroups_per_cluster to be 0 when params.num_consumers (e.g., 32) was less than NumThreadsPerWarpGroup (128), leading to a compiler failure during barrier initialization... | wrapper: `hopper/sm90_pipeline_no_cluster.hpp` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1511](https://github.com/Dao-AILab/flash-attention/pull/1511) Fix CUDA 12.1 build | 2025-03-01 | arch_pipeline | Building using CUDA 12.1 was resulting in: code block which appears to be from slight differences in template type deduction | wrapper: `hopper/mainloop_fwd_sm90_tma_gmma_ws.hpp` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1949](https://github.com/Dao-AILab/flash-attention/pull/1949) Fix hopper cuda 13 build | 2025-10-21 | arch_pipeline | Updates the hopper build to use system CCCL headers instead of downloading the toolchain. Without this patch, I get errors in the build about CUB headers being missing. From what I can tell, it seems like the toolchain download is incompatible with cuda 13? Not completely sure, but this implements t... | wrapper: `hopper/setup.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1240](https://github.com/Dao-AILab/flash-attention/pull/1240) Fix FAv3 compilation with MSVC | 2024-09-20 | arch_pipeline | This PR fixes FAv3 compilation with MSVC. There are 2 issues: - `-D_USE_MATH_DEFINES` is needed for Math constants in MSVC, specifically `M_LN2` used in `hopper/softmax.h`. - `-Xcompiler=/Zc:__cplusplus` sets `__cplusplus` correctly, in turn this enables `CUTLASS_CONSTEXPR_IF_CXX17` in Cutlass which... | wrapper: `hopper/setup.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1882](https://github.com/Dao-AILab/flash-attention/pull/1882) [NVIDIA] Enable Blackwell Family Specific | 2025-09-12 | arch_pipeline | - Article: https://developer.nvidia.com/blog/nvidia-blackwell-and-nvidia-cuda-12-9-introduce-family-specific-architecture-features/ - This make compatible with < 12.9 CUDA and add Family Specific for >=CUDA 12.9 - Also correct fix typo - Added 11.0f (Jetson Thor, new era of Robotics) - With family s... | wrapper: `setup.py`<br>other: `.github/workflows/publish.yml` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |

### Compiler / Runtime

Use this section for: Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests.
NCU first look: Generated kernel shape, launch overhead, occupancy, register pressure, and compile-time-selected schedule.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#1961](https://github.com/Dao-AILab/flash-attention/pull/1961) [CuTe DSL] Update "buffers" name to "aux_tensors"; fix flex bugs | 2025-10-24 | compiler_runtime, benchmark_test, attention_kv, arch_pipeline | This PR does two things: 1) changes the variable `buffers` -- which hold auxiliary tensors used by FlexAttention `score_mod` and `mask_mod` -- to `aux_tensors`. 2) fixes bugs impeding score_mod plus mask_mod tests from validating. | kernel: `flash_attn/cute/barrier.py`, `flash_attn/cute/block_sparsity.py`, `flash_attn/cute/flash_fwd.py`, `flash_attn/cute/flash_fwd_sm100.py`<br>benchmark: `flash_attn/cute/benchmark_mask_mod.py`<br>test: `tests/cute/test_flash_attn.py`, `tests/cute/test_mask_mod.py`, `tests/cute/test_score_mod.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2402](https://github.com/Dao-AILab/flash-attention/pull/2402) feat(cute): implement softcap backward pass, correct math formula, and resolve JIT cache bug | 2026-04-11 | compiler_runtime, arch_pipeline, attention_kv, memory_primitives, benchmark_test | Description This PR initially addressed the numerical deviation in FA4's softcap score modulation and aligned its output with the native FA2 and FA3 implementations. During the review and testing phase, it was expanded to fully implement the softcap backward pass and resolve underlying JIT compilati... | kernel: `flash_attn/cute/flash_bwd.py`, `flash_attn/cute/flash_bwd_sm100.py`, `flash_attn/cute/flash_bwd_sm90.py`, `flash_attn/cute/flash_fwd.py`<br>test: `tests/cute/test_flash_attn.py`, `tests/cute/test_flash_attn_race_condition.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2097](https://github.com/Dao-AILab/flash-attention/pull/2097) [Cute] Fix shuffle sync and enable pack gqa for varlen sm100 | 2025-12-24 | compiler_runtime, arch_pipeline, scheduler_autotune, attention_kv, benchmark_test | This PR fixes the layout for the value tensor used in `shuffle_sync` to make it compatible with `recast_tensor`. The problem was that `recast_tensor` for i64 -> i32 wasn't expanding stride 0 modes, for example with `val = cute.make_fragment(1, type(value))` we had code block This caused IMA when com... | kernel: `flash_attn/cute/flash_fwd_sm100.py`, `flash_attn/cute/interface.py`, `flash_attn/cute/utils.py`<br>test: `tests/cute/test_flash_attn.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2236](https://github.com/Dao-AILab/flash-attention/pull/2236) [Cute,Flex,Fwd] Allow vectorized score_mod definitions | 2026-02-11 | compiler_runtime, attention_kv, arch_pipeline, benchmark_test, scheduler_autotune | [Reupload of 2215 with Sm90] This PR is a `score_mod` "power user" update that allows the user to specify vectorization for a given `score_mod`. It does so in two ways: - One can set `score_mod.__vec_size__` and have the kernel read that, instead of using the current logic (vec_size = 2 if no `aux_t... | kernel: `flash_attn/cute/cute_dsl_utils.py`, `flash_attn/cute/flash_fwd.py`, `flash_attn/cute/flash_fwd_sm100.py`, `flash_attn/cute/interface.py`<br>test: `tests/cute/score_mod_definitions.py`, `tests/cute/test_score_mod.py`, `tests/cute/test_score_mod_varlen.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2275](https://github.com/Dao-AILab/flash-attention/pull/2275) [CuteDSL][SM90] varlen bwd works | 2026-03-10 | compiler_runtime, scheduler_autotune, arch_pipeline, attention_kv, benchmark_test | !image `tests/cute/test_flash_attn_varlen.py` passes. The code is a bit messy will clean it up during the weekend | kernel: `flash_attn/cute/flash_bwd_sm90.py`, `flash_attn/cute/interface.py`<br>test: `tests/cute/test_flash_attn_varlen.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2371](https://github.com/Dao-AILab/flash-attention/pull/2371) [CuTe,Flex] limit vec_size to 2 for score mod when not on Sm100 | 2026-03-18 | compiler_runtime, arch_pipeline, benchmark_test, attention_kv, scheduler_autotune | This addresses the discussion in 2365: `vec_size > 2` is generically only supported on Sm100 due to accumulator thread ownership pattern. Tests modified to skip unsupported `vec_size` while still checking supported ones. Theoretically we could write architecture-specific vectorized score mods and li... | kernel: `flash_attn/cute/flash_fwd.py`<br>test: `tests/cute/test_score_mod.py`, `tests/cute/test_score_mod_varlen.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2304](https://github.com/Dao-AILab/flash-attention/pull/2304) [Cute][Testing] Add persistent compile cache for cutedsl AOT compilation | 2026-03-04 | compiler_runtime, benchmark_test, memory_primitives, scheduler_autotune | Splitting main PR 2279 to smaller ones. Introduce `cache_utils.py` with dict-alike cache classes that support ahead-of-time (AOT) compiled kernel persistence: - `JITCache`: in-memory compile cache (drop-in replacement for `{}`) - `JITPersistentCache`: disk-backed cache using cutedsl `export_to_c` wi... | kernel: `flash_attn/cute/cache_utils.py`, `flash_attn/cute/interface.py`<br>test: `tests/cute/test_mask_mod.py`<br>other: `flash_attn/cute/pyproject.toml` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2189](https://github.com/Dao-AILab/flash-attention/pull/2189) [Cute][Flex] Fix expanded tensor bug | 2026-01-17 | compiler_runtime, arch_pipeline, attention_kv, benchmark_test | [Cute][Flex] Fix expanded tensor bug Ran on hopper:: <img width="850" height="846" alt="image" src="https://github.com/user-attachments/assets/e04a7805-0a8e-469f-9113-eae74cd5c97f" /> | kernel: `flash_attn/cute/flash_bwd.py`, `flash_attn/cute/flash_bwd_sm90.py`, `flash_attn/cute/flash_fwd.py`<br>test: `tests/cute/test_mask_mod.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2258](https://github.com/Dao-AILab/flash-attention/pull/2258) [Cute][Flex] Fix kernel hang w/ multiple empty tiles | 2026-02-16 | compiler_runtime, arch_pipeline, attention_kv, benchmark_test | [Cute][Flex] Fix kernel hang w/ multiple empty tiles I think these werent surfaced because I didnt have any test that goes from empty row to empty row This fixes basically 3 bugs with our phasing related to empty rows 1. `handle_block_sparse_empty_tile_correction_sm100` for empty tiles we need to st... | kernel: `flash_attn/cute/block_sparse_utils.py`, `flash_attn/cute/flash_fwd_sm100.py`<br>test: `tests/cute/test_mask_mod.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2470](https://github.com/Dao-AILab/flash-attention/pull/2470) [Cute,Fwd,Sm100] Fix the crash when seqlen_k=0 | 2026-04-20 | compiler_runtime, attention_kv, arch_pipeline, benchmark_test | Fix crash when K/V has `seqlen == 0` (e.g. vLLM CUDA-graph capture with an all-padding batch). Two crash sites, one root cause (`seqlen_k == 0` flows into tensor shapes): - **`causal=False`** — `StaticPersistentTileScheduler` passes host setup; kernel launches; the first TMA over `k.shape=(B, 0, H,... | kernel: `flash_attn/cute/interface.py`<br>test: `tests/cute/test_flash_attn.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2035](https://github.com/Dao-AILab/flash-attention/pull/2035) [Cute] Fix head dim 64 bwd | 2025-12-02 | compiler_runtime, attention_kv, arch_pipeline, benchmark_test | Adds support for headdim64, a myriad of small fixes: 1. https://github.com/Dao-AILab/flash-attention/pull/2035/filesdiff-ef5e5c95ec953948e93c912b7d92841bf839138364dc065c4d567bcc8bd7a3c5R326 we had a divison that made the num_stages -> 0 2. https://github.com/Dao-AILab/flash-attention/pull/2035/files... | kernel: `flash_attn/cute/flash_bwd_sm100.py`<br>test: `tests/cute/test_flash_attn.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2369](https://github.com/Dao-AILab/flash-attention/pull/2369) [Cute, Testing] Fix aot + tvm-ffi EnvStream related parameter mismatch | 2026-03-18 | compiler_runtime, arch_pipeline, benchmark_test | Follow-up the clean-up at 10bbfd0e246b99feabfe620a355f9213eeb6c9b5, which switched to use `cute.runtime.make_fake_stream(use_tvm_ffi_env_stream=True)` instead of relying on getting a concrete cuda stream at kernel compilation time. However, the tvm_ffi EnvStream feature excludes the `stream` paramet... | kernel: `flash_attn/cute/flash_bwd.py`, `flash_attn/cute/flash_bwd_postprocess.py`, `flash_attn/cute/flash_bwd_preprocess.py`, `flash_attn/cute/flash_bwd_sm100.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2031](https://github.com/Dao-AILab/flash-attention/pull/2031) [Cute,Fwd] enable mask mod without blocksparsity | 2025-11-25 | compiler_runtime, arch_pipeline, benchmark_test, attention_kv | This PR removes the limitation that `mask_mod` may only be used alongside block sparsity. Tested on both Sm90 and Sm100 <img width="1723" height="137" alt="Screenshot 2025-11-24 at 2 58 52 PM" src="https://github.com/user-attachments/assets/beadd762-8f90-4318-a942-50d8623005ec" /> <img width="815" h... | kernel: `flash_attn/cute/flash_fwd.py`, `flash_attn/cute/flash_fwd_sm100.py`, `flash_attn/cute/interface.py`<br>test: `tests/cute/test_mask_mod.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2145](https://github.com/Dao-AILab/flash-attention/pull/2145) [CUTE][SM90]Enable pack-gqa with broadcasted maskmods | 2026-01-10 | compiler_runtime, arch_pipeline, attention_kv, benchmark_test | Stacked PRs: * 2158 * __->__2145 * 2146 * 2143 * 2137 * 2136 --- --- --- [CUTE][SM90]Enable pack-gqa with broadcasted maskmods looks good just small tolerance issues with small N case <img width="1107" height="211" alt="image" src="https://github.com/user-attachments/assets/9ec394a0-ad22-4447-9698-3... | kernel: `flash_attn/cute/block_sparse_utils.py`, `flash_attn/cute/flash_fwd.py`, `flash_attn/cute/interface.py`<br>test: `tests/cute/test_mask_mod.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1983](https://github.com/Dao-AILab/flash-attention/pull/1983) [CuTe DSL] Block sparsity computation kernel | 2025-11-12 | compiler_runtime, benchmark_test, attention_kv | This PR adds a block sparsity computation kernel to compute `blocksparse_tensors: BlockSparseTensors` from `mask_mod`. This is an improvement over the PyTorch implementation on the whole. code block | kernel: `flash_attn/cute/compute_block_sparsity.py`, `flash_attn/cute/interface.py`, `flash_attn/cute/mask_definitions.py`<br>benchmark: `benchmarks/cute/benchmark_block_sparsity.py`, `benchmarks/cute/benchmark_mask_mod.py`<br>test: `tests/cute/test_block_sparsity.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2487](https://github.com/Dao-AILab/flash-attention/pull/2487) [Cute,hd256] Post-merge cleanup: dead code, duplicate imports | 2026-04-23 | compiler_runtime, attention_kv, benchmark_test, scheduler_autotune | Follow-up Cleanup for hd256 Feature (2412) Follow-up polish for the newly merged hd256 feature (2412), based on Copilot review comments. No kernel code was changed and there is no behavior change. Changes - **`flash_attn/cute/interface.py`** - Removed duplicate `Int32` import - Removed unused `MaskE... | kernel: `flash_attn/cute/interface.py`, `flash_attn/cute/mask.py`<br>test: `tests/cute/test_flash_attn.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2481](https://github.com/Dao-AILab/flash-attention/pull/2481) [cute,bwd] fix PDL race in bwd_preprocess, which corrupting dpsum on SM90+ | 2026-04-21 | compiler_runtime, attention_kv, scheduler_autotune, arch_pipeline | Bug: `bwd_preprocess` missing `griddepcontrol_wait()`, causing silent gradient corruption on SM90+ Root Cause `FlashAttentionBackwardPreprocess.kernel` is launched with `use_pdl=True` (on SM90+), but had no `griddepcontrol_wait()` before its first GMEM read. Under PDL, the GPU is allowed to start th... | kernel: `flash_attn/cute/flash_bwd_preprocess.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2544](https://github.com/Dao-AILab/flash-attention/pull/2544) [CuTe,Bwd] guard softcap for varlen backward | 2026-05-07 | compiler_runtime, scheduler_autotune, attention_kv, benchmark_test | Softcap is currently not supported in varlen backward, but is not properly guarded. This is a band-aid fix until score_mod is supported with varlen in backward. For example, `pytest tests/cute/test_flash_attn.py::test_flash_attn_varlen_output -k "True-True-True-False-random-128-217-128-False-True-0-... | kernel: `flash_attn/cute/interface.py`<br>test: `tests/cute/test_flash_attn.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2185](https://github.com/Dao-AILab/flash-attention/pull/2185) [Cute,Fwd,Sm100] Add r2p for local mask | 2026-01-17 | compiler_runtime, arch_pipeline, attention_kv, benchmark_test | The idea is similar to mask_r2p. Using ~~xor~~ `mask_range = mask_right & ~ mask_left` because: code block Notes: * Original idea was to use xor. But that would require assuming window_size_left + window_size_right >= 0. I can't tell if xor is really better. * I wanted to avoid taking min(right_s, 2... | kernel: `flash_attn/cute/mask.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2283](https://github.com/Dao-AILab/flash-attention/pull/2283) [Cute][Testing] Add fake tensor mode support for compile-only test passes | 2026-03-03 | compiler_runtime, benchmark_test, attention_kv | Splitting main PR 2279 to smaller ones. Use torch FakeTensorMode to enable cute.compile without allocating GPU memory or running kernels. This allows a fast pre-compilation pass (e.g., with pytest-xdist parallelism) to populate the compile cache. Changes: - Add `maybe_fake_tensor_mode` decorator and... | kernel: `flash_attn/cute/interface.py`<br>test: `flash_attn/cute/testing.py`, `tests/cute/test_flash_attn.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2149](https://github.com/Dao-AILab/flash-attention/pull/2149) [Cute] Fix two tests that were failing  | 2026-01-08 | compiler_runtime, benchmark_test, attention_kv | - Add missing COMPUTE_CAPABILITY definition in test_score_mod.py to fix paged KV cache tests (https://github.com/Dao-AILab/flash-attention/pull/2127) - Fix missing seqlen_info parameter in mask_mod call in flash_attn/cute/mask.py (cc @reubenconducts from https://github.com/Dao-AILab/flash-attention/... | kernel: `flash_attn/cute/mask.py`<br>test: `tests/cute/test_score_mod.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2177](https://github.com/Dao-AILab/flash-attention/pull/2177) [Cute][Flex] add back in contig | 2026-01-15 | compiler_runtime, attention_kv, benchmark_test | Stacked PRs: * 2180 * __->__2177 --- --- --- [Cute][Flex] add back in contig See comments. I add the helper to the cutedsl utils. I think in theory this could happen for q,k,v as well but its very unlikely. MQA -> we dont actually expand the tensors. we could I guess expand B for k, v in which case... | kernel: `flash_attn/cute/block_sparsity.py`, `flash_attn/cute/cute_dsl_utils.py`, `flash_attn/cute/interface.py`<br>test: `tests/cute/test_mask_mod.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2543](https://github.com/Dao-AILab/flash-attention/pull/2543) [CuTe,Bwd,Sm100] don't disable 2cta due to cuda 12 in bwd | 2026-05-06 | compiler_runtime, scheduler_autotune, arch_pipeline, attention_kv | [CuTe,Bwd,Sm100] don't disable 2cta due to cuda 12 in bwd | kernel: `flash_attn/cute/interface.py`, `flash_attn/cute/utils.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2485](https://github.com/Dao-AILab/flash-attention/pull/2485) [CuTe,Flex] Wire up interface for flex autograd support | 2026-04-23 | compiler_runtime, benchmark_test, attention_kv | Addresses 2424. Added `use_autograd` as an option for testing flex backward. cc @drisspg | kernel: `flash_attn/cute/interface.py`<br>test: `tests/cute/test_mask_mod.py`, `tests/cute/test_score_mod.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2048](https://github.com/Dao-AILab/flash-attention/pull/2048) Add hash attr to shortcut expensive check | 2025-12-07 | compiler_runtime, benchmark_test, attention_kv | I noticed that on the hot path this I very expensive, and since in inductor we can ensure that nothing changes under the hoood we can always map to the right cache entry. I tried just swapping to id(func) and we cache missed everytime, this feels robust enough. From microbenchmarks hashing score_mod... | kernel: `flash_attn/cute/utils.py`<br>test: `tests/cute/test_utils.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2146](https://github.com/Dao-AILab/flash-attention/pull/2146) [CUTE][SM100] Fix backward gqa on sm100 post mask-mod semantic change | 2026-01-10 | compiler_runtime, arch_pipeline, benchmark_test | Stacked PRs: * 2158 * 2145 * __->__2146 * 2143 * 2137 * 2136 --- --- --- [CUTE][SM100] Fix backward gqa on sm100 post mask-mod semantic change https://github.com/Dao-AILab/flash-attention/pull/2149 helps here | test: `tests/cute/test_mask_mod.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1892](https://github.com/Dao-AILab/flash-attention/pull/1892) [Cute] Make testing utils standalone for cute | 2025-09-17 | compiler_runtime, benchmark_test, attention_kv | - Extracts testing utilities into a standalone module for the cute DSL implementation - Creates `flash_attn/cute/testing.py` with commonly used test functions - Updates test imports to use the new standalone testing module Changes - Add `flash_attn/cute/testing.py` with extracted testing utilities -... | test: `flash_attn/cute/testing.py`, `tests/cute/test_flash_attn.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2143](https://github.com/Dao-AILab/flash-attention/pull/2143) [Cute] Clarify and fix subtle cachekey bug | 2026-01-10 | compiler_runtime, memory_primitives, attention_kv | Stacked PRs: * 2158 * 2145 * 2146 * __->__2143 * 2137 * 2136 --- --- --- [Cute] Clarify and fix subtle cachekey bug | kernel: `flash_attn/cute/interface.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2180](https://github.com/Dao-AILab/flash-attention/pull/2180) [Cute][Flex]Add pack-gqa divmod | 2026-01-15 | compiler_runtime, attention_kv, arch_pipeline | [Cute][Flex]Add pack-gqa divmod This really helps with gqa fwd perf for causal via blocksparse: <img width="788" height="245" alt="image" src="https://github.com/user-attachments/assets/511ba8ac-a08d-4e0d-adb6-11e5f39a0aff" /> With this change: <img width="555" height="309" alt="image" src="https://... | kernel: `flash_attn/cute/flash_fwd_sm100.py`, `flash_attn/cute/mask.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2065](https://github.com/Dao-AILab/flash-attention/pull/2065) [CUTE] Allow grads to be preallocated | 2025-12-15 | compiler_runtime, attention_kv, benchmark_test | See title :) | kernel: `flash_attn/cute/interface.py`<br>test: `tests/cute/test_flash_attn.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2171](https://github.com/Dao-AILab/flash-attention/pull/2171) [Cute,Flex] Add option to create and cache __cute_hash__ | 2026-01-12 | compiler_runtime, memory_primitives, attention_kv | Prevents unnecessary inspection and rehashing for callables. cc @drisspg @tridao | kernel: `flash_attn/cute/utils.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2172](https://github.com/Dao-AILab/flash-attention/pull/2172) [Cute][Flex] Remove no longer needed contig | 2026-01-12 | compiler_runtime, attention_kv, benchmark_test | [Cute][Flex] Remove no longer needed contig <img width="1186" height="934" alt="image" src="https://github.com/user-attachments/assets/13c5b4fa-efe3-49db-b635-fbe438565721" /> | kernel: `flash_attn/cute/block_sparsity.py`<br>test: `tests/cute/test_mask_mod.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2313](https://github.com/Dao-AILab/flash-attention/pull/2313) [Cute,Sm100] Introduce a flexible lambda-based R2P masking | 2026-03-12 | compiler_runtime, arch_pipeline, attention_kv | Introduce a flexible lambda-based R2P masking. Also switching from 24-col to 32-col bitmasks with inline PTX shifts. Motivation The masking code has 3 separate implementations (`mask_r2p`, `mask_r2p_transposed`, `mask_r2p_dual_bound`), each reimplementing the same 24-col R2P loop with slight variati... | kernel: `flash_attn/cute/mask.py`, `flash_attn/cute/utils.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2056](https://github.com/Dao-AILab/flash-attention/pull/2056) fixing cute bwd func def | 2025-12-09 | compiler_runtime, attention_kv, arch_pipeline | **Summary** fixes the error below when running `pytest tests/cute/test_flash_attn.py::test_flash_attn_output` <img width="1238" height="515" alt="Screenshot 2025-12-08 at 5 17 32 PM" src="https://github.com/user-attachments/assets/8e5ff715-7c51-4024-be80-4fb2565a731d" /> | kernel: `flash_attn/cute/flash_bwd_sm90.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2298](https://github.com/Dao-AILab/flash-attention/pull/2298) [CuTe] Include broadcast dims in backward compile cache keys | 2026-03-03 | compiler_runtime, memory_primitives, attention_kv | `to_cute_tensor()` calls `mark_layout_dynamic()` (from cutlass-dsl) to make tensor strides dynamic at compile time. However, `stride=0` is kept as a **static** constraint in the compiled kernel — a broadcast dimension gets baked in as `(0, ?, ?, 1)` rather than `(?, ?, ?, 1)`. The backward compile c... | kernel: `flash_attn/cute/interface.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2297](https://github.com/Dao-AILab/flash-attention/pull/2297) [CuTeDSL][Sm80] basic fix for new api | 2026-03-11 | compiler_runtime, attention_kv | fix CuTeDSL FA foward & backward basic API errors. Now FlashAttentionForwardSm80 & FlashAttentionBackwardSm80 can be enabled for sm80. Also there are many missing features under sm80, mentioned by https://github.com/Dao-AILab/flash-attention/pull/2103. For Backward, in my local test, dim 128 works,... | kernel: `flash_attn/cute/flash_bwd.py`, `flash_attn/cute/flash_fwd.py`, `flash_attn/cute/interface.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1858](https://github.com/Dao-AILab/flash-attention/pull/1858) [FIX] Allow m_block_size == 192 and mma_pv_is_rs == False in Sm90 CuTe DSL | 2025-09-02 | compiler_runtime, arch_pipeline | The purpose of this PR is twofold: 1. to add support for `mma_pv_is_rs == False`, which is currently always set to `True` 2. to allow 3 mma warp groups, thus enabling `m_block_size == 192`, and as a result head dimension 96. Currently, the kernel hangs with `m_block_size == 192` due to launching too... | kernel: `flash_attn/cute/flash_fwd.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2271](https://github.com/Dao-AILab/flash-attention/pull/2271) [cute] Add return_lse | 2026-02-26 | compiler_runtime, attention_kv | Adds `return_lse` as an explicit parameter to the cute interface. Currently the lse tensor is only not `None` if one of q/k/v requires grad. Question: Should this be called `return_attn_probs` to fit with the FA2/FA3 naming of this parameter or `return_lse` to fit with existing name in `_flash_attn_... | kernel: `flash_attn/cute/interface.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2335](https://github.com/Dao-AILab/flash-attention/pull/2335) [Cute] fix: rename logging module to avoid circular import at building | 2026-03-12 | compiler_runtime, attention_kv | Related to 2334 `flash_attn/cute/logging.py` has the same name as Python's stdlib logging module. When we run `pip install -e`, setuptools adds the package source directory (`flash_attn/cute/`) to sys.path. Then when setuptools internally does import logging (which it needs for its own setup), Pytho... | kernel: `flash_attn/cute/fa_logging.py`, `flash_attn/cute/interface.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1984](https://github.com/Dao-AILab/flash-attention/pull/1984) [Cute] Extract block-sparse utilities from SM80/90 | 2025-11-05 | compiler_runtime, attention_kv | summary - Create block_sparse_utils.py with SM80/90 block-sparse logic - Refactor flash_fwd.py to use extracted utilities This extracts the block-sparse consumer loop and related utilities from flash_fwd.py into a reusable module for SM80/90 architectures | kernel: `flash_attn/cute/block_sparse_utils.py`, `flash_attn/cute/block_sparsity.py`, `flash_attn/cute/flash_fwd.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |

### Benchmark / Test Evidence

Use this section for: Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code.
NCU first look: Use the PR's benchmark/profile command as the first replay target.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#1100](https://github.com/Dao-AILab/flash-attention/pull/1100) Fp8 kernel with "in-kernel" transpose of V in producer | 2024-07-30 | benchmark_test, arch_pipeline, attention_kv, gemm_quant, memory_primitives | This PR adds the separate code path for the fp8 kernel with transpose of V in the producer warpgroup, with all tests given in the script passing. There's a lot of duplication of code that we might want to address before merging into main. Also, I haven't added the max offset subtraction yet (by 8.0f... | kernel: `hopper/flash_fwd_kernel.h`, `hopper/kernel_traits.h`, `hopper/tile_scheduler.hpp`<br>benchmark: `hopper/benchmark_flash_attention_fp8.py`<br>test: `hopper/test_flash_attn.py`<br>wrapper: `hopper/epilogue_fwd_sm90_tma.hpp`, `hopper/flash_api.cpp`, `hopper/flash_fwd_launch_template.h`, `hopper/mainloop_fwd_sm90_tma_gmma_ws.hpp`<br>other: `hopper/flash_fwd_hdim128_e4m3_sm90.cu`, `hopper/flash_fwd_hdim256_e4m3_sm90.cu`, `hopper/flash_fwd_hdim64_e4m3_sm90.cu` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2224](https://github.com/Dao-AILab/flash-attention/pull/2224) [CuTe,Flex] varlen blocksparsity | 2026-05-07 | benchmark_test, scheduler_autotune, compiler_runtime, arch_pipeline, attention_kv | This PR extends blocksparsity to the variable sequence length case. Whereas in batched blocksparsity the metadata tensors take the shapes code block in varlen blocksparsity, we pack our metadata tensors to take the shapes code block where `total_m_blocks` is the sum of all `m` blocks per head (equiv... | kernel: `flash_attn/cute/block_sparse_utils.py`, `flash_attn/cute/block_sparsity.py`, `flash_attn/cute/compute_block_sparsity.py`, `flash_attn/cute/flash_fwd_sm100.py`<br>benchmark: `tests/cute/benchmark_block_sparsity.py`<br>test: `tests/cute/mask_mod_definitions.py`, `tests/cute/test_block_sparsity.py`, `tests/cute/test_mask_mod.py`, `tests/cute/test_mask_mod_varlen.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2100](https://github.com/Dao-AILab/flash-attention/pull/2100) [Cute,Fwd] improved block sparsity | 2026-01-05 | benchmark_test, compiler_runtime, arch_pipeline, attention_kv | This PR introduces a number of quality-of-life changes regarding block sparsity computation, including enabling `tvm-ffi` for the block sparsity computation interface. We expand the signature of `mask_mods` to code block to match those of `score_mods`, in particular enabling offset `mask_mods`. | kernel: `flash_attn/cute/block_sparse_utils.py`, `flash_attn/cute/block_sparsity.py`, `flash_attn/cute/compute_block_sparsity.py`, `flash_attn/cute/cute_dsl_utils.py`<br>benchmark: `tests/cute/benchmark_block_sparsity.py`, `tests/cute/benchmark_mask_mod.py`<br>test: `tests/cute/mask_mod_definitions.py`, `tests/cute/test_block_sparsity.py`, `tests/cute/test_mask_mod.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2187](https://github.com/Dao-AILab/flash-attention/pull/2187) [Cute][Flex] Allow q_offset 1 and add block-sizes to disambiguate edge cases | 2026-01-22 | benchmark_test, compiler_runtime, arch_pipeline, attention_kv | [Cute][Flex] Allow q_offset 1 and add block-sizes to disambiguate edge cases Main chunk of kernel work here is allowing subtiling of blocksparse data in the fwd for sm100 like what we do for the bwd. This lets us use q_offset 1. The builk of the other code is around adding (block_sparse_sizes). I ke... | kernel: `flash_attn/cute/block_sparse_utils.py`, `flash_attn/cute/block_sparsity.py`, `flash_attn/cute/compute_block_sparsity.py`, `flash_attn/cute/flash_fwd.py`<br>benchmark: `tests/cute/benchmark_mask_mod.py`<br>test: `tests/cute/test_block_sparsity.py`, `tests/cute/test_mask_mod.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#1072](https://github.com/Dao-AILab/flash-attention/pull/1072) Add var-seq-len to FA3 fp16 / bf16 fwd | 2024-07-23 | benchmark_test, arch_pipeline, attention_kv | This PR contains following changes: * Add var-seq-len support to fp16 / bf16 fwd kernel. * Add unittests for var-seq-len fwd. * Add a simple benchmark to compare fp16 / bf16 fwd kernel perf across different backends. | kernel: `hopper/flash_fwd_kernel.h`<br>benchmark: `hopper/benchmark_attn.py`<br>test: `hopper/test_flash_attn.py`, `tests/test_util.py`<br>wrapper: `hopper/epilogue_fwd_sm90_tma.hpp`, `hopper/flash.h`, `hopper/flash_api.cpp`, `hopper/flash_attn_interface.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2390](https://github.com/Dao-AILab/flash-attention/pull/2390) [Cute,Sm100,Bwd] refine bwd swizzle for deterministic | 2026-03-25 | benchmark_test, compiler_runtime, scheduler_autotune, arch_pipeline, attention_kv | This PR fixes the varlen swizzle num_n_blocks from num_m_blocks calculation for 2cta and includes dqaccum in the head size for not spilling l2. This noticeably improves backward deterministic FLOPs (for non-varlen, limited to non-causal but doesn't appear to cause regression for causal; for varlen,... | kernel: `flash_attn/cute/tile_scheduler.py`<br>benchmark: `benchmarks/benchmark_attn.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#1964](https://github.com/Dao-AILab/flash-attention/pull/1964) [Cute] Blocks tweaks | 2025-10-28 | benchmark_test, compiler_runtime, attention_kv, arch_pipeline | Not yet ready but going to keep blockmask data packed to clean up signature | kernel: `flash_attn/cute/block_sparsity.py`, `flash_attn/cute/flash_fwd.py`, `flash_attn/cute/flash_fwd_sm100.py`, `flash_attn/cute/interface.py`<br>benchmark: `flash_attn/cute/benchmark_mask_mod.py`<br>test: `tests/cute/test_mask_mod.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#1942](https://github.com/Dao-AILab/flash-attention/pull/1942) Block Sparsity and Flex Attention mask mod support | 2025-10-21 | benchmark_test, attention_kv, compiler_runtime | This PR introduces FlexAttention-style mask mod functionality with block sparse masking support, merged with score mod functionality from 1840. Block sparsity is handled as follows. The user passes to the kernel four `Int32` tensors: - `mask_block_cnt` of shape `(B, H, m_blocks)` encoding the number... | kernel: `flash_attn/cute/block_sparsity.py`, `flash_attn/cute/flash_fwd.py`, `flash_attn/cute/interface.py`, `flash_attn/cute/mask.py`<br>benchmark: `flash_attn/cute/benchmark_mask_mod.py`<br>test: `tests/cute/test_flash_attn.py`, `tests/cute/test_mask_mod.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2242](https://github.com/Dao-AILab/flash-attention/pull/2242) Fix Hopper tests | 2026-02-08 | benchmark_test, arch_pipeline, attention_kv, compiler_runtime | Stacked PRs: * 2218 * 2227 * __->__2242 --- --- --- Fix Hopper tests | kernel: `flash_attn/cute/interface.py`<br>test: `tests/cute/test_flash_attn.py`, `tests/cute/test_flash_attn_race_condition.py`, `tests/cute/test_mask_mod.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2091](https://github.com/Dao-AILab/flash-attention/pull/2091) Fix IMA in fwd on m boundary | 2025-12-20 | benchmark_test, compiler_runtime, attention_kv, arch_pipeline | We would use mask_seqlen to Gate whether or not we would do the aux wrapping. This good for first iteration for wrapping on K but doesnt account for m boundary no real hit to perf on big shapes <img width="12499" height="5995" alt="image" src="https://github.com/user-attachments/assets/28137cb4-09f3... | kernel: `flash_attn/cute/block_sparse_utils.py`, `flash_attn/cute/flash_bwd_sm100.py`, `flash_attn/cute/flash_fwd_sm100.py`, `flash_attn/cute/mask.py`<br>test: `tests/cute/test_mask_mod.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2127](https://github.com/Dao-AILab/flash-attention/pull/2127) Misc tests that should be xfailed for now | 2026-01-05 | benchmark_test, attention_kv, scheduler_autotune, compiler_runtime | Stacked PRs: * 2137 * 2136 * __->__2127 --- --- --- Misc tests that should be xfailed for now Lots of sm90 tests should be xfailed. It was hard to get good signal before these. And I think we land and then burn down | test: `tests/cute/test_flash_attn.py`, `tests/cute/test_flash_attn_race_condition.py`, `tests/cute/test_flash_attn_varlen.py`, `tests/cute/test_mask_mod.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2041](https://github.com/Dao-AILab/flash-attention/pull/2041) Add headdim64 tests to race condition | 2025-12-02 | benchmark_test, attention_kv, compiler_runtime | <img width="810" height="424" alt="Screenshot 2025-12-02 at 2 30 56 PM" src="https://github.com/user-attachments/assets/5fc38067-4550-4572-a06f-91be42735750" /> | test: `tests/cute/test_flash_attn_race_condition.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2473](https://github.com/Dao-AILab/flash-attention/pull/2473) Expose --pack-gqa and --num-splits in benchmark_attn.py | 2026-04-19 | benchmark_test, scheduler_autotune | - Add `--num-splits INT` and `--pack-gqa {auto,true,false}` flags to `benchmarks/benchmark_attn.py` - Both override kernel heuristics that were previously hardcoded (`num_splits = 0`, `pack_gqa = None`) - Default behavior unchanged (both default to kernel auto) Motivation These flags are already acc... | benchmark: `benchmarks/benchmark_attn.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2221](https://github.com/Dao-AILab/flash-attention/pull/2221) fix compute_block_sparsity broken in  benchmark_mask_mod.py | 2026-02-02 | benchmark_test | fix error in https://github.com/Dao-AILab/flash-attention/issues/2184 perhaps compute_block_sparsity is under reconstruction, currently this test failed when use_mask_mod | benchmark: `tests/cute/benchmark_mask_mod.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2241](https://github.com/Dao-AILab/flash-attention/pull/2241) pytest-dist round robin to gpus | 2026-02-08 | benchmark_test, compiler_runtime | Stacked PRs: * 2218 * 2227 * __->__2241 --- --- --- pytest-dist round robin to gpus With this conftest the pytest-xdist will launch on all available gpus: <img width="1083" height="460" alt="image" src="https://github.com/user-attachments/assets/2ab68164-4472-4156-885a-19de1b70e722" /> Sorry neighbo... | test: `tests/cute/conftest.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2495](https://github.com/Dao-AILab/flash-attention/pull/2495) benchmarks/tune_ex2_emu: hd256 sweep support and clock lock/unlock | 2026-05-01 | benchmark_test | Extends `benchmarks/tune_ex2_emu.py` with two improvements needed for tuning the hd256 forward kernel on B200 (SM100). hd256 sweep support The hd256 softmax loop steps `k` by 2 (rather than 1), so `k % freq` only takes even values. As a result, `ex2_emu_res` cannot be inferred from `freq` alone — it... | benchmark: `benchmarks/tune_ex2_emu.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2377](https://github.com/Dao-AILab/flash-attention/pull/2377) benchmarks: add MFU% column to benchmark output | 2026-03-22 | benchmark_test | Adds get_peak_flops() for known NVIDIA GPUs (B300, B200, H200, H100, A100, etc.) and shows ms/TFLOPS/MFU% per cell in the benchmark table. H100 Before <img width="749" height="334" alt="image" src="https://github.com/user-attachments/assets/1721989e-46e2-47a6-af4a-0b09a7548eec" /> H100 After: <img w... | benchmark: `benchmarks/benchmark_attn.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |

## Per-PR Ledger Fields

When using an idea from this page, add one row to `artifacts/source-idea-ledger.md` with:

| Field | Value to record |
| --- | --- |
| Source key | `<repo>#<pr-number>` |
| Code evidence | Kernel, wrapper, benchmark, and test paths opened from the PR diff |
| Hypothesis | The concrete optimization idea derived from the PR |
| First experiment | Candidate version and benchmark shape set |
| Result | Correctness, geomean, best/worst cases, and NCU digest path |
| Do-not-reread key | Same as source key unless a single PR yields multiple independent ideas |

## How To Use This Page

- During the initial knowledge pass, read the category matching the target kernel and copy PR URL, changed paths, and hypothesis into the source idea ledger.
- During plateau expansion, choose PRs not already present in ledger do-not-reread keys; inspect the diff, linked issue, changed tests, and benchmark files before using the idea.
- Treat PR code as baseline/prior art unless the task and license allow copying or adapting it. When copied, record exact PR, commit, files, notice, and first delta.

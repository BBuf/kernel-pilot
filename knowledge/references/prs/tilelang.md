# TileLang PR Knowledge Notes

Repository: <https://github.com/tile-ai/tilelang>

This page is the production-PR layer for kernel-knowledge. It favors merged PRs that changed kernels, dispatch, JIT/runtime integration, tuning policy, tests, benchmarks, or profiling evidence. Release, CI-only, formatting, pure version-bump, and non-target-backend PRs are filtered out.

## Repository Source Scan

Read these source regions before opening individual PR diffs:

- `examples`
- `examples/deepseek_mla`
- `python/tilelang`
- `python/tilelang/language`
- `python/tilelang/engine`
- `tests`
- `benchmark`

## Coverage Summary

| Category | CUDA-kernel PRs |
| --- | ---: |
| GEMM / Quantization | 36 |
| Attention / KV / Decode | 21 |
| MoE / Routing | 1 |
| Norm / Elementwise / Epilogue | 2 |
| Memory / Primitives | 19 |
| Scheduler / Autotune | 14 |
| Architecture / Pipeline | 50 |
| Compiler / Runtime | 53 |
| Benchmark / Test Evidence | 34 |
| Other Kernel Cases | 1 |

## Pull Request Case Notes

### GEMM / Quantization

Use this section for: Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate.
NCU first look: Tensor pipe %, DRAM/L2 bytes, active cycles, register pressure, and scale-load traffic.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#1509](https://github.com/tile-ai/tilelang/pull/1509) [Refactor] Support auto swizzling for tma store and phaseout related layout annotations | 2025-12-24 | gemm_quant, attention_kv, arch_pipeline, benchmark_test, scheduler_autotune | This pull request removes the usage of swizzled memory layouts and related annotations throughout the codebase, simplifying tensor allocation and layout management. It also makes minor improvements to variable allocation and thread binding in some kernels. These changes help streamline the code and... | kernel: `examples/attention_sink/example_gqa_sink_bwd_bhsd.py`, `examples/attention_sink/example_gqa_sink_fwd_bhsd_wgmma_pipelined.py`, `examples/attention_sink/example_mha_sink_bwd_bhsd.py`, `examples/attention_sink/example_mha_sink_fwd_bhsd.py`<br>benchmark: `benchmark/mamba2/benchmark_mamba_chunk_scan.py`, `benchmark/matmul/benchmark_matmul.py`, `benchmark/matmul_fp8/benchmark_matmul.py`, `examples/bitnet-1.58b/kernel_benchmark/tilelang_bitnet_158_int8xint2_prefill.py`<br>test: `examples/dequantize_gemm/test_example_dequantize_gemm.py`<br>wrapper: `examples/analyze/example_conv_analyze.py`, `examples/cast/example_group_per_split_token_cast_to_fp8.py`, `examples/cast/example_per_token_cast_to_fp8.py`, `examples/convolution/example_convolution.py`<br>docs: `examples/flash_attention/README.md` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2002](https://github.com/tile-ai/tilelang/pull/2002) [Refactor][Pipeline] Run pipeline rewriting before layout inference and stabilize tiled WS | 2026-04-07 | gemm_quant, attention_kv, benchmark_test, arch_pipeline, moe_routing | - run pipeline planning, software-pipeline injection, and tiled warp specialization before layout inference while preserving the stage context needed by downstream passes - harden multi-version buffer rewriting, layout inference, tile-op lowering, and sparse GEMM lowering for the new pass order, inc... | kernel: `examples/deepseek_nsa/example_tilelang_nsa_bwd.py`, `examples/deepseek_nsa/example_tilelang_nsa_decode.py`, `examples/deepseek_nsa/example_tilelang_nsa_fwd.py`, `examples/deepseek_nsa/example_tilelang_nsa_fwd_varlen.py`<br>benchmark: `examples/deepseek_nsa/benchmark/benchmark_nsa_fwd.py`<br>test: `examples/gdn/test_example_gdn_compilation.py`<br>wrapper: `examples/bitnet-1.58b/tokenization_bitnet.py`, `examples/deepseek_mhc/example_mhc_bwd.py`, `examples/deepseek_mhc/example_mhc_post.py`, `examples/deepseek_mhc/example_mhc_pre.py`<br>other: `.gitignore` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#1887](https://github.com/tile-ai/tilelang/pull/1887) [Refactor] Improve cp.async lowering and add async_copy op | 2026-03-06 | gemm_quant, compiler_runtime, attention_kv, arch_pipeline, memory_primitives | This PR improves TileLang's `cp.async` support and frontend async-copy semantics. Changes: - Add CPAsync as a `Copy` instruction and lower eligible global->shared vectorized copies to `ptx_cp_async`. - Introduce `T.async_copy` / `tl.tileop.async_copy` with explicit async semantics (emits `cp.async`... | kernel: `examples/dequantize_gemm/example_dequant_gemm_bf16_fp4_hopper.py`, `examples/gemm_fp8/example_tilelang_gemm_fp8_intrinsic.py`, `src/target/codegen_cutedsl.cc`, `src/transform/lower_tile_op.cc`<br>test: `examples/gdn/test_example_gdn_compilation.py`<br>wrapper: `examples/deepseek_mhc/example_mhc_post.py`, `examples/deepseek_mhc/example_mhc_pre.py`, `examples/deepseek_mhc/regression_example_mhc.py`, `examples/deepseek_mla/example_mla_decode_ws.py`<br>docs: `docs/programming_guides/instructions.md`, `docs/programming_guides/language_basics.md` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#1444](https://github.com/tile-ai/tilelang/pull/1444) [Refactor] Reduce direct dependency on PyTorch due to its limited type support | 2025-12-16 | gemm_quant, memory_primitives, compiler_runtime, arch_pipeline, benchmark_test | This pull request introduces improved support for FP4 and FP8 types in CUDA code generation, adds a new header for FP4 types, and refactors vector load/store utilities for better generalization and future extensibility. The changes enhance the handling of sub-byte floating-point types, enable larger... | kernel: `tilelang/engine/param.py`, `tilelang/jit/adapter/ctypes/adapter.py`, `tilelang/jit/adapter/nvrtc/adapter.py`, `tilelang/jit/adapter/tvm_ffi.py`<br>test: `testing/python/cache/test_tilelang_cache_matmul.py`, `testing/python/language/test_tilelang_language_copy.py`<br>wrapper: `src/target/codegen_cuda.cc`, `src/target/codegen_cuda.h`, `src/tl_templates/cuda/copy_sm100.h`, `src/tl_templates/cuda/cuda_fp4.h`<br>other: `tilelang/jit/adapter/cython/cython_wrapper.pyx` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#1757](https://github.com/tile-ai/tilelang/pull/1757) [Refactor] Unify the usage of cast-related operators | 2026-02-02 | gemm_quant, benchmark_test, attention_kv, arch_pipeline | TVM IR has the following type cast related operators: code block We can see that APIs are not very consistent and may cause some confusions. This PR unifies them to: code block by CodeRabbit * **Refactor** * Standardized cast/reinterpret APIs: unified argument order and introduced new cast/reinterpr... | kernel: `examples/deepseek_v32/inference/kernel.py`, `examples/dequantize_gemm/example_dequant_gemv_fp16xint4.py`, `examples/flash_attention/example_gqa_bwd_tma_reduce.py`, `examples/flash_attention/example_gqa_bwd_tma_reduce_varlen.py`<br>benchmark: `examples/bitnet-1.58b/kernel_benchmark/tilelang_bitnet_158_int8xint2_decode.py`<br>test: `testing/python/kernel/test_tilelang_kernel_fp8_gemv_simt.py`, `testing/python/kernel/test_tilelang_kernel_gemv_simt.py`, `testing/python/language/test_tilelang_language_ldg.py`, `testing/python/language/test_tilelang_language_select.py`<br>wrapper: `examples/deepseek_v32/topk_selector.py`, `examples/gemv/example_gemv.py`<br>docs: `docs/deeplearning_operators/gemv.md`<br>other: `tilelang/language/tir/ir.pyi` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#1893](https://github.com/tile-ai/tilelang/pull/1893) Refactor CUDA version checks for compute 9.0 | 2026-03-03 | gemm_quant, attention_kv, scheduler_autotune, compiler_runtime, benchmark_test | This PR updates several example/test guards for CUDA compute capability 9.0 to use exact-match checks instead of >= checks. Changes: - Replace `requires_cuda_compute_version_ge(9, 0)` with `requires_cuda_compute_version_eq(9, 0)` in relevant examples/tests. - Remove deprecated tests in `test_example... | test: `examples/blocksparse_attention/test_example_blocksparse_attention.py`, `examples/deepseek_v32/test_tilelang_example_deepseek_v32.py`, `examples/dequantize_gemm/test_example_dequantize_gemm.py`, `examples/flash_attention/test_example_flash_attention.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#1568](https://github.com/tile-ai/tilelang/pull/1568) [Refactor] Simplify do_bench calls by using default warmup and rep parameters | 2025-12-29 | gemm_quant, attention_kv, arch_pipeline, benchmark_test | - Remove explicit `warmup` and `rep` parameters from `do_bench` function calls across multiple example files - Use default values instead of hardcoded `warmup=10, rep=100` or `warmup=500, rep=10000` - Update regression performance test parameters to use larger default input sizes (M=N=K=4096, heads=... | kernel: `examples/attention_sink/example_gqa_sink_bwd_bhsd.py`, `examples/attention_sink/example_gqa_sink_fwd_bhsd_wgmma_pipelined.py`, `examples/attention_sink/example_mha_sink_bwd_bhsd.py`, `examples/attention_sink/example_mha_sink_fwd_bhsd.py`<br>wrapper: `examples/cast/example_per_token_cast_to_fp8.py`, `examples/deepseek_mla/example_mla_decode.py`, `examples/deepseek_v32/fp8_lighting_indexer.py`, `examples/deepseek_v32/sparse_mla_bwd.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#746](https://github.com/tile-ai/tilelang/pull/746) [Refactor] Merge bulk copy into copy and improve layout inference for bulk copy | 2025-08-22 | gemm_quant, attention_kv, benchmark_test | by CodeRabbit * **New Features** * Unified GPU copy with multiple transfer paths (including bulk/TMA and specialized paths), Conv2D im2col lowering, target predicate for bulk-copy support, warp-group register-allocation annotation pass, and new TileLang helpers to emit register hints. * **Refactor**... | kernel: `examples/deepseek_nsa/example_tilelang_nsa_decode.py`, `examples/dequantize_gemm/example_dequant_gemm_bf16_mxfp4_hopper.py`, `examples/flash_attention/example_gqa_bwd.py`, `examples/seer_attention/block_sparse_attn_tilelang.py`<br>benchmark: `benchmark/matmul/benchmark_matmul_sp.py`<br>test: `testing/python/kernel/test_tilelang_kernel_dequantize_gemm.py`<br>wrapper: `examples/deepseek_mla/experimental/example_mla_decode_kv_fp8.py`, `examples/gdn/example_chunk_o.py`, `examples/gdn/example_chunk_scaled_dot_kkt.py`, `examples/gdn/example_wy_fast.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#1372](https://github.com/tile-ai/tilelang/pull/1372) [Refactor] Generalize fp8 process | 2025-12-03 | gemm_quant, arch_pipeline, benchmark_test, memory_primitives, compiler_runtime | This pull request generalizes and simplifies support for float8 data types across the codebase, making it easier to add new float8 variants and improving maintainability. The main changes include consolidating float8 type checks to use a single `is_float8()` method, expanding supported float8 varian... | kernel: `examples/gemm_fp8/example_tilelang_gemm_fp8_intrinsic.py`, `src/op/gemm.cc`, `src/op/gemm_py.cc`<br>test: `testing/python/kernel/test_tilelang_kernel_bf16_gemm_mma.py`, `testing/python/kernel/test_tilelang_kernel_fp8_gemm_mma.py`, `testing/python/kernel/test_tilelang_kernel_gemm_mma_intrinsic.py`<br>wrapper: `src/op/copy.cc`, `src/op/tcgen5_meta.h` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2148](https://github.com/tile-ai/tilelang/pull/2148) [Examples] Add examples for operators in DeepSeek-V4 | 2026-05-07 | gemm_quant, benchmark_test, attention_kv, arch_pipeline, compiler_runtime | by CodeRabbit * **New Features** * Added a DSV4-style sparse attention example with PyTorch reference, correctness tests, benchmarking, and CLI. * Added block-wise activation quantization (FP8 and packed FP4) with reference implementations and round-trip tests. * **Improvements** * Adjusted a sparse... | kernel: `examples/deepseek_v4/act_quant.py`<br>test: `examples/deepseek_v4/test_tilelang_example_deepseek_v4.py`<br>wrapper: `examples/deepseek_v32/sparse_mla_fwd.py`, `examples/deepseek_v4/sparse_attn_fwd_sm90.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#1798](https://github.com/tile-ai/tilelang/pull/1798) [Feature] Introduce higher-dimensional gemm layout support | 2026-02-25 | gemm_quant, memory_primitives, compiler_runtime, benchmark_test | * Added extendLayoutToShape function to extend a 2D layout to higher dimensions, maintaining linear mapping for the first (n-2) dimensions and applying the 2D layout to the last two dimensions. * Updated layout functions to utilize the new extendLayoutToShape for shared memory layouts in various GEM... | kernel: `src/layout/gemm_layouts.cc`, `src/op/gemm.cc`, `src/op/gemm_sp.cc`, `tilelang/layout/layout.py`<br>test: `testing/python/kernel/test_tilelang_kernel_gemm_batched.py`, `testing/python/layout/test_tilelang_bank_swizzle_expand.py`<br>wrapper: `src/layout/layout.cc`, `src/layout/layout.h`, `src/op/atomic_add.cc`, `src/op/copy.cc` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#1827](https://github.com/tile-ai/tilelang/pull/1827) [Refactor] Introduce `T.access_of` to combine `T.address_of` and `access_ptr` | 2026-02-11 | gemm_quant, benchmark_test | Motivation TileLang previously lowered `T.access_ptr(...)` to one of two existing TIR patterns: - `tir.address_of(BufferLoad(...))` - Pros: keeps the original `BufferLoad`. - Cons: does not carry explicit access intent / scope metadata (read vs write), which forces downstream passes to be conservati... | kernel: `examples/dequantize_gemm/example_dequant_gemm_bf16_fp4_hopper.py`, `examples/dequantize_gemm/example_dequant_gemm_bf16_mxfp4_hopper.py`, `examples/dequantize_gemm/example_dequant_gemm_fine_grained.py`, `examples/dequantize_gemm/example_dequant_gemv_fp16xint4.py`<br>benchmark: `examples/bitnet-1.58b/kernel_benchmark/tilelang_bitnet_158_int8xint2_decode.py`, `examples/bitnet-1.58b/kernel_benchmark/tilelang_bitnet_158_int8xint2_prefill.py`<br>test: `testing/python/issue/test_tilelang_issue_1257.py`, `testing/python/language/test_tilelang_language_access_ptr.py`, `testing/python/language/test_tilelang_language_access_ptr_codegen.py`, `testing/python/language/test_tilelang_language_atomic.py`<br>wrapper: `src/op/atomic_add.cc`, `src/op/atomic_reduce.cc`, `src/op/builtin.cc`, `src/op/builtin.h` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2165](https://github.com/tile-ai/tilelang/pull/2165) [Refactor] Move backend-specific GEMM implementations and transforms into backend directories | 2026-05-08 | gemm_quant, benchmark_test | Restructure the codebase so that each backend (cpu, cuda, rocm) owns its GEMM implementations, sparse GEMM implementations, and transform passes under a consistent `op/` subdirectory layout. The shared GEMM registry and base classes remain in `tileop/` as the platform-agnostic dispatch layer. This m... | kernel: `examples/dequantize_gemm/example_dequant_gemm_fine_grained.py`, `examples/gemm/example_gemm_intrinsics.py`, `examples/gemm_fp8/example_tilelang_gemm_amd_fp8_preshuffle.py`, `examples/gemm_fp8/example_tilelang_gemm_fp8_intrinsic.py`<br>benchmark: `benchmark/matmul/benchmark_matmul_intrinsic.py`, `examples/bitnet-1.58b/kernel_benchmark/tilelang_bitnet_158_int8xint2_prefill.py`<br>test: `examples/gemm_fp8/test_example_gemm_fp8.py`, `testing/python/amd/test_tilelang_gemm_mfma_intrinsic.py`, `testing/python/amd/test_tilelang_gemm_mfma_preshuffle.py`, `testing/python/kernel/test_tilelang_kernel_bf16_gemm_mma.py`<br>wrapper: `examples/hadamard_transform/example_hadamard.py`, `examples/plot_layout/fragment_mfma_load_a.py`, `examples/plot_layout/fragment_mma_load_a.py`, `src/backend/cuda/runtime.cc`<br>docs: `docs/deeplearning_operators/matmul.md`, `examples/gemm/README.md`, `examples/plot_layout/README.md`<br>other: `src/backend/cuda/CMakeLists.txt` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#1923](https://github.com/tile-ai/tilelang/pull/1923) Support ptr-table grouped GEMM kernels | 2026-03-12 | gemm_quant, moe_routing, benchmark_test, compiler_runtime | - add frontend support for pointer-table tensor annotations via `T.Tensor([G], T.ptr)` and keep the runtime ABI lowered to `int64` - add a ptr-table grouped GEMM example that reconstructs tensors with `T.make_tensor(...)` instead of packing per-group tensors first - improve compiler handling around... | kernel: `examples/grouped_gemm/example_grouped_gemm_fwd_ptr.py`, `tilelang/language/__init__.py`, `tilelang/language/proxy.py`<br>test: `testing/python/language/test_tilelang_language_ptr.py`, `testing/python/transform/test_tilelang_transform_thread_sync.py`<br>wrapper: `src/transform/inject_pipeline.cc`, `src/transform/thread_storage_sync.cc` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2055](https://github.com/tile-ai/tilelang/pull/2055) [BugFix] Keep shared-prelude local vars in producer-consumer WS | 2026-04-17 | gemm_quant, moe_routing, benchmark_test, compiler_runtime, arch_pipeline | 2054 - Preserve prelude definitions needed by shared LetStmt values during warp-specialized pipeline rewriting - Avoid cloning local.var buffers into the producer branch. This keeps grouped GEMM batch index calculations shared and fixes incorrect TMA batch selection. This fixes the previously failed... | test: `examples/grouped_gemm/test_example_grouped_gemm.py`, `testing/python/transform/test_tilelang_transform_producer_consumer_ws.py`<br>wrapper: `src/transform/producer_consumer_ws.cc` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2097](https://github.com/tile-ai/tilelang/pull/2097) feat: support cdna4 v_mfma_i32_16x16x64_i8 & v_mfma_i32_32x32x32_i8 | 2026-04-26 | gemm_quant, benchmark_test, compiler_runtime, scheduler_autotune | * CDNA4 v_mfma_i32_16x16x64_i8 & v_mfma_i32_32x32x32_i8 * Matrix A preshuffle * TODO: use async copy by CodeRabbit Release Notes * **New Features** * Added support for MFMA instruction shape (32,32,32) for AMD architectures * Extended data type mappings for int8 and int32 vector operations * New pre... | kernel: `tilelang/intrinsics/mfma_layout.py`, `tilelang/intrinsics/mfma_macro_generator.py`, `tilelang/intrinsics/utils.py`<br>test: `testing/python/amd/test_tilelang_gemm_mfma_preshuffle.py`<br>wrapper: `src/target/codegen_hip.cc`, `src/tl_templates/hip/common.h` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#1945](https://github.com/tile-ai/tilelang/pull/1945) [Feature] Block-scaled GEMM support for MXFP8 on Blackwell | 2026-04-24 | gemm_quant, arch_pipeline | **MXFP8 blockscaled GEMM for 1d1d `group_size=128` reaches up to ~3PFLOPs, on par with latest DeepGEMM.** - Introduced a new Python file for MXFP8 block-scaled GEMM implementation. - Added intrinsic definitions for block-scaled MMA, UTCCP, and scale factor operations in the C++ backend. - Implemente... | kernel: `examples/gemm_sm100/gemm_mxfp8_blockscaled_1d1d.py`, `src/op/gemm.cc`, `src/op/gemm.h`, `tilelang/intrinsics/tcgen05_macro_generator.py`<br>wrapper: `src/op/builtin.cc`, `src/op/builtin.h`, `src/op/copy.cc`, `src/op/tcgen5_meta.h` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2073](https://github.com/tile-ai/tilelang/pull/2073) [CUDA] Improve int4 GEMM lowering and packed codegen support | 2026-04-21 | gemm_quant, compiler_runtime, benchmark_test | - improve int4 GEMM lowering by removing the obsolete sub-byte MMA path and consolidating the active TensorCore emitter flow - add packed int4 buffer load/store helpers and wire the CUDA codegen path to handle packed int4 local/shared access more consistently - extend int4 MMA support in the CUDA te... | kernel: `examples/gemm_int4/example_tilelang_gemm_int4.py`, `tilelang/intrinsics/mma_macro_generator.py`, `tilelang/intrinsics/utils.py`, `tilelang/tileop/gemm/gemm_mma.py`<br>test: `testing/python/amd/test_tilelang_gemm_mfma_intrinsic.py`, `testing/python/amd/test_tilelang_gemm_mfma_preshuffle.py`, `testing/python/debug/test_tilelang_debug_print.py`<br>wrapper: `src/target/codegen_cuda.cc`, `src/tl_templates/cuda/common.h`, `src/tl_templates/cuda/instruction/mma.h`<br>other: `.github/workflows/ci.yml` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#1722](https://github.com/tile-ai/tilelang/pull/1722) [Refactor] re-implement vector subtype and its access method | 2026-01-23 | gemm_quant, memory_primitives, benchmark_test, compiler_runtime | as title. Thanks for @Hamerlate to provide the test machine by CodeRabbit * **Refactor** * Improved FP4 tensor element handling with refined data structure organization and access patterns for various vector widths. * Enhanced FP4 data type implementation with explicit accessor methods replacing dir... | test: `testing/python/language/test_tilelang_language_copy.py`<br>wrapper: `src/target/codegen_cuda.cc`, `src/tl_templates/cuda/cuda_fp4.h` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#1696](https://github.com/tile-ai/tilelang/pull/1696) Enhance loop vectorization logic for CallNode handling | 2026-01-19 | gemm_quant, scheduler_autotune, benchmark_test, compiler_runtime | - **Introduce `src/config.h`**: Centralize PassContext configuration utilities with `VectorizePlannerVerboseEnabled()` and `Vectorize256Disabled()` helper functions for cleaner code reuse. - **Improve CallNode vectorization in `loop_vectorize.cc`**: Previously, most CallNodes were conservatively ass... | kernel: `examples/gemm_fp8/example_tilelang_gemm_fp8_intrinsic.py`<br>test: `testing/python/language/test_tilelang_language_vectorize.py`<br>wrapper: `src/config.h`, `src/transform/loop_vectorize.cc` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#1781](https://github.com/tile-ai/tilelang/pull/1781) [Bugfix] Fix thread storage sync conflict detection for loop carry write-after-read | 2026-02-04 | gemm_quant, attention_kv | - Fix thread storage synchronization logic in `thread_storage_sync.cc` to correctly identify conflicts between read and write operations based on loop carry conditions - The previous logic incorrectly checked for double buffer write with read operations without loop carry, but the correct behavior s... | kernel: `examples/dequantize_gemm/example_dequant_groupedgemm_bf16_mxfp4_hopper.py`<br>test: `examples/flash_decoding/test_example_flash_decoding.py`, `testing/python/transform/test_tilelang_transform_thread_sync.py`<br>wrapper: `examples/flash_decoding/example_gqa_decode_varlen_logits.py`, `examples/flash_decoding/example_gqa_decode_varlen_logits_paged.py`, `src/transform/common/constr_visitor.h`, `src/transform/thread_storage_sync.cc`<br>other: `3rdparty/tvm` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#1736](https://github.com/tile-ai/tilelang/pull/1736) Add swizzle layout detection and automatic merging for layout conflicts | 2026-01-27 | gemm_quant, arch_pipeline, compiler_runtime | - Add `SwizzleMode` enum to detect swizzle layout types (Quarter/32B, Half/64B, Full/128B) - Implement `DetectSwizzleMode` and `MergeSwizzleLayouts` functions in `gemm_layouts.cc` - When layout inference encounters conflicting swizzle layouts for the same buffer, automatically merge to the smaller g... | kernel: `maint/gemm_v2/correctness_evaluation.py`, `src/layout/gemm_layouts.cc`, `tilelang/tileop/gemm/gemm_wgmma.py`<br>wrapper: `src/layout/layout.h`, `src/target/ptx.cc`, `src/transform/layout_inference.cc` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#1741](https://github.com/tile-ai/tilelang/pull/1741) [BugFix] Fix FP4 related vectorized cast | 2026-01-27 | gemm_quant, benchmark_test, compiler_runtime | by CodeRabbit Release Notes * **Improvements** * Extended CUDA casting support for additional floating-point type combinations, including float16, float32, bfloat16, and FP8 formats. * Added explicit validation for type conversion operations to ensure correctness. * **Tests** * Reorganized floating-... | test: `testing/python/language/test_tilelang_language_vectorized_cast.py`<br>wrapper: `src/target/codegen_cuda.cc`, `src/target/utils.cc` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#1344](https://github.com/tile-ai/tilelang/pull/1344) [Enhancement] Add support for k_pack in gemm_mfma | 2025-11-26 | gemm_quant, benchmark_test | - Add support for k_pack in gemm_mfma MI308X: Performance will be better when kpack=2. `Tuned Latency 37.592288970947266 with config {'block_M': 128, 'block_N': 128, 'block_K': 64, 'num_stages': 1, 'thread_num': 256, 'k_pack': 1, 'policy': <GemmWarpPolicy.Square: 0>, 'enable_rasteration': False} at... | kernel: `tilelang/intrinsics/mfma_macro_generator.py`, `tilelang/tileop/gemm/gemm_mfma.py`<br>benchmark: `benchmark/matmul_fp8/benchmark_matmul.py`<br>wrapper: `src/tl_templates/hip/hip_fp8.h` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#811](https://github.com/tile-ai/tilelang/pull/811) [Enhancement] Add a MXFP4 grouped GEMM example for FusedMoE | 2025-09-17 | gemm_quant | This pull request refactors and optimizes the dequantization and bit-twiddling routines in `examples/dequantize_gemm`, focusing on improving performance and code clarity. The main changes include replacing slow nested loops with efficient vectorized PyTorch operations for scaling and bit manipulatio... | kernel: `examples/dequantize_gemm/example_dequant_gemm_bf16_mxfp4_hopper.py`, `examples/dequantize_gemm/example_dequant_gemm_fine_grained.py`, `examples/dequantize_gemm/example_dequant_groupedgemm_bf16_mxfp4_hopper.py`, `examples/dequantize_gemm/utils.py`<br>test: `examples/dequantize_gemm/test_example_dequantize_gemm.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#716](https://github.com/tile-ai/tilelang/pull/716) [CUDA] Init support for sm_120 | 2025-08-14 | gemm_quant, arch_pipeline | This is a poc for sm_120 support. Current it passes all tests under `testing/python/kernel/`. | kernel: `src/op/gemm.cc`, `src/tl_templates/cuda/gemm.h`, `src/tl_templates/cuda/gemm_mma.h`, `src/tl_templates/cuda/gemm_sm120.h`<br>wrapper: `src/target/utils.cc`, `src/target/utils.h` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2098](https://github.com/tile-ai/tilelang/pull/2098) [Example] Add MXFP8 blockscaled grouped gemm examples with transB support  | 2026-04-28 | gemm_quant | by CodeRabbit * **New Features** * Added a transpose-B option for MXFP8 block-scaled GEMM (kernels and reference) with CLI flag to select B layout. * Added a grouped MXFP8 block-scaled GEMM example with FP8 quantization, per-group scaling, correctness checks, optional persistent kernel, and benchmar... | kernel: `examples/blockscaled_gemm_sm100/gemm_mxfp8_blockscaled_1d1d.py`, `examples/blockscaled_gemm_sm100/grouped_gemm_mxfp8_blockscaled_1d1d.py`<br>docs: `examples/blockscaled_gemm_sm100/mxfp8_illustrated.md`<br>other: `examples/blockscaled_gemm_sm100/figures/blockscaled_data_path.svg`, `examples/blockscaled_gemm_sm100/figures/blockscaled_sf_layout.svg`, `examples/blockscaled_gemm_sm100/figures/blockscaled_variants.svg`, `examples/blockscaled_gemm_sm100/figures/blockscaled_warp_specialization.svg` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2091](https://github.com/tile-ai/tilelang/pull/2091) [Host CodeGen][Refactor] Cleanup namespace and remove useless C templates | 2026-04-25 | gemm_quant, compiler_runtime | As titled. by CodeRabbit * **Refactor** * Default host and device code generation now uses the C backend for C targets, clarifying host vs. device responsibilities. * Removed an obsolete C++ template dependency from generated declarations. * **Chores** * Toolchain registration updated to expose dist... | kernel: `src/tl_templates/cpp/gemm.h`, `tilelang/engine/lower.py`<br>wrapper: `src/target/codegen_c.cc`, `src/target/codegen_c.h`, `src/target/codegen_c_host.cc`, `src/target/codegen_c_host.h`<br>other: `CMakeLists.txt` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2173](https://github.com/tile-ai/tilelang/pull/2173) [BugFix] Fix T.gemm() on SM75 (Turing) GPUs (#1992) | 2026-05-10 | gemm_quant, compiler_runtime | - Route SM75 to the Volta MMA path (GemmMMASm70) instead of selecting SM80 MMA instructions (tilelang/cuda/op/gemm/__init__.py) - Set kNPerWarp=16 for Turing to match the Volta warp partition (src/backend/cuda/op/gemm.cc) - Remove invalid SM75_16x8x8_F16F16F16F16_TN reference from src/tl_templates/c... | kernel: `src/backend/cuda/op/gemm.cc`, `src/tl_templates/cuda/gemm_mma.h`, `tilelang/cuda/op/gemm/__init__.py`<br>wrapper: `src/tl_templates/cuda/common.h` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#1956](https://github.com/tile-ai/tilelang/pull/1956) Fix T.gemm() on SM75 Turing GPUs by including SM75 MMA headers | 2026-03-22 | gemm_quant | On SM75 (Turing/T4) GPUs, `T.gemm()` was failing because `CUTE_ARCH_MMA_SM80_ENABLED` is not defined, yet SM80 MMA instructions were being instantiated. This fix adds the proper `<cute/arch/mma_sm75.hpp>` include in the SM75 conditional branch of `gemm_mma.h`, and also adds half_t->half_t accumulati... | kernel: `src/tl_templates/cuda/gemm_mma.h` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#1524](https://github.com/tile-ai/tilelang/pull/1524) Fix fp4 pointer arithmetic in CUDA codegen | 2025-12-24 | gemm_quant | by CodeRabbit * **Bug Fixes** * Refined pointer/index arithmetic for 4-bit and 1-bit scalar types to ensure correct memory loads * Fixed fp4 (4-bit float) memory access index calculation for packed elements * Improved code generation for buffer element access to use simplified index expressions * **... | wrapper: `src/target/codegen_cuda.cc` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#1726](https://github.com/tile-ai/tilelang/pull/1726) [Bugfix] Fix incorrect alignment of vectorized subtype | 2026-01-23 | gemm_quant | This pull request makes a minor change to the memory alignment of the `fp4_e2_4_t` struct in `cuda_fp4.h`, reducing its alignment from 4 bytes to 2 bytes. This may improve memory efficiency or compatibility with certain data layouts. * Reduced the alignment of the `fp4_e2_4_t` struct from 4 bytes to... | wrapper: `src/tl_templates/cuda/cuda_fp4.h` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2000](https://github.com/tile-ai/tilelang/pull/2000) fix: add cudaGetLastError check after cuLaunchKernel in TVM FFI backend | 2026-04-04 | gemm_quant | - Update TVM submodule to include `cudaPeekAtLastError()` check after `cuLaunchKernel` in the TVM FFI backend - Detects asynchronous CUDA errors (e.g. illegal memory access) that `cuLaunchKernel`'s return value does not capture - Error format aligned with the Cython backend's `TILELANG_CHECK_LAST_ER... | other: `3rdparty/tvm` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#1724](https://github.com/tile-ai/tilelang/pull/1724) [Enhancement] Legalize subtype access | 2026-01-23 | gemm_quant | This pull request introduces support for efficient packed storage and access of scalar FP4 (4-bit floating point) buffers in CUDA code generation. The main improvements are the use of packed types to halve register usage for FP4 scalars, and new helper functions for packed buffer access. The changes... | wrapper: `src/target/codegen_cuda.cc`, `src/target/codegen_cuda.h`, `src/tl_templates/cuda/cuda_fp4.h` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#1580](https://github.com/tile-ai/tilelang/pull/1580) [BugFix] Change default rounding mode for fp4 conversions | 2025-12-31 | gemm_quant | by CodeRabbit * **Bug Fixes** * Improved FP4 conversion accuracy by updating rounding behavior to use nearest-value rounding instead of truncation for multiple data types, resulting in more precise numerical conversions. <sub>✏️ Tip: You can customize this high-level summary in your review settings.... | wrapper: `src/tl_templates/cuda/cuda_fp4.h` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#1880](https://github.com/tile-ai/tilelang/pull/1880) Avoid cvt instruction in FP4 before cuda 13.0 | 2026-03-05 | gemm_quant | by CodeRabbit * **Bug Fixes** * Improved FP4→FP16 conversion reliability across CUDA toolchains by adding a safe fallback path for older toolchains and ensuring correct handling of paired FP4 values. * **Chores** * Preserved existing public APIs while selecting the appropriate conversion implementat... | wrapper: `src/tl_templates/cuda/cuda_fp4.h` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |

### Attention / KV / Decode

Use this section for: Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization.
NCU first look: Long scoreboard, L2/DRAM traffic, global-load sectors, tensor pipe %, and shared-memory bank conflicts.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#1495](https://github.com/tile-ai/tilelang/pull/1495) [Refactor] Phaseout legacy `alloc_local` statement in examples and introduce processing for floating fragment buffers | 2025-12-22 | attention_kv, benchmark_test, scheduler_autotune, gemm_quant, moe_routing | This pull request refactors several attention and decoding kernels to improve code clarity and consistency in local variable allocation and usage. The main changes involve replacing single-element local array allocations with scalar variable allocations, simplifying assignments, and streamlining how... | kernel: `examples/attention_sink/example_gqa_sink_bwd_bhsd.py`, `examples/attention_sink/example_mha_sink_bwd_bhsd.py`, `examples/blocksparse_attention/example_tilelang_block_sparse_attn.py`, `examples/blocksparse_attention/example_tilelang_sparse_gqa_decode_paged.py`<br>benchmark: `benchmark/blocksparse_attention/benchmark_tilelang_block_sparse_fmha.py`, `examples/deepseek_mla/amd/benchmark_mla_decode_amd_tilelang.py`<br>test: `examples/gdn/test_example_gdn_compilation.py`<br>wrapper: `examples/deepseek_mla/example_mla_decode.py`, `examples/deepseek_mla/example_mla_decode_paged.py`, `examples/deepseek_mla/example_mla_decode_persistent.py`, `examples/deepseek_mla/example_mla_decode_ws.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1514](https://github.com/tile-ai/tilelang/pull/1514) [Cleanup] Remove unnecessary macros in tilelang examples | 2025-12-24 | attention_kv, scheduler_autotune, compiler_runtime, arch_pipeline, benchmark_test | by CodeRabbit * **Refactor** * Consolidated macro-based attention stages into inline, per-iteration implementations across attention kernels, simplifying control flow. * **API** * Added explicit split vs. no-split entry points and explicit partial-output parameters for split-mode flows, clarifying s... | kernel: `examples/attention_sink/example_gqa_sink_fwd_bhsd_wgmma_pipelined.py`, `examples/attention_sink/example_mha_sink_fwd_bhsd.py`, `examples/attention_sink/example_mha_sink_fwd_bhsd_wgmma_pipelined.py`, `examples/blocksparse_attention/example_tilelang_block_sparse_attn.py`<br>benchmark: `examples/deepseek_mla/amd/benchmark_mla_decode_amd_tilelang.py`<br>wrapper: `examples/deepseek_mla/example_mla_decode.py`, `examples/deepseek_mla/example_mla_decode_paged.py`, `examples/deepseek_mla/example_mla_decode_ws.py`, `examples/flash_decoding/example_gqa_decode.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1644](https://github.com/tile-ai/tilelang/pull/1644) [Feature] Introduce DecoupleTypeCast pass for mixed-precision vectorization | 2026-01-17 | attention_kv, scheduler_autotune, compiler_runtime, moe_routing, benchmark_test | Motivation In mixed-precision scenarios (e.g., fp4/fp8 combined with fp32), the vectorization length of a vectorized loop is constrained by the GCD of all involved dtypes. For example: code block Since the GCD of fp4 (4-bit) and fp32 (32-bit) is 4-bit, the entire loop can only be vectorized at 4-bit... | kernel: `examples/blocksparse_attention/example_tilelang_sparse_gqa_decode_varlen_indice.py`, `examples/fusedmoe/example_fusedmoe_tilelang.py`, `tilelang/engine/phase.py`, `tilelang/language/__init__.py`<br>test: `testing/python/transform/test_tilelang_transform_decouple_type_cast.py`<br>wrapper: `examples/minference/regression_vs_sparse_attn.py`, `src/op/builtin.cc`, `src/op/builtin.h`, `src/target/utils.cc` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1296](https://github.com/tile-ai/tilelang/pull/1296) Add sparse fine-tuning kernel for deepseek sparse attention to example | 2025-11-27 | attention_kv, scheduler_autotune, moe_routing, memory_primitives, benchmark_test | This PR integrates the kernel required for the sparse fine-tuning stage of deepseek sparse attention into the example. by CodeRabbit Release Notes * **New Features** * Added sparse attention mechanism with optimized forward and backward kernel implementations for efficient fine-tuning workflows * In... | wrapper: `examples/dsa_sparse_finetune/dsa.py`, `examples/dsa_sparse_finetune/index.py`, `examples/dsa_sparse_finetune/indexer_bwd.py`, `examples/dsa_sparse_finetune/indexer_topk_reducesum.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1517](https://github.com/tile-ai/tilelang/pull/1517) [Dtype] Improve host codegen handling for subtype | 2025-12-24 | attention_kv, compiler_runtime, gemm_quant, scheduler_autotune, benchmark_test | as title. Thanks @Hamerlate by CodeRabbit * **New Features** * Added FP4 data type conversions across multiple formats (Half, Float, Double, BFloat16) * Extended vectorized cast operations for FP4 and FP8 data types * Improved sub-byte data type handling with enhanced shape and stride management * B... | kernel: `tilelang/engine/param.py`, `tilelang/jit/adapter/tvm_ffi.py`, `tilelang/language/v2/dtypes.py`<br>test: `testing/python/language/test_tilelang_language_vectorized_cast.py`<br>wrapper: `examples/flash_decoding/example_gqa_decode_varlen_logits.py`, `src/target/codegen_cuda.cc`, `src/tl_templates/cuda/cuda_fp4.h`, `src/transform/arg_binder.cc` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1222](https://github.com/tile-ai/tilelang/pull/1222) [BugFix] Refactor attention kernel to handle OOB positions by filling with `-inf` instead of clearing accumulators. | 2025-11-15 | attention_kv, arch_pipeline, benchmark_test | by CodeRabbit * **Bug Fixes** * Improved out-of-bounds handling in multiple flash-attention examples: accumulator initialization now masks invalid sequence positions to avoid spurious contributions. * **Chores** * Updated example CLI defaults (including context size and causal flag) for clarity. * U... | kernel: `examples/flash_attention/example_gqa_bwd.py`, `examples/flash_attention/example_gqa_bwd_tma_reduce.py`, `examples/flash_attention/example_gqa_bwd_wgmma_pipelined.py`, `examples/flash_attention/example_gqa_fwd_bshd.py`<br>test: `examples/flash_attention/test_example_flash_attention.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1548](https://github.com/tile-ai/tilelang/pull/1548) [Enhancement] Support larger `H` in deepseek sparse mla backward via split-H | 2025-12-27 | attention_kv, scheduler_autotune, benchmark_test, compiler_runtime | 1252 by CodeRabbit * **Refactor** * Backward kernel refactored to process data in block-wise tiles, improving memory locality and compute efficiency for sparse attention backward pass. * **Tests** * Added a larger-scale test case to validate backward kernel behavior with increased sequence/height si... | test: `examples/deepseek_v32/test_tilelang_example_deepseek_v32.py`<br>wrapper: `examples/deepseek_v32/sparse_mla_bwd.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1672](https://github.com/tile-ai/tilelang/pull/1672) [Clean][Refactor] Phaseout Legacy Pass `ParallelLoopTransformer` | 2026-01-15 | attention_kv, scheduler_autotune, compiler_runtime | This pull request refactors and simplifies the kernel invocation pattern for the `flashattn` function in both `example_tilelang_sparse_gqa_decode_paged.py` and `example_tilelang_sparse_gqa_decode_varlen_indice.py`. The main improvements are making the kernel instantiation more dynamic and consistent... | kernel: `examples/blocksparse_attention/example_tilelang_sparse_gqa_decode_paged.py`, `examples/blocksparse_attention/example_tilelang_sparse_gqa_decode_varlen_indice.py`, `examples/blocksparse_attention/example_tilelang_sparse_gqa_decode_varlen_mask.py`<br>wrapper: `src/op/atomic_add.cc`, `src/op/copy.cc`, `src/op/fill.cc`, `src/transform/common/loop_parallel_transform_utils.h` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#853](https://github.com/tile-ai/tilelang/pull/853) [Example] Add examples to support efficient attention sink forward process | 2025-09-23 | attention_kv, benchmark_test, arch_pipeline | TODO - [x] MHA+attention sink - [x] sliding windows attention+attention sink - [x] Implement wgmma pipelined version for optimal performance on Hopper - [x] Add a benchmark to compare with official triton impl - [x] Optimize sliding window attention performance to surpass triton - [x] Fix cases wher... | kernel: `examples/attention_sink/example_gqa_sink_fwd_bhsd_wgmma_pipelined.py`, `examples/attention_sink/example_mha_sink_fwd_bhsd.py`, `examples/attention_sink/example_mha_sink_fwd_bhsd_wgmma_pipelined.py`<br>test: `examples/attention_sink/test_example_attention_sink.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2009](https://github.com/tile-ai/tilelang/pull/2009) [BugFix] Fix CI failures: clean /tmp on self-hosted runners and skip CuTeDSL alloc_global tests | 2026-04-02 | attention_kv, compiler_runtime, benchmark_test | - Add CI step to clean stale JIT temp files (`/tmp/*.so`, `*.cu`, `*.cubin`, `tvm-debug-mode-tempdirs`) before tests on self-hosted runners. These files accumulate across CI runs and can fill the container disk, causing `g++` to be killed (SIGTERM) during JIT `.so` linking. - Skip CuTeDSL-incompatib... | test: `examples/deepseek_mla/test_example_mla_decode.py`, `examples/flash_decoding/test_example_flash_decoding.py`<br>other: `.github/workflows/ci.yml` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1645](https://github.com/tile-ai/tilelang/pull/1645) [Example] Add attention sink varlen examples | 2026-01-09 | attention_kv, scheduler_autotune, benchmark_test | by CodeRabbit * **New Features** * Added variable-length attention with sinks, including both forward and backward (PyTorch autograd) paths. * Supports causal masking, optional sliding-window attention, grouped/packed sequences, and sink-aware softmax normalization. * Includes reference implementati... | kernel: `examples/attention_sink/example_gqa_sink_bwd_varlen.py`, `examples/attention_sink/example_gqa_sink_fwd_varlen.py`<br>test: `examples/attention_sink/test_example_attention_sink.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1269](https://github.com/tile-ai/tilelang/pull/1269) [Enhancement] Keep max score attention across blocks in FlashAttention for better numerical stablity | 2025-11-17 | attention_kv | as title cc @LeiWang1999 @chengyupku by CodeRabbit * **Refactor** * Softmax max-tracking consolidated across attention kernels: current and previous per-row maxima are merged to improve numerical stability and consistency during attention normalization. * **Tests** * Transformation tests updated to... | kernel: `examples/attention_sink/example_gqa_sink_bwd_bhsd.py`, `examples/attention_sink/example_gqa_sink_fwd_bhsd_wgmma_pipelined.py`, `examples/attention_sink/example_mha_sink_bwd_bhsd.py`, `examples/attention_sink/example_mha_sink_fwd_bhsd.py`<br>benchmark: `benchmark/blocksparse_attention/benchmark_tilelang_block_sparse_fmha.py`, `examples/deepseek_mla/amd/benchmark_mla_decode_amd_tilelang.py`<br>wrapper: `examples/amd/example_amd_flash_attn_bwd.py`, `examples/amd/example_amd_flash_attn_fwd.py`, `examples/deepseek_mla/example_mla_decode.py`, `examples/deepseek_mla/example_mla_decode_paged.py`<br>docs: `examples/flash_attention/README.md` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2069](https://github.com/tile-ai/tilelang/pull/2069) [Example]  Add HISA: hierarchical sparse attention indexer | 2026-04-25 | attention_kv, gemm_quant, compiler_runtime | Adds `examples/dsa_hisa/` — a Tilelang prefill implementation of **HISA**(Hierarchical Indexed Sparse Attention), a plug-and-play replacement for the DeepSeek sparse attention indexer that rewrites the flat-token search path into a two-stage hierarchical procedure. Paper: <https://arxiv.org/pdf/2603... | kernel: `examples/dsa_hisa/tilelang_utils.py`<br>wrapper: `examples/dsa_hisa/block_sparse_mqa_fp8.py`, `examples/dsa_hisa/clean_and_maintain_logits.py`, `examples/dsa_hisa/fp8_block_mean_pooling.py`, `examples/dsa_hisa/hisa.py`<br>docs: `examples/dsa_hisa/README.md` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#877](https://github.com/tile-ai/tilelang/pull/877) [Example] Add efficient attention sink backward implementations and tests | 2025-09-26 | attention_kv, benchmark_test | The second part of 831. This pull request introduces a new backward pass implementation for multi-head attention using the BHSD layout, along with corresponding tests. The main focus is on adding the `example_mha_bwd_bhsd.py` module, which provides a custom backward kernel for flash attention and in... | kernel: `examples/attention_sink/example_gqa_sink_bwd_bhsd.py`, `examples/attention_sink/example_mha_sink_bwd_bhsd.py`, `examples/flash_attention/example_mha_bwd_bhsd.py`, `examples/flash_attention/example_mha_bwd_wgmma_pipelined.py`<br>test: `examples/attention_sink/test_example_attention_sink.py`, `examples/flash_attention/test_example_flash_attention.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2094](https://github.com/tile-ai/tilelang/pull/2094) Add opt-out for prelower semantic checks for DeepSeek V4 Flash on ARM64  | 2026-04-25 | attention_kv, compiler_runtime, scheduler_autotune | TileLang / TVM Patch Investigation for DeepSeek V4 Flash on ARM64 Python 3.12 Exactly three TileLang wheel files were changed after install: 1. `tilelang/3rdparty/tvm/python/tvm/runtime/support.py` 2. `tilelang/analysis/nested_loop_checker.py` 3. `tilelang/engine/phase.py` Findings - The `support.py... | kernel: `tilelang/engine/phase.py`, `tilelang/transform/pass_config.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1910](https://github.com/tile-ai/tilelang/pull/1910) [Example] Flash Attention SM100 | 2026-03-22 | attention_kv, arch_pipeline | Blackwell (SM100) Flash Attention examples Add Flash Attention examples for Blackwell (SM100) using TCGEN05MMA + TMEM, covering MHA/GQA forward and backward and replacing the Hopper WGMMA approach. All kernels use the BSHD layout and support causal masking. Files in this PR \| File \| Description \| \|-... | kernel: `examples/flash_attention_sm100/gqa_bwd_bshd.py`, `examples/flash_attention_sm100/gqa_fwd_bshd.py`, `examples/flash_attention_sm100/mha_bwd_bshd.py`, `examples/flash_attention_sm100/mha_fwd_bshd.py`<br>wrapper: `src/op/copy.cc` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#924](https://github.com/tile-ai/tilelang/pull/924) [Example] Add support for `bfloat16` and user-defined `sm_scale` in attention sink examples | 2025-10-10 | attention_kv | This pull request introduces support for configurable data types (float16 and bfloat16) for attention kernels in the example scripts, improving flexibility and compatibility for different hardware and precision requirements. It also refactors kernel functions and input generation to accept a `dtype`... | kernel: `examples/attention_sink/example_gqa_sink_bwd_bhsd.py`, `examples/attention_sink/example_gqa_sink_fwd_bhsd_wgmma_pipelined.py`, `examples/attention_sink/example_mha_sink_bwd_bhsd.py`, `examples/attention_sink/example_mha_sink_fwd_bhsd.py`<br>wrapper: `examples/amd/example_amd_flash_attn_bwd.py`<br>docs: `examples/attention_sink/README.md` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1265](https://github.com/tile-ai/tilelang/pull/1265) [Example] Add GQA decoding kernel with varlen page table | 2025-11-16 | attention_kv, scheduler_autotune | As title by CodeRabbit * **New Features** * Added a new example for flash attention decoding with variable-length sequences and attention pooling, featuring TileLang kernel implementation, test cases for equal and variable-length scenarios, and built-in benchmarking tools with CLI interface. | wrapper: `examples/flash_decoding/example_gqa_decode_varlen_logits_paged.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1173](https://github.com/tile-ai/tilelang/pull/1173) [Example] Update GQA varlen fwd | 2025-11-05 | attention_kv, scheduler_autotune | This pull request refactors and improves the `example_gqa_fwd_varlen.py` implementation for variable-length Flash Attention with grouped query attention (GQA). The changes enhance correctness, efficiency, and clarity, especially around sequence masking, causal logic, and kernel launch parameters. At... | kernel: `examples/flash_attention/example_gqa_fwd_varlen.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1991](https://github.com/tile-ai/tilelang/pull/1991) [example] use alloc_global in split-kv decode kernel | 2026-03-30 | attention_kv | Improve readability by CodeRabbit * **Refactor** * Optimized internal memory allocation patterns in flash attention example implementations for improved code structure and maintainability across multiple decoding scenarios. | wrapper: `examples/deepseek_mla/example_mla_decode.py`, `examples/flash_decoding/example_gqa_decode.py`, `examples/flash_decoding/example_mha_inference.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1636](https://github.com/tile-ai/tilelang/pull/1636) [Example] Add Seesaw Sparse MLA Forward Kernel for DeepSeek-V3.2 | 2026-01-08 | attention_kv | Add a new sparse MLA forward kernel implementation using the "Seesaw" synchronization pattern, an alternative to the existing pipelined approach. **Dual-Consumer Parallel Architecture:** - Unlike pipelined version where WG1 depends on WG0's S_shared/alpha_shared, Seesaw allows both consumers to work... | wrapper: `examples/deepseek_v32/sparse_mla_fwd_seesaw.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |

### MoE / Routing

Use this section for: Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea.
NCU first look: Tensor pipe %, SM imbalance, branch divergence, L2 traffic, permutation traffic, and synchronization stalls.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#947](https://github.com/tile-ai/tilelang/pull/947) [Bugfix] Do not force inline let stmt | 2025-10-10 | moe_routing, scheduler_autotune, benchmark_test, attention_kv, compiler_runtime | as title. - [x] Add a doc about let stmt inline. by CodeRabbit - New Features - FusedMoE example main() is now configurable via function parameters (model size, experts, batch, seq_len). - Bug Fixes - More robust software-pipeline injection/planning that preserves wrappers and attributes. - Relaxed... | kernel: `examples/fusedmoe/example_fusedmoe_tilelang.py`, `src/transform/lower_tile_op.cc`, `tilelang/engine/phase.py`, `tilelang/transform/pass_config.py`<br>test: `examples/blocksparse_attention/test_example_blocksparse_attention.py`, `examples/fusedmoe/test_example_fusedmoe.py`, `testing/python/issue/test_tilelang_issue_814.py`, `testing/python/transform/test_tilelang_transform_multi_version_buffer.py`<br>wrapper: `src/op/builtin.cc`, `src/op/builtin.h`, `src/transform/inject_pipeline.cc`, `src/transform/merge_shared_memory_allocations.cc`<br>docs: `docs/compiler_internals/letstmt_inline.md`, `docs/index.md` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |

### Norm / Elementwise / Epilogue

Use this section for: Look for fusion, vectorization, dtype conversion, and store-traffic reductions before changing math.
NCU first look: DRAM %, L2 bytes, global load/store sectors, eligible warps, and long scoreboard.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#2168](https://github.com/tile-ai/tilelang/pull/2168) [Example] Add layernorm example in tilelang | 2026-05-12 | norm_elementwise, compiler_runtime | Adds `examples/norm/layernorm.py` — a TileLang LayerNorm with forward and backward kernels, wrapped in a `torch.autograd.Function` so it works as a drop-in replacement for `torch.nn.functional.layer_norm`. Forward saves `(mean, rstd)` per row. Backward fuses dx, dgamma, dbeta in one kernel: each CTA... | kernel: `examples/norm/layernorm.py` | Look for fusion, vectorization, dtype conversion, and store-traffic reductions before changing math. |
| [#2022](https://github.com/tile-ai/tilelang/pull/2022) Fix serial loop phase dtype mismatch in LowerTileOp | 2026-04-08 | norm_elementwise | This fixes a dtype mismatch in `LowerTileOp` when a serial loop variable is `int64`. `debug/normal.py` currently allocates several index vars as `int64`, which leads to an `int64` serial loop in the lowered TIR. The pass was still building the phase expression with `int32` literals, so `FloorDiv`/`F... | kernel: `src/transform/lower_tile_op.cc` | Look for fusion, vectorization, dtype conversion, and store-traffic reductions before changing math. |

### Memory / Primitives

Use this section for: Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract.
NCU first look: DRAM throughput, L2 hit rate, memory sectors, shared-memory conflicts, and synchronization overhead.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#1667](https://github.com/tile-ai/tilelang/pull/1667) [Feature] Support `cp.reduce.async.bulk.tensor` | 2026-01-14 | memory_primitives, attention_kv, arch_pipeline, benchmark_test, scheduler_autotune | fix 1655 - [x] check swizzle support - [x] refactor other examples containing tma reduce by CodeRabbit * **New Features** * Added TMA-based atomic-add support and descriptor-based TMA store helpers for improved GPU atomic operations and performance. * **Tests** * New tests validating TMA atomic-add... | kernel: `examples/autodd/tilelang_buggy.py`, `examples/autodd/tilelang_minimized_expected.py`, `examples/flash_attention/example_gqa_bwd_tma_reduce.py`, `examples/flash_attention/example_gqa_bwd_tma_reduce_varlen.py`<br>test: `testing/python/language/test_tilelang_language_atomic_add.py`<br>wrapper: `src/op/atomic_add.cc`, `src/op/atomic_add.h`, `src/op/copy.cc`, `src/op/utils.cc` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |
| [#2057](https://github.com/tile-ai/tilelang/pull/2057) [Cache] Refactor cache namespace layout | 2026-04-18 | memory_primitives, scheduler_autotune, compiler_runtime, benchmark_test, gemm_quant | - move kernel and autotuner artifacts under a shared namespace root derived from the TileLang version and platform - keep staging directories inside the same namespace so stale staging cleanup only scans a small local directory - align autotuner cache and staging paths with the kernel cache layout T... | kernel: `examples/grouped_gemm/example_grouped_gemm_fwd_ptr.py`, `tilelang/autotuner/param.py`, `tilelang/autotuner/tuner.py`, `tilelang/cache/kernel_cache.py`<br>test: `examples/grouped_gemm/test_example_grouped_gemm.py`, `testing/python/autotune/test_tilelang_autotune_atomic_save.py`, `testing/python/cache/test_tilelang_kernel_cache_atomic_save.py`<br>other: `.github/workflows/ci.yml` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |
| [#2156](https://github.com/tile-ai/tilelang/pull/2156) [Refactor][Backend] Split remaining TileOps by backend | 2026-05-07 | memory_primitives, gemm_quant, scheduler_autotune | - Move backend-specific atomic add, reduce, finalize reducer, and sparse GEMM lowering out of shared TileOp implementations. - Add backend registration files for CPU, CUDA, ROCm, Metal, and WebGPU reducer paths, plus CUDA and ROCm boundaries for atomic add and sparse GEMM. - Add a Python GEMM_SP bac... | kernel: `src/backend/cuda/op/gemm_sp.cc`, `src/op/gemm_sp.cc`, `src/op/gemm_sp.h`<br>wrapper: `src/backend/cpu/op/copy.cc`, `src/backend/cpu/op/fill.cc`, `src/backend/cpu/op/transpose.cc`, `src/backend/cuda/op/atomic_add.cc` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |
| [#1971](https://github.com/tile-ai/tilelang/pull/1971) Introduce T.deallocate_tmem and T.transpose | 2026-03-30 | memory_primitives, compiler_runtime, benchmark_test | - keep `shared.barrier` and `shared.cluster_barrier` allocations colocated with the annotated block so the tcgen05 no-TMA pipeline still reaches `LowerSharedBarrier` with valid `barrier_init` metadata - include cluster barriers in `MultiVersionBuffer(barrier_only=True)` handling and add a regression... | kernel: `examples/gemm/example_gemm_schedule.py`, `tilelang/language/__init__.py`, `tilelang/language/allocate.py`, `tilelang/language/builtin.py`<br>test: `testing/python/language/test_tilelang_language_transpose.py`, `testing/python/transform/test_tilelang_transform_lower_shared_barrier.py`, `testing/python/transform/test_tilelang_transform_lower_shared_tmem.py`<br>wrapper: `src/op/builtin.cc`, `src/op/builtin.h`, `src/op/transpose.cc`, `src/op/transpose.h`<br>docs: `docs/programming_guides/instructions.md` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |
| [#2004](https://github.com/tile-ai/tilelang/pull/2004) fix: fix copy+cast vectorize loop to use wider vector load/store instrcution | 2026-03-31 | memory_primitives, gemm_quant, compiler_runtime, benchmark_test | Problem When a copy involves a type cast (e.g., fp8 → float32), the `VectorizePlanner` was using the cast's vector width constraint to determine the memory access layout (`vector_extent`). This resulted in suboptimal layouts even though `DecoupleTypeCast` would later split the cast into a separate l... | test: `examples/gemm/test_example_gemm.py`, `testing/python/transform/test_tilelang_transform_decouple_type_cast.py`<br>wrapper: `src/transform/loop_vectorize.cc` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |
| [#1689](https://github.com/tile-ai/tilelang/pull/1689) [Refactor] Relocate layout transformation of `ptx_stmatrix`  | 2026-01-26 | memory_primitives, arch_pipeline, compiler_runtime, benchmark_test | as title. by CodeRabbit * **New Features** * Added two public layout helpers for 8x8 GEMM fragments (normal and transposed). * **Refactor** * Expanded layout-aware access remapping and rewriting for tile/matrix lowering with safer early-exit handling. * Improved loop-layout inference to avoid reusin... | kernel: `src/transform/lower_tile_op.cc`, `tilelang/layout/__init__.py`, `tilelang/layout/swizzle.py`<br>wrapper: `src/layout/layout.cc`, `src/op/copy.cc`, `src/op/parallel.cc` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |
| [#1863](https://github.com/tile-ai/tilelang/pull/1863) [Refactor] Refactor Pass InjectFenceProxy | 2026-02-23 | memory_primitives, compiler_runtime, benchmark_test | This pull request includes several documentation and API improvements related to the `InjectFenceProxy` TIR pass, as well as a minor code change in an example and the addition of a new TVM intrinsic wrapper. The most significant changes are updates to the documentation for clarity and completeness,... | kernel: `tilelang/language/__init__.py`, `tilelang/language/tir/ir.py`, `tilelang/language/tir/op.py`<br>test: `testing/python/transform/test_tilelang_transform_inject_fence_proxy.py`, `testing/python/transform/test_tilelang_transform_lower_tile_op.py`<br>wrapper: `examples/quickstart.py`, `src/op/atomic_add.cc`, `src/op/copy.cc`, `src/transform/inject_fence_proxy.cc`<br>docs: `docs/compiler_internals/inject_fence_proxy.md` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |
| [#1958](https://github.com/tile-ai/tilelang/pull/1958) Nan propagation option for bf16 and half16 | 2026-04-13 | memory_primitives, compiler_runtime, benchmark_test | This PR introduces option to propagate NaN specifically for data type bf_16 and half, which modifies the following files: \|file changed \| implementation \| \|---\|---\| `src/op/builtin.h` \| Declares `kReduceMaxMinNanPropagate` and `tl::max_nan` / `tl::min_nan` ops. \| \| `src/op/builtin.cc` \| Registers TI... | kernel: `tilelang/language/reduce_op.py`<br>test: `testing/python/language/test_tilelang_language_reduce_maxmin_nan.py`<br>wrapper: `src/op/builtin.cc`, `src/op/builtin.h`, `src/op/reduce.cc`, `src/op/reduce.h` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |
| [#2074](https://github.com/tile-ai/tilelang/pull/2074) Fix reduce layout | 2026-04-21 | memory_primitives, compiler_runtime, benchmark_test | The specific shape in the added test fails in the previous version of TileLang. Here is a short fix and a minimal test that fails on the previous TileLang but succeeds after the fix. by CodeRabbit * **Bug Fixes** * Improved error handling for edge cases in iteration and reduction operations to preve... | test: `testing/python/language/test_tilelang_language_reshape.py`<br>wrapper: `src/layout/utils.cc`, `src/op/reduce.cc` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |
| [#2072](https://github.com/tile-ai/tilelang/pull/2072) Refactor ptx_ldmatrix to use tl.access_ptr with simplified signature | 2026-04-21 | memory_primitives, compiler_runtime | - Replace the legacy 8-parameter TVM-style `ptx_ldmatrix` API with a 4-parameter TileLang-style API that uses `tl.access_ptr` wrapping `BufferLoad` - Replace `Buffer::access_ptr()` with `tl::access_ptr()` in `LowerLDSMCopy` - Update `get_ldmatrix_offset` to return `(row, col)` coordinates instead of... | kernel: `tilelang/intrinsics/mma_macro_generator.py`, `tilelang/intrinsics/mma_sp_layout.py`, `tilelang/intrinsics/mma_sp_macro_generator.py`, `tilelang/intrinsics/utils.py`<br>wrapper: `src/op/copy.cc` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |
| [#2163](https://github.com/tile-ai/tilelang/pull/2163) [Backend] Share common GPU tile op lowerers | 2026-05-07 | memory_primitives | - Move shared GPU tile op lowering code into `src/backend/common/op`. - Keep CUDA and ROCm files focused on target matching, registration, and platform-specific strategy hooks. - This refactor is motivated by the high structural similarity between several GPU tile ops across the CUDA and ROCm backen... | wrapper: `src/backend/common/op/atomic_reduce.h`, `src/backend/common/op/cumsum.h`, `src/backend/common/op/fill.h`, `src/backend/common/op/finalize_reducer.h` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |
| [#1986](https://github.com/tile-ai/tilelang/pull/1986) [BugFix] Fix auto vectorization for binary operations after wider copy instructions | 2026-04-04 | memory_primitives, benchmark_test | Fix bug that f32x2 vectorization not working for wider copy instructions. The below code successfully generate `tl::fadd2`. code block The below code fail to generate `tl::fadd2`. code block Code generation improvements for vectorized binary operations * Updated `PrintVecBinaryOp` in `codegen_cuda.c... | test: `testing/python/cuda/test_cuda_f32x2_intrinsics.py`<br>wrapper: `src/target/codegen_cuda.cc` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |
| [#2018](https://github.com/tile-ai/tilelang/pull/2018) [codex] Reduce slow pytest runtime in testing/python | 2026-04-07 | memory_primitives, benchmark_test | This PR reduces slow correctness-test wall time in `testing/python` without weakening semantic coverage. It does that by: - vectorizing slow reference and fixture paths that were still doing Python-scalar CUDA work - reducing representative tensor sizes for semantic-only tests while preserving the s... | test: `testing/python/math/test_math_bitwise_reduce.py` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |
| [#1933](https://github.com/tile-ai/tilelang/pull/1933) Add machine architecture in cache key | 2026-03-15 | memory_primitives, compiler_runtime | as title by CodeRabbit * **Bug Fixes** * Cache key generation now incorporates platform-specific identifiers to ensure proper cache segregation across different platforms, preventing potential cross-platform cache conflicts. | kernel: `tilelang/cache/kernel_cache.py` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |
| [#1977](https://github.com/tile-ai/tilelang/pull/1977) [BugFix] Use content hash instead of mtime for libtilelang cache key | 2026-03-26 | memory_primitives, compiler_runtime | Problem The kernel cache key includes a "library stamp" for `libtilelang.so` to detect C++ pass changes during development. Currently this stamp uses **mtime + file size** (`st.st_mtime_ns`), which causes cache misses when: - The same tilelang version is installed on different machines at different... | kernel: `tilelang/cache/kernel_cache.py` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |
| [#2080](https://github.com/tile-ai/tilelang/pull/2080) [Refactor] Strip build machine paths from LOG messages in wheel releases | 2026-04-22 | memory_primitives | When tilelang is installed from a PyPI wheel, `LOG(WARNING)` messages currently leak the build machine's absolute file paths (e.g. `/home/runner/work/tilelang/tilelang/src/op/copy.cc:520`) because `__FILE__` is baked in at compile time. This is a poor user experience and exposes CI infrastructure de... | wrapper: `src/op/atomic_add.cc`, `src/runtime/logging.cc`, `src/transform/producer_consumer_ws.cc`<br>other: `CMakeLists.txt` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |
| [#1845](https://github.com/tile-ai/tilelang/pull/1845) [Enhancement] Optimize templates for half/bfloat16 | 2026-02-14 | memory_primitives | by CodeRabbit * **Bug Fixes** * Corrected min/max behavior for bfloat16 and float16 scalars and added explicit bfloat16/float16 comparisons in reductions. * Ensured proper initialization, duplication, and update semantics for max/min/absmax reduction paths when using temporary buffers. * **Refactor*... | wrapper: `src/op/reduce.cc`, `src/target/codegen_cuda.cc`, `src/tl_templates/cuda/common.h`, `src/tl_templates/cuda/reduce.h` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |
| [#2001](https://github.com/tile-ai/tilelang/pull/2001) [Refactor] Refactor CUDA atomic helpers | 2026-03-31 | memory_primitives | This PR refactors src/tl_templates/cuda/atomic.h to reduce repeated inline PTX code and make the atomic add paths easier to maintain. by CodeRabbit * **Refactor** * Consolidated CUDA atomic implementations for floating-point types (fp16, bf16, f32) to reduce duplication. * Unified memory-order handl... | wrapper: `src/tl_templates/cuda/atomic.h` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |
| [#2030](https://github.com/tile-ai/tilelang/pull/2030) [Bugfix] Fix incorrect sync hoist for fragment buffer conditions in ThreadSync | 2026-04-11 | memory_primitives | - Fix a bug in `ConditionThreadPropertyChecker` where conditions derived from fragment (local-scope) buffer loads were incorrectly classified as non-block-uniform based solely on storage scope - This caused `ThreadSync` to hoist `__syncthreads()` out of an if-body, removing the write-before-read syn... | wrapper: `src/transform/thread_storage_sync.cc` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |

### Scheduler / Autotune

Use this section for: Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing.
NCU first look: SM occupancy, waves/SM, active cycles, tail effects, and load imbalance.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#1505](https://github.com/tile-ai/tilelang/pull/1505) [Misc] configure pymarkdown | 2025-12-23 | scheduler_autotune, gemm_quant, attention_kv, benchmark_test, compiler_runtime | by CodeRabbit * **Documentation** * Improved documentation clarity with refined explanations of TileLang operator calls and multi-level tiling concepts. * Reorganized documentation structure for better navigation. * **Chores** * Added markdown linting configuration to maintain consistent documentati... | benchmark: `benchmark/mamba2/README.md`<br>docs: `CODE_OF_CONDUCT.md`, `CONTRIBUTING.md`, `README.md`, `docs/README.md`<br>other: `.pre-commit-config.yaml`, `.pymarkdown` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#1486](https://github.com/tile-ai/tilelang/pull/1486) [Refactor] Phaseout PassConfig `kDisableDynamicTailSplit` and `kDynamicAlignment` as they are legacy | 2025-12-21 | scheduler_autotune, benchmark_test, compiler_runtime | This pull request removes support for two dynamic shape-related pass configuration options: `tl.disable_dynamic_tail_split` and `tl.dynamic_alignment`. The changes clean up the codebase by eliminating references to these options from both the Python and C++ code, as well as removing the associated d... | kernel: `tilelang/transform/pass_config.py`<br>benchmark: `testing/python/dynamic/test_tilelang_dynamic_symbolic_bench.py`<br>test: `examples/dynamic_shape/test_example_dynamic.py`, `testing/python/dynamic/test_tilelang_dynamic_symbolic.py`<br>wrapper: `examples/dynamic_shape/example_dynamic.py`, `src/op/builtin.cc`, `src/op/builtin.h` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#1969](https://github.com/tile-ai/tilelang/pull/1969) [BugFix] Fix bugs in `gemm_streamk` example on SM90 | 2026-03-24 | scheduler_autotune, gemm_quant, arch_pipeline, compiler_runtime | by CodeRabbit Release Notes * **Documentation** * Updated GEMM StreamK example with revised configuration and computation precision settings. | kernel: `examples/gemm_streamk/example_tilelang_gemm_streamk.py` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#1906](https://github.com/tile-ai/tilelang/pull/1906) [Enhancement] Add eager-mode support for tilelang.autotune | 2026-03-12 | scheduler_autotune, compiler_runtime, benchmark_test | Currently `@tilelang.autotune` only works with lazy mode kernels. This PR extends `@tilelang.autotune` to better support eager mode. by CodeRabbit * **New Features** * Added compile() for direct kernel compilation. * Introduced lazy/eager execution modes for autotuning. * Enhanced kernel cache to st... | kernel: `tilelang/autotuner/param.py`, `tilelang/autotuner/tuner.py`<br>test: `testing/python/autotune/test_tilelang_autotune.py`, `testing/python/autotune/test_tilelang_autotune_eager_mode.py`, `testing/python/autotune/test_tilelang_autotune_with_inputs.py` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#2006](https://github.com/tile-ai/tilelang/pull/2006) [Feature] Support T.annotate_compile_flags, T.annotate_pass_configs, and out_idx as PrimFunc attrs | 2026-04-02 | scheduler_autotune, compiler_runtime, benchmark_test | - Add `T.annotate_compile_flags(flags)` and `T.annotate_pass_configs(configs)` DSL functions to configure compile flags and pass configs inside function bodies - Store `out_idx` (from `T.empty()` + `return`) as PrimFunc attr `tilelang_out_idx` instead of monkey-patching `out_idx_override` - All thre... | kernel: `tilelang/autotuner/param.py`, `tilelang/jit/__init__.py`, `tilelang/language/eager/__init__.py`, `tilelang/language/eager/builder.py`<br>test: `testing/python/language/test_tilelang_language_func_attrs.py` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#2024](https://github.com/tile-ai/tilelang/pull/2024) Re-enable deprecated `TL_DISABLE_TMA_LOWER` pass config for TMA store | 2026-04-10 | scheduler_autotune, arch_pipeline, memory_primitives, compiler_runtime | by CodeRabbit * **Bug Fixes** * Temporarily re-enabled the global disable-TMA option so it again prevents auto-lowering of T.copy to TMA stores. * **Documentation** * Clarified deprecation messaging and added an explicit removal target (v0.1.10); advises using per-copy disable_tma=True instead. | kernel: `tilelang/transform/pass_config.py`<br>wrapper: `src/op/builtin.h`, `src/op/copy.cc` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#1223](https://github.com/tile-ai/tilelang/pull/1223) [GQA] Add varlen decoding kernel with logits saving | 2025-11-11 | scheduler_autotune, attention_kv, benchmark_test | As title by CodeRabbit Release Notes * **New Features** * Added variable-length flash attention example with attention pooling and score aggregation, featuring comprehensive validation against PyTorch and performance benchmarking utilities. * **Improvements** * Streamlined flash decoding example wit... | wrapper: `examples/flash_decoding/example_gqa_decode.py`, `examples/flash_decoding/example_gqa_decode_varlen_logits.py` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#1499](https://github.com/tile-ai/tilelang/pull/1499) [Enhancement] Optimize MHA varlen fwd and support autotune | 2025-12-22 | scheduler_autotune, attention_kv | fix 1122 by CodeRabbit * **New Features** * Autotuning support with a new tuning mode and a --tune CLI option. * Benchmarking flow added to compare the optimized kernel against a reference implementation. * **Improvements** * Updated default parallelization parameters for better scheduling and perfo... | kernel: `examples/flash_attention/example_mha_fwd_varlen.py` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#1900](https://github.com/tile-ai/tilelang/pull/1900) feat: configurable compiler temp file cleanup | 2026-03-05 | scheduler_autotune, compiler_runtime | - add generic env var TILELANG_CLEANUP_TEMP_FILES to control compiler temp cleanup - default to 0 so temp files are kept for debugging/profiling workflows (e.g. ncu) - wire nvcc compilation tempdir cleanup behavior to this env flag by CodeRabbit Release Notes * **New Features** * Introduced a new en... | kernel: `tilelang/contrib/nvcc.py`, `tilelang/env.py` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#2131](https://github.com/tile-ai/tilelang/pull/2131) Change disable_out_of_bound_warning default to True | 2026-04-30 | scheduler_autotune, compiler_runtime | - `src/transform/legalize_safe_memory_access.cc`: .value_or(false) → .value_or(true) - `tilelang/transform/pass_config.py`: update docstring accordingly by CodeRabbit * **Bug Fixes** * Out-of-bounds warnings are now disabled by default. * Warnings continue to be emitted in cases that remain local/no... | kernel: `tilelang/transform/pass_config.py`<br>wrapper: `src/transform/legalize_safe_memory_access.cc` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#2114](https://github.com/tile-ai/tilelang/pull/2114) [Refactor][Build] Separate CMakeLists into different backends | 2026-04-28 | scheduler_autotune | by CodeRabbit * **Chores** * Reorganized build system to delegate backend selection and configuration to backend-specific CMake files (CUDA, ROCm, Metal) instead of centralized logic, improving modularity and maintainability. | other: `CMakeLists.txt`, `src/backend/cuda/CMakeLists.txt`, `src/backend/metal/CMakeLists.txt`, `src/backend/rocm/CMakeLists.txt` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#2183](https://github.com/tile-ai/tilelang/pull/2183) [Build] Fix cross platform CMake and add messages when enabling backends | 2026-05-11 | scheduler_autotune | Address some issues in thread: https://github.com/tile-ai/tilelang/pull/2114 by CodeRabbit Release Notes * **Chores** * Enhanced build-time diagnostics with status messages for active backend configurations (CUDA, Metal, ROCm) * Improved backend dependency linking to better preserve existing library... | other: `src/backend/cuda/CMakeLists.txt`, `src/backend/metal/CMakeLists.txt`, `src/backend/rocm/CMakeLists.txt` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#2109](https://github.com/tile-ai/tilelang/pull/2109) fix: add missing TvmLogDebugSettings::ParseSpec and VerboseEnabledImpl for TVM_LOG_CUSTOMIZE builds | 2026-04-28 | scheduler_autotune | - Fixes runtime symbol lookup error `undefined symbol: TvmLogDebugSettings::ParseSpec` when building with `TVM_LOG_CUSTOMIZE=1` - Adds the implementations of `TvmLogDebugSettings::ParseSpec` and `TvmLogDebugSettings::VerboseEnabledImpl` that are missing when TVM's logging customization is enabled Ro... | wrapper: `src/runtime/logging.cc` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#1738](https://github.com/tile-ai/tilelang/pull/1738) [Bugfix] Handle offset handling for subtype ptr | 2026-01-27 | scheduler_autotune | as title. by CodeRabbit Bug Fixes * Improved pointer arithmetic and index calculation for 4-bit floating-point data handling in buffer operations, ensuring more accurate data access patterns in specific format configurations. <sub>✏️ Tip: You can customize this high-level summary in your review sett... | wrapper: `src/target/codegen_cuda.cc` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |

### Architecture / Pipeline

Use this section for: Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages.
NCU first look: Tensor pipe %, memory pipe utilization, barrier stalls, wait groups, and occupancy.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#1909](https://github.com/tile-ai/tilelang/pull/1909) [Feature] Add Producer-Consumer Warp Specialization and T.tma_copy() API | 2026-03-18 | arch_pipeline, gemm_quant, attention_kv, benchmark_test, memory_primitives | - **New `ProducerConsumerWarpSpecialized` pass** for sm90+ TMA pipelines: splits pipelined loops into producer (128 threads, TMA loads) and consumer (compute) warp groups with back-pressure mbarrier synchronization. Supports `num_stages >= 1`. - **New `T.tma_copy()` API**: fire-and-forget TMA copy w... | kernel: `examples/attention_sink/example_gqa_sink_bwd_bhsd.py`, `examples/attention_sink/example_gqa_sink_fwd_bhsd_wgmma_pipelined.py`, `examples/attention_sink/example_mha_sink_fwd_bhsd_wgmma_pipelined.py`, `examples/attention_sink/regression_attention_sink.py`<br>benchmark: `examples/attention_sink/benchmark_gqa_sink_fwd.py`, `examples/attention_sink/benchmark_mha_sink_fwd.py`<br>test: `examples/attention_sink/test_example_attention_sink.py`, `examples/flash_attention/test_example_flash_attention.py`, `examples/gdn/test_example_gdn_compilation.py`<br>wrapper: `examples/deepseek_v32/sparse_mla_fwd_pipelined.py`, `examples/flash_decoding/example_gqa_decode_varlen_logits.py`, `examples/warp_specialize/example_warp_specialize_flashmla.py`, `src/op/copy.cc` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2033](https://github.com/tile-ai/tilelang/pull/2033) [Refactor] Remove GEMM v1 and promote gemm_py to be the canonical gemm op | 2026-04-13 | arch_pipeline, gemm_quant, compiler_runtime, benchmark_test | GEMM v2 has been the default path for a while and no tests or examples still depend on v1. This PR removes the legacy C++ `GemmNode` / `tl_gemm` builtin path entirely and promotes the former `GemmPyNode` (Python-delegated lowering) to be the one and only GEMM tile op, collapsing the two parallel imp... | kernel: `maint/gemm_v2/correctness_evaluation.py`, `maint/gemm_v2/correctness_evaluation_sm70.py`, `maint/gemm_v2/correctness_evaluation_tcgen05.py`, `maint/gemm_v2/correctness_evaluation_tcgen05_2cta.py`<br>test: `testing/python/language/test_tilelang_language_if_range.py`, `testing/python/language/test_tilelang_language_min_blocks_per_sm.py`, `testing/python/language/test_tilelang_language_transpose.py`, `testing/python/tilelibrary/test_tilelang_tilelibrary_gemm.py`<br>wrapper: `src/op/builtin.h`, `src/transform/inject_pipeline.cc`, `src/transform/lower_blackwell_2sm.cc`, `src/transform/lower_opaque_block.cc`<br>docs: `docs/deeplearning_operators/matmul_sparse.md`<br>other: `maint/scripts/run_local_ci_test.sh` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1944](https://github.com/tile-ai/tilelang/pull/1944) [Refactor] Replace create_list_of_mbarrier with buffer-based T.alloc_barrier | 2026-03-18 | arch_pipeline, compiler_runtime, memory_primitives, benchmark_test | - Unify two mbarrier representation paths (compiler-internal `create_list_of_mbarrier`/`get_mbarrier` intrinsics vs user-facing `T.alloc_barrier` buffer approach) into a single buffer-based approach using `shared.barrier` scope buffers with `barrier_init` block annotations - `LowerTileOp` now inject... | kernel: `src/target/codegen_cutedsl.cc`, `src/transform/lower_tile_op.cc`, `tilelang/engine/phase.py`, `tilelang/language/builtin.py`<br>test: `testing/python/transform/test_tilelang_transform_lower_hopper_intrin.py`, `testing/python/transform/test_tilelang_transform_producer_consumer_ws.py`, `testing/python/transform/test_tilelang_transform_warp_specialized.py`<br>wrapper: `src/op/builtin.cc`, `src/op/builtin.h`, `src/op/copy.cc`, `src/op/operator.h` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#887](https://github.com/tile-ai/tilelang/pull/887) [Bugfix][Enhancement] Fix a bug in previous commit and enhance cuda backend | 2025-09-28 | arch_pipeline, gemm_quant, memory_primitives, compiler_runtime | Thank you for this great project. Our team (from IC) has extended the SM90 implementation to include SM100-related features, such as support for tcgen05 related components and ldg256. And also fix a bug for previous commits. by CodeRabbit - New Features - Preview support for NVIDIA SM100, including... | kernel: `examples/gemm_sm100/gemm_mma.py`, `examples/gemm_sm100/gemm_tcgen5mma.py`, `src/layout/gemm_layouts.cc`, `src/op/gemm.cc`<br>wrapper: `src/layout/layout.h`, `src/layout/tcgen05_layout.cc`, `src/layout/tcgen05_layout.h`, `src/op/builtin.cc`<br>docs: `examples/gemm_sm100/README.md`<br>other: `.clang-tidy` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1874](https://github.com/tile-ai/tilelang/pull/1874) [Feature] Support cluster launch, query, synchronization and barrier operations | 2026-02-28 | arch_pipeline, compiler_runtime, scheduler_autotune | This PR aims to support cluster launch, query, synchronization and barrier operations for CUDA sm90+, which is a preliminary for DSMEM and 2-cta tcgen5mma on Blackwell. by CodeRabbit * **New Features** * NVIDIA SM90+ cluster launch support with configurable cluster_dims; cluster_dims propagate throu... | kernel: `examples/gemm_sm100/gemm_tcgen5mma_ws_persistent.py`, `src/target/codegen_cutedsl.cc`, `src/transform/lower_device_kernel_launch.cc`, `tilelang/engine/phase.py`<br>test: `testing/python/language/test_tilelang_language_cluster.py`<br>wrapper: `src/layout/swizzle.cc`, `src/layout/swizzle.h`, `src/op/builtin.cc`, `src/op/builtin.h`<br>other: `3rdparty/tvm` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2107](https://github.com/tile-ai/tilelang/pull/2107) [TMA] Support FP4 TensorMap TMA copies | 2026-04-28 | arch_pipeline, gemm_quant, benchmark_test, memory_primitives, compiler_runtime | This PR adds FP4 TensorMap/TMA support for `float4_e2m1fn` copies and covers it with an explicit `T.tma_copy` roundtrip test. What changed - Map `float4_e2m1fn` to CUDA TensorMap data type value `13`, corresponding to `CU_TENSOR_MAP_DATA_TYPE_16U4_ALIGN8B`. - Update TMA byte and bit calculations to... | test: `testing/python/language/test_tilelang_language_tma_copy.py`<br>wrapper: `src/op/copy.cc`, `src/op/utils.cc`, `src/runtime/runtime.cc` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1774](https://github.com/tile-ai/tilelang/pull/1774) [Example][BugFix] 1SM GEMM example on Blackwell and fix handling of `mbar` | 2026-02-12 | arch_pipeline, gemm_quant, scheduler_autotune, compiler_runtime | - 1sm gemm on B200 - non persistent: ~1450T - persistent: ~1550T - Handle `mbar` as `BufferLoad` to avoid missing index Thanks @Hamerlate for providing the dev machine. by CodeRabbit * **New Features** * Added two comprehensive GEMM kernel examples demonstrating TileLang JIT compilation with integra... | kernel: `examples/gemm_sm100/gemm_tcgen5mma_ws.py`, `examples/gemm_sm100/gemm_tcgen5mma_ws_persistent.py`, `src/op/gemm.cc`, `src/op/gemm.h`<br>wrapper: `src/op/utils.cc`, `src/op/utils.h` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2153](https://github.com/tile-ai/tilelang/pull/2153) [codex] Split GEMM implementations by backend | 2026-05-06 | arch_pipeline, gemm_quant, scheduler_autotune, compiler_runtime | Split GEMM backend-specific logic out of the shared tile op path. The common `src/op/gemm.cc` now resolves backend implementations through a registry, while CUDA, ROCm, and CPU selection/lowering details live under their own backend directories. Add a Python GEMM implementation registry under `tilel... | kernel: `src/backend/cpu/op/gemm.cc`, `src/backend/cuda/op/gemm.cc`, `src/backend/rocm/op/gemm.cc`, `src/op/gemm.cc`<br>wrapper: `src/transform/instruction_annotation.cc` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1707](https://github.com/tile-ai/tilelang/pull/1707) [Refactor] Introduce GemmInst enumeration and update warp partitioning logic | 2026-01-22 | arch_pipeline, compiler_runtime, gemm_quant | - Use `GemmInst` enumeration to represent different GEMM instruction types. - Refactored `computeWarpPartition` methods across various classes to accept `GemmInst` instead of boolean flags `is_wgmma`. - Updated the `Lower` methods in `GemmPyNode` and `GemmSPPyNode` to compute warp partitions on the... | kernel: `src/op/gemm.cc`, `src/op/gemm_py.cc`, `src/op/gemm_sp.cc`, `src/op/gemm_sp.h` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#868](https://github.com/tile-ai/tilelang/pull/868) [SM89] Lint fix | 2025-09-27 | arch_pipeline, gemm_quant, memory_primitives | [SM89] Lint fix | kernel: `examples/blackwell_sm100/gemm_mma.py`, `examples/blackwell_sm100/gemm_umma.py`, `src/layout/gemm_layouts.cc`, `src/op/gemm.cc`<br>wrapper: `src/layout/layout.h`, `src/layout/tcgen05_layout.cc`, `src/layout/tcgen05_layout.h`, `src/op/builtin.cc`<br>docs: `examples/blackwell_sm100/README.md`<br>other: `.clang-tidy` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1764](https://github.com/tile-ai/tilelang/pull/1764) [Feature] Support tcgen5mma lowering for `.kind::i8` | 2026-02-02 | arch_pipeline, gemm_quant, benchmark_test, compiler_runtime | As titled. Thanks @Hamerlate for providing the blackwell machine for testing. by CodeRabbit * **New Features** * Adds end-to-end support for int8 and uint8 tensor-core GEMM paths, including encoding for 8-bit integral types and int32 accumulators. * **Tests** * Adds an int8/uint8 -> int32 GEMM corre... | kernel: `maint/gemm_v2/correctness_evaluation_tcgen05.py`, `tilelang/intrinsics/mma_macro_generator.py`, `tilelang/tileop/gemm/gemm_tcgen05.py`<br>test: `testing/python/kernel/test_tilelang_kernel_int8_gemm_tcgen5.py`<br>wrapper: `src/op/tcgen5_meta.h`, `src/target/codegen_cuda.cc`, `src/tl_templates/cuda/instruction/tcgen05mma.h` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1949](https://github.com/tile-ai/tilelang/pull/1949) [Refactor] Separate gemm into explicit `wgmma_gemm` and `tcgen05_gemm` functions | 2026-03-20 | arch_pipeline, gemm_quant | - Replaced instances of T.gemm with T.wgmma_gemm and T.tcgen05_gemm in various example scripts to enhance performance and control over asynchronous execution. - Updated related documentation to reflect the changes in GEMM function usage, ensuring clarity on the new explicit interfaces. - Removed unn... | kernel: `examples/gemm_fp8/example_tilelang_gemm_fp8_sm100.py`, `examples/gemm_sm100/gemm_tcgen5mma.py`, `examples/gemm_sm100/gemm_tcgen5mma_ws.py`, `examples/gemm_sm100/gemm_tcgen5mma_ws_persistent.py`<br>test: `testing/python/kernel/test_tilelang_kernel_bf16_gemm_tcgen5_ts.py`, `testing/python/kernel/test_tilelang_kernel_int8_gemm_tcgen5.py`, `testing/python/language/test_tilelang_language_tcgen05_gemm.py`, `testing/python/language/test_tilelang_language_wgmma_gemm.py`<br>wrapper: `examples/deepseek_mhc/example_mhc_pre.py`, `examples/deepseek_mla/example_mla_decode_ws.py`, `examples/deepseek_v32/sparse_mla_fwd_pipelined.py`, `examples/deepseek_v32/sparse_mla_fwd_seesaw.py`<br>docs: `examples/gemm_sm100/README.md` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2029](https://github.com/tile-ai/tilelang/pull/2029) [Feature][Example] Introduce CLC tile schedule and add example for sm100 GEMM | 2026-04-17 | arch_pipeline, scheduler_autotune, gemm_quant | - Introduce clc tile schedule - Optimize sm100 gemm to 1700+ tflops for randn inputs and 1770+ tflops for random by CodeRabbit * **New Features** * Added cluster launch control intrinsics for coordination across multiple CTAs: cancellation operations and state query functions to check cancellation s... | kernel: `examples/gemm_sm100/gemm_tcgen5mma_ws_clc.py`, `tilelang/language/__init__.py`, `tilelang/language/builtin.py`, `tilelang/language/cluster.py`<br>test: `testing/python/language/test_tilelang_language_cluster.py`<br>wrapper: `src/op/builtin.cc`, `src/op/builtin.h`, `src/target/codegen_cuda.cc`, `src/tl_templates/cuda/cluster.h` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#933](https://github.com/tile-ai/tilelang/pull/933) [Bugfix] Fix tensor memory copy layout | 2025-10-02 | arch_pipeline, memory_primitives, gemm_quant | Bug fix for issue 931 Implements tcgen05.ld instruction support for copying from shared.tmem to local.fragment on SM100/Blackwell architecture. Adds layout inference and lowering logic for tensor memory operations with proper physical coordinate range analysis and warpgroup alignment checks. Changes... | kernel: `examples/gemm_sm100/gemm_tcgen5mma.py`, `src/op/gemm.cc`, `src/op/gemm.h`, `src/op/gemm_py.cc`<br>wrapper: `src/op/copy.cc`, `src/op/copy.h`, `src/op/operator.cc`, `src/op/operator.h` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1229](https://github.com/tile-ai/tilelang/pull/1229) [WIP] support more dtypes for tcgen05 | 2025-11-21 | arch_pipeline, gemm_quant, compiler_runtime | by CodeRabbit Release Notes * **New Features** * Added FP8 GEMM example with performance benchmarking and accuracy validation * Extended FP8 data type support with additional precision formats * Added float16 accumulator option for GEMM operations * **Performance** * Optimized GEMM kernels for impro... | kernel: `examples/gemm_fp8/example_tilelang_gemm_fp8_sm100.py`, `src/op/gemm_py.cc`, `src/tl_templates/cuda/gemm_sm100.h`, `tilelang/intrinsics/mma_macro_generator.py`<br>wrapper: `src/op/copy.cc`, `src/op/tcgen5_meta.h`, `src/tl_templates/cuda/copy_sm100.h`, `src/tl_templates/cuda/tcgen_05_ld.h` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1327](https://github.com/tile-ai/tilelang/pull/1327) [Enhancement] add more dtype and fix mma.ws for fp16 for tcgen05 | 2025-11-26 | arch_pipeline, gemm_quant, compiler_runtime | by CodeRabbit * **New Features** * Added an FP8 GEMM example with end-to-end compilation, execution and performance profiling on SM100. * Broadened FP8 datatype support and global-memory load/store for new FP8 variants. * Added pack-aware memory paths to improve FP8/16 data movement efficiency. * GE... | kernel: `examples/gemm_fp8/example_tilelang_gemm_fp8_sm100.py`, `src/op/gemm_py.cc`, `src/tl_templates/cuda/gemm_sm100.h`, `tilelang/intrinsics/mma_macro_generator.py`<br>wrapper: `src/op/copy.cc`, `src/op/tcgen5_meta.h`, `src/tl_templates/cuda/copy_sm100.h`, `src/tl_templates/cuda/tcgen_05_ld.h` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1762](https://github.com/tile-ai/tilelang/pull/1762) [Feature] Hierarchical reduction and warp reduction intrinsics support | 2026-02-13 | arch_pipeline, benchmark_test, memory_primitives, compiler_runtime | For https://github.com/tile-ai/tilelang/issues/1761 * Add hierarchical reduction from warp to block to reduce workspace size * Add `redux.sync` PTX templates to support faster reduction on (u)int32 with `__CUDA_ARCH__>=800` by CodeRabbit * **Performance** * Faster and more memory-efficient reduction... | test: `testing/python/language/test_tilelang_language_reduce.py`<br>wrapper: `src/op/finalize_reducer.cc`, `src/op/reduce.cc`, `src/tl_templates/cuda/reduce.h` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#986](https://github.com/tile-ai/tilelang/pull/986) [Language] Expose `T.warpgroup_fence_operand` for nvcc code motion | 2025-11-02 | arch_pipeline, compiler_runtime | This pull request introduces several improvements and new features to the CUDA code generation and tensor core support in the codebase. The most significant changes include the addition of support for the new `warpgroup_fence_operand` intrinsic, refactoring and modernization of MMA and WGMMA code ge... | kernel: `src/op/gemm.cc`, `src/op/gemm_py.cc`, `src/op/gemm_py.h`, `tilelang/intrinsics/mma_macro_generator.py`<br>test: `testing/python/language/test_tilelang_language_get_warp_info.py`, `testing/python/transform/test_tilelang_transform_inject_fence_proxy.py`<br>wrapper: `src/layout/layout.cc`, `src/op/builtin.cc`, `src/op/builtin.h`, `src/op/tcgen5_meta.h`<br>docs: `docs/compiler_internals/inject_fence_proxy.md`<br>other: `3rdparty/tvm` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1989](https://github.com/tile-ai/tilelang/pull/1989) Add regression test for 1D TMA load compilation and execution | 2026-04-15 | arch_pipeline, benchmark_test, memory_primitives, compiler_runtime | This issue is a compile-time mismatch; a focused regression test must validate both source emission and runtime correctness for the reported pattern. Without this, similar signature regressions can reappear silently. Files changed - `testing/python/tilelang/test_tma_load.py` (new) Testing - Not run... | test: `testing/python/tilelang/test_tma_load.py`<br>other: `3rdparty/tvm` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2137](https://github.com/tile-ai/tilelang/pull/2137) [codex] Fix 1D TMA store layout inference | 2026-05-03 | arch_pipeline, memory_primitives, compiler_runtime, benchmark_test | This fixes a layout-inference regression that caused plain shared -> global copies to fall back from the pointer-based 1D TMA store path to descriptor-based multidimensional TMA. Root cause: `CopyNode::InferLayout()` could still attach a swizzled shared-memory layout during free inference after the... | test: `testing/python/language/test_tilelang_language_tma_store.py`<br>wrapper: `src/op/copy.cc` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1975](https://github.com/tile-ai/tilelang/pull/1975) Fix wrapped pre-loop TMA prefixes in producer-consumer WS | 2026-03-26 | arch_pipeline, benchmark_test, compiler_runtime, scheduler_autotune | This PR fixes a producer-consumer warp-specialized corner case where a pipelined loop is wrapped by outer `if` guards and has a pre-loop TMA preload. The change makes the WS rewrite: - find and rebuild pipeline loops under `IfThenElse` wrappers - recognize pre-loop TMA producer/wait pairs that have... | test: `testing/python/transform/test_tilelang_transform_producer_consumer_ws.py`<br>wrapper: `src/transform/producer_consumer_ws.cc` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1980](https://github.com/tile-ai/tilelang/pull/1980) [BugFix] Add missing fences in GEMM SM100 examples and canonicalize the order of blockIdx | 2026-03-31 | arch_pipeline, compiler_runtime, gemm_quant | by CodeRabbit * **New Features** * Added TCGEN05 thread synchronization intrinsics (`tcgen05_before_thread_sync` and `tcgen05_after_thread_sync`) for improved kernel execution control across compute pipelines. * Updated example tensor operation kernels with enhanced synchronization and corrected ind... | kernel: `examples/gemm_sm100/gemm_tcgen5mma_ws.py`, `examples/gemm_sm100/gemm_tcgen5mma_ws_persistent.py`, `src/target/codegen_cutedsl.cc`, `tilelang/language/builtin.py`<br>wrapper: `src/op/builtin.cc`, `src/op/builtin.h`, `src/target/codegen_cuda.cc` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2003](https://github.com/tile-ai/tilelang/pull/2003) [Transform] Add InjectTcgen05Fence pass | 2026-04-16 | arch_pipeline, compiler_runtime | - Add `InjectTcgen05Fence`, a conservative TIR pass for Blackwell (`SM100+`) kernels that use TCGEN05/TMEM operations. - On Blackwell, regular thread synchronization primitives do **not** automatically synchronize TMEM state across threads. This pass inserts `tcgen05.fence::before_thread_sync` / `tc... | kernel: `examples/gemm_sm100/gemm_tcgen5mma_ws.py`, `examples/gemm_sm100/gemm_tcgen5mma_ws_persistent.py`, `src/target/codegen_cutedsl.cc`, `tilelang/engine/phase.py`<br>test: `testing/python/transform/test_tilelang_transform_inject_tcgen05_fence.py`<br>wrapper: `src/op/builtin.cc`, `src/op/builtin.h`, `src/op/copy.cc`, `src/target/codegen_cuda.cc`<br>docs: `examples/gemm_sm100/README.md` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1858](https://github.com/tile-ai/tilelang/pull/1858) [Feature] Add TIR builtins for warp-level vote and block-level predicate sync | 2026-04-13 | arch_pipeline, compiler_runtime, benchmark_test | This pull request adds builtin support for warp-level vote/ballot intrinsics and block-wide predicated synchronization operations. https://github.com/tile-ai/tilelang/issues/1861 These primitives are fundamental building blocks for high-performance GPU programming. They enable warp-wide early termin... | kernel: `tilelang/language/__init__.py`, `tilelang/language/builtin.py`<br>test: `testing/python/language/test_tilelang_language_warp_vote.py`<br>wrapper: `src/op/builtin.cc`, `src/op/builtin.h`, `src/target/codegen_cuda.cc`, `src/target/codegen_hip.cc`<br>docs: `docs/programming_guides/instructions.md` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1866](https://github.com/tile-ai/tilelang/pull/1866) [CUDA] Support tcgen5mma gemm ts | 2026-02-28 | arch_pipeline, gemm_quant | Fixes 1862 This PR implements two missing Blackwell (SM100) primitives in TileLang: 1. **`tcgen05.st` (Register → Tensor Memory Store)**: Enables writing data from registers back into Tensor Memory (TMEM), which is the reverse of the existing `tcgen05.ld`. This is essential for chained GEMM patterns... | kernel: `src/op/gemm_py.cc`, `tilelang/intrinsics/tcgen05_macro_generator.py`, `tilelang/tileop/gemm/gemm_base.py`, `tilelang/tileop/gemm/gemm_tcgen05.py`<br>test: `testing/python/kernel/test_tilelang_kernel_bf16_gemm_tcgen5_ts.py`<br>wrapper: `src/layout/tcgen05_layout.cc`, `src/layout/tcgen05_layout.h`, `src/op/copy.cc`, `src/tl_templates/cuda/copy_sm100.h` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1882](https://github.com/tile-ai/tilelang/pull/1882) [Feature] 2-SM support for TMA, TMEM and TCGEN5MMA on Blackwell | 2026-03-24 | arch_pipeline | This PR aims to support writing high-performance warp-specialized 2cta gemm on Blackwell, optimizing GEMM on (8192, 8192, 8192) to ~1670TFlops. Thanks @Hamerlate for providing the dev machine. by CodeRabbit * **New Features** * Added optional 2-CTA cluster mode for Blackwell GEMM kernels with runtim... | kernel: `examples/gemm_sm100/gemm_tcgen5mma_ws.py`, `examples/gemm_sm100/gemm_tcgen5mma_ws_persistent.py`, `maint/gemm_v2/correctness_evaluation_tcgen05.py`, `maint/gemm_v2/correctness_evaluation_tcgen05_2cta.py`<br>wrapper: `src/op/builtin.h`, `src/op/copy.cc`, `src/op/operator.h`, `src/op/tcgen5_meta.h` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2005](https://github.com/tile-ai/tilelang/pull/2005) [Test] Add 1D TMA regression test for issue #1842 | 2026-04-03 | arch_pipeline, benchmark_test, compiler_runtime | - Add regression test for issue 1842: 1D TMA load compilation failure when warp specialization is disabled - The bug was already fixed by 1840, but no test covered the 1D single-dimension tensor case - Test verifies: 1D bulk TMA load/store compiles, mbarrier is emitted, and copy is correct Context I... | test: `testing/python/issue/test_tilelang_issue_tma_no_ws.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2151](https://github.com/tile-ai/tilelang/pull/2151) [TMA] Fix TMA descriptor init placement | 2026-05-06 | arch_pipeline, compiler_runtime, benchmark_test | - Keep TMA descriptor initialization after the allocation that defines its base pointer when the descriptor targets an allocated workspace. - Preserve the existing function-prologue initialization path for descriptors whose base pointer is not introduced by a local Allocate. - Add a Hopper regressio... | test: `testing/python/transform/test_tilelang_transform_lower_hopper_intrin.py`<br>wrapper: `src/transform/lower_hopper_intrin.cc` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1931](https://github.com/tile-ai/tilelang/pull/1931) [Runtime] Improve TMA descriptor diagnostics | 2026-03-13 | arch_pipeline, compiler_runtime, benchmark_test | - validate tiled and im2col TMA descriptors before calling CUDA and report the violated constraints directly in the error message - print richer TMA debug context, including enum names, pointer alignment, raw strides, and the effective CUDA strides passed to `cuTensorMapEncodeTiled` / `cuTensorMapEn... | test: `testing/python/runtime/test_tilelang_runtime_tma_validation.py`<br>wrapper: `src/runtime/runtime.cc`, `src/target/codegen_c_host.cc`, `src/target/codegen_cpp.cc` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2039](https://github.com/tile-ai/tilelang/pull/2039) [API] Default warp-lane mask to 0xFFFFFFFF for warp-sync builtins | 2026-04-13 | arch_pipeline, compiler_runtime, benchmark_test | Follow-up to 1858. Reorder the `mask` parameter to the end (with a default of `0xFFFFFFFF` as a uint32 TIR const) for the warp-level sync builtins in `tilelang/language/builtin.py`: - shfl family: `shfl_xor`, `shfl_down`, `shfl_up`, `shfl_sync` — new shape `(value, delta/srcLane, width=32, mask=0xFF... | kernel: `tilelang/language/builtin.py`<br>test: `testing/python/language/test_tilelang_language_warp_sync.py`, `testing/python/language/test_tilelang_language_warp_vote.py`<br>docs: `docs/programming_guides/instructions.md` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2166](https://github.com/tile-ai/tilelang/pull/2166) [BugFix] Consider non-local store in external call and SIMT producer for warp specialize | 2026-05-12 | arch_pipeline, benchmark_test, compiler_runtime | Two bugs caused incorrect results when T.Parallel loops use T.call_extern with T.address_of(shared_buffer) inside pipelined, warp-specialized kernels: Bug 1 (lower_tile_op.cc): has_non_local_store did not recognize address_of(shared_buffer) inside call_extern as a non-local access, so parallel_loop... | kernel: `src/transform/lower_tile_op.cc`<br>test: `testing/python/transform/test_tilelang_transform_producer_consumer_ws.py`<br>wrapper: `src/transform/producer_consumer_ws.cc` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2126](https://github.com/tile-ai/tilelang/pull/2126) [Feature][Fix] Extend TCGEN5 F8F6F4 dtype plumbing | 2026-05-06 | arch_pipeline, compiler_runtime | Extend the TCGEN5 low-bit codegen path for FP6 and FP4 inputs. Feature - add descriptor encoding for FP6 E2M3/E3M2 and FP4 E2M1 - add FP6 PTX/CUDA dtype mappings - route FP6 TCGEN5 lowering through the existing `kind::f8f6f4` instruction path Bugfix - fix FP6 byte-width handling in TCGEN5 swizzle/of... | kernel: `tilelang/intrinsics/mma_macro_generator.py`, `tilelang/intrinsics/tcgen05_macro_generator.py`<br>wrapper: `src/op/tcgen5_meta.h`, `src/target/ptx.cc`, `src/target/ptx.h`, `src/tl_templates/cuda/common.h` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1920](https://github.com/tile-ai/tilelang/pull/1920) [Bugfix] Minor fix for warp specialized gemm swizzling | 2026-03-11 | arch_pipeline, gemm_quant | Thanks for @Hamerlate to provide the machine to reproduce and test, in this pr, we: - Updated make_tcgen05mma_swizzled_layout to include error handling for argument mismatches, ensuring compatibility with older FFI signatures. - Introduced infer_shared_layout method in GemmTCGEN5 to streamline layou... | kernel: `tilelang/layout/swizzle.py`, `tilelang/tileop/gemm/gemm_tcgen05.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1921](https://github.com/tile-ai/tilelang/pull/1921) [Refactor] Align infer_shared_layout method in GemmTCGEN5 with WGMMA  | 2026-03-11 | arch_pipeline, gemm_quant | as title. by CodeRabbit * **Refactor** * Internal improvements to layout selection logic for enhanced code maintainability and efficiency. | kernel: `tilelang/tileop/gemm/gemm_tcgen05.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2046](https://github.com/tile-ai/tilelang/pull/2046) [Refactor] Remove obsolete RewriteWgmmaSync pass | 2026-04-15 | arch_pipeline, compiler_runtime | - Remove the `RewriteWgmmaSync` transform pass, which is no longer needed. - Delete `src/transform/wgmma_sync_rewriter.cc`, its Python wrapper in `tilelang/transform/__init__.py`, and the call site in `tilelang/engine/phase.py` (along with the now-unused `is_hopper` import). Test plan - [ ] Build ti... | kernel: `tilelang/engine/phase.py`, `tilelang/transform/__init__.py`<br>wrapper: `src/transform/wgmma_sync_rewriter.cc` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2161](https://github.com/tile-ai/tilelang/pull/2161) [Refactor] Refactor multiple TensorCoreIntrinEmitter to provide atom-level mma control interface | 2026-05-09 | arch_pipeline | - [x] Enhance intrinsic modules by adding WGMMATensorCoreIntrinEmitter and TCGEN05TensorCoreIntrinEmitter. - [x] Introduce descriptor parameter classes for WGMMA and TCGEN05, improving the initialization and atom offset computation processes. - [x] Update the TensorCoreIntrinEmitter to support atom-... | kernel: `tilelang/cuda/intrinsics/macro/mma_macro_generator.py`, `tilelang/cuda/intrinsics/macro/tcgen05_macro_generator.py`, `tilelang/cuda/intrinsics/macro/wgmma_macro_generator.py`, `tilelang/intrinsics/__init__.py`<br>test: `testing/python/language/test_tilelang_language_atom_mma.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1840](https://github.com/tile-ai/tilelang/pull/1840) [BugFix] Fix Hopper TMA lowering without warp specialization | 2026-02-25 | arch_pipeline | When `tl.disable_warp_specialized=True` but `tl.disable_tma_lower=False`, the optimization pipeline skipped the `WarpSpecialized` + `InjectTmaBarrier` passes, leaving TMA load instructions referencing undeclared `mbarrier` symbols. This caused NVCC compilation failure: `identifier "mbarrier" is unde... | kernel: `tilelang/engine/phase.py`<br>test: `testing/python/issue/test_tilelang_issue_tma_no_ws.py`<br>wrapper: `src/transform/inject_tma_barrier.cc` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#801](https://github.com/tile-ai/tilelang/pull/801) [Refactor] Use new namespace and enhance dispatch macros for mma | 2025-09-11 | arch_pipeline, gemm_quant | - Moved GEMM-related dispatch instructions to the `cute::tl_mma` namespace for better organization. - Introduced `TL_DISPATCH_MMA` and `TL_DISPATCH_MMA_TEMPLATE` macros to streamline the definition of dispatch instructions for various data types and architectures. - Updated the handling of CUDA arch... | kernel: `src/tl_templates/cuda/gemm_mma.h`, `src/tl_templates/cuda/gemm_sm90.h` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2083](https://github.com/tile-ai/tilelang/pull/2083) [codex] Remove dead transform pass leftovers | 2026-04-22 | arch_pipeline, compiler_runtime | This PR removes stale transform-pass leftovers that were no longer used by the in-tree lowering pipeline. The cleanup includes: - removing `warp_specialized_rewriter.h`, whose detector logic no longer had any real call sites - removing `AlignDynamicSharedMemoryAllocations`, which was no longer part... | kernel: `tilelang/transform/__init__.py`<br>wrapper: `src/transform/align_dynamic_shared_memory_allocations.cc`, `src/transform/annotate_warp_group_reg_alloc.cc`, `src/transform/eliminate_storage_sync_for_mbarrier.cc`, `src/transform/producer_consumer_ws.cc` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2087](https://github.com/tile-ai/tilelang/pull/2087) [Bugfix] Enable `.shared::cta` in TMA copy paths only on CUDA 12.8+ | 2026-04-22 | arch_pipeline, memory_primitives | This PR updates TileLang SM90 TMA copy paths to use `.shared::cta` only when compiling with CUDA 12.8 or newer. For older CUDA toolchains, these paths continue to use `shared::cluster`. Changes - Update regular SM90 TMA load paths in `src/tl_templates/cuda/copy_sm90.h` - Update `im2col` TMA load pat... | wrapper: `src/target/ptx.cc`, `src/tl_templates/cuda/copy_sm90.h` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2134](https://github.com/tile-ai/tilelang/pull/2134) fix: TMA alignment to 1024 bytes on Blackwell | 2026-05-03 | arch_pipeline | Previously 1024 byte alignemnt was guarded only by `IsHopper`. This commit changes it to be guarded by `TargetHasBulkCopy` with the same threshold as Hopper is using. The kernel added in the test was failing on my RTX5090 GPU. by CodeRabbit * **Bug Fixes** * Improved shared-memory alignment for GPU... | test: `testing/python/issue/test_tilelang_issue_sm120_tma_smem_alignment.py`<br>wrapper: `src/transform/merge_shared_memory_allocations.cc` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2052](https://github.com/tile-ai/tilelang/pull/2052) [Bugfix] Use shared::cta instead of shared::cluster for non-cluster T… | 2026-04-18 | arch_pipeline, memory_primitives | On SM120 (RTX 5090), using `shared::cluster` in `cp.async.bulk` PTX instructions without an actual cluster launch causes the NVIDIA driver to allocate ~3.5 GB of extra device memory that persists for the CUDA context lifetime. **Changes:** - `copy_sm90.h`: replace `shared::cluster` with `shared::cta... | wrapper: `src/target/ptx.cc`, `src/tl_templates/cuda/copy_sm90.h` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2068](https://github.com/tile-ai/tilelang/pull/2068) Add frontmatter for the build skill | 2026-04-21 | arch_pipeline, compiler_runtime | The Agent Skills spec requires SKILL.md to contain YAML frontmatter followed by Markdown content, with name and description as required fields. Reference: https://agentskills.io/specificationskill-md-format by CodeRabbit * **Documentation** * Added metadata to skill documentation to better organize... | docs: `.agents/skills/tilelang-build/SKILL.md` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#768](https://github.com/tile-ai/tilelang/pull/768) [CUTLASS] hot fix blackwell | 2025-08-29 | arch_pipeline, benchmark_test | by CodeRabbit - Chores - Updated third-party cutlass submodule to a newer upstream commit. - Metadata-only change: no impact on features, performance, or build behavior. - No user-facing changes; app behavior and interfaces remain unchanged. - Aligns repository with latest upstream reference without... | other: `3rdparty/cutlass` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1946](https://github.com/tile-ai/tilelang/pull/1946) [BugFix] Update usage of tma load in SM100 manual warp-specialized examples | 2026-03-18 | arch_pipeline | by CodeRabbit Release Notes * **Refactor** * Updated data transfer mechanisms in GPU kernel examples to use optimized copy operations with improved synchronization handling. * Added alignment validation for tile configuration parameters to ensure proper kernel execution. | kernel: `examples/gemm_sm100/gemm_tcgen5mma_ws.py`, `examples/gemm_sm100/gemm_tcgen5mma_ws_persistent.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1953](https://github.com/tile-ai/tilelang/pull/1953) [PIpeline] Enable software pipelining when warp specialization is unavailable | 2026-03-22 | arch_pipeline, compiler_runtime | as title. by CodeRabbit * **Refactor** * Reorganized compiler optimization pipeline for warp-specialized code to unconditionally execute shared barrier lowering, followed by pipeline planning and software pipelining passes in a streamlined sequence. | kernel: `tilelang/engine/phase.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1973](https://github.com/tile-ai/tilelang/pull/1973) Refactor producer-consumer WS access tracking for WGMMA-local state | 2026-03-25 | arch_pipeline | - split producer-consumer WS access summaries into all-buffer accesses vs branch-private accesses - teach the access collector to recognize raw `buffer.data` operands used by `warpgroup_fence_operand`, `ptx_wgmma_ss`, and `ptx_wgmma_rs` - make loop-prefix hoisting use branch-private liveness so cons... | wrapper: `src/transform/producer_consumer_ws.cc` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#1828](https://github.com/tile-ai/tilelang/pull/1828) [BugFix] Reset cur_expect_idx_ correctly for multi-kernel TMA barrier injection | 2026-02-11 | arch_pipeline | This pr fixes an error mentioned in issue 1393. Problem When a T.prim_func contains multiple kernels with TMA enabled, the second kernel uses stale indices from the first kernel, leading to incorrect mbarrier intrinsic generation. Reproduction code block Before this pr, the generated kernel source c... | wrapper: `src/transform/inject_tma_barrier.cc` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2102](https://github.com/tile-ai/tilelang/pull/2102) [CUDA][SM100] Include cuda_fp6.h when emitting FP6 types | 2026-04-26 | arch_pipeline | Add `include <cuda_fp6.h>` when CUDA codegen emits FP6 types. Why On Blackwell, FP6 can be used as a storage dtype in kernel parameters, for example in copy, staging, or layout-transform kernels. Since codegen already emits native CUDA FP6 types like `__nv_fp6_e2m3` / `__nv_fp6_e3m2`, the generated... | wrapper: `src/target/codegen_cuda.cc` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2117](https://github.com/tile-ai/tilelang/pull/2117) [Enhancement][CUDA][SM100] Report unsupported FP6 vector types earlier | 2026-04-28 | arch_pipeline | Improve CUDA codegen diagnostics for unsupported FP6 vector types (`lanes > 4`). Previously this path could return without emitting a type, producing malformed CUDA and confusing downstream NVCC errors. This change lets it reach TileLang’s existing unsupported-type error path instead. Why FP6 tensor... | wrapper: `src/target/codegen_cuda.cc` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |

### Compiler / Runtime

Use this section for: Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests.
NCU first look: Generated kernel shape, launch overhead, occupancy, register pressure, and compile-time-selected schedule.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#2063](https://github.com/tile-ai/tilelang/pull/2063) [CUDA] Support int4 `T.gemm` | 2026-04-20 | compiler_runtime, gemm_quant, attention_kv, memory_primitives, benchmark_test | - add end-to-end `T.int4` support for the `T.gemm` frontend path and switch the int4 GEMM tests to the generic tensor core emitters - lower eligible global-to-shared async copies directly to their final packed `cp.async` width during async copy injection instead of relying on a later vectorization p... | kernel: `examples/gemm_int4/example_tilelang_gemm_int4.py`, `src/target/codegen_cutedsl.cc`, `tilelang/engine/phase.py`, `tilelang/intrinsics/mma_macro_generator.py`<br>test: `testing/python/kernel/test_tilelang_kernel_int4_gemm.py`, `testing/python/kernel/test_tilelang_kernel_int4_gemm_mma.py`, `testing/python/language/test_tilelang_language_access_ptr_codegen.py`, `testing/python/transform/test_tilelang_transform_lower_ptx_async_copy.py`<br>wrapper: `examples/deepseek_mla/example_mla_decode_ws.py`, `examples/deepseek_v32/sparse_mla_fwd_pipelined.py`, `examples/deepseek_v32/sparse_mla_fwd_seesaw.py`, `src/op/builtin.h` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1717](https://github.com/tile-ai/tilelang/pull/1717) [Enhancement] Add explicit global memory load/store intrinsics (ldg/stg 32/64/128) | 2026-01-26 | compiler_runtime, memory_primitives, scheduler_autotune, arch_pipeline, benchmark_test | This PR introduces explicit global memory load/store intrinsics with various vector widths for fine-grained control over memory operations: - **Load intrinsics**: `ldg32`, `ldg64`, `ldg128`, `ldg256` - Load 32/64/128/256 bits from global memory - **Store intrinsics**: `stg32`, `stg64`, `stg128`, `st... | kernel: `tilelang/engine/phase.py`, `tilelang/jit/__init__.py`, `tilelang/language/__init__.py`, `tilelang/language/builtin.py`<br>test: `testing/python/language/test_tilelang_language_ldg.py`, `testing/python/language/test_tilelang_language_stg.py`, `testing/python/transform/test_tilelang_transform_lower_ldgstg.py`<br>wrapper: `src/op/builtin.cc`, `src/op/builtin.h`, `src/target/codegen_cuda.cc`, `src/tl_templates/cuda/copy.h`<br>other: `3rdparty/tvm` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1855](https://github.com/tile-ai/tilelang/pull/1855) [Enhancement] GEMM V2 on SM90/SM100 CuTeDSL backend | 2026-02-22 | compiler_runtime, arch_pipeline, gemm_quant, memory_primitives | Comprehensive improvements to the CuTeDSL backend, covering codegen correctness, new op support, WGMMA V2 descriptor-based GEMM, tcgen05 (SM100/Blackwell) MMA support, and sparse MMA (ptx_mma_sp). This PR brings the CuTeDSL backend from partial coverage to near-full parity with the CUDA backend acro... | kernel: `examples/attention_sink/example_mha_sink_bwd_bhsd.py`, `src/target/codegen_cutedsl.cc`, `src/target/codegen_cutedsl.h`, `tilelang/contrib/cutedsl/__init__.py`<br>test: `testing/python/jit/test_tilelang_jit_cutedsl.py`<br>wrapper: `examples/conftest.py`, `src/op/copy.cc`, `src/target/utils.cc`, `src/target/utils.h`<br>docs: `docs/spelling_wordlist.txt`<br>other: `.github/workflows/ci.yml`, `maint/scripts/run_local_ci_test.sh` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1632](https://github.com/tile-ai/tilelang/pull/1632) [Refactor] Unify @jit and @lazy_jit into a single @jit decorator | 2026-01-09 | compiler_runtime, gemm_quant, attention_kv | This PR unifies `@tilelang.jit` and `@tilelang.lazy_jit` into a single `@tilelang.jit` decorator that automatically infers the execution mode based on function behavior. Two JIT Writing Styles TileLang now supports two kernel writing styles with a unified decorator: 1. Lazy Style (explicit PrimFunc... | kernel: `examples/blocksparse_attention/example_triton_sparse_gqa_decode_varlen_indice.py`, `examples/gemm/example_gemm_intrinsics.py`, `examples/gemm_fp8/example_tilelang_gemm_fp8_intrinsic.py`, `src/transform/lower_tile_op.cc`<br>test: `testing/python/arith/test_arith_hard.py`, `testing/python/issue/test_tilelang_issue_1549.py`, `testing/python/issue/test_tilelang_issue_1601.py`, `testing/python/kernel/test_tilelang_kernel_int4_gemm_mma.py`<br>wrapper: `src/transform/layout_inference.cc`<br>other: `examples/lazy_jit/lazyjit.en.ipynb`, `examples/lazy_jit/lazyjit.zh.ipynb` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1733](https://github.com/tile-ai/tilelang/pull/1733) [Refactor] Enhance `T.alloc_barrier` with new features and deprecate legacy mbarrier related intrinsics | 2026-01-28 | compiler_runtime, arch_pipeline, benchmark_test | Features - `T.alloc_barrier` creates and returns one or more mbarriers allocated in one smem buffer - Plan to deprecate `T.create_list_of_barrier` and `T.get_mbarrier` - Other related operators (e.g. `T.mbarrier_wait_parity`) only accept explicit mbarriers (e.g. `mbars[k]`) as inputs by CodeRabbit *... | kernel: `examples/warp_specialize/example_warp_specialize_gemm_barrierpipe_stage2.py`, `src/target/codegen_cutedsl.cc`, `tilelang/engine/phase.py`, `tilelang/language/allocate.py`<br>test: `testing/python/jit/test_tilelang_jit_cutedsl.py`, `testing/python/transform/test_tilelang_transform_inject_set_max_nreg.py`, `testing/python/transform/test_tilelang_transform_lower_hopper_intrin.py`, `testing/python/transform/test_tilelang_transform_lower_shared_barrier.py`<br>wrapper: `examples/minference/example_vertical_slash_sparse_attn.py`, `src/op/builtin.h`, `src/target/codegen_cuda.cc`, `src/target/codegen_cuda.h`<br>docs: `docs/programming_guides/instructions.md` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1981](https://github.com/tile-ai/tilelang/pull/1981) [Feature] Support TMA store in T.tma_copy() | 2026-03-27 | compiler_runtime, memory_primitives, arch_pipeline, benchmark_test | - Extend `T.tma_copy()` to support TMA stores (shared -> global) with user-managed synchronization - Make the `barrier` parameter optional — it is required for TMA loads but not needed for stores - For stores, emit only `tma_store + tma_store_arrive` (no `tma_store_wait`), so users can batch multipl... | kernel: `src/target/codegen_cutedsl.cc`, `tilelang/language/builtin.py`, `tilelang/language/copy_op.py`<br>test: `testing/python/language/test_tilelang_language_tma_copy.py`, `testing/python/language/test_tilelang_language_tma_store.py`<br>wrapper: `src/op/atomic_add.cc`, `src/op/builtin.cc`, `src/op/copy.cc`, `src/target/codegen_cuda.cc` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1982](https://github.com/tile-ai/tilelang/pull/1982) [Enhancement] Use atomic directory rename for cache writes | 2026-04-16 | compiler_runtime, memory_primitives, benchmark_test, scheduler_autotune, arch_pipeline | - Replace per-file `os.replace` with write-to-staging + `os.rename` for both `KernelCache` and `AutotuneResult`, ensuring other processes never observe an incomplete cache directory. - Add `_cleanup_stale_staging_dirs()` to remove orphaned staging directories (>1h old) left by crashed processes, cal... | kernel: `tilelang/autotuner/param.py`, `tilelang/cache/kernel_cache.py`, `tilelang/jit/adapter/nvrtc/kernel_cache.py`<br>test: `testing/python/autotune/test_tilelang_autotune_atomic_save.py`, `testing/python/cache/test_tilelang_kernel_cache_atomic_save.py`, `testing/python/tilelang/test_tma_load.py`<br>other: `3rdparty/tvm` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1932](https://github.com/tile-ai/tilelang/pull/1932) test: reduce CI runtime for slow Python suites | 2026-03-18 | compiler_runtime, benchmark_test, memory_primitives | - skip benchmark-oriented test cases by default behind a new `perf` marker and `--run-perf` opt-in - trim slow Python test matrices and tensor sizes in the biggest CI hotspots to reduce repeated compile and execution cost - remove duplicated slow coverage and fix standalone collection for the kernel... | benchmark: `testing/python/profiler/test_tilelang_profiler.py`, `testing/python/profiler/test_tilelang_profiler_dynamic_symbolic.py`<br>test: `testing/conftest.py`, `testing/python/cache/test_tilelang_kernel_cache.py`, `testing/python/debug/test_device_assert.py`, `testing/python/jit/test_tilelang_jit_cutedsl.py`<br>other: `3rdparty/tvm`, `pyproject.toml` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1976](https://github.com/tile-ai/tilelang/pull/1976) [Feature] Batched AllReduce for better T.reduce performance | 2026-04-28 | compiler_runtime, memory_primitives, benchmark_test, moe_routing | Resolves 1928. When each thread holds multiple values to reduce across warps, the current `AllReduce` serializes butterfly reductions with one call per element, causing excessive thread synchronizations (e.g., 48 syncs instead of 6 for 8 values × 256 threads). This PR extends the `tl::AllReduce` C++... | kernel: `tilelang/contrib/cutedsl/reduce.py`, `tilelang/language/reduce_op.py`<br>test: `testing/python/language/test_tilelang_language_reduce.py`<br>wrapper: `src/op/finalize_reducer.cc`, `src/op/finalize_reducer.h`, `src/op/reduce.cc`, `src/op/reduce.h` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2138](https://github.com/tile-ai/tilelang/pull/2138) [Refactor][Backend] Split tl.copy lowering by backend | 2026-05-06 | compiler_runtime, memory_primitives, scheduler_autotune | - Introduce a lightweight `tl.copy` implementation registry in the existing copy op files. - Move CUDA-specific copy lowering into `src/backend/cuda/op/copy.cc`. - Add CPU and ROCm copy implementations: CPU supports normal copy lowering, while ROCm supports normal and async copy lowering. - Add back... | kernel: `tilelang/contrib/nvcc.py`, `tilelang/jit/adapter/cutedsl/libgen.py`, `tilelang/jit/adapter/libgen.py`, `tilelang/utils/sparse.py`<br>wrapper: `src/backend/cpu/op/copy.cc`, `src/backend/cuda/op/copy.cc`, `src/backend/cuda/op/copy.h`, `src/backend/cuda/op/copy_analysis.cc`<br>other: `CMakeLists.txt`, `src/backend/cuda/CMakeLists.txt`, `src/backend/rocm/CMakeLists.txt` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1367](https://github.com/tile-ai/tilelang/pull/1367) [Feat] Integrate Z3 in TVM Arith Analyzer | 2025-12-17 | compiler_runtime, gemm_quant, memory_primitives, benchmark_test | This pr integrates Z3 into the TVM arith analyzer. - Only support integer expressions. TVM lanes, vscales are not supported. - It uses the Z3 integer theorem to prove expressions in TVM TODO - [x] Fix the vectorization bug in `AtomicAdd` op (1366) (I don't know which pr fixed it, anyway, it is fixed... | kernel: `tilelang/language/reduce.py`, `tilelang/utils/sparse.py`<br>test: `testing/python/arith/test_arith_hard.py`, `testing/python/arith/test_arith_intset.py`, `testing/python/arith/test_arith_iter_affine_map.py`, `testing/python/arith/test_arith_simplify.py`<br>other: `3rdparty/tvm`, `CMakeLists.txt`, `cmake/pypi-z3/FindZ3.cmake`, `pyproject.toml` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2075](https://github.com/tile-ai/tilelang/pull/2075) [Refactor] Phaseout legacy util `map_torch_type` with `T.dtype.as_torch` | 2026-04-21 | compiler_runtime, gemm_quant | `map_torch_type` was a standalone function that converted dtype strings or TVM `DataType` objects to `torch.dtype`. This was redundant since `T.dtype.as_torch()` (and `T.dtype(x).as_torch()`) already provides the same functionality with better handling (HIP/CUDA awareness, proper dtype lookup tables... | kernel: `examples/deepseek_deepgemm/example_deepgemm_fp8_2xAcc.py`, `examples/gemm_fp8/example_tilelang_gemm_fp8_intrinsic.py`, `examples/gemm_fp8/example_tilelang_gemm_fp8_sm100.py`, `tilelang/jit/adapter/cython/adapter.py`<br>test: `testing/python/jit/test_tilelang_jit_cutedsl.py`, `testing/python/jit/test_tilelang_jit_gemm_cython.py`, `testing/python/jit/test_tilelang_jit_nullptr.py`, `testing/python/jit/test_tilelang_jit_nvrtc.py`<br>other: `tilelang/jit/adapter/cython/cython_wrapper.pyx` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1962](https://github.com/tile-ai/tilelang/pull/1962) [Bugfix] Fix double buffer versioning when TMA is used without warp specialization | 2026-03-24 | compiler_runtime, arch_pipeline, benchmark_test | - When TMA is enabled but warp specialization is disabled, `MultiVersionBuffer` and `InjectSoftwarePipeline` both independently added version dimensions to shared data buffers, resulting in **4x** buffer allocation instead of the intended **2x**. - Add a `barrier_only` mode to `MultiVersionBuffer`.... | kernel: `src/transform/lower_tile_op.cc`, `tilelang/engine/phase.py`, `tilelang/transform/__init__.py`<br>test: `testing/python/jit/test_tilelang_jit_cutedsl.py`, `testing/python/transform/test_tilelang_transform_lower_shared_barrier.py`<br>wrapper: `src/op/builtin.h`, `src/transform/multi_version_buffer_rewriter.cc` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1972](https://github.com/tile-ai/tilelang/pull/1972) [Bugfix] Fix CuTeDSL autotune cache invalid ELF header (#1967) | 2026-03-31 | compiler_runtime, memory_primitives, scheduler_autotune | - Fix autotune cache for CuTeDSL backend: save kernel as `kernel.py` instead of `kernel_lib.so` to prevent `importlib` from treating Python source as a native extension module - Add `cutedsl` branches in both `_save_kernel_to_disk` and `_load_kernel_from_disk` for correct file naming - Also save lau... | kernel: `tilelang/autotuner/param.py`, `tilelang/jit/kernel.py`, `tilelang/language/dtypes.py`<br>test: `testing/python/autotune/test_tilelang_autotune_cutedsl_cache.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2088](https://github.com/tile-ai/tilelang/pull/2088) [Refactor] Refactor register annotation lowering | 2026-05-06 | compiler_runtime, arch_pipeline, benchmark_test | * Refactor SetMaxNRegCollector to return a structured result, including a flag for preserving explicit register settings. * Update SetMaxNRegInjector to utilize the new result structure, ensuring proper handling of register hints. * Modify logic to conditionally inject register allocation based on t... | kernel: `src/target/codegen_cutedsl.cc`, `tilelang/language/builtin.py`<br>test: `testing/python/transform/test_tilelang_transform_inject_set_max_nreg.py`<br>wrapper: `src/op/builtin.cc`, `src/op/builtin.h`, `src/target/codegen_cuda.cc`, `src/target/codegen_hip.cc` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1970](https://github.com/tile-ai/tilelang/pull/1970) [Feature] Introduce T.CUDASourceCodeKernel | 2026-04-17 | compiler_runtime, scheduler_autotune, benchmark_test | This PR introduces `T.CUDASourceCodeKernel`, which is another approach in TileLang to launch a GPU kernel. Instead of expressing the device-side body through TileLang DSL, users can provide CUDA source directly, either as inline code or via a source file path, and launch it from a TileLang PrimFunc.... | kernel: `src/transform/lower_device_kernel_launch.cc`, `tilelang/engine/lower.py`, `tilelang/language/__init__.py`, `tilelang/language/kernel.py`<br>test: `testing/python/language/test_tilelang_language_source_kernel.py`<br>wrapper: `src/target/codegen_c_host.cc`, `src/target/codegen_cuda.cc`, `src/target/rt_mod_cuda.cc`, `src/transform/common/attr.h` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2164](https://github.com/tile-ai/tilelang/pull/2164) [Refactor] Move backend stubs out of codegen | 2026-05-07 | compiler_runtime, benchmark_test | - Move CUDA and ROCm stub libraries out of backend codegen directories into backend-level stubs directories. - Keep stub sources separate from code generation sources while preserving build and link behavior. Changes - Relocated CUDA stubs to `src/backend/cuda/stubs` and ROCm stubs to `src/backend/r... | test: `testing/python/amd/test_tilelang_hip_codegen.py`<br>wrapper: `src/backend/cuda/stubs/cuda.cc`, `src/backend/cuda/stubs/cuda.h`, `src/backend/cuda/stubs/cudart.cc`, `src/backend/cuda/stubs/dynlib.h`<br>docs: `docs/runtime_internals/stubs.md`<br>other: `src/backend/cuda/CMakeLists.txt`, `src/backend/rocm/CMakeLists.txt` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2037](https://github.com/tile-ai/tilelang/pull/2037) [Bugfix][Subtype] Fix scalar fp4 store/load codegen for non-packed buffers | 2026-04-14 | compiler_runtime, gemm_quant, benchmark_test | Problem Two issues with `float4_e2m1fn` (`fp4`) when `T.StridedTensor` uses **dynamic** strides (`T.dynamic()`): 1. Frontend stride capture is wrong for dynamic strides fp4 packs 2 elements per byte, so the logical element stride is **2x the byte stride** reported by PyTorch's DLTensor. When strides... | kernel: `tilelang/jit/adapter/cython/adapter.py`, `tilelang/jit/adapter/tvm_ffi.py`<br>test: `testing/python/language/test_tilelang_language_subtype.py`<br>wrapper: `src/target/codegen_cuda.cc`, `src/transform/inject_assumes.cc`<br>other: `tilelang/jit/adapter/cython/cython_wrapper.pyx` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1899](https://github.com/tile-ai/tilelang/pull/1899) [BugFix] add target context and avoid redundant re-lowering in TLCPUSourceWrapper | 2026-03-05 | compiler_runtime, gemm_quant, benchmark_test | Problem When compiling CPU kernels that utilize `T.copy` via `tilelang.compile(target="c", execution_backend="cpython")`, the compiler crashes with a `tvm.error.InternalError: Target context required`. Minimal Reproducer: code block Root Cause Analysis The issue stems from `TLCPUSourceWrapper.parse_... | kernel: `tilelang/jit/adapter/wrapper.py`<br>test: `testing/python/cpu/test_tilelang_cpu_gemm.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1978](https://github.com/tile-ai/tilelang/pull/1978) Unified packed x2 intrinsics with multi-dtype support and bug fixes | 2026-03-26 | compiler_runtime, benchmark_test | What changed **Renamed APIs**: `T.fadd2`/`T.fmul2` replaced with `T.add2`/`T.mul2`. Old backward-compat aliases (`fadd2`, `fmul2`, `fadd_f32x2`, `fmul_f32x2`, `fma_f32x2`) removed. **Expanded op surface**: Added 4 new packed x2 intrinsics (`T.sub2`, `T.max2`, `T.min2`, `T.abs2`) alongside the existi... | kernel: `tilelang/language/dtypes.py`, `tilelang/language/math_intrinsics.py`, `tilelang/utils/language.py`<br>test: `testing/python/cuda/test_cuda_f32x2_intrinsics.py`<br>wrapper: `src/op/builtin.cc`, `src/op/builtin.h`, `src/target/codegen_cuda.cc`, `src/target/codegen_cuda.h`<br>other: `3rdparty/tvm` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1818](https://github.com/tile-ai/tilelang/pull/1818) [BugFix] Fix fp16 annotate_l2_hit_ratio host stub compilation (issue #1810) | 2026-02-09 | compiler_runtime, arch_pipeline, benchmark_test | What Using `annotate_l2_hit_ratio` on a `float16` (fp16) buffer could fail during `export_library()` because the generated C host stub referenced an undefined `half` type. This shows up as a host-side compilation error (e.g. `half was not declared in this scope`) when TileLang tries to save the comp... | test: `testing/python/issue/test_tilelang_issue_1810.py`<br>wrapper: `src/transform/lower_hopper_intrin.cc` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1940](https://github.com/tile-ai/tilelang/pull/1940) [Feature] Support alloc global workspace | 2026-03-23 | compiler_runtime, benchmark_test | This PR supports `T.alloc_global`, which can be used either in Legacy JIT or EagerJIT to allocate a global memory as a workspace for TileLang kernel. The API directly calls TVM's alloc buffer and will be lowered into TVMBackendAllocWorkspace in host code. To achieve this, some passes need to be modi... | kernel: `tilelang/engine/phase.py`, `tilelang/language/__init__.py`, `tilelang/language/allocate.py`, `tilelang/transform/__init__.py`<br>test: `testing/python/language/test_tilelang_language_alloc.py`<br>wrapper: `src/ir.cc`, `src/transform/common/attr.h`, `src/transform/hoist_global_buffer_allocations.cc`, `src/transform/inject_assumes.cc` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2121](https://github.com/tile-ai/tilelang/pull/2121) [Refactor][CodeGen] Refactor CodeGen part for multi-backend decoupling | 2026-05-06 | compiler_runtime | As titled. Currently we put the `c` and `c_host` under the original folder. Note that metal backend uses `CodeGenC` to build. So the wrapper under `backend/metal/codegen` is just a proxy. by CodeRabbit * **New Features** * Added Metal backend codegen/compile entry to build TileLang for Metal. * Adde... | kernel: `src/backend/cuda/codegen/codegen_cutedsl.cc`, `src/backend/cuda/codegen/codegen_cutedsl.h`, `src/backend/cuda/codegen/rt_mod_cutedsl.cc`<br>wrapper: `src/backend/cuda/codegen/codegen_cuda.cc`, `src/backend/cuda/codegen/codegen_cuda.h`, `src/backend/cuda/codegen/codegen_py.cc`, `src/backend/cuda/codegen/codegen_py.h`<br>other: `CMakeLists.txt`, `src/backend/cuda/CMakeLists.txt`, `src/backend/metal/CMakeLists.txt`, `src/backend/rocm/CMakeLists.txt` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1776](https://github.com/tile-ai/tilelang/pull/1776) [Bugfix] Copy pass_configs dict to prevent mutation across multiple JIT compilations | 2026-02-03 | compiler_runtime, memory_primitives, scheduler_autotune | - Fix a bug where `pass_configs` dictionary was shared and mutated across multiple JIT kernel compilations - When calling a JIT-decorated function multiple times, the `compile_flags` would accumulate (e.g., `['-O3']` → `['-O3', '-O3']`) - Solution: Create a copy of `pass_configs` before modifying it... | kernel: `tilelang/jit/kernel.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1779](https://github.com/tile-ai/tilelang/pull/1779) [Refactor] Reorganize ParallelOp code structure and move ProveFragmentContains to layout utils | 2026-02-06 | compiler_runtime, benchmark_test | - Move `ProveFragmentContains` function from `parallel.cc` to `layout/utils.cc` for better code organization - Move `IfBufferRemapLoopGenerator` helper class into anonymous namespace - Extract `buffer_is_completed_replicated` lambda to private method `IsBufferCompletelyReplicated` - Move buffer acce... | test: `testing/python/layout/test_tilelang_annotate_loop_layout.py`<br>wrapper: `src/layout/layout.cc`, `src/layout/utils.cc`, `src/layout/utils.h`, `src/op/parallel.cc` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2071](https://github.com/tile-ai/tilelang/pull/2071) [FFI] Remove upper version bound on apache-tvm-ffi | 2026-04-21 | compiler_runtime, benchmark_test | - Remove the `<0.1.10` version cap from `apache-tvm-ffi` in `pyproject.toml`, `requirements.txt`, and `requirements-dev.txt` - The pin was originally added to work around a `derived_object` regression, which has since been resolved in newer releases Test plan - [ ] Verify CI passes with the latest `... | kernel: `tilelang/engine/lower.py`, `tilelang/jit/adapter/cutedsl/wrapper.py`, `tilelang/jit/adapter/libgen.py`, `tilelang/jit/adapter/utils.py`<br>test: `testing/python/language/test_tilelang_language_let.py`, `testing/python/transform/test_tilelang_transform_thread_sync.py`<br>other: `3rdparty/tvm`, `pyproject.toml`, `requirements-dev.txt`, `requirements.txt` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1903](https://github.com/tile-ai/tilelang/pull/1903) [Feature] Add DumpIR PassConfig in TileLang side | 2026-03-05 | compiler_runtime, scheduler_autotune | Feat: https://github.com/tile-ai/tilelang/issues/1896 Previously TVM has a pass instrument to dump all IRs between passes, but it's not exposed in TileLang side and it's hard for new users to find this option. This PR explictly adds this feature as a pass config in TileLang. by CodeRabbit * **New Fe... | kernel: `tilelang/jit/kernel.py`, `tilelang/transform/pass_config.py`<br>wrapper: `src/op/builtin.cc`, `src/op/builtin.h` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1114](https://github.com/tile-ai/tilelang/pull/1114) [Feature] Support None type as input for `T.ptr` and `T.Tensor` | 2025-10-23 | compiler_runtime, benchmark_test | For issue 828 by CodeRabbit * **New Features** * Support for optional or missing buffers in kernel calls, allowing kernels to accept null inputs where appropriate. * **Tests** * New test suite validating JIT GEMM-like kernels with tiled/pipelined execution and optional bias, verifying results agains... | kernel: `tilelang/language/allocate.py`<br>test: `testing/python/jit/test_tilelang_jit_nullptr.py`<br>other: `tilelang/jit/adapter/cython/cython_wrapper.pyx` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1947](https://github.com/tile-ai/tilelang/pull/1947) Support packed subtype views during layout reshape | 2026-03-19 | compiler_runtime, benchmark_test | This fixes dtype-changing alias views on packed sub-byte dtypes such as `float4_e2m1fn -> uint8`. The main issue was that layout inference still reasoned in bytes, which breaks as soon as two logical elements share the same byte. On top of that, reshape would flatten packed sub-elements and lose the... | test: `testing/python/language/test_tilelang_language_view.py`<br>wrapper: `src/layout/layout.cc`, `src/layout/layout.h`, `src/transform/layout_inference.cc` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1937](https://github.com/tile-ai/tilelang/pull/1937) Fix predicated cp.async pipeline scheduling | 2026-03-17 | compiler_runtime, benchmark_test | - treat predicated `cp.async` conservatively during pipeline planning instead of assuming it fully overwrites the destination shared buffer - skip producer-side first-stage statements when binding `cp.async` consumers and wait stages so fill/copy/commit are not misclassified as downstream consumers... | test: `testing/python/transform/test_tilelang_transform_pipeline_planning.py`<br>wrapper: `src/transform/pipeline_planning.cc` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1974](https://github.com/tile-ai/tilelang/pull/1974) [Bugfix] Fix alloc_var re-bind warning when assigned with comparison ops | 2026-03-27 | compiler_runtime, benchmark_test | - `EqualOp`/`NotEqualOp` are not `PrimExpr` subclasses, causing `bind()` to miss the `alloc_var` store path and trigger a spurious "Immutable value is re-bound" warning - Convert them to `PrimExpr` via `.asobject()` before the Ref/alloc_var check by CodeRabbit * **Tests** * Added CUDA-only tests tha... | kernel: `tilelang/language/eager/builder.py`<br>test: `testing/python/language/test_tilelang_language_alloc_var_cmp.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2031](https://github.com/tile-ai/tilelang/pull/2031) [Bugfix] Fix stage-expanded annotated-layout aliases in LayoutInference | 2026-04-17 | compiler_runtime, benchmark_test | Fix `LayoutInference` annotated layout propagation for sibling aliases that only add leading stage dimensions and have matching storage bits. For `[64, 64] -> [2, 64, 64]`, the layout should be expanded over the leading stage dimension instead of going through the generic `Reshape(...)` fallback. De... | test: `testing/python/language/test_tilelang_language_view.py`<br>wrapper: `src/transform/layout_inference.cc` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2174](https://github.com/tile-ai/tilelang/pull/2174) Fix float4 storage dtype torch mapping | 2026-05-10 | compiler_runtime, benchmark_test | Fix `T.float4_e2m1fnx2.as_torch()` to check the correct PyTorch dtype name, `torch.float4_e2m1fn_x2` Torch does not have `float4_e2m1fnx2` the correct attribute is `float4_e2m1fn_x2`. by CodeRabbit * **Bug Fixes** * Fixed float4_e2m1fnx2 dtype conversion to use the correct PyTorch dtype name. * **Te... | kernel: `tilelang/language/dtypes.py`<br>test: `testing/python/language/test_tilelang_language_dtype_as_torch.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1024](https://github.com/tile-ai/tilelang/pull/1024) [Enhancement] Add support for symbolic dimensions in Cython kernel adapter and improve static shape validation in wrapper | 2025-10-17 | compiler_runtime, moe_routing | Fixed 1011. This pull request introduces improvements to buffer shape handling and validation in the Cython adapter layer, as well as updates to CUDA type-specific functions for numerical types. The main changes enhance the robustness of shape checks for tensors and ensure correct usage of absolute... | kernel: `examples/fusedmoe/example_fusedmoe_tilelang.py`, `tilelang/jit/adapter/cython/adapter.py`<br>other: `tilelang/jit/adapter/cython/cython_wrapper.pyx` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2047](https://github.com/tile-ai/tilelang/pull/2047) [Refactor] Move target gating into InjectFenceProxy pass entry | 2026-04-15 | compiler_runtime, arch_pipeline | - `InjectFenceProxy` now reads the `PrimFunc`'s target at the pass entry and becomes a no-op when the hardware lacks the TMA / async-proxy programming model (non-CUDA or pre-sm_90). The hardware precondition is an intrinsic property of the pass, so it belongs with the pass rather than being duplicat... | kernel: `tilelang/engine/phase.py`<br>wrapper: `src/transform/inject_fence_proxy.cc` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2187](https://github.com/tile-ai/tilelang/pull/2187) [WIP] Handle CuTeDSL FP4 torch dtype | 2026-05-12 | compiler_runtime | Fix CuTeDSL handling for PyTorch FP4 storage dtype. PyTorch exposes packed FP4 as `torch.float4_e2m1fn_x2` which was introduced by pr2174, while CuTeDSL expects the logical scalar type `cutlass.Float4E2M1FN`. This PR adds the missing dtype mapping so CuTeDSL can recognize FP4 tensors instead of fail... | kernel: `tilelang/jit/adapter/cutedsl/wrapper.py`<br>wrapper: `examples/conftest.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1961](https://github.com/tile-ai/tilelang/pull/1961) [Bugfix] Fix thread race in getPlaceholder during par_compile | 2026-03-23 | compiler_runtime, benchmark_test | - Fix data race in `getPlaceholder()` (`src/layout/layout.cc`) that caused random segfaults during `par_compile` - Pre-allocate all placeholder variables (`_rep`, `_i` through `_x`) at static init time so the map is immutable after initialization - C++11 guarantees thread-safe initialization of func... | wrapper: `src/layout/layout.cc` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1994](https://github.com/tile-ai/tilelang/pull/1994) [Bugfix] Raise error on zero grid dimension instead of silent clamp | 2026-03-30 | compiler_runtime, benchmark_test | Fixes 1993. TVM's `ThreadWorkLoad::Extract()` pre-fills all grid/block dims with 1, and only overwrites them when the actual value is > 0. So if a grid dimension is 0 — either from a dynamic shape like `num_tokens` being 0 at runtime, or from a literal `T.Kernel(0)` — the kernel still launches with... | test: `testing/python/issue/test_tilelang_issue_1993.py`<br>other: `3rdparty/tvm` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2128](https://github.com/tile-ai/tilelang/pull/2128) [FIX] Fix kernel file suffix for cutedsl when only target is set | 2026-04-30 | compiler_runtime | by CodeRabbit * **Bug Fixes** * Corrected debug kernel source file naming logic when using the cutedsl execution backend or target. | kernel: `tilelang/jit/__init__.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#702](https://github.com/tile-ai/tilelang/pull/702) Trivial update to calculate target arch | 2025-08-08 | compiler_runtime | Remove hard-coded `compute_version == "90"` and prepare for blackwell. | kernel: `tilelang/contrib/nvcc.py`, `tilelang/contrib/nvrtc.py`, `tilelang/engine/lower.py`, `tilelang/env.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1557](https://github.com/tile-ai/tilelang/pull/1557) [Refactor] Use cuda capability from torch to be more generic | 2025-12-29 | compiler_runtime | This shall reduce confusion caused by the default `sm_50` from tvm when failed to detect arch. by CodeRabbit * **Refactor** * Tightened parsing of NVIDIA compute-version strings for more consistent architecture detection, including handling of trailing modifiers and simplifying 3-character cases. *... | kernel: `tilelang/contrib/nvcc.py`, `tilelang/utils/target.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1996](https://github.com/tile-ai/tilelang/pull/1996) Add `annotations` parameter to `alloc_buffer` in `tilelang/language/ast/ir.py` | 2026-03-30 | compiler_runtime | `alloc_buffer` in `tilelang/language/ast/ir.py` lacks the `annotations` parameter present in Apache TVM, preventing attachment of backend-specific metadata (e.g., rocwmma hints for `codegen_hip`) to buffer allocations. Changes - **`tilelang/language/ast/ir.py`**: Added `annotations: Optional[Dict[st... | kernel: `tilelang/language/ast/ir.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2064](https://github.com/tile-ai/tilelang/pull/2064) fix: improve warning output in eager frontend | 2026-04-20 | compiler_runtime | Root Cause Multiple `logger.warning` calls in `tilelang/language/eager/builder.py` pass `stack_info=True`, causing every warning to append a full Python call stack. The stack frames are all internal tilelang builder/AST paths and provide no useful information to users. At the same time, the tilelang... | kernel: `tilelang/__init__.py`, `tilelang/language/eager/builder.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1786](https://github.com/tile-ai/tilelang/pull/1786) fix(intrinsics): add missing _legalize_to_buffer_region in SM70 emitter | 2026-02-04 | compiler_runtime | 1782 device1 simple test: <img width="1510" height="907" alt="屏幕截图 2026-02-04 132749" src="https://github.com/user-attachments/assets/62f3fe57-7df6-4206-9f00-fbd835028aba" /> device2 simple test : <img width="886" height="946" alt="屏幕截图 2026-02-04 124835" src="https://github.com/user-attachments/ass... | kernel: `tilelang/intrinsics/mma_sm70_macro_generator.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1884](https://github.com/tile-ai/tilelang/pull/1884) [Analysis] Refactor FragmentLoopChecker visiting style | 2026-03-10 | compiler_runtime | Check the cases: https://github.com/tile-ai/tilelang/issues/1868 by CodeRabbit * **Refactor** * Improved nested-loop validation for fragment buffer accesses using a scoped loop stack, ensuring checks consider innermost loop context and symbolic-range loops. * Error messages and rules updated to refe... | kernel: `tilelang/analysis/fragment_loop_checker.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2045](https://github.com/tile-ai/tilelang/pull/2045) [Feature] autodd: add __freeze__ annotation to protect code regions from reduction | 2026-04-15 | compiler_runtime | Background autodd reduces every AST node it can. There is no way to tell it to leave known-critical code alone. Solution Export a `__freeze__` sentinel from `tilelang.autodd` (a runtime no-op) that marks regions exempt from reduction: code block Comment annotations are also supported and converted t... | kernel: `tilelang/autodd.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2191](https://github.com/tile-ai/tilelang/pull/2191) Fix SM70 buffer region indexing | 2026-05-12 | compiler_runtime | - preserve leading buffer-region dimensions in the SM70 `ldmatrix` path - fix lowering for replicated shared-memory regions such as 3D `A_shared` / `B_shared` - resolve the Volta quickstart regression reported in 2190 Test plan - [x] run `examples/quickstart.py` on an SM70/Volta GPU - [x] verify low... | kernel: `tilelang/cuda/intrinsics/macro/mma_sm70_macro_generator.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1357](https://github.com/tile-ai/tilelang/pull/1357) [Bugfix] Fix the jit_kernel issue | 2025-11-30 | compiler_runtime | by CodeRabbit * **Refactor** * Internal change to where a computation kernel source is obtained; no changes to behavior, outputs, public interfaces, or performance. <sub>✏️ Tip: You can customize this high-level summary in your review settings.</sub> | docs: `README.md` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1963](https://github.com/tile-ai/tilelang/pull/1963) [Enhancement] Enhance compatibility for older torch versions and dynamic linking of cudart | 2026-03-23 | compiler_runtime | by CodeRabbit Release Notes * **Improvements** * Enhanced CUDA runtime library initialization with fallback mechanisms to support additional library packaging variants. * Improved data type compatibility layer to better support older PyTorch versions by conditionally detecting available data types.... | kernel: `tilelang/language/dtypes.py`, `tilelang/language/tir/op.py`, `tilelang/utils/tensor.py`<br>wrapper: `src/target/stubs/cudart.cc` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#1950](https://github.com/tile-ai/tilelang/pull/1950) [Enhancement] Use stronger prover in `ProveFragmentContains` to avoid false layout conflicts | 2026-03-20 | compiler_runtime | …nflicts by CodeRabbit * **Bug Fixes** * Stronger layout equality checks now use symbolic provability to reduce incorrect mismatches. * **New Features** * Visualization output now includes replicate-size information. * Fragment visualization prints and attempts plots for every fragment instance (ded... | kernel: `tilelang/analysis/layout_visual.py`<br>wrapper: `src/layout/utils.cc` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2100](https://github.com/tile-ai/tilelang/pull/2100) [Language] Small cleanup and notes for alloc global | 2026-04-25 | compiler_runtime | by CodeRabbit * **Refactor** * Reorganized the internal module structure for the allocation utilities. * Repositioned `alloc_global` function within the module while maintaining existing functionality and API compatibility. | kernel: `tilelang/language/__init__.py`, `tilelang/language/allocate.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2104](https://github.com/tile-ai/tilelang/pull/2104) [Language][UX] User-friendly error report when incorrectly indexing buffer | 2026-04-27 | compiler_runtime | Address https://github.com/tile-ai/tilelang/issues/1832 by CodeRabbit * **New Features** * Added validation for buffer indexing operations to verify the correct number of indices are provided, with enhanced error messages including buffer dimensionality and shape details. | kernel: `tilelang/language/overrides/__init__.py`, `tilelang/language/overrides/buffer.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2078](https://github.com/tile-ai/tilelang/pull/2078) [Enhancement] Enhance alloc_var function to handle _ptr_sentinel dtype | 2026-04-22 | compiler_runtime | Updated the alloc_var function to check for the _ptr_sentinel dtype and default to int64 if encountered. This change improves type handling and ensures compatibility with new tensor allocation scenarios. by CodeRabbit * **Bug Fixes** * Fixed buffer allocation handling for pointer-typed variables. Po... | kernel: `tilelang/language/allocate.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |

### Benchmark / Test Evidence

Use this section for: Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code.
NCU first look: Use the PR's benchmark/profile command as the first replay target.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#1660](https://github.com/tile-ai/tilelang/pull/1660) [Example] Add KDA algorithm implementation in tilelang | 2026-01-26 | benchmark_test, compiler_runtime, scheduler_autotune, attention_kv | The KDA algorithm, originally implemented with Triton in the flash-linear-attention repo, is fully reimplemented based on TileLang (0.1.6.post2+cuda.git729e66ca). Precision alignment and performance testing have been finished for every individual operator. by CodeRabbit * **New Features** * Added co... | test: `examples/kda/test_utils_kda.py`<br>wrapper: `examples/kda/FLA_KDA/cumsum.py`, `examples/kda/FLA_KDA/fla_chunk_delta.py`, `examples/kda/FLA_KDA/fla_chunk_inter.py`, `examples/kda/FLA_KDA/fla_chunk_intra.py`<br>docs: `examples/kda/README.md`<br>other: `.pre-commit-config.yaml` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2159](https://github.com/tile-ai/tilelang/pull/2159) [Autotune] Add pipeline, grouped compilation, and multi-GPU benchmark support | 2026-05-11 | benchmark_test, scheduler_autotune, moe_routing, compiler_runtime, gemm_quant | This PR adds pipeline mode, grouped compilation, and multi-GPU benchmark support to autotuning. Key Updates 1) Grouped compilation (tvm-ffi only) - Added grouped compilation support for autotune execution. - Current backend scope is **tvm-ffi only** (CUDA + tvm-ffi path). - Grouped compile helper an... | kernel: `examples/gemm/example_gemm_advanced_autotune.py`, `tilelang/autotuner/grouped_compile.py`, `tilelang/autotuner/param.py`, `tilelang/autotuner/tuner.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2023](https://github.com/tile-ai/tilelang/pull/2023) [Codegen] Add lexical_alloc_scope for scoped local variable lifetime | 2026-04-11 | benchmark_test, gemm_quant, memory_primitives, compiler_runtime | - Introduce a `lexical_alloc_scope` AttrStmt marker that generates `{ ... }` in C/CUDA codegen, providing the underlying compiler with accurate variable lifetime information for better register allocation. - `LowerOpaqueBlock` wraps block-local allocations in the new AttrStmt when `alloc_buffers` is... | kernel: `src/op/gemm_py.cc`, `src/op/gemm_sp_py.cc`, `tilelang/engine/phase.py`, `tilelang/transform/__init__.py`<br>test: `testing/python/transform/test_tilelang_transform_lexical_alloc_scope.py`, `testing/python/transform/test_tilelang_transform_plan_update_buffer_allocation_location.py`, `testing/python/transform/test_tilelang_transform_reuse_local_descriptor_allocations.py`<br>wrapper: `src/op/builtin.h`, `src/op/copy.cc`, `src/target/codegen_cuda.cc`, `src/target/codegen_hip.cc`<br>other: `.clang-tidy`, `.github/workflows/ci.yml`, `.github/workflows/pr-regression-test-bot.yml`, `.gitignore` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#1519](https://github.com/tile-ai/tilelang/pull/1519) [Enhancement] Improve GitHub Actions permissions check and refine performance regression testing | 2025-12-24 | benchmark_test, scheduler_autotune, gemm_quant, attention_kv | by CodeRabbit Release Notes * **New Features** * Enhanced PR regression testing workflow with improved multi-runner support and dynamic caching configuration. * Improved performance regression analysis visualization with detailed results reporting. * **Bug Fixes** * Enhanced error handling for perfo... | kernel: `examples/flash_attention/example_gqa_bwd_wgmma_pipelined.py`, `examples/flash_attention/example_gqa_fwd_bshd.py`, `examples/flash_attention/example_gqa_fwd_bshd_wgmma_pipelined.py`, `examples/flash_attention/example_mha_fwd_varlen.py`<br>test: `maint/scripts/test_perf_regression.py`, `tilelang/testing/perf_regression.py`<br>wrapper: `examples/dynamic_shape/example_dynamic.py`<br>other: `.github/workflows/pr-regression-test-bot.yml` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#1447](https://github.com/tile-ai/tilelang/pull/1447) [Refactor] Use `pytest.mark.parameterize` to speedup parallel testing | 2025-12-16 | benchmark_test, gemm_quant, arch_pipeline, compiler_runtime | This pr refactors some slow sequential multiple tests by `pytest.mark.parameterize` to parallel them. In 64 threads, this can reduce the total test time to about only **3 minutes** by CodeRabbit * **Tests** * Converted many unit tests to pytest parameterized tests, consolidating repetitive cases int... | test: `testing/python/amd/test_tilelang_gemm_mfma_intrinsic.py`, `testing/python/amd/test_tilelang_gemm_mfma_preshuffle.py`, `testing/python/amd/test_tilelang_test_amd.py`, `testing/python/fastmath/test_mathops_fastmath.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#775](https://github.com/tile-ai/tilelang/pull/775) [Example]Adds example for top-k operation | 2025-09-02 | benchmark_test, compiler_runtime, moe_routing, memory_primitives, scheduler_autotune | Adds an example demonstrating the top-k operation using tilelang. by CodeRabbit * **New Features** * Added a runnable TileLang top‑k example with autotuning, JIT integration, correctness check, and latency reporting on CUDA. * Introduced CUDA-accelerated modules for top‑k reduction, softmax/top‑k ga... | test: `examples/topk/test_topk_tilelang.py`<br>wrapper: `examples/topk/example_topk.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#1658](https://github.com/tile-ai/tilelang/pull/1658) [Feat] profiler support cudagraph backend | 2026-02-08 | benchmark_test, compiler_runtime, scheduler_autotune | this pr is related to https://github.com/tile-ai/tilelang/issues/1633, add cudagraph backend to profiler and allows AutoTuner to specify the profiling backend. Changes 1. Profiler CUDA Graph Backend - **`tilelang/profiler/bench.py`**: - Added `_bench_with_cudagraph()` function implementing CUDA Grap... | kernel: `examples/gemm/example_gemm_autotune.py`, `tilelang/autotuner/param.py`, `tilelang/autotuner/tuner.py`<br>benchmark: `tilelang/profiler/__init__.py`, `tilelang/profiler/bench.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#1670](https://github.com/tile-ai/tilelang/pull/1670) Refactor: Use centralized do_bench from tilelang.profiler | 2026-01-17 | benchmark_test, attention_kv, compiler_runtime | - Replace local `do_bench` function definitions with centralized `from tilelang.profiler import do_bench` import - Replace `from triton.testing import do_bench` with `from tilelang.profiler import do_bench` in examples Changed Files Import replacement (triton → tilelang.profiler) - `examples/gemm_sp... | kernel: `examples/gemm_sp/example_custom_compress.py`, `examples/gemm_sp/example_gemm_sp.py`<br>benchmark: `tilelang/profiler/__init__.py`<br>wrapper: `examples/flash_decoding/example_gqa_decode_varlen_logits.py`, `examples/gdn/example_chunk_delta_bwd.py`, `examples/gdn/example_chunk_delta_h.py`, `examples/gdn/example_chunk_o_bwd.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#1010](https://github.com/tile-ai/tilelang/pull/1010) [Refactor][Example] Update linear attention examples and add tests | 2025-10-19 | benchmark_test, attention_kv, compiler_runtime | Fix 975. This pull request refactors and improves the example implementations of fused chunked linear attention in both the forward (`example_linear_attn_fwd.py`) and backward (`example_linear_attn_bwd.py`) passes. The changes focus on making the TileLang kernels more idiomatic, improving correctnes... | kernel: `examples/linear_attention/example_linear_attn_bwd.py`, `examples/linear_attention/example_linear_attn_fwd.py`, `tilelang/language/atomic.py`<br>test: `examples/linear_attention/test_linear_attn.py`<br>other: `requirements.txt` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#1739](https://github.com/tile-ai/tilelang/pull/1739) [Refactor] Disable Predicated LDG PTX Lowering by default | 2026-01-27 | benchmark_test, scheduler_autotune, compiler_runtime | As our benchmark shows that the performance is not always better than the default nvcc optimiztaions. - Changed the configuration option from `kDisableLowerLDGSTGPredicated` to `kEnableLowerLDGSTGPredicated` to enable predicated lowering of global load/store operations. - Updated relevant comments a... | kernel: `tilelang/transform/pass_config.py`<br>test: `testing/python/transform/test_tilelang_transform_lower_ldgstg.py`<br>wrapper: `src/op/builtin.cc`, `src/op/builtin.h`, `src/transform/lower_ldg_stg.cc` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#1883](https://github.com/tile-ai/tilelang/pull/1883) [Enhancement] Clarify the semantic rule of copy operator and add shape mismatched tests | 2026-02-27 | benchmark_test, memory_primitives, compiler_runtime | We don't support broadcast for tile ops like T.copy. Current documents are misdirecting. by CodeRabbit * **New Features** * Copy operations now include safer extent resolution when source and destination shapes differ, with clearer handling of whole-buffer cases. * Direct-path optimization for scala... | kernel: `tilelang/language/copy_op.py`<br>test: `testing/python/language/test_tilelang_language_copy.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#1067](https://github.com/tile-ai/tilelang/pull/1067) [Benchmark] Add matmul FP16 benchmark results | 2025-10-19 | benchmark_test, gemm_quant | This pull request updates the FP16 and FP8 matrix multiplication benchmarks and their implementations to improve performance and reproducibility. The main changes include enhancements to the swizzling optimization in the benchmarking code, improved documentation for FP16 benchmarks, and updated FP8... | benchmark: `benchmark/matmul/README.md`, `benchmark/matmul/benchmark_matmul.py`, `benchmark/matmul_fp8/README.md`, `benchmark/matmul_fp8/benchmark_matmul.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#1109](https://github.com/tile-ai/tilelang/pull/1109) [Benchmark] Add Mamba2_chunk_scan benchmark | 2025-10-22 | benchmark_test, memory_primitives | This pull request introduces a new benchmark for the Mamba2 chunk scan kernel, including both documentation and a Python benchmarking script. The main focus is to provide reproducible performance measurements and a reference implementation for the chunk scan operation, as well as a configurable auto... | benchmark: `benchmark/mamba2/README.md`, `benchmark/mamba2/benchmark_mamba_chunk_scan.py`, `benchmark/mamba2/mamba_benchmark_result.png` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#1585](https://github.com/tile-ai/tilelang/pull/1585) [Enhancement] Improve performance regression output with timing and streaming | 2025-12-31 | benchmark_test, gemm_quant | by CodeRabbit * **Refactor** * Simplified example scripts by removing redundant parameter handling and validation blocks. * Restructured performance regression test framework with improved progress tracking and per-function execution-time reporting. * **New Features** * Added verbose parameter to re... | kernel: `examples/blocksparse_gemm/example_blocksparse_gemm.py`, `examples/seer_attention/block_sparse_attn_tilelang.py`<br>test: `maint/scripts/test_perf_regression.py`, `tilelang/testing/perf_regression.py`<br>wrapper: `examples/deepseek_v32/fp8_lighting_indexer.py`, `examples/deepseek_v32/topk_selector.py`, `maint/scripts/regression_all.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#1839](https://github.com/tile-ai/tilelang/pull/1839) [CUDA][Feature] Add packed FP32x2 math intrinsics and auto vectorized support | 2026-02-12 | benchmark_test, compiler_runtime | This commit introduces new packed FP32x2 math operations: fadd2, fmul2, and fma2, which leverage PTX instructions on supported architectures. by CodeRabbit * **New Features** * Added packed FP32x2 vector operations — fadd2 (add), fmul2 (mul), fma2 (fused mul-add) — exposed in the public API with opt... | kernel: `tilelang/language/math_intrinsics.py`<br>test: `testing/conftest.py`, `testing/python/cuda/test_cuda_f32x2_intrinsics.py`<br>wrapper: `src/op/builtin.cc`, `src/op/builtin.h`, `src/target/codegen_cuda.cc`, `src/target/codegen_hip.cc` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#1677](https://github.com/tile-ai/tilelang/pull/1677) [Refactor] Move AtomicAdd Vectorization to VectorizeLoop Pass | 2026-01-16 | benchmark_test, compiler_runtime | - Move atomic add vectorization logic from `atomicadd_vectorize.cc` to the unified `vectorize_loop.cc` pass - This consolidates vectorization transformations into a single pass for better maintainability - The `atomicadd_vectorize.cc` now focuses only on atomic-specific rewrites while general vector... | test: `testing/python/language/test_tilelang_language_atomic.py`<br>wrapper: `src/op/atomic_add.cc`, `src/tl_templates/cuda/atomic.h`, `src/transform/atomicadd_vectorize.cc`, `src/transform/atomicadd_vectorize.h` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#1820](https://github.com/tile-ai/tilelang/pull/1820) [Bugfix] Remove mistaken coalesced_width parameter in regression test of fusedmoe kernel | 2026-02-09 | benchmark_test, moe_routing, compiler_runtime | For issue 1802 by CodeRabbit * **Tests** * Updated performance benchmarking methodology to report mean latency across multiple kernel execution paths instead of a single path. | kernel: `examples/fusedmoe/example_fusedmoe_tilelang.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#1414](https://github.com/tile-ai/tilelang/pull/1414) [Enhancement] Introduce `T.__ldg` | 2025-12-12 | benchmark_test, compiler_runtime | for issue 841 This pull request introduces a new CUDA read-only cache load intrinsic (`__ldg`) to the TileLang language, allowing users to explicitly request cached loads for performance-sensitive memory accesses. The implementation covers the TileLang Python API, CUDA and HIP code generation, docum... | kernel: `tilelang/language/__init__.py`, `tilelang/language/builtin.py`<br>test: `testing/python/language/test_tilelang_language_intrinsics_codegen.py`<br>wrapper: `src/op/builtin.cc`, `src/op/builtin.h`, `src/target/codegen_cuda.cc`, `src/target/codegen_hip.cc` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#1926](https://github.com/tile-ai/tilelang/pull/1926) [Bugfix] Fix concurrent TempDirectory creation during CUDA compilation | 2026-03-13 | benchmark_test, compiler_runtime | - update the TVM submodule to include the TempDirectory race-condition fix for debug temp dir creation - avoid secondary cleanup failures when TempDirectory initialization aborts partway through - make NVCC artifact paths unique when `cuda.kernels_output_dir` is shared across concurrent compilations... | kernel: `tilelang/contrib/nvcc.py`<br>test: `testing/python/language/test_tilelang_language_ptr.py`<br>other: `3rdparty/tvm` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2026](https://github.com/tile-ai/tilelang/pull/2026) [Refactor] Refactor `DecoupleTypeCast` Pass | 2026-04-14 | benchmark_test, compiler_runtime | by CodeRabbit * **Bug Fixes** * More reliable mixed-precision handling by detecting explicit casts and ensuring correct insertion/reuse of temporary cast buffers; now accounts for local bindings so casts hidden behind locals are handled. * **Refactor** * Improved vectorization decisions so accesses... | kernel: `tilelang/transform/decouple_type_cast.py`<br>test: `testing/python/issue/test_tilelang_issue_1106.py`, `testing/python/transform/test_tilelang_transform_decouple_type_cast.py`<br>wrapper: `src/transform/loop_vectorize.cc` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#1835](https://github.com/tile-ai/tilelang/pull/1835) [Refactor] Treat `local.var` as `local` buffers when deciding vectorization for stable actions | 2026-02-11 | benchmark_test, compiler_runtime | by CodeRabbit * **Refactor** * Improved buffer detection logic to better handle local memory optimization. * **Tests** * Added test coverage for vectorization behavior with local variable handling. | kernel: `src/transform/lower_tile_op.cc`<br>test: `testing/python/language/test_tilelang_language_parallel.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#1898](https://github.com/tile-ai/tilelang/pull/1898) [BugFix] Fix type mismatch when lowering to AtomicAddx2 template | 2026-03-04 | benchmark_test, compiler_runtime | by CodeRabbit * **New Features** * Added conversion utilities for flexible half2/bfloat16 vector handling. * Atomic add operations now accept both pointer and direct value inputs and support vectorized half2/bfloat16 adds. * **Tests** * Added unit tests exercising auto-vectorized atomic add variants... | test: `testing/python/language/test_tilelang_language_atomic.py`<br>wrapper: `src/tl_templates/cuda/atomic.h` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#1925](https://github.com/tile-ai/tilelang/pull/1925) [Feature] Add CUDA intrinsic for isfinite operation | 2026-03-12 | benchmark_test, compiler_runtime | - Introduced DispatchCUDAIsFinite function to handle the isfinite intrinsic for CUDA, enabling support for checking finite values in floating-point computations. - Registered the new intrinsic with TVM under the operation name "tir.isfinite", enhancing the CUDA intrinsic capabilities. by CodeRabbit... | test: `testing/python/language/test_tilelang_language_isfinite_codegen.py`<br>wrapper: `src/target/intrin_rule_cuda.cc`<br>other: `3rdparty/tvm` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#1979](https://github.com/tile-ai/tilelang/pull/1979) [Feature] Introduce annotation for `minBlocksPerMultiprocessor` in `__launch_bounds__` | 2026-03-26 | benchmark_test, compiler_runtime | by CodeRabbit * **New Features** * Added support for annotating minimum blocks per streaming multiprocessor (SM) in CUDA kernel launches, allowing developers to optimize kernel occupancy and performance by controlling launch bounds. | kernel: `tilelang/language/__init__.py`, `tilelang/language/annotations.py`<br>test: `testing/python/language/test_tilelang_language_min_blocks_per_sm.py`<br>wrapper: `src/op/builtin.h`, `src/target/codegen_cuda.cc` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#1365](https://github.com/tile-ai/tilelang/pull/1365) [Enhancemnet] Minor fix to speed up testing | 2025-12-02 | benchmark_test, compiler_runtime | This pull request includes minor fixes to improve code clarity and reliability in both the benchmarking logic and the sparse library utility. Benchmarking logic correction: * In `example_gemv.py`, the condition for running the benchmarking code in the `main` function was corrected to execute when `d... | kernel: `tilelang/utils/sparse.py`<br>wrapper: `examples/gemv/example_gemv.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2162](https://github.com/tile-ai/tilelang/pull/2162) [Examples] Remove duplicated sparse TensorCore examples | 2026-05-07 | benchmark_test, compiler_runtime | - Remove sparse TensorCore example scripts from the examples tree. - Keep the examples directory from retaining test and regression wrappers for an example that is being removed. Changes - Delete `examples/sparse_tensorcore/tilelang_example_sparse_tensorcore.py`. - Delete the related sparse TensorCo... | kernel: `examples/sparse_tensorcore/tilelang_example_sparse_tensorcore.py`<br>test: `examples/sparse_tensorcore/test_example_sparse_tensorcore.py`<br>wrapper: `examples/sparse_tensorcore/regression_example_sparse_tensorcore.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#1753](https://github.com/tile-ai/tilelang/pull/1753) [Enhancement] Add dynamic symbolic constraints support for Profiler benchmarking | 2026-01-30 | benchmark_test | - Add `dynamic_symbolic_constraints` parameter to `Profiler.do_bench()` method, allowing users to specify concrete values for dynamic symbolic variables when benchmarking kernels with dynamic shapes. - Add `_substitute_dynamic_symbols()` helper method to substitute `tir.Var` in parameter shapes with... | benchmark: `testing/python/profiler/test_tilelang_profiler_dynamic_symbolic.py`, `tilelang/profiler/__init__.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2092](https://github.com/tile-ai/tilelang/pull/2092) [BugFix] Relax loop wait and adjust trailing drain behavior in async pipeline tests | 2026-04-23 | benchmark_test, compiler_runtime | Updated the async pipeline logic to progressively relax loop waits and modify the trailing drain suffix. The test case was also adjusted to reflect changes in the expected behavior, ensuring that the pipeline maintains the correct number of groups in flight and descends through the drain suffix as i... | test: `testing/python/transform/test_tilelang_transform_Inject_software_pipeline.py`<br>wrapper: `src/transform/inject_pipeline.cc` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#1756](https://github.com/tile-ai/tilelang/pull/1756) [BugFix] Always build guard in loop partitioning to prevent out-of-bounds access | 2026-01-30 | benchmark_test, compiler_runtime | - Fixed a bug where loop partitioning could generate out-of-bounds memory access when loop extent differs from Fragment layout input shape - Simplified guard construction by always building guard conditions and letting the analyzer decide if they can be proved true - This handles both non-bijective... | test: `testing/python/issue/test_tilelang_issue_1744.py`<br>wrapper: `src/transform/loop_partition.cc` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#1952](https://github.com/tile-ai/tilelang/pull/1952) [Bugfix] Handle int64 offsets in ThreadSync for tvm_access_ptr | 2026-03-20 | benchmark_test, compiler_runtime | Updated the ThreadSync implementation to support int64 offsets in tvm_access_ptr, preventing crashes during index reconstruction. Added a regression test to ensure correct behavior with shared/shared.dyn pointers. by CodeRabbit * **Bug Fixes** * Enhanced integer offset data type handling in thread s... | test: `testing/python/transform/test_tilelang_transform_thread_sync.py`<br>wrapper: `src/transform/thread_storage_sync.cc` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2017](https://github.com/tile-ai/tilelang/pull/2017) [codex] Fuse packed x2 mul-add into fma2 in CUDA codegen | 2026-04-07 | benchmark_test | This change teaches the CUDA code generator to fuse packed x2 `mul + add` patterns into `tl::fma2` when the operands already qualify for packed x2 emission. What Changed - Added a shared `CanEmitPackedX2Math` helper for packed x2 math eligibility in CUDA codegen. - Extended the packed x2 vector bina... | test: `testing/python/cuda/test_cuda_f32x2_intrinsics.py`<br>wrapper: `src/target/codegen_cuda.cc` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2015](https://github.com/tile-ai/tilelang/pull/2015) [BugFix] Enhance CUDA vectorization for binary operations | 2026-04-07 | benchmark_test | Update kernel generation to accommodate variable width, improving auto-vectorization for float32 types. Add tests for width-8 scenarios to ensure correct emission of packed operations. This is a fix for 1986 by CodeRabbit * **Bug Fixes** * Improved vector operation code generation for half-precision... | test: `testing/python/cuda/test_cuda_f32x2_intrinsics.py`<br>wrapper: `src/target/codegen_cuda.cc` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#1966](https://github.com/tile-ai/tilelang/pull/1966) [Bugfix] Fix vectorize planner ignoring cast source type bit width | 2026-03-24 | benchmark_test | - Fix `VectorizePlanner::VisitExpr_(CastNode*)` to consider **both** source and target type bit widths when computing vectorization lane constraints - Previously only the target type was considered, causing over-vectorization when casting from wider to narrower types (e.g., `int32` → `float8_e4m3fn`... | wrapper: `src/transform/loop_vectorize.cc` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#760](https://github.com/tile-ai/tilelang/pull/760) cutlass v4.2.0 supporting cuda 13 | 2025-08-26 | benchmark_test | by CodeRabbit - Chores - Updated a third-party library dependency to the latest upstream version. - No changes to user-facing functionality or interface. - Improves alignment with upstream and maintains compatibility with ongoing platform updates. - No action required from users; existing workflows... | other: `3rdparty/cutlass` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |

### Other Kernel Cases

Use this section for: Use the PR as grounded prior art; inspect diff, linked tests, and benchmark evidence before applying the idea.
NCU first look: Choose metrics based on the changed kernel family after opening the diff.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#1693](https://github.com/tile-ai/tilelang/pull/1693) [Enhancement] Log warnings for OOB acceses to non-global buffers | 2026-01-19 | kernel_other | For some thread programs (non-tile programs), OOB accesses to non-global buffers may also happen. For better developing these kernels, we add additional warnings if the compiler detects such cases. by CodeRabbit * **Chores** * Improved internal memory access safety validation mechanisms to enhance c... | wrapper: `src/transform/legalize_safe_memory_access.cc` | Use the PR as grounded prior art; inspect diff, linked tests, and benchmark evidence before applying the idea. |

## Per-PR Ledger Fields

When using an idea from this page, add one row to `artifacts/source-idea-ledger.md` with:

| Field | Value to record |
| --- | --- |
| Source key | `<repo>#<pr-number>` |
| Code evidence | Kernel, wrapper, benchmark, and test paths opened from the PR diff |
| Hypothesis | The concrete optimization idea derived from the PR |
| First experiment | Candidate version and benchmark shape set |
| Result | Correctness, geomean, best/worst cases, and NCU digest path |
| Do-not-reread key | Same as source key unless a single PR yields multiple independent ideas |

## How To Use This Page

- During the initial knowledge pass, read the category matching the target kernel and copy PR URL, changed paths, and hypothesis into the source idea ledger.
- During plateau expansion, choose PRs not already present in ledger do-not-reread keys; inspect the diff, linked issue, changed tests, and benchmark files before using the idea.
- Treat PR code as baseline/prior art unless the task and license allow copying or adapting it. When copied, record exact PR, commit, files, notice, and first delta.

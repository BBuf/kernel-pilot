# PyTorch PR Knowledge Notes

Repository: <https://github.com/pytorch/pytorch>

This page is the production-PR layer for kernel-knowledge. It favors merged PRs that changed kernels, dispatch, JIT/runtime integration, tuning policy, tests, benchmarks, or profiling evidence. Release, CI-only, formatting, pure version-bump, and non-target-backend PRs are filtered out.

## Repository Source Scan

Read these source regions before opening individual PR diffs:

- `aten/src/ATen/native/cuda`
- `aten/src/ATen/native/transformers/cuda`
- `aten/src/ATen/native/cudnn`
- `torch/_inductor`
- `torch/csrc/distributed`

## Coverage Summary

| Category | CUDA-kernel PRs |
| --- | ---: |
| GEMM / Quantization | 1 |
| Attention / KV / Decode | 2 |
| Memory / Primitives | 1 |
| Scheduler / Autotune | 4 |
| Compiler / Runtime | 19 |
| Benchmark / Test Evidence | 10 |
| Other Kernel Cases | 2 |

## Pull Request Case Notes

### GEMM / Quantization

Use this section for: Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate.
NCU first look: Tensor pipe %, DRAM/L2 bytes, active cycles, register pressure, and scale-load traffic.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#170659](https://github.com/pytorch/pytorch/pull/170659) [ATen][FBGEMM] Add sm103a and sm110a flags to the FBGEMM GENAI kernels | 2025-12-17 | gemm_quant | Per title, adds `103a` and `110a` flags to the FBGEMM GENAI kernels to make them functional on B300/GB300 and THOR. cc @ptrblck @msaroufim @eqy @jerryzh168 @tinglvv @nWEIdia | other: `aten/src/ATen/CMakeLists.txt` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |

### Attention / KV / Decode

Use this section for: Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization.
NCU first look: Long scoreboard, L2/DRAM traffic, global-load sectors, tensor pipe %, and shared-memory bank conflicts.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#170486](https://github.com/pytorch/pytorch/pull/170486) [flex_attention] adds support for low precision K/V inputs in compiled mode with GPU | 2026-01-05 | attention_kv, compiler_runtime, benchmark_test | Stack from ghstack (oldest at bottom): * 171160 * __->__ 170486 Allow Keys and Values to be in lower precision than Queries for memory efficiency. K and V are automatically upcast to Q's dtype in the kernel before matmul operations. In eager mode or compiled mode with CPU, error will be thrown with... | kernel: `torch/_higher_order_ops/flex_attention.py`, `torch/_inductor/kernel/flex/flex_attention.py`, `torch/_inductor/kernel/flex/flex_cpu.py`, `torch/_inductor/kernel/flex/flex_flash_attention.py`<br>test: `test/inductor/test_flex_attention.py`, `test/inductor/test_flex_decoding.py`, `test/inductor/test_flex_flash.py`<br>other: `torch/_inductor/kernel/flex/templates/common.py.jinja` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#128767](https://github.com/pytorch/pytorch/pull/128767) [Inductor][FlexAttention] Tune backwards kernel block sizes | 2024-06-17 | attention_kv, compiler_runtime | Stack from ghstack (oldest at bottom): * __->__ 128767 Before: code block After: code block cc @voznesenskym @penguinwu @EikanWang @jgong5 @Guobing-Chen @XiaobingSuper @zhuhaozhe @blzheng @wenzhe-nrv @jiayisunx @peterbell10 @ipiszy @yf225 @chenyang78 @kadeng @muchulee8 @ColinPeppler @amjames @desert... | kernel: `torch/_inductor/kernel/flex_attention.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |

### Memory / Primitives

Use this section for: Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract.
NCU first look: DRAM throughput, L2 hit rate, memory sectors, shared-memory conflicts, and synchronization overhead.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#177291](https://github.com/pytorch/pytorch/pull/177291) [c10d] Fix FakeProcessGroup allgather for uneven output tensor sizes | 2026-03-20 | memory_primitives, benchmark_test | Stack from ghstack (oldest at bottom): * __->__ 177291 | kernel: `torch/csrc/distributed/c10d/FakeProcessGroup.hpp`<br>test: `test/distributed/test_fake_pg.py` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |

### Scheduler / Autotune

Use this section for: Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing.
NCU first look: SM occupancy, waves/SM, active cycles, tail effects, and load imbalance.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#166985](https://github.com/pytorch/pytorch/pull/166985) [Graph Partition] fix graph partition input signature for fallback kernels | 2025-11-05 | scheduler_autotune, compiler_runtime, benchmark_test | Scheduler relies on node.last_usage to free buffers. `last_usage` may contain a buffer that is allocated in previous graph partition AND not directly accessed in the current graph partition. Example code block In the generated code, we have code block Note: view is treated as a fallback kernel due t... | test: `test/inductor/test_cudagraph_trees.py`<br>wrapper: `torch/_inductor/scheduler.py` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#172577](https://github.com/pytorch/pytorch/pull/172577) [Graph Partition] Improve support for mutation ops | 2026-01-15 | scheduler_autotune, compiler_runtime, benchmark_test | When collecting inputs/outputs of a graph partition, we skip NoneLayout buffers since they are not allocated. One special case is mutation ops (e.g., `buf1 = mutation(buf0)`), whose output `buf1` has NoneLayout and we will access `buf0` in the future. In this case, we should still include `buf0` as... | test: `test/inductor/test_cudagraph_trees.py`<br>wrapper: `torch/_inductor/scheduler.py` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#161816](https://github.com/pytorch/pytorch/pull/161816) [Reland][Inductor] Prune configs that require more shared memory than the hardware limit | 2025-08-30 | scheduler_autotune, compiler_runtime | Stack from ghstack (oldest at bottom): * __->__ 161816 Summary: This is a re-land of PR161040, which had previously caused test failures on AMD GPUs. The tests are now configured to target only NVIDIA GPUs. This diff removes configurations that exceed the hardware shared memory limit, which causes t... | kernel: `torch/_inductor/template_heuristics/triton.py`<br>test: `test/inductor/test_max_autotune.py`, `test/inductor/test_triton_heuristics.py`<br>wrapper: `torch/_inductor/config.py`, `torch/_inductor/select_algorithm.py` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#170633](https://github.com/pytorch/pytorch/pull/170633) [c10d] Add thread safety when calling ncclCommGetAsyncError | 2025-12-18 | scheduler_autotune | Fixes 169484 | kernel: `torch/csrc/distributed/c10d/NCCLUtils.cpp`, `torch/csrc/distributed/c10d/NCCLUtils.hpp`, `torch/csrc/distributed/c10d/ProcessGroupNCCL.cpp` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |

### Compiler / Runtime

Use this section for: Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests.
NCU first look: Generated kernel shape, launch overhead, occupancy, register pressure, and compile-time-selected schedule.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#181441](https://github.com/pytorch/pytorch/pull/181441) [Inductor] Improve materialization heuristic for a chain of computaions (#176994) | 2026-04-29 | compiler_runtime, benchmark_test, scheduler_autotune, gemm_quant | When inductor compiles a model without graph breaks, multi-user pointwise buffers (e.g. residual adds in transformers) may not be materialized because should_realize_on_reuse only checked num_reads > 4. For a residual add with 2 reads and 3 users, this never triggered — causing each subsequent reduc... | test: `test/inductor/pallas_expected_failures/CpuTests.test_nll_loss_backward_1d_input_cpu`, `test/inductor/test_compiled_optimizers.py`, `test/inductor/test_cpu_repro.py`, `test/inductor/test_cpu_select_algorithm.py`<br>wrapper: `torch/_inductor/graph.py`, `torch/_inductor/ir.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#170884](https://github.com/pytorch/pytorch/pull/170884) [inductor] Fix cudagraph skip for index_put_ with boolean indices, gr… | 2025-12-22 | compiler_runtime, scheduler_autotune, benchmark_test | Fixes https://github.com/pytorch/pytorch/issues/169951 Cherrypick of https://github.com/pytorch/pytorch/pull/170103 cc @voznesenskym @penguinwu @EikanWang @jgong5 @Guobing-Chen @XiaobingSuper @zhuhaozhe @blzheng @wenzhe-nrv @jiayisunx @ipiszy @kadeng @muchulee8 @amjames @chauhang @aakhundov @coconut... | test: `test/inductor/test_compiled_autograd.py`, `test/inductor/test_cudagraph_trees.py`<br>wrapper: `torch/_inductor/compile_fx.py`, `torch/_inductor/ir.py`, `torch/_inductor/lowering.py`, `torch/_inductor/scheduler.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#133381](https://github.com/pytorch/pytorch/pull/133381) add an option to use do_bench_using_profiling in TORCHINDUCTOR_PROFILE | 2024-08-15 | compiler_runtime, benchmark_test, scheduler_autotune | When I did profiling using the "TORCHINDUCTOR_PROFILE" option, some kernel shows less bandwidth than expected. So, added the option to exclude the CPU overheads from the profiling time: code block Stack from ghstack (oldest at bottom): * __->__ 133381 cc @voznesenskym @penguinwu @EikanWang @jgong5 @... | kernel: `torch/_inductor/codegen/triton.py`, `torch/_inductor/runtime/triton_heuristics.py`<br>wrapper: `torch/_inductor/config.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#176495](https://github.com/pytorch/pytorch/pull/176495) [inductor] avoid multi-stage for mix-order-red by default (#176228) | 2026-03-09 | compiler_runtime, scheduler_autotune, benchmark_test | The default >1 num_stages have causing multiple out of shared memory issues. Make it to be 1 by default. We could explore other alternatives 1. always add a config with num_stages=1 while keeping the current heuristics. Could increase compilation time 2. dynamically scale down num-stages if all conf... | kernel: `torch/_inductor/codegen/triton.py`, `torch/_inductor/runtime/triton_heuristics.py`<br>test: `test/inductor/test_mix_order_reduction.py`<br>wrapper: `torch/_inductor/config.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#172116](https://github.com/pytorch/pytorch/pull/172116) Touch `__init__.py` in `vendored_templates` for CuTeDSL Grouped MM template | 2026-01-09 | compiler_runtime, moe_routing, benchmark_test | This pull request makes a small improvement to the `mirror_inductor_external_kernels` function in `setup.py` to ensure that newly created directories are recognized as Python packages by `find_packages`. * When creating a new directory for mirrored files, the code now adds an empty `__init__.py` fil... | test: `test/test_public_bindings.py`, `test/test_testing.py`<br>wrapper: `setup.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#180916](https://github.com/pytorch/pytorch/pull/180916) [user-streams] Fix cudagraphs compatibility with current stream (#180… | 2026-04-22 | compiler_runtime, benchmark_test | Fixes https://github.com/pytorch/pytorch/issues/180396 This is very important for VLLM in the next release. cc @voznesenskym @penguinwu @EikanWang @jgong5 @Guobing-Chen @XiaobingSuper @zhuhaozhe @blzheng @wenzhe-nrv @jiayisunx @ipiszy @kadeng @muchulee8 @amjames @chauhang @aakhundov @coconutruben @j... | test: `test/dynamo/test_streams.py`, `test/functorch/test_leaf_function.py`, `test/inductor/test_compiled_autograd.py`, `test/inductor/test_user_streams.py`<br>wrapper: `torch/_dynamo/graph_bytecode_inputs.py`, `torch/_dynamo/variables/builder.py`, `torch/_dynamo/variables/streams.py`, `torch/_inductor/codegen/wrapper.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#157241](https://github.com/pytorch/pytorch/pull/157241) [user triton] AOT inductor support for device-side TMA | 2025-07-11 | compiler_runtime | Stack from ghstack (oldest at bottom): * __->__ 155896 Tests: `python test/inductor/test_aot_inductor.py -vvv -k device_tma` Device-side TMA in Triton allows the kernel author to construct the TMA descriptor on the device (which composes with things like autotuning much better). However, it also req... | kernel: `torch/_inductor/runtime/triton_heuristics.py`<br>test: `test/inductor/test_aot_inductor.py`, `test/inductor/test_triton_kernels.py`<br>wrapper: `torch/_inductor/codegen/common.py`, `torch/_inductor/codegen/cpp_wrapper_gpu.py`, `torch/_inductor/codegen/cuda/device_op_overrides.py`, `torch/_inductor/codegen/xpu/device_op_overrides.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#138260](https://github.com/pytorch/pytorch/pull/138260) Bugfix for passing None args to user defined Triton kernel | 2024-10-21 | compiler_runtime, benchmark_test | This is a PR to fix the following issue: https://github.com/pytorch/pytorch/issues/115344 In short, passing None as an arg. to a Triton kernel would cause problems: Short repro of the specific bug fixed here: code block I've added a unit test to catch this issue in the future and the tests in "test/... | kernel: `torch/_higher_order_ops/triton_kernel_wrap.py`, `torch/_inductor/runtime/triton_heuristics.py`<br>test: `test/inductor/test_triton_kernels.py`<br>wrapper: `torch/_inductor/codegen/wrapper.py`, `torch/_inductor/ir.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#163262](https://github.com/pytorch/pytorch/pull/163262) [SymmMem] Fix NVSHMEM plugin + Triton 3.5 | 2025-09-19 | compiler_runtime, benchmark_test | Stack from ghstack (oldest at bottom): * 163194 * __->__ 163152 1. The dispatch signatures defined in `core.extern_elementwise` call must match the C signature of the NVSHMEM functions, in particular the dtypes. Otherwise, there would be weird errors, such as IMA or hang. When matched, most of time... | kernel: `torch/distributed/_symmetric_memory/_nvshmem_triton.py`<br>test: `test/distributed/test_nvshmem_triton.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#181294](https://github.com/pytorch/pytorch/pull/181294) [Bugfix] Fix triton crash in mixed device case for bmm | 2026-04-23 | compiler_runtime, benchmark_test | Partial fix of https://github.com/pytorch/pytorch/issues/180909 PyTorch's bmm_outer_product Triton kernel override crashes with ValueError: Pointer argument cannot be accessed from Triton (cpu tensor?) when a non-CUDA tensor reaches the CUDA dispatch key handler. This happens in practice when transf... | kernel: `torch/_native/ops/bmm_outer_product/triton_impl.py`<br>test: `test/test_bmm_outer_product.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#181549](https://github.com/pytorch/pytorch/pull/181549) [AOTI] Add BC-safe c_shim v2 for _scaled_dot_product_attention_math_for_mps enable_gqa | 2026-05-04 | compiler_runtime, attention_kv | Stack from ghstack (oldest at bottom): * __->__ 181549 * 181548 * 181550 PR 179309 added enable_gqa to the _scaled_dot_product_attention_math_for_mps op schema, which broke the AOTI c_shim ABI for downstream runtimes that provide their own implementation (e.g. ExecuTorch Metal backend). Add the op t... | kernel: `torch/csrc/inductor/aoti_torch/generated/c_shim_mps.h`<br>wrapper: `torchgen/aoti/fallback_ops.py`, `torchgen/gen_aoti_c_shim.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#182178](https://github.com/pytorch/pytorch/pull/182178) Avoid raw stream name collisions in Inductor | 2026-05-04 | compiler_runtime, benchmark_test | Use raw_streamN for generated raw CUDA stream handles so they cannot clobber user stream object bindings such as stream1. Add a CUDA:1 regression test covering the aux-stream, Triton-launch, aux-stream ordering that previously crashed. Authored with Claude. submit of https://github.com/pytorch/pytor... | kernel: `torch/_inductor/codegen/triton.py`, `torch/_inductor/codegen/triton_combo_kernel.py`<br>test: `test/inductor/test_user_streams.py`<br>wrapper: `torch/_inductor/codegen/wrapper.py`, `torch/_inductor/stream_utils.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#181287](https://github.com/pytorch/pytorch/pull/181287) [inductor] makes cuda 13.0 cross compliation works (#179229) | 2026-04-23 | compiler_runtime, benchmark_test | Summary: CUDA 13.0+ ships a hybrid cudart.lib that contains MSVC-compiled static objects referencing GS security symbols (__security_cookie, __security_check_cookie, etc.) which MinGW cannot resolve, causing undefined reference errors during Windows cross-compilation. This diff adds a _ensure_mingw_... | test: `test/inductor/test_aoti_cross_compile_windows.py`<br>wrapper: `torch/_inductor/cpp_builder.py`<br>other: `.ci/docker/build.sh`, `.ci/docker/common/install_mingw.sh`, `.github/workflows/_linux-test.yml`, `.github/workflows/_win-build.yml` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#181751](https://github.com/pytorch/pytorch/pull/181751) [Bugfix][Caching] Fix Caching error by avoiding failure for triton kernels in pickling error (#181463) | 2026-04-28 | compiler_runtime | Cherrypick of https://github.com/pytorch/pytorch/pull/181463 Fixes https://github.com/pytorch/pytorch/issues/180911 - AOTAutogradCache.save(): Catch pickle.PicklingError from triton kernel and treat it as a bypass (warn + continue) instead of a fatal error. The cache is an optimization — failing to... | kernel: `torch/_inductor/runtime/triton_heuristics.py`<br>test: `test/dynamo/test_aot_autograd_cache.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#157200](https://github.com/pytorch/pytorch/pull/157200) [inductor][static launcher] Skip correctness test for test_floats | 2025-07-07 | compiler_runtime, benchmark_test | Stack from ghstack (oldest at bottom): * __->__ 157023 https://github.com/triton-lang/triton/issues/6176 causes kernels that take fp64 scalar inputs to generate wrong results. Until we get around to fixing this, just skip the accuracy check (it'll fail on Triton's launcher anyway). cc @voznesenskym... | test: `test/inductor/test_static_cuda_launcher.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#172105](https://github.com/pytorch/pytorch/pull/172105) A few weights_only unpickler fixes | 2026-01-09 | compiler_runtime, benchmark_test | Fixes the following - Adds type validation to `SETITEM`/`SETITEMS`, to prevent tensor updates during `torch.load` - Only print `type(pid[0])` rather than `pid[0]` in error message for `BINPERSID` - Validate `numel` in .pkl for storage matches size of storage saved to zipfile in `get_storage_from_rec... | kernel: `torch/csrc/jit/python/init.cpp`<br>test: `test/test_serialization.py`<br>wrapper: `torch/_weights_only_unpickler.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#171129](https://github.com/pytorch/pytorch/pull/171129) [Inductor] Fix constants handling for Triton constexpr (triton#8248) | 2025-12-23 | compiler_runtime | Up until recently (triton-lang/triton8248), Triton did not explicitly interpret entries in `ASTSource.constants` , and Inductor historically treated "non-tensor-like" arguments as `constexpr`, including runtime scalars. Triton’s updated `constexpr` handling now requires stricter semantics, so this c... | kernel: `torch/_higher_order_ops/triton_kernel_wrap.py`<br>test: `test/inductor/test_triton_kernels.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#149993](https://github.com/pytorch/pytorch/pull/149993) [inductor][triton 3.3] Fix cpp_wrapper w/ TMA in triton 3.3 | 2025-03-28 | compiler_runtime | Stack from ghstack (oldest at bottom): * __->__ 149973 Fixes 148938 Context: In triton 3.3, triton kernels expect a global scratch space arg to be passed in. This is fixed in 148051, which fixed most of the AOTI/cpp_wrapper failures; the fix is to inject a (null) global scratch space arg passed as a... | wrapper: `torch/_inductor/codegen/cpp_wrapper_gpu.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#181548](https://github.com/pytorch/pytorch/pull/181548) [Inductor] Call latest c_shim version for versioned fallback ops | 2026-05-04 | compiler_runtime, benchmark_test | Stack from ghstack (oldest at bottom): * 181549 * __->__ 181548 * 181550 When an aten fallback op has a versioned c_shim entry in torchgen/aoti/fallback_ops.py (e.g. {"v2": [...]}), the cpp wrapper was still emitting the unversioned shim name with the current-schema args. The unversioned shim keeps... | wrapper: `torch/_inductor/ir.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |

### Benchmark / Test Evidence

Use this section for: Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code.
NCU first look: Use the PR's benchmark/profile command as the first replay target.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#164151](https://github.com/pytorch/pytorch/pull/164151) Add operator benchmarking run to CI nightly | 2025-09-29 | benchmark_test | This PR introduces a new "operator microbenchmark" CI workflow and GitHub Actions for operator microbenchmarks, updating test scripts and job matrices to support new parameters, and broadening the operator benchmark tests to include more data types, larger shapes, and gradient tests. The benchmark c... | benchmark: `.github/workflows/operator_microbenchmark.yml`, `benchmarks/operator_benchmark/benchmark_core.py`, `benchmarks/operator_benchmark/benchmark_pytorch.py`, `benchmarks/operator_benchmark/pt/add_test.py`<br>test: `.ci/pytorch/test.sh`<br>other: `.github/workflows/_linux-test.yml` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#175096](https://github.com/pytorch/pytorch/pull/175096) Update inductor expected accuracy files | 2026-02-16 | benchmark_test, compiler_runtime | This PR updates the expected accuracy CSV files for inductor benchmarks based on CI results from PyTorch commit 93dd7743c6577271a81f2fef0fdeafc5fe06e553. These files serve as reference points for dynamo/inductor CI to track: - Graph breaks - Model accuracy Changes - Updated CUDA expected accuracy fi... | benchmark: `benchmarks/dynamo/ci_expected_accuracy/cpu_inductor_torchbench_inference.csv` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#163380](https://github.com/pytorch/pytorch/pull/163380) [Graph Partition] improve custom op output alias | 2025-09-19 | benchmark_test, compiler_runtime | For a custom op with multiple outputs, we will see the following generated code: code block If `buf1` is not accessed in the future, it's good to deallocate early. So we don't delay `del` until both buf3 and buf4 are not used anymore. Note that buf3 and buf4 hold reference to the data such that `del... | test: `test/inductor/test_cudagraph_trees.py`, `test/inductor/test_perf.py`<br>wrapper: `torch/_inductor/ir.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#172672](https://github.com/pytorch/pytorch/pull/172672) Fast memory snapshot | 2026-01-30 | benchmark_test | Snapshot can be slow since we get the whole trace entry history every time. This change adds an include_traces option to memory_snapshot() and MemPool.snapshot() that allows skipping trace entries and annotations when taking a CUDA memory snapshot. max_entries already exists to limit the number of t... | kernel: `torch/csrc/cuda/CUDAPluggableAllocator.cpp`, `torch/csrc/cuda/CUDAPluggableAllocator.h`, `torch/csrc/cuda/Module.cpp`<br>test: `test/test_cuda.py`<br>wrapper: `aten/src/ATen/hip/impl/HIPAllocatorMasqueradingAsCUDA.h`, `aten/src/ATen/hip/impl/HIPCachingAllocatorMasqueradingAsCUDA.h`, `c10/cuda/CUDACachingAllocator.cpp`, `c10/cuda/CUDACachingAllocator.h`<br>other: `torch/_C/__init__.pyi.in` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#170695](https://github.com/pytorch/pytorch/pull/170695) Unit test for ExternKernelBenchmarkRequest TensorMeta construction | 2025-12-17 | benchmark_test | Stack from ghstack (oldest at bottom): * __->__ 170445 Unit test for https://github.com/pytorch/pytorch/pull/170246 in release cc @voznesenskym @penguinwu @EikanWang @jgong5 @Guobing-Chen @XiaobingSuper @zhuhaozhe @blzheng @wenzhe-nrv @jiayisunx @ipiszy @kadeng @muchulee8 @amjames @chauhang @aakhund... | test: `test/inductor/test_select_algorithm.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#176603](https://github.com/pytorch/pytorch/pull/176603) Fix the torch.Stream context manager reentrance | 2026-03-09 | benchmark_test | Stack from ghstack (oldest at bottom): * __->__ 176568 Motivation This PR aims to fix `torch.Stream` as a context manager nested/reentrance scenario. `torch.cuda.stream` and `torch.xpu.stream` could support these usages. The following scenario would be fixed with this PR: code block code block Addti... | kernel: `torch/csrc/Stream.cpp`<br>test: `test/test_accelerator.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#180809](https://github.com/pytorch/pytorch/pull/180809) [NCCL][Symmetric Memory] Fix groupName in IntraNodeComm | 2026-04-23 | benchmark_test | This PR fixes: code block in `python test/distributed/test_c10d_nccl.py CommTest.test_intra_node_comm_all_reduce`. Just FYI: A quite similar error was attempted to be fixed in 177700, however the refactor 176506 partially fixed the original one by lazy pg registration, though missed the process grou... | kernel: `torch/csrc/distributed/c10d/symm_mem/intra_node_comm.cu`<br>test: `test/distributed/test_c10d_nccl.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#181019](https://github.com/pytorch/pytorch/pull/181019) [dynamo] Fix cuda_stream pointer extraction for generic torch.Stream | 2026-04-22 | benchmark_test | Fixes 180179 [dynamo] Fix cuda_stream pointer extraction for generic torch.Stream I noticed that `torch.compile` narrows `torch.cuda.Stream` down to generic `torch.Stream` objects during tracing. This is a problem because generic streams don't have the `.cuda_stream` property, so Dynamo falls back t... | test: `test/dynamo/test_streams.py`<br>wrapper: `torch/_dynamo/variables/streams.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#181137](https://github.com/pytorch/pytorch/pull/181137) Add TLS stack_bounds so we dont pay expensive reads | 2026-04-23 | benchmark_test | Stack from ghstack (oldest at bottom): * __->__ 181018 Before this PR code block After: code block Script I worked on with claude as a proxy: code block cc @snadampal @milpuz01 @aditew01 @nikhil-arm @fadara01 @robert-hardwick @nWEIdia | benchmark: `torch/csrc/profiler/unwind/unwind.cpp` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#163766](https://github.com/pytorch/pytorch/pull/163766) [CD] CUDA 13.0 fix preload logic to include nvidia/cu13/lib/ | 2025-09-25 | benchmark_test | Preload logic no longer works with CUDA 13.0 See the installation path: code block Test using script from : https://github.com/pytorch/pytorch/issues/162367 code block | wrapper: `torch/__init__.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |

### Other Kernel Cases

Use this section for: Use the PR as grounded prior art; inspect diff, linked tests, and benchmark evidence before applying the idea.
NCU first look: Choose metrics based on the changed kernel family after opening the diff.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#182369](https://github.com/pytorch/pytorch/pull/182369) Fix stale PYTORCH_RELEASES_CODE_CC dict (fixes #182250) | 2026-05-04 | kernel_other | The release-to-CC dict in \`torch/cuda/__init__.py\` drove the *\"install a PyTorch release that supports one of these CUDA versions: ...\"* recommendation, but had drifted from the actual binary build matrix: - **\"12.6\"** was missing CC \`7.5\` — `.ci/manywheel/build_cuda.sh` puts 7.5 in the base... | wrapper: `torch/cuda/__init__.py` | Use the PR as grounded prior art; inspect diff, linked tests, and benchmark evidence before applying the idea. |
| [#172010](https://github.com/pytorch/pytorch/pull/172010) Rename _get_cuda_runtime_library to _get_cuda_library | 2026-01-08 | kernel_other | The rename was unintentional. cc @msaroufim | wrapper: `torch/cuda/_utils.py` | Use the PR as grounded prior art; inspect diff, linked tests, and benchmark evidence before applying the idea. |

## Per-PR Ledger Fields

When using an idea from this page, add one row to `artifacts/source-idea-ledger.md` with:

| Field | Value to record |
| --- | --- |
| Source key | `<repo>#<pr-number>` |
| Code evidence | Kernel, wrapper, benchmark, and test paths opened from the PR diff |
| Hypothesis | The concrete optimization idea derived from the PR |
| First experiment | Candidate version and benchmark shape set |
| Result | Correctness, geomean, best/worst cases, and NCU digest path |
| Do-not-reread key | Same as source key unless a single PR yields multiple independent ideas |

## How To Use This Page

- During the initial knowledge pass, read the category matching the target kernel and copy PR URL, changed paths, and hypothesis into the source idea ledger.
- During plateau expansion, choose PRs not already present in ledger do-not-reread keys; inspect the diff, linked issue, changed tests, and benchmark files before using the idea.
- Treat PR code as baseline/prior art unless the task and license allow copying or adapting it. When copied, record exact PR, commit, files, notice, and first delta.

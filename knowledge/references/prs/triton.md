# Triton PR Knowledge Notes

Repository: <https://github.com/triton-lang/triton>

This page is the production-PR layer for kernel-knowledge. It favors merged PRs that changed kernels, dispatch, JIT/runtime integration, tuning policy, tests, benchmarks, or profiling evidence. Release, CI-only, formatting, pure version-bump, and non-target-backend PRs are filtered out.

## Repository Source Scan

Read these source regions before opening individual PR diffs:

- `python/tutorials`
- `python/triton/language`
- `python/triton/runtime`
- `lib/Conversion`
- `test/TritonGPU`

## Coverage Summary

| Category | CUDA-kernel PRs |
| --- | ---: |
| GEMM / Quantization | 4 |
| MoE / Routing | 1 |
| Norm / Elementwise / Epilogue | 7 |
| Memory / Primitives | 5 |
| Scheduler / Autotune | 2 |
| Architecture / Pipeline | 15 |
| Compiler / Runtime | 47 |
| Benchmark / Test Evidence | 11 |

## Pull Request Case Notes

### GEMM / Quantization

Use this section for: Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate.
NCU first look: Tensor pipe %, DRAM/L2 bytes, active cycles, register pressure, and scale-load traffic.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#10249](https://github.com/triton-lang/triton/pull/10249) [triton_kernels] nvfp4 x nvfp4 tuning | 2026-05-06 | gemm_quant, compiler_runtime, scheduler_autotune | Add support for nvfp4 output with swizzled scales and tune fp4 opt flags, mostly increasing block sizes and adjusting stage counts. For nvfp4 x nvfp4 -> nvfp4 mm1, I was able to easily hit >= 5 pflops. For mm2, this PR will give 4.5 pflops but you can autotune up to 4.8. However the winning config i... | kernel: `python/triton_kernels/triton_kernels/matmul.py`, `python/triton_kernels/triton_kernels/matmul_details/_common.py`, `python/triton_kernels/triton_kernels/matmul_details/_matmul.py`, `python/triton_kernels/triton_kernels/matmul_details/_p_matmul.py`<br>test: `python/triton_kernels/tests/test_matmul.py`<br>docs: `AGENTS.md` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#10234](https://github.com/triton-lang/triton/pull/10234) [ BACKEND ]  Enable `tl.dot` with TF32 precision on tiles with N=8 and K=8 | 2026-05-13 | gemm_quant, compiler_runtime, benchmark_test, arch_pipeline | Enable `tl.dot` with TF32 precision on tiles with **N=8** and **K=8** (e.g. `wgmma.mma_async.sync.aligned.m64n8k8.f32.tf32.tf32`) via the standard `tt.dot` → `AccelerateMatmul` path on sm90+. Related to https://github.com/triton-lang/triton/pull/10060event-24679689497 I am trying Triton for Finite E... | test: `python/test/unit/language/test_core.py`, `test/TritonGPU/accelerate-matmul.mlir`<br>wrapper: `lib/Analysis/Utility.cpp`, `third_party/nvidia/backend/compiler.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#10190](https://github.com/triton-lang/triton/pull/10190) [mxfp4] Fix Hopper scale padding mask | 2026-05-04 | gemm_quant, arch_pipeline | Why Hopper MXFP4 weight-scale swizzling can introduce padded scale bytes when the RHS K dimension does not fill the final scale tile. The regular RHS value load already masks invalid K-tail values, but the Hopper scale path was loading and unswizzling the full scale tile without clearing invalid K-s... | kernel: `python/triton_kernels/triton_kernels/matmul_details/_matmul.py`, `python/triton_kernels/triton_kernels/matmul_details/_p_matmul.py`<br>test: `python/triton_kernels/tests/test_matmul_details/test_opt_flags_nvidia.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#10214](https://github.com/triton-lang/triton/pull/10214) Allow MXFP8 LHS with Hopper-swizzled MXFP4 RHS | 2026-05-07 | gemm_quant | This is a targeted fix for the Hopper-swizzled MXFP4 RHS path. MXFP4/MXFP8 matmul coverage already exists elsewhere; the missing piece here was the Hopper value/scale-swizzled MXFP4 RHS path when the LHS is also microscaled, primarily MXFP8. - In the regular and persistent matmul kernels, the `SWIZZ... | kernel: `python/triton_kernels/triton_kernels/matmul.py`, `python/triton_kernels/triton_kernels/matmul_details/_matmul.py`, `python/triton_kernels/triton_kernels/matmul_details/_p_matmul.py`, `python/triton_kernels/triton_kernels/matmul_details/opt_flags.py`<br>test: `python/triton_kernels/tests/test_matmul.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |

### MoE / Routing

Use this section for: Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea.
NCU first look: Tensor pipe %, SM imbalance, branch divergence, L2 traffic, permutation traffic, and synchronization stalls.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#10280](https://github.com/triton-lang/triton/pull/10280) [BACKEND] Recognise fneg in clamp and use it in the MoE | 2026-05-10 | moe_routing, compiler_runtime, norm_elementwise, memory_primitives, arch_pipeline | https://github.com/triton-lang/triton/pull/10270 subtly broke this pattern. We fix that. We also use gl.clamp in the MoE kernel to generate `FMNMX.XORSIGN` instead of the min/max pattern. This gives about a 0.4% speed-up | kernel: `python/examples/gluon/05-moe-bmm1-fused-gather.py`, `third_party/nvidia/lib/TritonNVIDIAGPUToLLVM/ElementwiseOpToLLVM.cpp`<br>test: `test/Conversion/tritongpu_to_llvm_hopper.mlir` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |

### Norm / Elementwise / Epilogue

Use this section for: Look for fusion, vectorization, dtype conversion, and store-traffic reductions before changing math.
NCU first look: DRAM %, L2 bytes, global load/store sectors, eligible warps, and long scoreboard.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#10060](https://github.com/triton-lang/triton/pull/10060) [BACKEND] Extend support for small MMAv2 FP64: single 8x8x4 instructions. | 2026-04-20 | norm_elementwise, compiler_runtime, benchmark_test, gemm_quant | The fp64 MMA path now operates at native `m8n8k4` granularity, supporting any shape that is a multiple of 8×8×4, including the minimal 8×8×4 case. This is an extension of https://github.com/triton-lang/triton/pull/7310 (The implementation was based on that PR) Tests passed on A100. Files Changed `li... | kernel: `lib/Dialect/TritonGPU/IR/Dialect.cpp`, `lib/Dialect/TritonGPU/IR/LinearLayoutConversions.cpp`, `lib/Dialect/TritonGPU/Transforms/Utility.cpp`, `third_party/nvidia/lib/TritonNVIDIAGPUToLLVM/DotOpToLLVM/MMAv2.cpp`<br>test: `python/test/unit/language/test_core.py`, `test/Conversion/tritongpu_to_llvm.mlir`, `test/TritonGPU/accelerate-matmul.mlir`<br>wrapper: `third_party/nvidia/backend/compiler.py` | Look for fusion, vectorization, dtype conversion, and store-traffic reductions before changing math. |
| [#10263](https://github.com/triton-lang/triton/pull/10263) [BACKEND] add TMEM barrier insertion pass | 2026-05-08 | norm_elementwise, compiler_runtime, benchmark_test | Inset bar.sync to ensure that tmem operations are ordered correctly. This replace the hardcoded barrier after store | kernel: `include/triton/Analysis/Membar.h`, `lib/Dialect/TritonNvidiaGPU/Transforms/TMemBarrierInsertion.cpp`, `third_party/nvidia/lib/TritonNVIDIAGPUToLLVM/TensorMemoryToLLVM.cpp`, `third_party/nvidia/triton_nvidia.cc`<br>test: `test/TritonNvidiaGPU/tmem_barrier_insertion.mlir`<br>wrapper: `lib/Analysis/Membar.cpp`, `third_party/nvidia/backend/compiler.py`<br>other: `include/triton/Dialect/TritonNvidiaGPU/Transforms/Passes.td`, `lib/Dialect/TritonNvidiaGPU/Transforms/CMakeLists.txt` | Look for fusion, vectorization, dtype conversion, and store-traffic reductions before changing math. |
| [#10129](https://github.com/triton-lang/triton/pull/10129) Account for duplicated elementwise ops in backward remat cost | 2026-05-10 | norm_elementwise, compiler_runtime, benchmark_test | If remat will increase the number of elements in each thread, that can make elementwise operations much more expensive. Multiply the cost by the increase in the number of elements to account for this. This is intended to disturb the current cost heuristic as little as possible, so the weight is only... | kernel: `include/triton/Dialect/TritonGPU/IR/Dialect.h`, `lib/Dialect/TritonGPU/IR/Dialect.cpp`, `lib/Dialect/TritonGPU/Transforms/RemoveLayoutConversions.cpp`<br>test: `test/TritonGPU/combine.mlir` | Look for fusion, vectorization, dtype conversion, and store-traffic reductions before changing math. |
| [#10128](https://github.com/triton-lang/triton/pull/10128) Treat pure register reorder as free in backward remat | 2026-04-30 | norm_elementwise, compiler_runtime, benchmark_test | --- [//]: (BEGIN SAPLING FOOTER) * 10129 * __->__ 10128 | kernel: `lib/Dialect/TritonGPU/Transforms/RemoveLayoutConversions.cpp`<br>test: `test/TritonGPU/combine.mlir` | Look for fusion, vectorization, dtype conversion, and store-traffic reductions before changing math. |
| [#10233](https://github.com/triton-lang/triton/pull/10233) [fpsan] Store to scratch buffers in embedded form | 2026-05-07 | norm_elementwise, compiler_runtime, benchmark_test | <git-pr-chain> [fpsan] Store to scratch buffers in embedded form Currently in the emulated mma loop, we have a sequence like: code block and since we accumulate many times into the same accumulator, this ends up with a lot of redundant embedding and unembedding. Instead, this changes the scratch man... | kernel: `lib/Dialect/TritonInstrument/Transforms/FpSanitizer.cpp`<br>test: `test/TritonGPU/fpsan.mlir`, `test/TritonGPU/nvidia-fpsan.mlir` | Look for fusion, vectorization, dtype conversion, and store-traffic reductions before changing math. |
| [#10235](https://github.com/triton-lang/triton/pull/10235) [NFC] Remove getElementType helper functions | 2026-05-07 | norm_elementwise, compiler_runtime | <git-pr-chain> [NFC] Remove getElementType helper functions This already exists in mlir itself, called getElementTypeOrSelf. PR chain 1. 👉 10235 👈 **YOU ARE HERE** </git-pr-chain> | kernel: `lib/Conversion/TritonGPUToLLVM/ElementwiseOpToLLVM.cpp`, `lib/Dialect/TritonInstrument/Transforms/FpSanitizer.cpp`, `third_party/amd/lib/TritonAMDGPUToLLVM/ElementwiseOpToLLVM.cpp`, `third_party/nvidia/lib/TritonNVIDIAGPUToLLVM/ElementwiseOpToLLVM.cpp` | Look for fusion, vectorization, dtype conversion, and store-traffic reductions before changing math. |
| [#10090](https://github.com/triton-lang/triton/pull/10090) [FPSAN] Clean up fpsan pattern failures | 2026-04-21 | norm_elementwise, compiler_runtime | Prevent silent fpsan miscompiles by correctly distinguishing between pattern failures that skip the pattern and invariant violations / unsupported constructs that fail compilation. | kernel: `lib/Dialect/TritonInstrument/Transforms/FpSanitizer.cpp` | Look for fusion, vectorization, dtype conversion, and store-traffic reductions before changing math. |

### Memory / Primitives

Use this section for: Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract.
NCU first look: DRAM throughput, L2 hit rate, memory sectors, shared-memory conflicts, and synchronization overhead.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#10163](https://github.com/triton-lang/triton/pull/10163) [BACKEND] Enable i16 descriptor gather/scatter indices on NVIDIA | 2026-05-04 | memory_primitives, norm_elementwise, arch_pipeline, compiler_runtime, benchmark_test | [BACKEND] Enable i16 descriptor gather/scatter indices on NVIDIA | kernel: `include/triton/Dialect/TritonNvidiaGPU/Transforms/TMAUtilities.h`, `lib/Dialect/TritonGPU/Transforms/Pipeliner/LowerLoops.cpp`, `lib/Dialect/TritonGPU/Transforms/Pipeliner/TMAStoresPipeline.cpp`, `lib/Dialect/TritonNvidiaGPU/Transforms/TMALowering.cpp`<br>test: `python/test/unit/language/test_tensor_descriptor.py`, `test/TritonGPU/amd/amd-convert-tensor-ops.mlir` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |
| [#10053](https://github.com/triton-lang/triton/pull/10053) [GSan] Model TMAReduceOp as atomic | 2026-04-17 | memory_primitives, arch_pipeline, norm_elementwise, compiler_runtime, benchmark_test | <git-pr-chain> [GSan] Model TMAReduceOp as atomic Previously we were modelling it as a non-atomic store. This now models it as atomic, but is still not 100% right as we're doing the shadow cell update eagerly before the TMA operation has completed. PR chain 1. 👉 10053 👈 **YOU ARE HERE** </git-pr-cha... | kernel: `lib/Conversion/TritonInstrumentToLLVM/GSanToLLVM.cpp`, `lib/Dialect/TritonInstrument/Transforms/GlobalSanitizer.cpp`, `python/triton/experimental/gsan/src/GSanLibrary.cu`<br>test: `python/test/gsan/test_gsan.py`, `python/test/gsan/test_gsan_failures.py`<br>other: `include/triton/Dialect/TritonInstrument/IR/TritonInstrumentOps.td` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |
| [#10038](https://github.com/triton-lang/triton/pull/10038) Use TargetInfo for shared memory accesses | 2026-04-17 | memory_primitives, arch_pipeline, compiler_runtime, benchmark_test | Use TargetInfo for shared memory accesses | kernel: `lib/Conversion/TritonGPUToLLVM/GatherOpToLLVM.cpp`, `lib/Conversion/TritonGPUToLLVM/HistogramOpToLLVM.cpp`, `lib/Conversion/TritonGPUToLLVM/MemoryOpToLLVM.cpp`, `lib/Conversion/TritonGPUToLLVM/ScanOpToLLVM.cpp`<br>test: `test/Conversion/tritongpu_to_llvm.mlir` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |
| [#10317](https://github.com/triton-lang/triton/pull/10317) Speed up reduce_forward for the common case "broadcast_n" masking. | 2026-05-15 | memory_primitives, benchmark_test, compiler_runtime | We "sort" the mask index so that rows with similar number of active input rows are grouped together. Here, "number of active input rows" are defined as `mask[:, :, 0].sum(dim=1)`, assuming the reduction dimension is 1 and the mask is broadcast over dim=2. Then, for each block, we call the main logic... | kernel: `python/triton_kernels/triton_kernels/reduce.py`<br>test: `python/triton_kernels/tests/test_reduce.py` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |
| [#10238](https://github.com/triton-lang/triton/pull/10238) [CD] Reduce wheel size by excluding libtriton.so from auditwheel repair | 2026-05-07 | memory_primitives, compiler_runtime, benchmark_test | `cibuildwheel`'s default `auditwheel repair` step scans `libtriton.so` for its `DT_NEEDED` entries and bundles every transitive shared library into `triton.libs/` inside the wheel. This inflates the published nightly wheel to ~330MB. PyTorch's `build_triton_wheel.py` avoids this by passing `--exclud... | other: `.github/workflows/wheels.yml` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |

### Scheduler / Autotune

Use this section for: Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing.
NCU first look: SM occupancy, waves/SM, active cycles, tail effects, and load imbalance.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#10236](https://github.com/triton-lang/triton/pull/10236) Custom Intermediate split-k dtype  | 2026-05-06 | scheduler_autotune, gemm_quant, benchmark_test, compiler_runtime | Adds PrecisionConfig.intermediate_out_dtype so split-K matmul can store intermediate scratch outputs in a configurable dtype, defaulting to the existing fp32 behavior. Also threads the dtype through split-K allocation and opt-flag shared-memory sizing, with tests covering bf16 scratch allocation and... | kernel: `python/triton_kernels/triton_kernels/matmul.py`, `python/triton_kernels/triton_kernels/matmul_details/opt_flags.py`<br>test: `python/triton_kernels/tests/test_matmul_details/test_opt_flags_split_k.py` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#10125](https://github.com/triton-lang/triton/pull/10125) Add AutotuneListener hook to triton knobs | 2026-04-30 | scheduler_autotune, compiler_runtime, benchmark_test | Add an `AutotuneListener` callback hook to `knobs.autotuning`, mirroring the existing `CompilationListener` pattern. The listener is invoked after each autotune decision (fresh benchmark or disk cache hit) with the following keyword arguments: - `fn` — the unwrapped `JITFunction` - `key` — the autot... | kernel: `python/triton/knobs.py`, `python/triton/runtime/autotuner.py`<br>test: `python/test/unit/runtime/test_autotune_listener.py` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |

### Architecture / Pipeline

Use this section for: Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages.
NCU first look: Tensor pipe %, memory pipe utilization, barrier stalls, wait groups, and occupancy.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#10095](https://github.com/triton-lang/triton/pull/10095) [Frontend] Promote `_aggregate` to public | 2026-04-21 | arch_pipeline, gemm_quant, compiler_runtime, memory_primitives | Instead of the previous private name `triton.language.core._aggregate`, aggregates are now publicly available as `triton.aggregate` and `gluon.aggregate`. The old spelling is also retained for backward compatibility. | kernel: `python/examples/gluon/01-attention-forward.py`, `python/examples/gluon/05-moe-bmm1-fused-gather.py`, `python/triton/__init__.py`, `python/triton/experimental/gluon/__init__.py`<br>test: `python/test/gluon/test_frontend.py`, `python/test/unit/language/test_compile_errors.py`, `python/test/unit/language/test_frontend.py`<br>wrapper: `python/examples/gluon/02-conv-common.py`, `python/examples/gluon/02-conv-dgrad.py`, `python/examples/gluon/02-conv-fprop.py`, `python/examples/gluon/02-conv-wgrad.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#10089](https://github.com/triton-lang/triton/pull/10089) [Gluon][Translator] Hopper support | 2026-04-22 | arch_pipeline, compiler_runtime, memory_primitives, benchmark_test | [Gluon][Translator] Hopper support | kernel: `python/triton/compiler/code_generator.py`, `python/triton/tools/triton_to_gluon_translator/amd_helpers.py`, `python/triton/tools/triton_to_gluon_translator/blackwell_helpers.py`, `python/triton/tools/triton_to_gluon_translator/common_helpers.py`<br>test: `python/test/unit/language/test_frontend.py`, `python/test/unit/tools/test_slice_kernel.py`, `python/test/unit/tools/test_stable_toposort.py`, `python/test/unit/tools/test_triton_to_gluon.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#10261](https://github.com/triton-lang/triton/pull/10261) [NFC] Delete dead variables and functions | 2026-05-08 | arch_pipeline, norm_elementwise, compiler_runtime, gemm_quant | Delete some dead variables and functions identified by running with `-Wunused`. --- [//]: (BEGIN SAPLING FOOTER) * 10267 * __->__ 10261 | kernel: `lib/Conversion/TritonGPUToLLVM/ElementwiseOpToLLVM.cpp`, `lib/Dialect/Triton/IR/Ops.cpp`, `lib/Dialect/TritonGPU/IR/Dialect.cpp`, `lib/Dialect/TritonGPU/IR/LinearLayoutConversions.cpp`<br>wrapper: `lib/Dialect/Gluon/Transforms/ResolveAutoEncodings.cpp`, `lib/Target/LLVMIR/LLVMDILocalVariable.cpp`, `lib/Target/LLVMIR/LLVMDIScope.cpp`, `python/src/interpreter.cc`<br>other: `include/triton/Dialect/Triton/IR/TritonOpInterfaces.td` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#10083](https://github.com/triton-lang/triton/pull/10083) [Gluon] Rename async_copy_... -> async_{load,store} | 2026-04-20 | arch_pipeline, memory_primitives, compiler_runtime | This renames both the ampere style async_copy and tma functions. The original names are pretty much copied directly from ptx and while they are very descriptive, are also rather verbose. IMO these shorter names have a better glance-value and better conveys the intent of the programmer. I've also kep... | kernel: `python/examples/gluon/01-attention-forward.py`, `python/triton/experimental/gluon/language/nvidia/ampere/async_copy.py`, `python/triton/experimental/gluon/language/nvidia/blackwell/tma.py`, `python/triton/experimental/gluon/language/nvidia/hopper/tma.py`<br>test: `python/test/gluon/test_consan.py`, `python/test/gluon/test_core.py`, `python/test/gluon/test_frontend.py`<br>wrapper: `python/examples/gluon/02-convolution.py`, `python/examples/gluon/03-matmul-multicta.py`, `python/examples/gluon/04-2cta-block-scale-matmul.py`, `python/tutorials/gluon/03-async-copy.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#10058](https://github.com/triton-lang/triton/pull/10058) Limit warp specialization partition attrs to WS pass | 2026-04-16 | arch_pipeline, norm_elementwise, compiler_runtime, benchmark_test | Limit warp specialization partition attrs to WS pass | kernel: `include/triton/Dialect/TritonGPU/IR/Dialect.h`, `lib/Dialect/TritonGPU/IR/Dialect.cpp`, `lib/Dialect/TritonGPU/Transforms/WarpSpecialization/AutomaticWarpSpecialization.cpp`, `lib/Dialect/TritonGPU/Transforms/WarpSpecialization/Partition.cpp`<br>test: `test/TritonGPU/automatic-warp-specialization.mlir`, `test/TritonGPU/partition-verifier-locality.mlir`<br>wrapper: `third_party/nvidia/lib/Dialect/NVWS/Transforms/AssignStagePhase.cpp`, `third_party/nvidia/lib/Dialect/NVWS/Transforms/HoistTmemStore.cpp`, `third_party/nvidia/lib/Dialect/NVWS/Transforms/InsertAref.cpp`, `third_party/nvidia/lib/Dialect/NVWS/Transforms/InsertTmemAref.cpp` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#10148](https://github.com/triton-lang/triton/pull/10148) [Reland][NVIDIA] Support swizzle 0 TMA + MMA for Hopper and Blackwell | 2026-04-30 | arch_pipeline, norm_elementwise, compiler_runtime, benchmark_test | Compared to https://github.com/triton-lang/triton/pull/9931: * When looking for an equivalent LL in the loop https://github.com/masahi/triton/blob/62b3a08c1b205522991148b4d0b6d761e0ecb369/lib/Dialect/TritonNvidiaGPU/Transforms/OptimizeDescriptorEncoding.cppL69-L79, we are now guarding against an LL... | kernel: `include/triton/Dialect/TritonGPU/IR/LinearLayoutConversions.h`, `include/triton/Dialect/TritonGPU/Transforms/DescriptorMemoryLayouts.h`, `lib/Dialect/TritonGPU/IR/Dialect.cpp`, `lib/Dialect/TritonGPU/IR/LinearLayoutConversions.cpp`<br>test: `test/TritonGPU/dot-operands.mlir`, `test/TritonNvidiaGPU/optimize_descriptor_encoding.mlir` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#10112](https://github.com/triton-lang/triton/pull/10112) [FPSAN] Add support for wgmma in fpsan | 2026-04-23 | arch_pipeline, compiler_runtime, norm_elementwise, benchmark_test | Add in-register emulation of Hopper's wgmma op to the fpsan. Fixes small bug in warpgroup_mma in the frontend. | kernel: `lib/Dialect/TritonInstrument/Transforms/FpSanitizer.cpp`, `python/triton/experimental/gluon/language/nvidia/hopper/__init__.py`<br>test: `python/test/gluon/test_fpsan.py`, `test/TritonGPU/fpsan.mlir` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#10275](https://github.com/triton-lang/triton/pull/10275) Support zero-sized Hopper MX scale layouts | 2026-05-11 | arch_pipeline, gemm_quant, compiler_runtime, benchmark_test | Fix Hopper MX scale layout conversion for zero-sized scale tensors. `torch.nn.functional.pad` rejects some valid zero-numel outputs. This shows up in Hopper scale swizzle after RHS transpose: for example, `(0, 64)` becomes `(64, 0)`, and row padding still leaves a zero-width output. The fix mirrors... | kernel: `python/triton_kernels/triton_kernels/tensor_details/layout_details/hopper_scale.py`, `python/triton_kernels/triton_kernels/tensor_details/layout_details/strided.py`<br>test: `python/triton_kernels/tests/test_tensor_details/test_layout_hopper.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#10196](https://github.com/triton-lang/triton/pull/10196) [MULTICTA] Fix multicast pattern for tcgen05_mma_scaled | 2026-05-04 | arch_pipeline, compiler_runtime, benchmark_test | The helper we had was missing the necessary `two_ctas` flag to compute the number of arrivals correctly for an mma that goes into a multicast TMA. I changed the API all across I think it would be much better if we just had a TTNG_InitMmaBarrierOp as proposed in https://github.com/triton-lang/triton/... | kernel: `lib/Dialect/TritonNvidiaGPU/IR/Ops.cpp`, `python/triton/experimental/gluon/language/nvidia/blackwell/__init__.py`<br>test: `python/test/gluon/test_consan.py`, `python/test/gluon/test_core.py`<br>wrapper: `python/examples/gluon/03-matmul-multicta.py`, `python/examples/gluon/04-2cta-block-scale-matmul.py`, `python/tutorials/gluon/14-multicta.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#10126](https://github.com/triton-lang/triton/pull/10126) Do not send f64 dots through tcgen05 | 2026-04-29 | arch_pipeline, gemm_quant, compiler_runtime, benchmark_test | nvidia blackwell does not support f64 https://docs.nvidia.com/cutlass/4.3.1/media/docs/cpp/blackwell_functionality.htmlblackwell-sm100-gemms I'm not sure if you intend to support f64 in your dots overall, but I couldn't find it in the documentation so I thought preventing it here makes sense. | test: `test/TritonGPU/accelerate-matmul.mlir`<br>wrapper: `lib/Analysis/Utility.cpp` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#10180](https://github.com/triton-lang/triton/pull/10180) [Backend] Respect IEEE input precision for tcgen05 | 2026-05-01 | arch_pipeline, gemm_quant, compiler_runtime, benchmark_test | Fixes 10176 Currently the version==5 path early-exits before the input precision check. | test: `test/TritonGPU/accelerate-matmul.mlir`<br>wrapper: `lib/Analysis/Utility.cpp` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#10040](https://github.com/triton-lang/triton/pull/10040) [Gluon] Expose TMA atomic ops | 2026-04-15 | arch_pipeline, compiler_runtime, benchmark_test | tbh, I think this is also the way we should represent this in our IR. I will add an end-to-end test once https://github.com/triton-lang/triton/pull/9977 is merged (I will extend the test in that PR to test this op) | kernel: `python/triton/experimental/gluon/language/nvidia/blackwell/tma.py`, `python/triton/experimental/gluon/language/nvidia/hopper/tma.py`<br>test: `python/test/gluon/test_frontend.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#10035](https://github.com/triton-lang/triton/pull/10035) [nvidia] Always insert bar sync before all mbarrier arrives | 2026-04-15 | arch_pipeline, compiler_runtime, benchmark_test | Barrier arrives are codegen'd to be executed by a leader warp. When mbarriers are used to create a ringbuffer between two asynchronously execution partitions, deadlocks can occur if the leader warp signals the ringbuffer before other warps in the partition finish waiting. Example: code block Current... | kernel: `third_party/nvidia/lib/TritonNVIDIAGPUToLLVM/BarrierOpToLLVM.cpp`, `third_party/nvidia/lib/TritonNVIDIAGPUToLLVM/DotOpToLLVM/MMAv5.cpp`<br>test: `test/Analysis/test-membar-ttng.mlir`, `test/Conversion/tritongpu_to_llvm_blackwell.mlir`, `test/Conversion/tritonnvidiagpu_to_llvm.mlir`<br>wrapper: `lib/Analysis/Membar.cpp` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#10217](https://github.com/triton-lang/triton/pull/10217) [ConSan] Fix missing captureBytes argument in passToWarpSpecialize call | 2026-05-04 | arch_pipeline, compiler_runtime | https://github.com/triton-lang/triton/pull/10171 changed the API for passToWarpSpecialize. Need to update stale callsites. | kernel: `lib/Dialect/TritonInstrument/IR/Utility.cpp` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#10066](https://github.com/triton-lang/triton/pull/10066) Fix maskSpanAffineOffset bitmask in ldmatrix/stmatrix subslice check | 2026-04-20 | arch_pipeline, compiler_runtime | The subslice safety check in lowerLdStMatrix uses a bitmask to verify that affine offsets don't touch the contiguous part of the tile's offset dimension. It was using getOutDimSizeLog2 (which returns log2 of the size) instead of getOutDimSize (the actual size) to construct this mask. For outDimSize=... | kernel: `third_party/amd/lib/TritonAMDGPUToLLVM/MemoryOpToLLVM.cpp`, `third_party/nvidia/lib/TritonNVIDIAGPUToLLVM/Utility.cpp` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |

### Compiler / Runtime

Use this section for: Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests.
NCU first look: Generated kernel shape, launch overhead, occupancy, register pressure, and compile-time-selected schedule.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#10052](https://github.com/triton-lang/triton/pull/10052) [Consan] Support CLC | 2026-04-18 | compiler_runtime, benchmark_test, scheduler_autotune, arch_pipeline, norm_elementwise | CLC gets its own partition, running over threads 48-63. We model CLC as we model TMA writes, via a Barrier::EffectWrites. The idea of this mode is that we link all the writes on the op to the barrier. We also annotate in the table `barrierWriteRecipients` which CTAs will become visible once we wait... | kernel: `include/triton/Dialect/TritonInstrument/IR/FunctionBuilder.h`, `include/triton/Dialect/TritonInstrument/IR/Utility.h`, `include/triton/Dialect/TritonInstrument/Transforms/ConSanTargetHooks.h`, `lib/Dialect/TritonInstrument/IR/FunctionBuilder.cpp`<br>benchmark: `lib/Dialect/TritonInstrument/Transforms/ConcurrencySanitizer.cpp`<br>test: `python/test/gluon/test_consan.py`, `test/TritonGPU/amd/amd-consan.mlir`, `test/TritonGPU/consan.mlir`, `test/TritonNvidiaGPU/membar-cluster.mlir`<br>wrapper: `lib/Analysis/BufferRegion.cpp`<br>docs: `include/triton/Dialect/TritonInstrument/IR/TritonInstrument.md` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10243](https://github.com/triton-lang/triton/pull/10243) [BACKEND] Reinterpreted memory should represent the same amount of memory | 2026-05-07 | compiler_runtime, benchmark_test, attention_kv, arch_pipeline | We also disallow performing reinterpret layouts of subslices as they'd be rather cursed when the subslice is not contiguous. Instead we ask the user to reinterpret the base layout instead. We also improve the API for `_reinterpret` in gluon by allowing to pass in just the attributes you want to chan... | kernel: `lib/Dialect/TritonGPU/IR/Ops.cpp`, `python/examples/gluon/01-attention-forward.py`, `python/triton/experimental/gluon/language/_core.py`, `python/triton/experimental/gluon/language/nvidia/blackwell/__init__.py`<br>test: `python/test/gluon/test_core.py`, `python/test/gluon/test_frontend.py`, `test/Analysis/test-membar.mlir`, `test/Conversion/tritongpu_to_llvm.mlir`<br>wrapper: `python/tutorials/gluon/07-persistence.py`<br>other: `include/triton/Dialect/TritonGPU/IR/TritonGPUOps.td` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10050](https://github.com/triton-lang/triton/pull/10050) [GLUON] Infer CLC multicast from multicta | 2026-04-16 | compiler_runtime, scheduler_autotune, arch_pipeline | It doesn't make sense currently to have CLC in a multicta context without multicast. As such, we hide this flag and we infer it automatically. The reason why it doesn't make sense it's because in multicta just one CTA is allowed to request to cancel. In CUDA you can imagine patterns like doing CLC w... | kernel: `python/triton/experimental/gluon/language/nvidia/blackwell/clc.py`, `third_party/nvidia/lib/TritonNVIDIAGPUToLLVM/BarrierOpToLLVM.cpp`<br>test: `python/test/gluon/test_core.py`, `test/Conversion/clc_to_llvm.mlir`, `test/TritonNvidiaGPU/invalid.mlir`, `third_party/proton/test/test_instrumentation.py`<br>wrapper: `python/examples/gluon/03-matmul-multicta.py`, `python/examples/gluon/04-2cta-block-scale-matmul.py`, `python/src/gluon_ir.cc`, `python/tutorials/gluon/12-cluster-launch-control.py`<br>other: `include/triton/Dialect/TritonNvidiaGPU/IR/TritonNvidiaGPUOps.td` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10051](https://github.com/triton-lang/triton/pull/10051) [GLUON] Infer CLC multicast from multicta | 2026-04-16 | compiler_runtime, scheduler_autotune, arch_pipeline | It doesn't make sense currently to have CLC in a multicta context without multicast. As such, we hide this flag and we infer it automatically. The reason why it doesn't make sense it's because in multicta just one CTA is allowed to request to cancel. In CUDA you can imagine patterns like doing CLC w... | kernel: `python/triton/experimental/gluon/language/nvidia/blackwell/clc.py`, `third_party/nvidia/lib/TritonNVIDIAGPUToLLVM/BarrierOpToLLVM.cpp`<br>test: `python/test/gluon/test_core.py`, `test/Conversion/clc_to_llvm.mlir`, `test/TritonNvidiaGPU/invalid.mlir`, `third_party/proton/test/test_instrumentation.py`<br>wrapper: `python/examples/gluon/03-matmul-multicta.py`, `python/examples/gluon/04-2cta-block-scale-matmul.py`, `python/src/gluon_ir.cc`, `python/tutorials/gluon/12-cluster-launch-control.py`<br>other: `include/triton/Dialect/TritonNvidiaGPU/IR/TritonNvidiaGPUOps.td` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10046](https://github.com/triton-lang/triton/pull/10046) [FPSAN] Fix crash on incorrect layout for tmem copy | 2026-04-16 | compiler_runtime, memory_primitives, norm_elementwise, benchmark_test | TMEMCopy pattern is using stale tmem encoding. This may cause a crash in the validator if the encodings mismatch. | kernel: `lib/Dialect/TritonInstrument/Transforms/FpSanitizer.cpp`<br>test: `python/test/gluon/test_fpsan.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10118](https://github.com/triton-lang/triton/pull/10118) [FPSAN] Add support for batched matmul, make fpsan errors fatal | 2026-04-25 | compiler_runtime, gemm_quant, norm_elementwise, benchmark_test | Support batched tt.dot operands in fpsan emulation, make fpsan diagnostics fatal, and add lit plus Gluon end-to-end coverage for rank-3 dot payload correctness. | kernel: `lib/Dialect/TritonInstrument/Transforms/FpSanitizer.cpp`<br>test: `python/test/gluon/test_fpsan.py`, `test/TritonGPU/fpsan.mlir` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10173](https://github.com/triton-lang/triton/pull/10173) [PROTON] Centralize and categorize exception handling | 2026-05-13 | compiler_runtime, attention_kv, benchmark_test | [PROTON] Centralize and categorize exception handling | kernel: `third_party/proton/csrc/include/Data/Metric.h`, `third_party/proton/csrc/include/Data/PhaseStore.h`, `third_party/proton/csrc/include/Driver/Dispatch.h`, `third_party/proton/csrc/include/Utility/Errors.h`<br>benchmark: `third_party/proton/csrc/lib/Profiler/Cupti/CuptiPCSampling.cpp`, `third_party/proton/csrc/lib/Profiler/Cupti/CuptiProfiler.cpp`, `third_party/proton/csrc/lib/Profiler/GPUProfiler.cpp`, `third_party/proton/csrc/lib/Profiler/Graph.cpp`<br>wrapper: `third_party/proton/common/include/TraceDataIO/ByteSpan.h`, `third_party/proton/common/include/TraceDataIO/EntryDecoder.h`, `third_party/proton/common/lib/TraceDataIO/ByteSpan.cpp`, `third_party/proton/common/lib/TraceDataIO/Parser.cpp` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10192](https://github.com/triton-lang/triton/pull/10192) [CONSAN] Fix deadlock detection in WS code | 2026-05-02 | compiler_runtime, benchmark_test, norm_elementwise | Before, we statically looked at the mask within a WS code to figure out whether there was a deadlock. This is problematic because perhaps one partition finishes early (rather common when using CLC) and it doesn't deadlock, but the others deadlock. Here we store a mask per CTA globally. The lifetime... | kernel: `include/triton/Dialect/TritonInstrument/IR/FunctionBuilder.h`, `include/triton/Dialect/TritonInstrument/IR/Utility.h`, `lib/Dialect/TritonGPU/IR/Ops.cpp`, `lib/Dialect/TritonInstrument/IR/FunctionBuilder.cpp`<br>benchmark: `lib/Dialect/TritonInstrument/Transforms/ConcurrencySanitizer.cpp`<br>test: `python/test/gluon/test_consan.py`, `test/TritonGPU/amd/amd-consan.mlir`, `test/TritonGPU/consan.mlir`<br>other: `include/triton/Dialect/TritonGPU/IR/TritonGPUOps.td` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10212](https://github.com/triton-lang/triton/pull/10212) [CONSAN] Multi CTA model v2 | 2026-05-08 | compiler_runtime, benchmark_test, norm_elementwise | Reviewers This PR includes https://github.com/triton-lang/triton/pull/10167 and https://github.com/triton-lang/triton/pull/10196 which I'll kill after they are merged. It is also separated in logical commits so that reviewing is simpler, as I had to add a few optimisations to generate less code as i... | kernel: `include/triton/Dialect/TritonInstrument/IR/FunctionBuilder.h`, `include/triton/Dialect/TritonInstrument/IR/Utility.h`, `include/triton/Dialect/TritonInstrument/Transforms/ConSanTargetHooks.h`, `lib/Conversion/TritonInstrumentToLLVM/InstrumentationToLLVM.cpp`<br>benchmark: `lib/Dialect/TritonInstrument/Transforms/ConcurrencySanitizer.cpp`<br>test: `python/test/gluon/test_consan.py`, `test/Conversion/tritoninstrument_to_llvm.mlir`, `test/TritonGPU/amd/amd-consan.mlir`, `test/TritonGPU/consan.mlir`<br>wrapper: `third_party/nvidia/backend/compiler.py`<br>docs: `include/triton/Dialect/TritonInstrument/IR/TritonInstrument.md` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#9765](https://github.com/triton-lang/triton/pull/9765) Introduce GenericLinearEncodingAttr | 2026-04-23 | compiler_runtime, norm_elementwise, benchmark_test | Introduce GenericLinearEncodingAttr, a relaxed variant of LinearEncodingAttr that supports swizzled warp bases and non-bijective (but still surjective) layouts. This enables the safe use of non-injective (after removing broadcast registers) or warp-swizzled layouts for ops that don't depend on these... | kernel: `include/triton/Dialect/TritonGPU/IR/Dialect.h`, `lib/Conversion/TritonGPUToLLVM/ConvertLayoutOpToLLVM.cpp`, `lib/Conversion/TritonGPUToLLVM/Utility.cpp`, `lib/Dialect/TritonGPU/IR/Dialect.cpp`<br>test: `python/test/gluon/test_lowerings.py`, `test/TritonGPU/invalid.mlir`, `test/TritonNvidiaGPU/invalid.mlir`<br>wrapper: `lib/Analysis/AxisInfo.cpp`, `python/src/gluon_ir.cc`<br>other: `include/triton/Dialect/TritonGPU/IR/TritonGPUAttrDefs.td`, `include/triton/Dialect/TritonGPU/IR/TritonGPUAttrInterfaces.td` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10183](https://github.com/triton-lang/triton/pull/10183) [GLUON] Generalize `local_atomic_scatter_add` to `local_atomic_scatter_rmw` | 2026-05-05 | compiler_runtime, memory_primitives, benchmark_test | [GLUON] Generalize `local_atomic_scatter_add` to `local_atomic_scatter_rmw` | kernel: `lib/Dialect/TritonGPU/IR/Ops.cpp`, `python/triton/experimental/gluon/language/_core.py`, `python/triton/experimental/gluon/language/_semantic.py`, `third_party/nvidia/include/TritonNVIDIAGPUToLLVM/AtomicPTXBuilder.h`<br>test: `python/test/gluon/test_core.py`, `python/test/gluon/test_frontend.py`, `test/Conversion/tritongpu_to_llvm.mlir`, `test/TritonGPU/invalid.mlir`<br>wrapper: `lib/Analysis/Allocation.cpp`, `lib/Analysis/Membar.cpp`, `python/src/gluon_ir.cc`<br>other: `include/triton/Dialect/TritonGPU/IR/TritonGPUOps.td` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10271](https://github.com/triton-lang/triton/pull/10271) [PROTON] Group metadata kernels under the corresponding triton operator if available | 2026-05-11 | compiler_runtime, benchmark_test | Example code block | kernel: `third_party/proton/csrc/Proton.cpp`, `third_party/proton/csrc/include/Context/Context.h`, `third_party/proton/csrc/include/Data/Data.h`, `third_party/proton/csrc/include/Session/Session.h`<br>benchmark: `third_party/proton/csrc/include/Profiler/Graph.h`, `third_party/proton/csrc/include/Profiler/Instrumentation/InstrumentationMetadata.h`, `third_party/proton/csrc/include/Profiler/Instrumentation/InstrumentationProfiler.h`, `third_party/proton/csrc/lib/Profiler/Cupti/CuptiProfiler.cpp`<br>test: `third_party/proton/test/test_viewer.py`<br>wrapper: `third_party/proton/proton/hooks/instrumentation.py`, `third_party/proton/proton/hooks/launch.py`, `third_party/proton/proton/metric.py`, `third_party/proton/proton/scope.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10132](https://github.com/triton-lang/triton/pull/10132) [TritonGPU] Split RemoveLayoutConversions cleanup; tolerate SCF non-convergence | 2026-04-29 | compiler_runtime, scheduler_autotune, norm_elementwise, benchmark_test | Fixes pytorch/pytorch180908. Compiling certain kernels on Triton 3.7 fails with `RuntimeError: PassManager::run failed` and no diagnostic. The trigger is any IR that reaches `RemoveLayoutConversions` cleanup with roughly ten or more unrolled `scf.if` regions carrying tensor values — e.g. `tl.static_... | kernel: `lib/Dialect/TritonGPU/Transforms/RemoveLayoutConversions.cpp`<br>test: `test/TritonGPU/remove-layout-conversions-scf-cleanup.mlir` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10150](https://github.com/triton-lang/triton/pull/10150) [triton_kernels] Optimize matmul metadata metrics | 2026-04-29 | compiler_runtime, gemm_quant, benchmark_test | Why Proton's Triton hook needs matmul launch metadata to report `flops*` and `bytes` without CPU syncs. For ragged matmuls, the existing no-sync metadata path computed those values through several small Torch GPU ops over `slice_sizes`. That path is numerically correct, but it adds avoidable metadat... | kernel: `python/triton_kernels/triton_kernels/matmul_details/_common.py`<br>benchmark: `python/triton_kernels/bench/bench_matmul_metadata.py`<br>test: `python/triton_kernels/tests/test_matmul_metadata.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10270](https://github.com/triton-lang/triton/pull/10270) Fix negation of +0.0 | 2026-05-08 | compiler_runtime, benchmark_test, norm_elementwise | Triton was compiling `-x` into `0 - x`, which is wrong for negating `+0.0` (positive floating-point zero). Instead, add an fneg operation that directly negates floats. New contributor declaration - [x] I am not making a trivial change, such as fixing a typo in a comment. - [x] I have written a PR de... | kernel: `lib/Conversion/TritonGPUToLLVM/ElementwiseOpToLLVM.cpp`, `lib/Conversion/TritonToTritonGPU/TritonToTritonGPUPass.cpp`, `python/triton/language/semantic.py`, `python/triton/runtime/interpreter.py`<br>test: `python/test/unit/language/test_core.py`<br>wrapper: `python/src/ir.cc` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10293](https://github.com/triton-lang/triton/pull/10293) [GLUON] Support broadcasting of shared scatter operations and unify operand checks | 2026-05-14 | compiler_runtime, memory_primitives, benchmark_test | [GLUON] Support broadcasting of shared scatter operations and unify operand checks | kernel: `python/triton/experimental/gluon/language/_core.py`, `python/triton/experimental/gluon/language/_semantic.py`<br>test: `python/test/gluon/test_core.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10232](https://github.com/triton-lang/triton/pull/10232) [fpsan] Make embed/unembed IR nodes to enable canonicalization | 2026-05-06 | compiler_runtime, norm_elementwise, benchmark_test | <git-pr-chain> Commits in this PR 1. [fpsan] Make embed/unembed IR nodes to enable canonicalization Currently the embed and unembed operations generate a tonne of IR for every operation, and then we rely on llvm instcombine to realize that the unembed -> embed flow is a no-op. This instead adds IR n... | kernel: `include/triton/Conversion/TritonGPUToLLVM/PatternTritonGPUOpToLLVM.h`, `lib/Conversion/TritonInstrumentToLLVM/FpSanToLLVM.cpp`, `lib/Dialect/TritonInstrument/IR/Ops.cpp`, `lib/Dialect/TritonInstrument/Transforms/FpSanitizer.cpp`<br>test: `test/Conversion/tritoninstrument_to_llvm.mlir`, `test/TritonGPU/fpsan.mlir`<br>other: `include/triton/Dialect/TritonInstrument/IR/TritonInstrumentOps.td`, `lib/Conversion/TritonInstrumentToLLVM/CMakeLists.txt` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10306](https://github.com/triton-lang/triton/pull/10306) [fpsan] Support arith.negf | 2026-05-13 | compiler_runtime, norm_elementwise, benchmark_test | Since we now generate arith.negf, it needs to be supported by fpsan. | kernel: `lib/Dialect/TritonInstrument/Transforms/FpSanitizer.cpp`<br>test: `python/test/gluon/test_fpsan.py`, `test/TritonGPU/fpsan.mlir`<br>docs: `docs/programming-guide/chapter-3/fpsan.rst` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10100](https://github.com/triton-lang/triton/pull/10100) [GLUON] `shared_atomic_add` implementation | 2026-04-30 | compiler_runtime, benchmark_test | [GLUON] `shared_atomic_add` implementation | kernel: `include/triton/Conversion/TritonGPUToLLVM/Utility.h`, `lib/Conversion/TritonGPUToLLVM/MemoryOpToLLVM.cpp`, `lib/Conversion/TritonGPUToLLVM/Utility.cpp`, `lib/Dialect/TritonGPU/IR/Ops.cpp`<br>test: `python/test/gluon/test_core.py`, `python/test/gluon/test_frontend.py`, `test/Conversion/tritongpu_to_llvm.mlir`, `test/TritonGPU/invalid.mlir`<br>wrapper: `lib/Analysis/Allocation.cpp`, `lib/Analysis/Membar.cpp`, `python/src/gluon_ir.cc`<br>other: `include/triton/Dialect/TritonGPU/IR/TritonGPUOps.td` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10114](https://github.com/triton-lang/triton/pull/10114) [Gluon][Examples] Add 2 CTA to 05-moe-bmm1-fused-gather | 2026-04-23 | compiler_runtime, moe_routing, memory_primitives | 2cta vs. 1cta is flat in the medium batch sizes because it's not actually faster and disabled by the config selector. Gains are primarily concentrated at the large-ish batch sizes, and some small wins at low batch sizes. With 2CTA, some of the low-ish batch sizes are able to hit nearly 90% of roofli... | kernel: `python/examples/gluon/05-moe-bmm1-fused-gather.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10199](https://github.com/triton-lang/triton/pull/10199) [Proton] Normalize roofline metric dtypes | 2026-05-02 | compiler_runtime, norm_elementwise, benchmark_test | Proton currently preserves the Python scalar type returned by `launch_metadata`, while device tensor metrics are normalized to either double or int64 in `transform_tensor_metrics()`. That means the same built-in roofline metric can be registered with different types depending on which producer runs... | test: `third_party/proton/test/test_api.py`<br>wrapper: `third_party/proton/proton/hooks/launch.py`, `third_party/proton/proton/metric.py`, `third_party/proton/proton/viewer.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10247](https://github.com/triton-lang/triton/pull/10247) [NFC] Remove some dead functions | 2026-05-07 | compiler_runtime, gemm_quant, norm_elementwise | I asked codex to find and remove unused functions. This is the result after leaving in a few debug dump helpers. | kernel: `lib/Conversion/TritonInstrumentToLLVM/GSanToLLVM.cpp`, `lib/Conversion/TritonToTritonGPU/TritonToTritonGPUPass.cpp`, `lib/Dialect/TritonGPU/Transforms/AccelerateMatmul.cpp`, `lib/Dialect/TritonInstrument/Transforms/FpSanitizer.cpp`<br>wrapper: `python/build_helpers.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10047](https://github.com/triton-lang/triton/pull/10047) [gluon][examples] MoE bmm1 in Gluon | 2026-04-17 | compiler_runtime, moe_routing, memory_primitives | The main beneficial optimizations are: 1. Separate loader for weight and weight scales, allowing asymmetric pipelining. Potentially we can have a separate partition for scale factor load as well, but I didn't experiment with it. 2. Epilogue optimizations to decrease the number of instructions. Epilo... | kernel: `python/examples/gluon/05-moe-bmm1-fused-gather.py`, `python/triton/experimental/gluon/language/__init__.py`, `python/triton/experimental/gluon/language/_core.py`<br>other: `Makefile` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10059](https://github.com/triton-lang/triton/pull/10059) [TritonGPU] Coalesce integer atomics | 2026-04-19 | compiler_runtime, benchmark_test, norm_elementwise | Hello! The coalescing pass currently chooses `sizePerThread` for atomics using the same pointer alignment / contiguity heuristic as regular stores. For some atomic lowerings this is too optimistic: the pass will select a 128-bit per-thread layout even when the backend lowers the atomic as a narrower... | kernel: `lib/Dialect/TritonGPU/Transforms/Utility.cpp`<br>test: `test/TritonGPU/coalesce.mlir` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10039](https://github.com/triton-lang/triton/pull/10039) [FPSan] explicit lowerings for fmin/fmax | 2026-04-16 | compiler_runtime, norm_elementwise, benchmark_test | [FPSan] explicit lowerings for fmin/fmax | kernel: `lib/Dialect/TritonInstrument/Transforms/FpSanitizer.cpp`<br>test: `python/test/gluon/test_fpsan.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10220](https://github.com/triton-lang/triton/pull/10220) [PROTON] Using sequence id to find data entry instead of relying on the capturing order | 2026-05-06 | compiler_runtime, benchmark_test | [PROTON] Using sequence id to find data entry instead of relying on the capturing order | kernel: `third_party/proton/csrc/Proton.cpp`, `third_party/proton/csrc/include/Data/Metric.h`, `third_party/proton/csrc/lib/Data/Metric.cpp`<br>benchmark: `third_party/proton/csrc/include/Profiler/GPUProfiler.h`, `third_party/proton/csrc/include/Profiler/Graph.h`, `third_party/proton/csrc/lib/Profiler/Cupti/CuptiProfiler.cpp`, `third_party/proton/csrc/lib/Profiler/Graph.cpp`<br>wrapper: `third_party/proton/proton/metric.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#9572](https://github.com/triton-lang/triton/pull/9572) [Language] Add field inheritance, defaults, and aggregate_replace to @aggregate | 2026-05-08 | compiler_runtime, benchmark_test | Adds dataclass-like OOP features to the `@aggregate` decorator, as discussed in 8781: - **Field inheritance**: child aggregates inherit parent fields via MRO resolution - **Default values**: fields with defaults can be omitted from constructor; defaults propagate through inheritance - **`aggregate_r... | kernel: `python/triton/experimental/gluon/language/__init__.py`, `python/triton/experimental/gluon/language/_core.py`, `python/triton/language/__init__.py`, `python/triton/language/core.py`<br>test: `python/test/unit/language/test_frontend.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10189](https://github.com/triton-lang/triton/pull/10189) Support returning tensors in TritonToTritonGPU | 2026-05-12 | compiler_runtime, benchmark_test | The conversion patterns in `TritonToTritonGPU` (`TritonFuncOpPattern`, `TritonCallOpPattern`, `TritonReturnOpPattern`) did not handle `tt.func` ops returning tensors: `TritonFuncOpPattern` reused the original `FunctionType` verbatim, and the `triton::FuncOp` legality check inspected only argument ty... | kernel: `lib/Conversion/TritonToTritonGPU/TritonGPUConversion.cpp`, `lib/Conversion/TritonToTritonGPU/TritonToTritonGPUPass.cpp`<br>test: `python/test/unit/language/test_core.py`, `test/Conversion/triton_to_tritongpu.mlir` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10030](https://github.com/triton-lang/triton/pull/10030) [Nvidia][Gluon] Refactor convolution fprop kernel, add wgrad and dgrad kernels  | 2026-04-21 | compiler_runtime, scheduler_autotune | This PR cleans up the Gluon convolution examples and makes the three kernels easier to understand as a family: - `02-conv-fprop.py` implements forward convolution (`fprop`) - `02-conv-wgrad.py` implements weight-gradient computation (`wgrad`) - `02-conv-dgrad.py` implements input-gradient computatio... | wrapper: `python/examples/gluon/02-conv-common.py`, `python/examples/gluon/02-conv-dgrad.py`, `python/examples/gluon/02-conv-fprop.py`, `python/examples/gluon/02-conv-wgrad.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10122](https://github.com/triton-lang/triton/pull/10122) Enable local load/store lowering with generic linear encoding | 2026-04-24 | compiler_runtime, benchmark_test | Make actionAdditiveStrides responsible for the nAdditive ≥ regsPerInst invariant instead of relying on callers pre-zeroing bases or on tileBases hack. The fix: add a regsPerInst parameter and force the first log2(regsPerInst) register bases into the additive group unconditionally, since the inner lo... | kernel: `include/triton/Tools/LayoutUtils.h`, `lib/Conversion/TritonGPUToLLVM/Utility.cpp`, `lib/Dialect/TritonGPU/IR/Dialect.cpp`, `third_party/amd/lib/TritonAMDGPUToLLVM/MemoryOpToLLVM.cpp`<br>test: `python/test/gluon/test_lowerings.py`<br>wrapper: `lib/Tools/LayoutUtils.cpp` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10074](https://github.com/triton-lang/triton/pull/10074) Fix `tl.fdiv` to respect `ieee_rounding` flag | 2026-04-21 | compiler_runtime, benchmark_test | Fixes 10073 `tl.fdiv(x, y, ieee_rounding=True)` silently ignores the `ieee_rounding` parameter. Both `a / b` and `tl.fdiv(a, b, ieee_rounding=True)` generate `div.full.f32` (non-IEEE), when the latter should generate `div.rn.f32` (IEEE-754 compliant). This was a regression — the flag was recommended... | kernel: `python/triton/language/semantic.py`<br>test: `python/test/unit/language/test_core.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10197](https://github.com/triton-lang/triton/pull/10197) [gsan] Fix uses of node->size that should be node->allocSize | 2026-05-05 | compiler_runtime, benchmark_test | <git-pr-chain> [gsan] Fix uses of node->size that should be node->allocSize When writing the allocator I initially had only `node->size` which is always a power of 2, but decided that rounding every allocation up to a power of 2 might be too pessimistic. Looks like I missed a few cases that should h... | kernel: `python/triton/experimental/gsan/src/GSanAllocator.cc`<br>test: `python/test/gsan/test_allocator.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10226](https://github.com/triton-lang/triton/pull/10226) Fix ptr->int->ptr canonicalizer if types don't match | 2026-05-05 | compiler_runtime, benchmark_test | The canonicalization of `ptr->int->ptr` to avoid the roundtrip through int does not check that both pointers have the same type. This can result in verifier errors if the types don't match. To fix this, turn the cast sequence into a bitcast, and add a pattern to flatten away identity bitcasts. | kernel: `lib/Dialect/Triton/IR/Ops.cpp`<br>test: `test/Triton/canonicalize.mlir`<br>other: `include/triton/Dialect/Triton/IR/TritonOps.td` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10042](https://github.com/triton-lang/triton/pull/10042) [Triton] Fix LLVMDILocalVariable pass crash with LLVM_EXTRACT_DI_LOCAL_VARIABLES | 2026-04-28 | compiler_runtime, benchmark_test | Summary: Fix multiple bugs in the LLVMDILocalVariable pass that caused crashes when running with LLVM_EXTRACT_DI_LOCAL_VARIABLES=1. Root cause: The MLIR DIRecursiveTypeAttrInterface uses isRecSelf=true as a placeholder and isRecSelf=false as its resolver. During MLIR-to-LLVM IR translation, the reso... | test: `test/LLVMIR/di-local-variable-with-callsite-loc.mlir`<br>wrapper: `lib/Target/LLVMIR/LLVMDILocalVariable.cpp` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10099](https://github.com/triton-lang/triton/pull/10099) Enable PartitionedSharedEncodingAttr in the memdesc_subslice lowering  | 2026-04-29 | compiler_runtime, benchmark_test | The verifier is generalized to handle partitioned-wrapping-padded encodings, and the conversion now supports slices along the partition dim by computing S = partition(L^-1(o)) at compile time and XOR-rotating the per-partition base pointers (newBases[i] = oldBases[i ^ S]). Correctness follows from l... | kernel: `lib/Conversion/TritonGPUToLLVM/ViewOpToLLVM.cpp`, `lib/Dialect/TritonGPU/IR/Ops.cpp`<br>test: `python/test/gluon/test_lowerings.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10175](https://github.com/triton-lang/triton/pull/10175) [NVIDIA] Decouple target feature queries from `TargetInfo` | 2026-04-29 | compiler_runtime, norm_elementwise | Existing `nvidia::TargetInfo` couples target feature queries like `supportLdMatrix()` with nvidia-specific llvm codegen utilties. Querying target feature availabilities based on the compute capability is useful for code in `lib/Dialect` as well. I'm proposing to add the `TargetFeatures` class under... | kernel: `include/triton/Dialect/TritonNvidiaGPU/IR/Dialect.h`, `include/triton/Dialect/TritonNvidiaGPU/IR/TargetFeatures.h`, `lib/Dialect/TritonGPU/Transforms/Utility.cpp`, `third_party/nvidia/lib/TritonNVIDIAGPUToLLVM/TargetInfo.cpp`<br>other: `include/triton/Dialect/TritonNvidiaGPU/IR/TritonNvidiaGPUOps.td` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10139](https://github.com/triton-lang/triton/pull/10139) [EZ][BACKEND] Reject memdesc reinterpret changing CTA layout size | 2026-04-27 | compiler_runtime, benchmark_test | This changes the per-CTA size of the shared memory which is quite counterintuitive and it breaks a number of invariants inside the compiler. The one I hit when doing this was the `length` we compute in buffer regions, which changes when you do this. | kernel: `lib/Dialect/TritonGPU/IR/Ops.cpp`<br>test: `test/TritonGPU/invalid.mlir`<br>other: `include/triton/Dialect/TritonGPU/IR/TritonGPUOps.td` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10156](https://github.com/triton-lang/triton/pull/10156) [BACKEND][MMAv2] Move unsupported MMA instruction checks to an early stage | 2026-04-29 | compiler_runtime, benchmark_test | [BACKEND][MMAv2] Move unsupported MMA instruction checks to an early stage | kernel: `third_party/nvidia/lib/TritonNVIDIAGPUToLLVM/DotOpToLLVM/MMAv2.cpp`<br>test: `test/Conversion/invalid_dot_op.mlir` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10219](https://github.com/triton-lang/triton/pull/10219) [Gluon] Unwrap constexpr bool in dot_scaled and associative_scan | 2026-05-05 | compiler_runtime, memory_primitives | To tight arg types before sending to bindings. | kernel: `python/triton/experimental/gluon/language/_semantic.py`, `python/triton/language/semantic.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10286](https://github.com/triton-lang/triton/pull/10286) [BACKEND] Allow reinterpret to modify the rank | 2026-05-13 | compiler_runtime, benchmark_test | We also check that when reinterpreting a pipelining buffer, the intial dimensions are the same. Addresses https://github.com/triton-lang/triton/pull/10243issuecomment-4411914971 | kernel: `lib/Dialect/TritonGPU/IR/Ops.cpp`<br>test: `test/Conversion/tritongpu_to_llvm.mlir`, `test/TritonGPU/ops.mlir` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10069](https://github.com/triton-lang/triton/pull/10069) [gluon][translator] Upstream some fixes | 2026-04-18 | compiler_runtime | * Convert `mask` to a tensor so it broadcasts correctly in gluon. NFC for triton * fix the `tl_obj_*` helpers * add an extra hook for rewriting some argument annotations | kernel: `python/triton/language/semantic.py`, `python/triton/tools/triton_to_gluon_translator/nvidia_helpers.py`, `python/triton/tools/triton_to_gluon_translator/slice_kernel.py`<br>test: `python/test/gluon/test_frontend.py`, `python/test/unit/language/test_frontend.py`, `python/test/unit/tools/test_slice_kernel.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10269](https://github.com/triton-lang/triton/pull/10269) [triton-ext] Allow testing triton plugins in isolation | 2026-05-11 | compiler_runtime, benchmark_test | Here's another ease-of-use commit that I'm separating out from an unrelated change. This makes it easy to test Triton's plugin system with a single command: `make test-plugins`. | other: `Makefile` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10227](https://github.com/triton-lang/triton/pull/10227) [Frontend] Unwrap constexpr before semantic | 2026-05-05 | compiler_runtime | Small update to 10219 Unwrapping constexpr is usually done in core.py, not semantic.py. Keeping it in one place makes it easier to spot omissions. | kernel: `python/triton/experimental/gluon/language/_semantic.py`, `python/triton/experimental/gluon/language/amd/_ops.py`, `python/triton/experimental/gluon/language/amd/cdna4/__init__.py`, `python/triton/experimental/gluon/language/amd/gfx1250/__init__.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10048](https://github.com/triton-lang/triton/pull/10048) [triton] Python typing improvements | 2026-04-16 | compiler_runtime | [triton] Python typing improvements | kernel: `python/triton/experimental/gluon/_runtime.py`, `python/triton/language/core.py`, `python/triton/runtime/jit.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10294](https://github.com/triton-lang/triton/pull/10294) Use ptxas 13.1.80 for all NVIDIA targets | 2026-05-13 | compiler_runtime | Use ptxas 13.1.80 for all NVIDIA targets | kernel: `python/triton/knobs.py`<br>wrapper: `python/build_helpers.py`, `setup.py`, `third_party/nvidia/backend/compiler.py`<br>other: `.gitignore`, `CMakeLists.txt`, `cmake/nvidia-toolchain-version.json` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10120](https://github.com/triton-lang/triton/pull/10120) Python typing followup to #10048 | 2026-04-29 | compiler_runtime | @jeffniu-openai said `PR plz` here: https://github.com/triton-lang/triton/pull/10048issuecomment-4293849402 Making this PR on behalf of @saagarjha (he is waiting on 8913) New contributor declaration - [x] I am not making a trivial change, such as fixing a typo in a comment. - [x] I have written a PR... | kernel: `python/triton/runtime/jit.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#10117](https://github.com/triton-lang/triton/pull/10117) [Sanitizers] Run RemoveLayoutConversions when FPSan or ConSan are on | 2026-04-23 | compiler_runtime | In some triton->gluon translated code on H100, this reduces compilation by a factor of 10x. Enable RemoveLayoutConversions because presumably in sanitizer modes, we care more about compile time than performance. | wrapper: `third_party/nvidia/backend/compiler.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |

### Benchmark / Test Evidence

Use this section for: Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code.
NCU first look: Use the PR's benchmark/profile command as the first replay target.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#10171](https://github.com/triton-lang/triton/pull/10171) [CONSAN] Fix i32 scratch offset overflow | 2026-05-04 | benchmark_test, norm_elementwise, compiler_runtime | For very big grid sizes and big scratch allocations we can overflow i32 offsets, leading to corruptions and IMA errors. | kernel: `include/triton/Dialect/TritonInstrument/IR/ConSanConstants.h`, `include/triton/Dialect/TritonInstrument/IR/Utility.h`, `include/triton/Dialect/TritonInstrument/Transforms/ConSanTargetHooks.h`, `lib/Conversion/TritonGPUToLLVM/Utility.cpp`<br>benchmark: `lib/Dialect/TritonInstrument/Transforms/ConcurrencySanitizer.cpp`<br>test: `test/Conversion/tritongpu_to_llvm.mlir`, `test/TritonGPU/amd/amd-consan.mlir`, `test/TritonGPU/consan-capture-reservation.mlir`, `test/TritonGPU/consan.mlir`<br>wrapper: `python/src/passes.cc`, `third_party/amd/backend/compiler.py`, `third_party/amd/lib/Analysis/AMDGPUAllocation.cpp`, `third_party/nvidia/backend/compiler.py`<br>other: `include/triton/Dialect/TritonInstrument/Transforms/Passes.td`, `lib/Dialect/TritonInstrument/Transforms/CMakeLists.txt`, `third_party/amd/include/TritonAMDGPUToLLVM/Passes.td`, `third_party/amd/lib/TritonAMDGPUToLLVM/CMakeLists.txt` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#10311](https://github.com/triton-lang/triton/pull/10311) [INTERPRETER] Implement tl.dot_scaled for the interpreter | 2026-05-14 | benchmark_test, gemm_quant, compiler_runtime | Implement InterpreterBuilder.create_dot_scaled. deduceScaleFactor relied on mlir::Values. Refactor to be independent of mlir so it can be called when using the interpreter. Mark several tests that use tl.dot_scaled to run during the interpreter tests. New contributor declaration - [x] I am not makin... | kernel: `include/triton/Dialect/Triton/IR/Dialect.h`, `lib/Dialect/Triton/IR/Ops.cpp`, `python/triton/language/semantic.py`, `python/triton/runtime/interpreter.py`<br>test: `python/test/unit/language/test_core.py`, `python/test/unit/language/test_matmul.py`, `python/test/unit/language/test_tensor_descriptor.py`<br>wrapper: `python/src/ir.cc` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#9850](https://github.com/triton-lang/triton/pull/9850) Add dense matmul benchmark for triton_kernels | 2026-04-23 | benchmark_test, gemm_quant, compiler_runtime | Add dense matmul benchmark for triton_kernels | benchmark: `python/triton_kernels/bench/bench_dense_matmul.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#10167](https://github.com/triton-lang/triton/pull/10167) [CONSAN] Add read before any write check | 2026-05-04 | benchmark_test, compiler_runtime, norm_elementwise | We add a check that a buffer had been written before its first read. This fixes a multicast repro that had been flaky for sometime. Note that this is analogous to the "see the previous write" race conditions we might find if mbarriers are not correct only that this one catches when races when there... | kernel: `include/triton/Dialect/TritonInstrument/IR/FunctionBuilder.h`, `lib/Dialect/TritonInstrument/IR/FunctionBuilder.cpp`<br>benchmark: `lib/Dialect/TritonInstrument/Transforms/ConcurrencySanitizer.cpp`<br>test: `python/test/gluon/test_consan.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#10168](https://github.com/triton-lang/triton/pull/10168) [BACKEND] Simplify mbarrier.expect lowering | 2026-04-29 | benchmark_test, norm_elementwise, compiler_runtime | We split the expect across all the CTAs that share the same barrier rather than emitting expects and arrives. This aligns the lowering with that of arrive and heavily simplifies parts of the logic of multicta consan. We also use elect in both arrive and expect. | kernel: `lib/Dialect/TritonNvidiaGPU/Transforms/ConSanNVIDIA.cpp`, `third_party/nvidia/lib/TritonNVIDIAGPUToLLVM/BarrierOpToLLVM.cpp`, `third_party/nvidia/lib/TritonNVIDIAGPUToLLVM/Utility.cpp`, `third_party/nvidia/lib/TritonNVIDIAGPUToLLVM/Utility.h`<br>benchmark: `lib/Dialect/TritonInstrument/Transforms/ConcurrencySanitizer.cpp`<br>test: `test/Conversion/tritonnvidiagpu_to_llvm.mlir`, `test/TritonGPU/consan.mlir` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#10055](https://github.com/triton-lang/triton/pull/10055) [FPSAN] Insert barriers in between scratch loads and stores | 2026-04-20 | benchmark_test, norm_elementwise, compiler_runtime | Fpsan insufficiently synchronized global scratch accesses - we were missing a barrier in between memory accesses, which led to random failures in the tests and racy behavior. | kernel: `lib/Dialect/TritonInstrument/Transforms/FpSanitizer.cpp`<br>test: `test/TritonGPU/nvidia-fpsan.mlir` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#10147](https://github.com/triton-lang/triton/pull/10147)  [PROTON] Fix flakey test failure due to GPU memory pressure. | 2026-04-28 | benchmark_test, compiler_runtime | Fixed flakey test failure in `test_periodic_flushing` and `test_periodic_flushing_cudagraph` manifesting when executed with 8 threads over 10,000 iterations. Changes Reduced the number of iterations from 10,000 to 5,000 within each test and recycled GPU memory before starting `test_periodic_flushing... | benchmark: `third_party/proton/test/test_profile.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#10149](https://github.com/triton-lang/triton/pull/10149) [BENCH][PROTON] Implement `do_bench_proton` and `do_bench_cudagraph_proton` as alternatives for benchmarking | 2026-04-28 | benchmark_test, compiler_runtime | Without this PR, `do_cudagraph_bench` may overestimate performance because it does not flush the L2 cache between iterations. Using 03-matrix-multiplication as an example: do_bench_cudagraph code block do_bench In contrast, `do_bench` may underestimate performance due to CPU-side launch overhead. co... | test: `python/triton/testing.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#10237](https://github.com/triton-lang/triton/pull/10237) [PROTON][TEST] Fix test flakiness | 2026-05-07 | benchmark_test, compiler_runtime | 1. Skip checking number of kernels as we now insert even frames of empty metrics (we shouldn't!), which cause noise. Empty metric frames will be taken out in another PR. 2. Disable python gc that unload cuda modules during cuda graph capture. Reenable `test_periodic_flushing_cudagraph` | benchmark: `third_party/proton/test/test_profile.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#10186](https://github.com/triton-lang/triton/pull/10186) [RUNTIME] Initialize profile scratch on stream 0 | 2026-05-01 | benchmark_test, compiler_runtime | Codex found an intersting one that was causing some consan failures (i.e., kernels that need an HBM scratch) to fail. This empirically fixes a race that was consistently reproduced in my multicta consan branch. Here's the post mortem Root cause: - Triton launches kernels on the current raw CUDA stre... | kernel: `python/triton/backends/driver.py`<br>test: `python/test/unit/runtime/test_driver.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#10027](https://github.com/triton-lang/triton/pull/10027) [TESTS] Run all tests in CI | 2026-04-15 | benchmark_test | Revert the cap to the number of tests run in CI from https://github.com/triton-lang/triton/pull/10016 CI times are quite reasonable and we want to catch regressions early. | test: `python/test/conftest.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |

## Per-PR Ledger Fields

When using an idea from this page, add one row to `artifacts/source-idea-ledger.md` with:

| Field | Value to record |
| --- | --- |
| Source key | `<repo>#<pr-number>` |
| Code evidence | Kernel, wrapper, benchmark, and test paths opened from the PR diff |
| Hypothesis | The concrete optimization idea derived from the PR |
| First experiment | Candidate version and benchmark shape set |
| Result | Correctness, geomean, best/worst cases, and NCU digest path |
| Do-not-reread key | Same as source key unless a single PR yields multiple independent ideas |

## How To Use This Page

- During the initial knowledge pass, read the category matching the target kernel and copy PR URL, changed paths, and hypothesis into the source idea ledger.
- During plateau expansion, choose PRs not already present in ledger do-not-reread keys; inspect the diff, linked issue, changed tests, and benchmark files before using the idea.
- Treat PR code as baseline/prior art unless the task and license allow copying or adapting it. When copied, record exact PR, commit, files, notice, and first delta.

# FlashInfer PR Knowledge Notes

Repository: <https://github.com/flashinfer-ai/flashinfer>

This page is the production-PR layer for kernel-knowledge. It favors merged PRs that changed kernels, dispatch, JIT/runtime integration, tuning policy, tests, benchmarks, or profiling evidence. Release, CI-only, formatting, pure version-bump, and non-target-backend PRs are filtered out.

## Repository Source Scan

Read these source regions before opening individual PR diffs:

- `csrc`
- `csrc/fmha_v2/fmha`
- `csrc/fmha_v2/fmha/hopper`
- `csrc/fmha_v2/fmha/warpspec`
- `include/flashinfer`
- `benchmarks/bench_blackwell_attention.py`
- `python/flashinfer`

## Coverage Summary

| Category | CUDA-kernel PRs |
| --- | ---: |
| GEMM / Quantization | 48 |
| Attention / KV / Decode | 98 |
| MoE / Routing | 47 |
| Norm / Elementwise / Epilogue | 6 |
| Memory / Primitives | 3 |
| Scheduler / Autotune | 4 |
| Architecture / Pipeline | 7 |
| Compiler / Runtime | 19 |
| Benchmark / Test Evidence | 29 |

## Pull Request Case Notes

### GEMM / Quantization

Use this section for: Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate.
NCU first look: Tensor pipe %, DRAM/L2 bytes, active cycles, register pressure, and scale-load traffic.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#2740](https://github.com/flashinfer-ai/flashinfer/pull/2740) misc: Update gemm/batched gemm cubins from trtllm-gen, gemm header refactor | 2026-04-07 | gemm_quant, benchmark_test, attention_kv, memory_primitives, moe_routing | 📌 Description 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Checks - [ ] I have installed `pre-commit` by running `pip install pre-commit` (or used your preferred... | kernel: `csrc/trtllm_gemm_runner.cu`, `csrc/trtllm_low_latency_gemm_runner.cu`, `flashinfer/artifacts.py`, `flashinfer/jit/gemm/core.py`<br>test: `tests/moe/test_trtllm_gen_fused_moe.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2235](https://github.com/flashinfer-ai/flashinfer/pull/2235) refactor: pull trtllm-gen batch-gemm/gemm headers from artifactory; update tma descriptor shape init | 2026-03-02 | gemm_quant, arch_pipeline, attention_kv, memory_primitives, compiler_runtime | 📌 Description 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Checks - [ ] I have installed `pre-commit` by running `pip install pre-commit` (or used your preferred... | kernel: `csrc/trtllm_batched_gemm_runner.cu`, `csrc/trtllm_gemm_runner.cu`, `csrc/trtllm_low_latency_gemm_runner.cu`, `flashinfer/artifacts.py`<br>other: `.gitignore` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2775](https://github.com/flashinfer-ai/flashinfer/pull/2775) chore: cute dsl nvfp4 moe clean up | 2026-03-19 | gemm_quant, compiler_runtime, moe_routing, benchmark_test | 📌 Description This is to clean up cute dsl nvfp4 moe 1. Remove incorrect statement in code that sm110 is supported 2. Add cute dsl moe to benchmark script 3. Adjust autouning strategy and tactics. And use unit test to test all tactics 4. Remove unused blockscaled fp4 grouped gemm in moe 5. Make pdl... | kernel: `flashinfer/fused_moe/cute_dsl/blackwell/__init__.py`, `flashinfer/fused_moe/cute_dsl/blackwell/blockscaled_contiguous_gather_grouped_gemm_swiglu_fusion.py`, `flashinfer/fused_moe/cute_dsl/blackwell/blockscaled_contiguous_grouped_gemm.py`, `flashinfer/fused_moe/cute_dsl/blackwell/blockscaled_contiguous_grouped_gemm_finalize_fusion.py`<br>benchmark: `benchmarks/bench_moe_deepseek.py`, `benchmarks/routines/flashinfer_benchmark_utils.py`, `benchmarks/routines/moe.py`, `benchmarks/samples/sample_testlist.txt`<br>test: `tests/moe/test_cute_dsl_fused_moe.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2914](https://github.com/flashinfer-ai/flashinfer/pull/2914) feat: Add cuBLASLt backend for `mm_bf16` and enable multi-tactic autotuning for FP8/MXFP8 runners | 2026-05-01 | gemm_quant, benchmark_test, scheduler_autotune, memory_primitives | - **Add cuBLASLt backend for `mm_bf16`**: new `backend="cublaslt"` option (gated to SM100/SM103). Autotuning across all available cuBLASLt algorithms via `get_valid_tactics()`. - **Enable multi-tactic autotuning for single-tactic GEMM runners**: `CublasFp8GemmRunner`, `CudnnFp8GemmRunner`, and `Cudn... | kernel: `csrc/bmm_fp8.cu`, `csrc/flashinfer_gemm_binding.cu`, `csrc/mm_bf16_cublaslt.cu`, `flashinfer/aot.py`<br>benchmark: `benchmarks/routines/gemm.py`<br>test: `tests/autotuner/test_autotuner_bmm_fp8.py`, `tests/gemm/test_bmm_bf16.py`, `tests/gemm/test_mm_bf16.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2416](https://github.com/flashinfer-ai/flashinfer/pull/2416) feat: update trtllm-gen MoE cubins | 2026-01-28 | gemm_quant, moe_routing, attention_kv, memory_primitives, arch_pipeline | 📌 Description Update TRT-LLM Gen MoE Rubins to include bias support for NVFP4 and element-wise fused activation function. 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-c... | kernel: `csrc/trtllm_batched_gemm_runner.cu`, `flashinfer/artifacts.py`, `include/flashinfer/trtllm/batched_gemm/trtllmGen_bmm_export/BatchedGemmInterface.h`, `include/flashinfer/trtllm/batched_gemm/trtllmGen_bmm_export/BatchedGemmOptions.h` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2443](https://github.com/flashinfer-ai/flashinfer/pull/2443) Add cute-dsl backends to mxfp[8,4]_quantization for future refactor | 2026-03-18 | gemm_quant, benchmark_test, compiler_runtime | 📌 Description This PR adds CuTe-DSL backend support for MXFP8 and MXFP4 quantization kernels as alternatives to JIT-compiled CUDA backends Key changes: - Add CuTe-DSL MXFP8 and MXFP4 quantization kernels - Reorganize quantization module structure for better maintainability - Add benchmarks and unit... | kernel: `flashinfer/__init__.py`, `flashinfer/activation.py`, `flashinfer/fp4_quantization.py`, `flashinfer/fp8_quantization.py`<br>benchmark: `benchmarks/bench_mxfp4_quantize_backend_comparison.py`, `benchmarks/bench_mxfp8_quantize_backend_comparison.py`, `benchmarks/routines/flashinfer_benchmark_utils.py`, `benchmarks/routines/quantization.py`<br>test: `tests/utils/test_fp4_quantize.py`, `tests/utils/test_fp8_quantize.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2653](https://github.com/flashinfer-ai/flashinfer/pull/2653) [feat] trtllm-gen mxfp8 gemm | 2026-03-21 | gemm_quant, attention_kv, benchmark_test | 📌 Description - Create `flashinfer/tllm_enums.py` for storing Trtllm-gen related enums. - Add `trtllm` backend to `mm_mxfp8` - Api change: add `use_8x4_sf_layout` as the last argument - Refactor `get_trtllm_gemm_module()` - Refactor `fp8Quantize.cpp`. It supports either 128x4 or 8x4 swizzle layout.... | kernel: `csrc/nv_internal/tensorrt_llm/thop/fp8Quantize.cpp`, `csrc/nv_internal/tensorrt_llm/thop/fp8Quantize.h`, `csrc/trtllm_gemm_runner.cu`, `csrc/trtllm_low_latency_gemm_runner.cu`<br>benchmark: `benchmarks/routines/flashinfer_benchmark_utils.py`, `benchmarks/routines/gemm.py`, `benchmarks/routines/quantization.py`<br>test: `tests/attention/test_xqa_batch_decode.py`, `tests/gemm/test_mm_mxfp8.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2233](https://github.com/flashinfer-ai/flashinfer/pull/2233) feat: Fused RMSNorm + FP4 Quantization Kernels in CuTe-DSL | 2025-12-20 | gemm_quant, norm_elementwise, benchmark_test, compiler_runtime | 📌 Description Fused RMSNorm + FP4 Quantization Kernels (CuTe-DSL) This PR adds two high-performance fused kernels implemented in CuTe-DSL for Blackwell (SM100+) GPUs: - **`rmsnorm_fp4quant`**: Fuses RMSNorm and FP4 quantization into a single kernel - **`add_rmsnorm_fp4quant`**: Fuses residual add, R... | kernel: `flashinfer/__init__.py`, `flashinfer/cute_dsl/__init__.py`, `flashinfer/cute_dsl/add_rmsnorm_fp4quant.py`, `flashinfer/cute_dsl/rmsnorm_fp4quant.py`<br>benchmark: `benchmarks/bench_cute_dsl_add_rmsnorm_fp4quant.py`, `benchmarks/bench_cute_dsl_rmsnorm_fp4quant.py`<br>test: `tests/norm/test_add_rmsnorm_fp4_quant_cute_dsl.py`, `tests/norm/test_rmsnorm_fp4_quant_cute_dsl.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#3008](https://github.com/flashinfer-ai/flashinfer/pull/3008) feat: add PDL support to rmsnorm_fp4quant and add_rmsnorm_fp4quant CuTe DSL kernels | 2026-04-08 | gemm_quant, norm_elementwise, compiler_runtime, benchmark_test, scheduler_autotune | 📌 Description - Add programmatic dependent launch (PDL) support to `rmsnorm_fp4quant` and `add_rmsnorm_fp4quant` CuTe DSL kernels - Wire `--enable_pdl` through to the FP4 quant benchmarks in `flashinfer_benchmark.py` The regular rmsnorm variants (rmsnorm, fused_add_rmsnorm, rmsnorm_quant, fused_add_... | kernel: `flashinfer/cute_dsl/add_rmsnorm_fp4quant.py`, `flashinfer/cute_dsl/rmsnorm_fp4quant.py`<br>benchmark: `benchmarks/routines/norm.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#3271](https://github.com/flashinfer-ai/flashinfer/pull/3271) feat(moe): add SM120 W4A16 b12x kernels | 2026-05-11 | gemm_quant, moe_routing, arch_pipeline, attention_kv | 📌 Description Adds SM120 b12x MoE W4A16 support alongside the existing W4A4 path. This PR adds activation precision plumbing for W4A4/W4A16 dispatch, wires W4A16 static, dynamic, and direct micro kernels, and preserves the intended scale semantics: fc2_input_scale is required only for activation_pre... | kernel: `flashinfer/cute_dsl/fp4_common.py`, `flashinfer/cute_dsl/utils.py`, `flashinfer/fused_moe/cute_dsl/b12x_moe.py`, `flashinfer/fused_moe/cute_dsl/blackwell_sm12x/moe_direct_micro_kernel.py`<br>test: `tests/moe/test_b12x_fused_moe.py`, `tests/trace/test_b12x_moe_trace.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#3084](https://github.com/flashinfer-ai/flashinfer/pull/3084) perf: optimize MXFP4xBF16 & INT4xFP8 CUTLASS MoE backend for SM90 | 2026-04-23 | gemm_quant, arch_pipeline, moe_routing, benchmark_test | Port TensorRT-LLM PR 12451 to FlashInfer's `cutlass_fused_moe` SM90 path. Adds an LDSM + interleaved-LUT weight-load pipeline for 4-bit weights × 16/8-bit activations, plus the two preprocessing helpers the new kernel layout requires. Changes Kernel - `mixed_input_utils.hpp` / `sm90_mma_array_tma_gm... | kernel: `csrc/fused_moe/cutlass_backend/flashinfer_cutlass_fused_moe_binding.cu`, `csrc/nv_internal/tensorrt_llm/cutlass_extensions/include/cutlass_extensions/detail/collective/mixed_input_utils.hpp`, `csrc/nv_internal/tensorrt_llm/cutlass_extensions/include/cutlass_extensions/gemm/collective/sm90_mma_array_tma_gmma_rs_warpspecialized_mixed_input_.hpp`, `csrc/nv_internal/tensorrt_llm/kernels/cutlass_kernels/cutlass_heuristic.cpp`<br>test: `tests/moe/test_trtllm_cutlass_fused_moe.py`<br>docs: `docs/api/fused_moe.rst`<br>other: `csrc/nv_internal/tensorrt_llm/kernels/cutlass_kernels/moe_gemm/launchers/moe_gemm_tma_ws_mixed_input_launcher.inl` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2090](https://github.com/flashinfer-ai/flashinfer/pull/2090) refactor: pass hopper deepgemm include directory through python | 2025-12-01 | gemm_quant, attention_kv, arch_pipeline, compiler_runtime, moe_routing | 📌 Description This PR implements the refactor mentioned in https://github.com/flashinfer-ai/flashinfer/pull/1969/filesr2461856020 In our current design we rely on calling `pip show flashinfer-python 2>/dev/null \|\| uv pip show flashinfer-python 2>/dev/null` to obtain deepgemm jit include directory, w... | kernel: `csrc/fused_moe/cutlass_backend/deepgemm_jit_setup.cu`, `csrc/fused_moe/cutlass_backend/flashinfer_cutlass_fused_moe_binding.cu`, `csrc/nv_internal/tensorrt_llm/deep_gemm/compiler.cuh`, `csrc/nv_internal/tensorrt_llm/deep_gemm/jit_utils.cuh` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2540](https://github.com/flashinfer-ai/flashinfer/pull/2540) feat: cute dsl mmfp4 for blackwell | 2026-02-21 | gemm_quant, benchmark_test, arch_pipeline, compiler_runtime | 📌 Description Issue 2466 The PR integrate cute_dsl as a new backend for mm_fp4 dense_blockscaled_gemm_sm100.py comes from dense_blockscaled_gemm_persistent.py from TensorRT-LLM. dense_blockscaled_gemm_sm103.py comes from sm103_dense_blockscaled_gemm_persistent.py from CUTLASS. This file is integrate... | kernel: `flashinfer/gemm/gemm_base.py`, `flashinfer/gemm/kernels/dense_blockscaled_gemm_sm100.py`, `flashinfer/gemm/kernels/dense_blockscaled_gemm_sm103.py`<br>benchmark: `benchmarks/routines/gemm.py`<br>test: `tests/gemm/test_mm_fp4.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2898](https://github.com/flashinfer-ai/flashinfer/pull/2898) fix: snap weight_scale_vec_size to handle block_scale_interleave padding for SM120 | 2026-04-05 | gemm_quant, arch_pipeline, moe_routing, benchmark_test, attention_kv | Fixes 2847 SM120 GPUs (RTX PRO 6000, RTX 5090) cannot use MXFP4 MoE CUTLASS kernels because the `weight_scale_vec_size` check in `trtllm_fp4_block_scale_moe` rejects valid inputs. **Root cause**: `block_scale_interleave` pads scale columns via `round_up(cols, 4)`, inflating `gemm1_weights_scale.nume... | kernel: `csrc/trtllm_fused_moe_kernel_launcher.cu`, `flashinfer/quantization/fp4_quantization.py`<br>test: `tests/moe/test_trtllm_cutlass_fused_moe.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2913](https://github.com/flashinfer-ai/flashinfer/pull/2913) [NVIDIA] fix(jit): enable GDC for CUTLASS fused MoE PDL — prevent random crashes on SM12x | 2026-04-01 | gemm_quant, moe_routing, compiler_runtime, attention_kv, scheduler_autotune | - Add missing `-DCUTLASS_ENABLE_GDC_FOR_SM100=1` compile flag to all CUTLASS fused MoE JIT modules (SM100/SM103/SM120) and `-DCUTLASS_ENABLE_GDC_FOR_SM90=1` to SM90 modules - Sync nv_internal `grid_dependency_control.h` with upstream CUTLASS to support SM100/SM103/SM110/SM120/SM121 GDC - Add `-DCUTL... | kernel: `csrc/nv_internal/tensorrt_llm/cutlass_extensions/include/cutlass_extensions/arch/grid_dependency_control.h`, `flashinfer/jit/fused_moe.py`, `flashinfer/jit/gemm/fp8_blockscale.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#3246](https://github.com/flashinfer-ai/flashinfer/pull/3246) Cutlass dsl 4.5 bump | 2026-05-07 | gemm_quant, compiler_runtime, attention_kv, arch_pipeline, benchmark_test | 📌 Description Bump cutlass-dsl to 4.5 so sm121 is supported in BlockScaledMmaOp 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Checks - [x] I have installed `pre-c... | kernel: `flashinfer/fused_moe/cute_dsl/blackwell_sm12x/moe_dispatch.py`<br>test: `scripts/test_utils.sh`, `tests/gemm/test_mm_fp4.py`, `tests/moe/test_b12x_fused_moe.py`<br>other: `docker/install/install_python_packages.sh`, `pyproject.toml`, `requirements.txt` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2534](https://github.com/flashinfer-ai/flashinfer/pull/2534) fix: support fp32 logits for fp8_per_tensor and fp8_block | 2026-03-30 | gemm_quant, moe_routing, attention_kv, benchmark_test | 📌 Description This PR adds more template instantiation for supporting FP32 logits for routing when using `fp8_per_tensor` and `fp8_block` quantization. - Differentiates between `mDtypeScore` and `mDtypeExpW` and adds more template instantiation. - Adds testing for different logits data types. 🔍 Rela... | kernel: `csrc/trtllm_fused_moe_kernel_launcher.cu`, `csrc/trtllm_fused_moe_runner.cu`, `include/flashinfer/trtllm/fused_moe/DevKernel.h`, `include/flashinfer/trtllm/fused_moe/RoutingKernel.h`<br>test: `tests/moe/test_dpsk_fused_moe_fp8.py`, `tests/moe/test_trtllm_gen_fused_moe.py`, `tests/moe/utils.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2716](https://github.com/flashinfer-ai/flashinfer/pull/2716) fix(jit): GEMM kernels produce NaN under concurrency — missing GDC flags cause PDL synchronization barriers to compile as no-ops | 2026-03-09 | gemm_quant, compiler_runtime, scheduler_autotune, benchmark_test, attention_kv | All CUTLASS GEMM templates use `enablePDL=true` (Programmatic Dependent Launch), but the JIT compilation is missing `-DCUTLASS_ENABLE_GDC_FOR_SM100=1` and `-DCUTLASS_ENABLE_GDC_FOR_SM90=1` compile flags. Without these flags, `wait_on_dependent_grids()` and `launch_dependent_grids()` in CUTLASS `grid... | kernel: `flashinfer/jit/gemm/core.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#3059](https://github.com/flashinfer-ai/flashinfer/pull/3059) Support Allreduce + Norm + Per-token Group Fp8 Quant Fusion | 2026-05-06 | gemm_quant, moe_routing, memory_primitives, norm_elementwise | 📌 Description This PR adds support for allreduce + Norm + Per-token Group Fp8 Quant Fusion, which quantizes activation for DeepGemm gemm (`per_token_group_quant_fp8_packed_for_deepgemm` in vllm) <img width="1021" height="240" alt="Screenshot 2026-04-13 at 9 44 49 PM" src="https://github.com/user-att... | kernel: `csrc/trtllm_allreduce_fusion.cu`, `flashinfer/comm/allreduce.py`, `flashinfer/comm/trtllm_ar.py`, `include/flashinfer/comm/trtllm_allreduce_fusion.cuh`<br>test: `tests/comm/test_trtllm_allreduce_fusion_group_fp8_quant.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2587](https://github.com/flashinfer-ai/flashinfer/pull/2587) feat: trtllm tinygemm2 in flashinfer as bf16 routergemm | 2026-02-24 | gemm_quant, attention_kv, benchmark_test, compiler_runtime | 📌 Description SM100: code block 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Checks - [ ] I have installed `pre-commit` by running `pip install pre-commit` (or u... | kernel: `csrc/tinygemm2.cu`, `flashinfer/gemm/__init__.py`, `flashinfer/gemm/routergemm.py`, `flashinfer/jit/__init__.py`<br>test: `tests/model_optimizations/test_tinygemm2.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2260](https://github.com/flashinfer-ai/flashinfer/pull/2260) fix: Add global scale support and optional output allocation for RMSNorm+FP4Quant fusion kernels | 2026-01-01 | gemm_quant, norm_elementwise, benchmark_test | 📌 Description This PR enhances the `rmsnorm_fp4quant` and `add_rmsnorm_fp4quant` CuTe-DSL kernels with two key improvements: * **Optional output allocation**:` y_fp4` and `block_scale` outputs can now be either provided for in-place update or omitted for automatic allocation and return * **Global sc... | kernel: `flashinfer/cute_dsl/add_rmsnorm_fp4quant.py`, `flashinfer/cute_dsl/rmsnorm_fp4quant.py`<br>benchmark: `benchmarks/bench_cute_dsl_add_rmsnorm_fp4quant.py`, `benchmarks/bench_cute_dsl_rmsnorm_fp4quant.py`<br>test: `tests/norm/test_add_rmsnorm_fp4_quant_cute_dsl.py`, `tests/norm/test_rmsnorm_fp4_quant_cute_dsl.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#3027](https://github.com/flashinfer-ai/flashinfer/pull/3027) [feat] Trtllm-gen Per-token Nvfp4 MoE | 2026-05-01 | gemm_quant, moe_routing | 📌 Description coauthor: @mxz297, @zianglih This PR aims to enable the per-token quantization for Trtllm-gen MoE. - In `trtllm_fp4_block_scale_moe` and `trtllm_fp4_block_scale_routed_moe`, added a new optional argument `per_token_scale`. - Optimize fp4 quantization kernel. Use 256bit vectorized load.... | kernel: `csrc/fused_moe/cutlass_backend/cutlass_fused_moe_kernels.cuh`, `csrc/nv_internal/cpp/kernels/quantization.cu`, `csrc/nv_internal/tensorrt_llm/kernels/quantization.cuh`, `csrc/nv_internal/tensorrt_llm/kernels/quantization.h`<br>test: `tests/moe/test_trtllm_gen_per_token_moe.py`, `tests/test_helpers/utils_fp4.py`, `tests/utils/test_fp4_quantize.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#3051](https://github.com/flashinfer-ai/flashinfer/pull/3051) feat: Add backend="b12x" for mm_fp4 on SM120 | 2026-04-14 | gemm_quant, benchmark_test, arch_pipeline | 📌 Description - Add a new `backend="b12x"` option for `mm_fp4` targeting SM120 GPUs. Supports nvfp4 only. - Port the `b12x` block-scaled NVFP4 dense GEMM kernel using CuTe DSL. Ported from the b12x library - On SM120, `backend="auto"` now prefers "b12x" over "cutlass" and "cudnn" for NVFP4 - SM121 (... | kernel: `flashinfer/cute_dsl/utils.py`, `flashinfer/gemm/__init__.py`, `flashinfer/gemm/gemm_base.py`, `flashinfer/gemm/kernels/dense_blockscaled_gemm_sm120.py`<br>benchmark: `benchmarks/routines/gemm.py`<br>test: `tests/gemm/test_mm_fp4.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2385](https://github.com/flashinfer-ai/flashinfer/pull/2385) fix: In-place Residual Update for add_rmsnorm_fp4quant | 2026-01-22 | gemm_quant, norm_elementwise, benchmark_test | 📌 Description **In-place Residual Update for add_rmsnorm_fp4quant** This PR modifies the add_rmsnorm_fp4quant CuTe-DSL kernel to update the residual tensor in-place, matching the behavior of other fused add+rmsnorm APIs in FlashInfer. Before: - `h = input + residual` (computed internally, residual u... | kernel: `flashinfer/cute_dsl/add_rmsnorm_fp4quant.py`<br>benchmark: `benchmarks/bench_cute_dsl_add_rmsnorm_fp4quant.py`, `benchmarks/routines/norm.py`<br>test: `tests/norm/test_add_rmsnorm_fp4_quant_cute_dsl.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2395](https://github.com/flashinfer-ai/flashinfer/pull/2395) feat: Add output_both_sf_layouts option to add_rmsnorm_fp4quant API | 2026-01-23 | gemm_quant, norm_elementwise, benchmark_test | 📌 Description **Must be merged after 2385** This PR extends the `add_rmsnorm_fp4quant` API to support outputting both swizzled and unswizzled scale factors simultaneously. This is useful for scenarios where the quantized output needs to be consumed by both GEMMs (experts) and All-to-All without requ... | kernel: `flashinfer/cute_dsl/add_rmsnorm_fp4quant.py`<br>benchmark: `benchmarks/routines/norm.py`, `benchmarks/samples/sample_testlist.txt`<br>test: `tests/norm/test_add_rmsnorm_fp4_quant_cute_dsl.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2904](https://github.com/flashinfer-ai/flashinfer/pull/2904) perf: Optimize CuTe-DSL fp4 and fp8 quantization kernels | 2026-04-01 | gemm_quant, benchmark_test | 📌 Description **Summary** - Adopt dual-path kernel architecture (linear flat + swizzled row-based) for MXFP4 and NVFP4 CuTe-DSL quantization kernels. - Architecture chanes to MXFP8 quantization for better perf. - Expand benchmark scripts and test coverage across all three quantization kernels. Compa... | kernel: `flashinfer/cute_dsl/fp4_common.py`, `flashinfer/quantization/kernels/__init__.py`, `flashinfer/quantization/kernels/mxfp4_quantize.py`, `flashinfer/quantization/kernels/mxfp8_quantize.py`<br>benchmark: `benchmarks/bench_mxfp4_quantize_backend_comparison.py`, `benchmarks/bench_mxfp8_quantize_backend_comparison.py`, `benchmarks/bench_nvfp4_quantize_backend_comparison.py`<br>test: `tests/utils/test_fp4_quantize.py`, `tests/utils/test_fp4_quantize_padding.py`, `tests/utils/test_fp8_quantize.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2838](https://github.com/flashinfer-ai/flashinfer/pull/2838) feat: Add CuTe-DSL backend for NVFP4 quantization | 2026-03-26 | gemm_quant, benchmark_test | 📌 Description - Adds backend="cute-dsl" support to nvfp4_quantize with two kernel variants: - Default kernel: vectorized global loads (ld.global.v4.u32), optimal for small-to-medium problems - TMA kernel: producer-consumer warp specialization (1 producer + 8 consumer warps), 3D TMA with SWIZZLE_128B... | kernel: `flashinfer/cute_dsl/fp4_common.py`, `flashinfer/quantization/__init__.py`, `flashinfer/quantization/fp4_quantization.py`, `flashinfer/quantization/kernels/__init__.py`<br>benchmark: `benchmarks/routines/flashinfer_benchmark_utils.py`, `benchmarks/routines/quantization.py`<br>test: `tests/utils/test_fp4_quantize.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#3152](https://github.com/flashinfer-ai/flashinfer/pull/3152) Integrate CUTLASS Small Tile N Blockscaled GEMMs/Grouped GEMMs for SM120 and SM121 | 2026-05-05 | gemm_quant, arch_pipeline | 📌 Description This MR bumps the CUTLASS commit and adds support for the new small tile N Blockscaled GEMMs in a variety of interfaces including `mm_fp4` and the TRT-LLM CUTLASS MoE plugin. Compared to fb3bb44c63d594f1452eea1af18b81d7b7693d48 when running nvidia/Qwen3-30B-A3B-NVFP4 on VLLM (commit 24... | kernel: `csrc/fp4_gemm_cutlass_sm120.cu`, `csrc/group_gemm_mxfp4_groupwise_sm120.cu`, `csrc/group_gemm_nvfp4_groupwise_sm120.cu`, `csrc/group_gemm_sm120_binding.cu`<br>test: `tests/gemm/test_group_gemm_fp4.py`, `tests/gemm/test_groupwise_scaled_gemm_mxfp4.py`, `tests/gemm/test_mm_mxfp8_sm120.py`<br>other: `3rdparty/cutlass`, `csrc/fp4_gemm_cutlass_sm120.jinja`, `csrc/group_gemm_mxfp4_groupwise_sm120_kernel_inst.jinja`, `csrc/group_gemm_nvfp4_groupwise_sm120_kernel_inst.jinja` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2738](https://github.com/flashinfer-ai/flashinfer/pull/2738) Support for MXFP4 and NVFP4 group GEMMs on GeForce and Spark | 2026-03-28 | gemm_quant | 📌 Description This MR adds functional support for CUTLASS MXFP4 group GEMMs and NVFP4 group GEMMs on Blackwell GeForce and DGX Spark. It implements MXFP4 group GEMMs to match the existing interface and adds a new interface for NVFP4. The NVFP4 interface aims to match the baseline GEMM interface incl... | kernel: `csrc/group_gemm_mxfp4_groupwise_sm120.cu`, `csrc/group_gemm_nvfp4_groupwise_sm120.cu`, `csrc/group_gemm_sm120_binding.cu`, `csrc/tvm_ffi_utils.h`<br>benchmark: `benchmarks/bench_groupwise_grouped_gemm_mxfp4_blackwell.py`, `benchmarks/bench_groupwise_grouped_gemm_nvfp4_blackwell_geforce.py`<br>test: `tests/gemm/test_group_gemm_fp4.py`, `tests/gemm/test_groupwise_scaled_gemm_mxfp4.py`<br>other: `csrc/group_gemm_mxfp4_groupwise_sm120_kernel_inst.jinja`, `csrc/group_gemm_nvfp4_groupwise_sm120_kernel_inst.jinja` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2751](https://github.com/flashinfer-ai/flashinfer/pull/2751) [Spark unit test debugging] Fix for tests/gemm/test_groupwise_scaled_gemm_fp8.py | 2026-03-17 | gemm_quant, arch_pipeline, benchmark_test, attention_kv | 📌 Description Problem `test_groupwise_scaled_gemm_fp8.py` fails on DGX Spark nightly CI: - **CUDA 13.0**: All 800 SM12x tests fail with `RuntimeError: Ninja build failed` (CUTLASS compilation error) - **CUDA 12.9**: All 800 SM12x tests fail with `ValueError: Unsupported device for FP8 GEMM: cuda:0`... | kernel: `flashinfer/utils.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2581](https://github.com/flashinfer-ai/flashinfer/pull/2581) Implement `cutlass_fused_moe` mxfp8 | 2026-03-07 | gemm_quant, moe_routing | 📌 Description @humansand https://github.com/flashinfer-ai/flashinfer/pull/2505 implements mxfp8 for trtllm backend. However, in SGLang, `--moe-runner-backend flashinfer_trtllm` bypasses SGLang topk implementation and does not work with expert routing replay in MoE RL. We want to implement `mxfp8 x m... | kernel: `csrc/fused_moe/cutlass_backend/cutlass_fused_moe_instantiation.cu`, `csrc/fused_moe/cutlass_backend/cutlass_fused_moe_kernels.cuh`, `csrc/fused_moe/cutlass_backend/flashinfer_cutlass_fused_moe_binding.cu`, `csrc/nv_internal/tensorrt_llm/kernels/cutlass_kernels/include/moe_gemm_kernels.h`<br>test: `tests/moe/test_trtllm_cutlass_fused_moe.py`<br>other: `csrc/nv_internal/tensorrt_llm/kernels/cutlass_kernels/moe_gemm/launchers/moe_gemm_tma_ws_launcher.inl` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#3239](https://github.com/flashinfer-ai/flashinfer/pull/3239) Update moe gemm | 2026-05-08 | gemm_quant, moe_routing, benchmark_test, attention_kv | 📌 Description Update the trtllm-gen cubin to improve mxfp4 moe, DeepSeek-V3 MoE, and mxfp8 gemm performance. 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Checks... | kernel: `csrc/trtllm_batched_gemm_runner.cu`, `csrc/trtllm_gemm_runner.cu`, `csrc/trtllm_low_latency_gemm_runner.cu`, `flashinfer/artifacts.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2757](https://github.com/flashinfer-ai/flashinfer/pull/2757) feat: Add FP4 KV cache quant/dequant kernels  | 2026-03-18 | gemm_quant, attention_kv | Add GPU-accelerated NVFP4 quantization and dequantization kernels for KV cache with linear (non-swizzled) block scale layout. - **Dequantization** (`nvfp4_kv_dequantize`): SM80+, E2M1 LUT + vectorized memory access, dynamic shared memory to support arbitrary K dimensions - **Quantization** (`nvfp4_k... | kernel: `csrc/fp4_kv_dequantization.cu`, `csrc/fp4_kv_quantization.cu`, `flashinfer/__init__.py`, `flashinfer/aot.py`<br>test: `tests/utils/test_fp4_kv_quantization.py`<br>docs: `docs/api/fp4_quantization.rst` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#3185](https://github.com/flashinfer-ai/flashinfer/pull/3185) feat: enable glm5 router gemm | 2026-05-14 | gemm_quant, benchmark_test, attention_kv | 📌 Description It uses hidden_dim = 6144, need to add it to allowlist (ideally could have router gemm API to avoid duplication), but this is faster for now. On the performance: GLM-5 Router GEMM Benchmark (Flashinfer vs Torch) with CUPTI benchmark \| M (Tokens) \| N (Experts) \| K (Dim) \| Torch (ms) \| F... | kernel: `csrc/dsv3_router_gemm.cu`, `flashinfer/gemm/__init__.py`, `flashinfer/gemm/routergemm.py`, `flashinfer/trace/templates/gemm.py`<br>test: `tests/model_optimizations/test_router_gemms.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2996](https://github.com/flashinfer-ai/flashinfer/pull/2996) fix: tinygemm2 hang issue due to barrier sync | 2026-04-07 | gemm_quant, attention_kv, benchmark_test | 📌 Description The `tinygemm2` kernel hung intermittently on H100 CI runners. Running `compute-sanitizer --tool synccheck` confirmed a barrier synchronization failure. Root Cause The kernel launches 384 threads (12 warps of 32): \| Warps \| Threads \| Role \| \|-------\|-----------\|------------------------... | kernel: `csrc/tinygemm2.cu`, `flashinfer/gemm/routergemm.py`<br>test: `tests/model_optimizations/test_tinygemm2.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#3026](https://github.com/flashinfer-ai/flashinfer/pull/3026) perf: Port TRT-LLM SM120/SM121 FP4 CUTLASS GEMM optimizations. Add PDL | 2026-04-10 | gemm_quant, arch_pipeline, scheduler_autotune | 📌 Description - Port TRT-LLM's SM120 CUTLASS FP4 GEMM template parameters for better kernel performance - TRT-LLM reference file: nvfp4_nvfp4_gemm_template_sm120.h - Enable PDL (programmatic dependent launch) for SM120/SM121 FP4 GEMM Details TRT-LLM kernel parameter port Updated the SM120 FP4 GEMM k... | kernel: `include/flashinfer/gemm/fp4_gemm_template_sm120.h` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#3237](https://github.com/flashinfer-ai/flashinfer/pull/3237) perf: optimize per-token nvfp4 quantization kernel. | 2026-05-08 | gemm_quant, benchmark_test | 📌 Description Optimize the performance of the per-token nvfp4 quantization kernel introduced by 3027. 1. default block size to 128. 2. default to fast math path. rename `TE_EXACT_FP4` to `TRTLLM_DISABLE_FP4_QUANT_FAST_MATH` and controlled by environmental variable. 3. change argument list of `get_sf... | kernel: `csrc/nv_internal/cpp/common/envUtils.cpp`, `csrc/nv_internal/cpp/kernels/quantization.cu`, `csrc/nv_internal/tensorrt_llm/common/envUtils.h`, `csrc/nv_internal/tensorrt_llm/kernels/quantization.cuh`<br>test: `tests/utils/test_fp4_quantize.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2343](https://github.com/flashinfer-ai/flashinfer/pull/2343) Optimize quantization function in large problem size | 2026-01-23 | gemm_quant, benchmark_test, moe_routing | 📌 Description Optimize quantization function in large problem size by using TMA and warp specialized programming. 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Ch... | kernel: `csrc/fused_moe/cutlass_backend/cutlass_fused_moe_kernels.cuh`, `csrc/nv_internal/cpp/kernels/quantization.cu`, `csrc/nv_internal/tensorrt_llm/kernels/quantization.cuh`, `csrc/nv_internal/tensorrt_llm/kernels/quantization_utils.cuh`<br>test: `tests/utils/test_fp4_quantize.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2940](https://github.com/flashinfer-ai/flashinfer/pull/2940) CuTe DSL FP4 GEMM Heuristic | 2026-04-15 | gemm_quant, compiler_runtime, scheduler_autotune | 📌 Description Currently when using the `mm_fp4` API with the "cute-dsl" engine selected and autotuning disabled, the CuTe-DSL GEMM kernel is run with the default tile shape and CGA shape (128x128 and 1x1). This MR introduces a heuristic for selecting a configuration based on minimizing wave and tile... | kernel: `flashinfer/gemm/gemm_base.py`, `flashinfer/gemm/kernels/utils.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#3113](https://github.com/flashinfer-ai/flashinfer/pull/3113) Fix: Extend b12x FP4 GEMM support to SM121 (GB10/DGX Spark) | 2026-04-20 | gemm_quant, arch_pipeline, attention_kv | SM121 (GB10, used in DGX Spark and RTX Pro 6000) supports the same b12x CuTe DSL warp-level MMA FP4 GEMM kernels as SM120, but was excluded from _b12x_gemm_fp4_requirement's supported_compute_capability list. On SM121, calling mm_fp4(..., backend="b12x") raised: BackendSupportedError: mm_fp4 does no... | kernel: `flashinfer/gemm/gemm_base.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2660](https://github.com/flashinfer-ai/flashinfer/pull/2660) feat: support mxfp4 & mxfp8 entrypoint for blackwell cutedsl dense gemm | 2026-03-06 | gemm_quant | 📌 Description This kernel supports mxfp4 and mxfp8. Pass in block size = 32 and E8M0 scale. For most shapes of mxfp4, it's performance is quite good. Geomean speedup around 1.20x. Passed refcheck. <img width="2384" height="4830" alt="image" src="https://github.com/user-attachments/assets/4e1bf17f-ad... | kernel: `flashinfer/gemm/gemm_base.py`, `flashinfer/gemm/kernels/dense_blockscaled_gemm_sm100.py`<br>benchmark: `benchmarks/routines/flashinfer_benchmark_utils.py`, `benchmarks/routines/gemm.py`<br>test: `tests/gemm/test_mm_fp4.py`, `tests/gemm/test_mm_mxfp8.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2670](https://github.com/flashinfer-ai/flashinfer/pull/2670) fix: reduce smem allocation for tinygemm2 kernel in SM120 | 2026-03-04 | gemm_quant, arch_pipeline, memory_primitives | 📌 Description The tinygemm2 kernel used hardcoded STAGES=16 requiring 192KB of dynamic shared memory. This exceeds SM120's (Blackwell RTX) max opt-in shared memory of ~99KB (vs SM90's 228KB), causing cudaFuncSetAttribute to fail with "invalid argument". Fix is to refactor launch_tinygemm2 to query d... | kernel: `csrc/tinygemm2.cu` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2902](https://github.com/flashinfer-ai/flashinfer/pull/2902) feat: add MXFP8 GEMM support for SM120 | 2026-04-03 | gemm_quant | Resolves 2728 Key changes: - New CUTLASS kernel template: `mxfp8_gemm_cutlass_template_sm120.h` using `Sm1xxBlkScaledConfig` with 3 CTA tile configs (128×128×128, 256×128×128, 128×256×128) - New C++ launcher: `csrc/mxfp8_gemm_cutlass_sm120.cu` with full input validation (K%32==0, N%32==0, M unconstr... | kernel: `csrc/mxfp8_gemm_cutlass_sm120.cu`, `flashinfer/aot.py`, `flashinfer/gemm/gemm_base.py`, `flashinfer/jit/gemm/__init__.py`<br>test: `tests/gemm/test_mm_mxfp8_sm120.py`<br>docs: `docs/api/gemm.rst`<br>other: `csrc/mxfp8_gemm_cutlass_sm120.jinja` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2726](https://github.com/flashinfer-ai/flashinfer/pull/2726) Added missing padding | 2026-03-12 | gemm_quant, attention_kv, benchmark_test | 📌 Description Linear sf is missing padding (or the kernel shouldn't try to write to non-existent padding), which is required when `quantize_with_block_size_tma` is called. There's a few requirements to hit this code path, some notable ones being m>=1024, n%512==0 and sm100. The offending code is htt... | kernel: `flashinfer/fp4_quantization.py`<br>test: `tests/utils/test_fp4_quantize_padding.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#3313](https://github.com/flashinfer-ai/flashinfer/pull/3313) Fix: remove nvfp4 llama4 blocker | 2026-05-13 | gemm_quant, benchmark_test | 📌 Description not set `usePerTokenScalingGemm1` for nvfp4+llama4 to unblock the vLLM ci. 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Checks - [ ] I have install... | kernel: `csrc/trtllm_batched_gemm_runner.cu`, `csrc/trtllm_fused_moe_kernel_launcher.cu`<br>test: `tests/moe/test_trtllm_gen_fused_moe.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#3082](https://github.com/flashinfer-ai/flashinfer/pull/3082) fix: guard MXFP8 fc1 weight shape check for non-gated activations | 2026-04-24 | gemm_quant, norm_elementwise | Fixes 2731. What's broken? When using the CUTLASS fused MoE backend with **non-gated activations** (e.g., Relu2, Gelu, Silu) and MXFP8 quantization, the fc1 weight shape validation unconditionally rejects the input — even when the shape is correct. Who is affected? Anyone using the **CUTLASS fused M... | kernel: `csrc/fused_moe/cutlass_backend/flashinfer_cutlass_fused_moe_binding.cu` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2994](https://github.com/flashinfer-ai/flashinfer/pull/2994)   Fix MXFP4/MXFP8 failures in SM120 FAST_BUILD and expand all_tiles[]                                                   | 2026-04-13 | gemm_quant | **Problem** MXFP4 and MXFP8 GEMM operations were failing on SM120 because: - The FAST_BUILD path returned a single hardcoded CtaShape128x128x64B tile regardless of GROUPED_GEMM, and that tile is not valid for all MXFP4/MXFP8 configurations - The full-build all_tiles[] table was missing tiles needed... | kernel: `csrc/nv_internal/tensorrt_llm/kernels/cutlass_kernels/cutlass_heuristic.cpp`, `flashinfer/compilation_context.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |
| [#2974](https://github.com/flashinfer-ai/flashinfer/pull/2974) test: skip unsupported mm_mxfp8 configurations on SM12x | 2026-04-03 | gemm_quant | 📌 Description - On SM12x GPUs, only the CUTLASS backend is available for `mm_mxfp8`, and it requires 1D swizzled scales (`SfLayout.layout_128x4`). The `trtllm` and `cute-dsl` backends do not support SM12x at all. - The backend requirement checks in `gemm_base.py` already enforce this correctly, but... | test: `tests/gemm/test_mm_mxfp8.py` | Inspect scale layout, accumulator type, tile/schedule choice, epilogue fusion, and partial-tile guards before deriving a candidate. |

### Attention / KV / Decode

Use this section for: Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization.
NCU first look: Long scoreboard, L2/DRAM traffic, global-load sectors, tensor pipe %, and shared-memory bank conflicts.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#2111](https://github.com/flashinfer-ai/flashinfer/pull/2111) refactor: update fa3 codebase and fix hopper unittest [part 1] | 2025-12-06 | attention_kv, benchmark_test, arch_pipeline, gemm_quant, compiler_runtime | 📌 Description This PR refactors the out-dated fa3 codebase, more specifically, for page_size>1, the page offset calculation is performed inside the kernel, without the need of a standalone function call to block_sparse_indices_to_vector_sparse_offsets, and optimize the kv_offset calculation with pre... | kernel: `csrc/batch_prefill_fp8_sm90.cu`, `csrc/batch_prefill_sm90.cu`, `csrc/flashinfer_page_binding.cu`, `csrc/page.cu`<br>benchmark: `benchmarks/bench_hopper_fp8_attention.py`<br>test: `tests/attention/test_batch_prefill_kernels.py`, `tests/attention/test_block_sparse.py`, `tests/attention/test_hopper.py`, `tests/attention/test_hopper_fp8_attention.py`<br>other: `csrc/batch_prefill_fp8_ragged_sm90_kernel_inst.jinja`, `csrc/batch_prefill_sm90_customize_config.jinja`, `scripts/task_jit_run_tests_part2.sh` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2276](https://github.com/flashinfer-ai/flashinfer/pull/2276) feat: add GDN Attention | 2026-01-03 | attention_kv, arch_pipeline, benchmark_test, compiler_runtime | 📌 Description This PR adds implementation for Gated Delta Rule (or Gated Delta Net) on Hopper architecture to better support Qwen-next like architecture. 🔍 Related Issues 1690 🚀 Pull Request Checklist ✅ Pre-commit Checks - [x] I have installed `pre-commit` by running `pip install pre-commit` (or use... | kernel: `csrc/flat/ampere/collective/flat_collective_inverse.hpp`, `csrc/flat/ampere/collective/flat_collective_load.hpp`, `csrc/flat/common.hpp`, `csrc/flat/cute_ext.hpp`<br>benchmark: `benchmarks/bench_gdn_prefill.py`<br>test: `tests/gdn/conftest.py`, `tests/gdn/reference_delta_rule.py`, `tests/gdn/test_prefill_delta_rule.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2951](https://github.com/flashinfer-ai/flashinfer/pull/2951) feat: Add DCP All-to-All kernel for context-parallel attention reduction | 2026-04-23 | attention_kv, moe_routing, benchmark_test, compiler_runtime | What is DCP? **DCP (Decode Context Parallelism)** splits the KV cache across multiple GPUs for long-context inference. During the decode phase, each GPU computes partial attention over its local KV shard, producing two tensors: - `partial_o` (bf16/fp16): partial attention output, shape `[B, cp_size,... | kernel: `csrc/nv_internal/tensorrt_llm/kernels/cudaAsyncOps.cuh`, `csrc/nv_internal/tensorrt_llm/kernels/helixAllToAll.cu`, `csrc/nv_internal/tensorrt_llm/kernels/helixAllToAll.h`, `csrc/nv_internal/tensorrt_llm/kernels/ll128Proto.cuh`<br>benchmark: `benchmarks/bench_dcp_alltoall.py`<br>test: `tests/comm/test_dcp_alltoall.py`, `tests/comm/test_mnnvl_dcp_alltoall.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2931](https://github.com/flashinfer-ai/flashinfer/pull/2931) feat: add get flashinfer-trace interface .fi_trace | 2026-04-25 | attention_kv, moe_routing, norm_elementwise, gemm_quant | 📌 Description Adds a **trace layer** to FlashInfer so every public kernel can be described as a portable benchmark / replay definition without tying the description to any particular launcher. - **`flashinfer/trace/template.py`** — new `TraceTemplate` schema with named `axes` (`Var`/`Const`), typed... | kernel: `flashinfer/__init__.py`, `flashinfer/activation.py`, `flashinfer/api_logging.py`, `flashinfer/attention/_core.py`<br>docs: `.claude/skills/add-cuda-kernel/SKILL.md`, `CLAUDE.md`, `docs/fi_trace.rst`, `docs/index.rst` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2702](https://github.com/flashinfer-ai/flashinfer/pull/2702) Add NVFP4 KV cache quantization support for SM100 | 2026-03-19 | attention_kv, gemm_quant, benchmark_test | 📌 Description add nvfp4 kv cache prefill and decode kernels to flashinfer 🔍 Related Issues https://github.com/flashinfer-ai/flashinfer/issues/2458 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete.... | kernel: `csrc/trtllm_fmha_kernel_launcher.cu`, `flashinfer/__init__.py`, `flashinfer/decode.py`, `flashinfer/fp4_quantization.py`<br>benchmark: `benchmarks/bench_trtllm_fmha.py`, `benchmarks/routines/attention.py`, `benchmarks/routines/flashinfer_benchmark_utils.py`<br>test: `tests/attention/test_trtllm_gen_attention.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2743](https://github.com/flashinfer-ai/flashinfer/pull/2743) Add cute dsl mla decode op | 2026-03-26 | attention_kv, compiler_runtime, benchmark_test | 📌 Description Integrate NVIDIA's CuTe DSL MLA (Multi-Head Latent Attention) decode kernels for Blackwell SM100 into FlashInfer, supporting both BF16/FP16 and FP8 dtypes. - Add CuTe DSL MLA decode kernel files (mla_helpers.py, mla_decode_fp16.py, mla_decode_fp8.py) and compilation wrapper (mla_decode... | kernel: `flashinfer/cute_dsl/__init__.py`, `flashinfer/cute_dsl/utils.py`, `flashinfer/mla/__init__.py`, `flashinfer/mla/_core.py`<br>benchmark: `benchmarks/README.md`, `benchmarks/bench_trtllm_gen_mla.py`, `benchmarks/routines/attention.py`, `benchmarks/routines/flashinfer_benchmark_utils.py`<br>test: `tests/attention/test_cute_dsl_mla_decode.py`, `tests/attention/test_trtllm_gen_mla.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2631](https://github.com/flashinfer-ai/flashinfer/pull/2631) fix: add SM121 support to SM120 version guards | 2026-02-25 | attention_kv, arch_pipeline, gemm_quant | 📌 Description 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Checks - [x] I have installed `pre-commit` by running `pip install pre-commit` (or used your preferred... | kernel: `csrc/gemm_groupwise_sm120.cu`, `csrc/group_gemm_fp8_groupwise_sm120.cu`, `csrc/trtllm_fmha_v2_binding.cu`, `flashinfer/decode.py`<br>test: `tests/attention/test_trtllm_gen_mla.py`, `tests/attention/test_xqa.py`, `tests/attention/test_xqa_batch_decode.py`, `tests/attention/test_xqa_mla_batch_decode.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2142](https://github.com/flashinfer-ai/flashinfer/pull/2142) feat: TRTLLM FMHAv2 backend for ctx attention | 2025-11-28 | attention_kv, arch_pipeline, gemm_quant | 📌 Description Porting over the trtllm fmhav2 library to support prefill cases. 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Checks - [ ] I have installed `pre-co... | kernel: `csrc/fmha_v2/convert.cu`, `csrc/fmha_v2/fmha/alibi_params.h`, `csrc/fmha_v2/fmha/fragment.h`, `csrc/fmha_v2/fmha/gemm.h` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2446](https://github.com/flashinfer-ai/flashinfer/pull/2446) feat: Add TRTLLM fmha_v2 library for SM90 attention with Skip-Softmax  | 2026-03-09 | attention_kv, arch_pipeline, gemm_quant | 📌 Description Integrates TRT-LLM's Fused Multi-Head Attention v2 (FMHAv2) kernels into FlashInfer as a prefill attention backend via a new trtllm_fmha_v2_prefill Python API, backed by a CUDA runtime dispatch layer (fmha_v2_run.cu) that bridges FlashInfer's tensor conventions with TRT-LLM's fused ker... | kernel: `csrc/fmha_v2/convert.cu`, `csrc/fmha_v2/fmha/gmem_tile_o_packed.h`, `csrc/fmha_v2/fmha/gmem_tile_ps.h`, `csrc/fmha_v2/fmha/hopper/gmem_tile_o_packed.h`<br>other: `csrc/fmha_v2/templates/fa_kernel.jinja`, `csrc/fmha_v2/templates/kernel.jinja`, `csrc/fmha_v2/templates/kernel_hopper.jinja`, `csrc/fmha_v2/templates/kernel_hopper_ws.jinja` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2841](https://github.com/flashinfer-ai/flashinfer/pull/2841) [Perf] Add FMHAv2 to flashinfer_benchmark.py and eliminate unnecessary H2D | 2026-05-06 | attention_kv, benchmark_test, arch_pipeline | 📌 Description This PR wires `trtllm_fmha_v2_prefill` into the `flashinfer_benchmark` harness and fixes several CUDA-graph-incompatible allocations on the FMHAv2 prefill hot path. Benchmark integration - Add `trtllm-fmha-v2` as a backend option for `BatchPrefillWithPagedKVCacheWrapper` and `BatchPref... | kernel: `csrc/fmha_v2/fmha/gmem_tile_qkv_packed.h`, `csrc/fmha_v2/fmha/paged_kv_cache.h`, `csrc/fmha_v2/fmha/warpspec/dma.h`, `csrc/fmha_v2/fmha/warpspec/epilogue.h`<br>benchmark: `benchmarks/routines/attention.py`, `benchmarks/routines/flashinfer_benchmark_utils.py`<br>test: `tests/attention/test_fmha_v2_prefill.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#3039](https://github.com/flashinfer-ai/flashinfer/pull/3039) feat: Integrate CuTe DSL FMHA prefill kernels by loading cubin | 2026-04-24 | attention_kv, compiler_runtime, benchmark_test | 📌 Description feat: Integrate CuTe DSL FMHA cubin kernels into FlashInfer prefill backend **Summary** - Integrate pre-compiled CuTe DSL FMHA kernels (Blackwell SM100/SM103/SM110) into FlashInfer's prefill attention backend - Load AOT-compiled .so cubins from NVIDIA artifactory at runtime, no JIT com... | kernel: `flashinfer/artifacts.py`, `flashinfer/attention/__init__.py`, `flashinfer/attention/_core.py`, `flashinfer/attention/cute_dsl/__init__.py`<br>benchmark: `benchmarks/routines/attention.py`, `benchmarks/routines/flashinfer_benchmark_utils.py`<br>test: `tests/attention/test_trtllm_gen_attention.py`, `tests/test_artifacts.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2037](https://github.com/flashinfer-ai/flashinfer/pull/2037) feat: Add flashinfer.rope.rope_quantize_fp8_append_paged_kv_cache (fused RoPE + Q + KV cache, supports MLA/GQA/MHA)  | 2025-11-18 | attention_kv, gemm_quant, benchmark_test | 📌 Description Add `flashinfer.rope.rope_quantize_fp8_append_paged_kv_cache`, which runs a fused RoPE + Quantization (16 -> 8) + append KV Cache operation kernel. Note that this does not support optional quantization (there is no "RoPE + append KV Cache" fused operation available). Tested on NVIDIA H... | kernel: `csrc/flashinfer_rope_binding.cu`, `csrc/rope.cu`, `flashinfer/rope.py`, `flashinfer/utils.py`<br>benchmark: `benchmarks/bench_rope_quantize_fp8_append_cache.py`<br>test: `tests/attention/test_rope.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#3001](https://github.com/flashinfer-ai/flashinfer/pull/3001) [feat] Add blackwell GDN prefill kernel | 2026-04-13 | attention_kv, benchmark_test, arch_pipeline | 📌 Description - Add Blackwell GDN prefill kernel written in cutedsl code block 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Checks - [x] I have installed `pre-co... | kernel: `flashinfer/gdn_kernels/__init__.py`, `flashinfer/gdn_kernels/blackwell/__init__.py`, `flashinfer/gdn_kernels/blackwell/gated_delta_net_chunked.py`, `flashinfer/gdn_kernels/blackwell/gated_delta_net_tile_scheduler.py`<br>benchmark: `benchmarks/bench_gdn_prefill.py`<br>test: `tests/gdn/test_prefill_delta_rule.py`<br>docs: `README.md`<br>other: `pyproject.toml` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2805](https://github.com/flashinfer-ai/flashinfer/pull/2805) [CuTe DSL] Add modular FMHA prefill and MLA decode attention kernels | 2026-04-14 | attention_kv, compiler_runtime | Modular rewrite of the CuTe DSL attention kernels, refactored into composable building blocks. Includes FMHA prefill (from 1549) and MLA decode (replacing the monolithic `flashinfer/mla/cute_dsl/` kernels), with bug fixes, new attention variants, and comprehensive tests. Architecture - Composable "r... | kernel: `flashinfer/cute_dsl/attention/__init__.py`, `flashinfer/cute_dsl/attention/collective_builder.py`, `flashinfer/cute_dsl/attention/compat.py`, `flashinfer/cute_dsl/attention/config.py`<br>benchmark: `benchmarks/bench_blackwell_attention_cutedsl.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2370](https://github.com/flashinfer-ai/flashinfer/pull/2370) feat: [Qwen3-Next] Add Cute DSL GDN decode kernel and  tests | 2026-01-22 | attention_kv, benchmark_test, compiler_runtime | Co-author: @zhou9402 @liz-badada @xutizhou 📌 Description This PR integrates **three versions** of the Gated Delta Rule (GDN) **Decode kernels** into FlashInfer, implemented using **CuTe DSL** for SM90 (Hopper) and SM100 (Blackwell) GPUs. These kernels enable efficient linear attention decoding for m... | kernel: `flashinfer/gdn_decode.py`<br>benchmark: `benchmarks/bench_gdn_decode.py`<br>test: `tests/gdn/conftest.py`, `tests/gdn/reference_delta_rule.py`, `tests/gdn/test_decode_delta_rule.py`, `tests/gdn/test_prefill_delta_rule.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#3097](https://github.com/flashinfer-ai/flashinfer/pull/3097) Support NVFP4 KV for prefill and batch attention kernels | 2026-04-28 | attention_kv, gemm_quant | 📌 Description This MR supports NVFP4 KV input for batch prefill and batch attention kernels. It widely supports all arch (SM80+). 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete.... | kernel: `csrc/xqa/utils.cuh`, `flashinfer/attention/_core.py`, `flashinfer/decode.py`, `flashinfer/jit/attention/modules.py`<br>test: `tests/attention/test_batch_attention.py`, `tests/attention/test_batch_decode_kernels.py`, `tests/attention/test_batch_prefill_kernels.py`, `tests/attention/test_single_prefill.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2792](https://github.com/flashinfer-ai/flashinfer/pull/2792) feat: Support padding tokens with seqlen=0 for rope+quant+kv cache update fusion kernel | 2026-04-09 | attention_kv, gemm_quant, memory_primitives, benchmark_test | 📌 Description vLLM is using seqlen=0 padding tokens for running a full cudagraph: https://github.com/vllm-project/vllm/blob/95c0f928cdeeaa21c4906e73cee6a156e1b3b995/vllm/v1/worker/gpu/model_runner.pyL652-L654 Update the following functions: `get_batch_indices_positions_kernel`: initialize `batch_ind... | kernel: `flashinfer/page.py`, `include/flashinfer/pos_enc.cuh`<br>test: `tests/attention/test_rope.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#3181](https://github.com/flashinfer-ai/flashinfer/pull/3181) cute-dsl fmha prefill (cubin integration): remove front-padding, add attention_sink, and pdl support | 2026-05-06 | attention_kv, benchmark_test, compiler_runtime | Update cute-dsl fmha prefill (cubin integration) by removing front-pading, and add support for attention_sink and pdl. Runtime API changes (flashinfer/attention/cute_dsl/fmha.py): - Drop front-padding requirement and remove the docstring section about it. - Reshape q/k/v/o tensors from 4D (B, S, H,... | kernel: `flashinfer/artifacts.py`, `flashinfer/attention/cute_dsl/fmha.py`, `flashinfer/prefill.py`<br>benchmark: `benchmarks/routines/attention.py`<br>test: `tests/attention/test_trtllm_gen_attention.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2160](https://github.com/flashinfer-ai/flashinfer/pull/2160) feat: C++ side tensor validation | 2025-12-03 | attention_kv, norm_elementwise, benchmark_test | 📌 Description Originally in this PR https://github.com/flashinfer-ai/flashinfer/pull/1652 and https://github.com/flashinfer-ai/flashinfer/pull/2127 we added better error messaging / prevent silent failures for wrong dtype / shape of tensors. However, it was on the python side when we can instead mov... | kernel: `csrc/batch_decode.cu`, `csrc/batch_decode_mla_plan.cu`, `csrc/batch_decode_mla_run.cu`, `csrc/batch_mla_plan.cu`<br>test: `tests/utils/test_sampling.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2498](https://github.com/flashinfer-ai/flashinfer/pull/2498) Ameyn/gdn decode cutedsl kernel | 2026-02-17 | attention_kv, benchmark_test, compiler_runtime | Implements high-performance Gated Delta Rule linear attention kernel supporting fixed sequence lengths T=1, T=2, T=3, T=4 using NVIDIA CuTe-DSL. Key features: - H state layout: K-last [B, HV, V, K] where K is the contiguous (fastest) dimension - L2-normalized Q/K with configurable scale - Gated expo... | kernel: `flashinfer/cute_dsl/__init__.py`, `flashinfer/gdn_decode.py`, `flashinfer/gdn_kernels/__init__.py`, `flashinfer/gdn_kernels/gdn_decode_bf16_state.py`<br>benchmark: `benchmarks/bench_gdn_decode.py`<br>test: `tests/gdn/reference_delta_rule.py`, `tests/gdn/test_decode_delta_rule.py`, `tests/gdn/test_prefill_delta_rule.py`<br>other: `.gitignore` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#3235](https://github.com/flashinfer-ai/flashinfer/pull/3235) Support Kimi K2.5 H64 CuTe DSL MLA decode | 2026-05-11 | attention_kv, scheduler_autotune, compiler_runtime | https://github.com/flashinfer-ai/flashinfer/issues/3161 - Enable CuTe DSL MLA decode for Kimi K2.5-style 64 query heads. - Allow H=64 split-KV by padding split-KV workspace storage to the physical 128-head MMA lane width. - Fold 64-head coverage into the existing CuTe DSL MLA decode test sweep. Note... | kernel: `flashinfer/cute_dsl/attention/mla_config.py`, `flashinfer/cute_dsl/attention/mla_decode.py`, `flashinfer/cute_dsl/attention/mla_decode_fp8.py`, `flashinfer/cute_dsl/attention/scheduler/mla_persistent.py`<br>test: `tests/attention/test_cute_dsl_mla_decode.py`, `tests/attention/test_trtllm_gen_mla.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2126](https://github.com/flashinfer-ai/flashinfer/pull/2126) fix flaky xqa test | 2025-11-25 | attention_kv, benchmark_test, arch_pipeline | 📌 Description WIP. Do not merge, see if this could fix xqa flaky test. 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Checks - [x] I have installed `pre-commit` by... | kernel: `csrc/xqa/mha.cu`, `csrc/xqa/mha_sm90.cu`, `csrc/xqa/mla_sm120.cu`<br>test: `tests/attention/test_xqa.py`, `tests/comm/test_trtllm_mnnvl_allreduce_custom_comm.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2520](https://github.com/flashinfer-ai/flashinfer/pull/2520) Support NVFP4 KV cache decode on SM120 | 2026-03-09 | attention_kv, gemm_quant | 📌 Description Supports NVFP4 KV cache. 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Checks - [x] I have installed `pre-commit` by running `pip install pre-commit... | kernel: `csrc/flashinfer_xqa_binding.cu`, `csrc/xqa/defines.h`, `csrc/xqa/mha.cu`, `csrc/xqa/mha.h`<br>test: `tests/attention/test_xqa.py`, `tests/attention/test_xqa_batch_decode.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#3129](https://github.com/flashinfer-ai/flashinfer/pull/3129) feat: Enable FP8 (E4M3/E5M2) in concat_mla_k for optimize long-context prefill performance and refactor type dispatch for BF16/FP16 | 2026-05-07 | attention_kv, benchmark_test, gemm_quant | Enable FP8 (E4M3/E5M2) support in `concat_mla_k`, fixing a crash that blocks FP8 chunked prefill for all MLA models (DeepSeek-V2/V3/R1) on long-context workloads. Motivation For long-context inference (ISL >= 4K) in vLLM, chunked prefill + FP8 quantization (`use_prefill_query_quantization: true`) is... | kernel: `csrc/concat_mla.cu`, `csrc/tvm_ffi_utils.h`, `flashinfer/concat_ops.py`, `include/flashinfer/concat_mla.cuh`<br>test: `tests/utils/test_concat_mla.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2044](https://github.com/flashinfer-ai/flashinfer/pull/2044) perf: improve sampling/mask/softmax performance (part 1/2) | 2025-11-07 | attention_kv, benchmark_test, arch_pipeline | 📌 Description This is the first part of the performance improvement PR for sampling/mask/softmax operator, in this PR, we defer the cross thread reduction till the end of the loop (similar to how FA2 handles denominator) to reduce the number of shuffling and thread sync instructions. For the second... | kernel: `include/flashinfer/sampling.cuh`<br>benchmark: `benchmarks/bench_sampling.py`, `benchmarks/bench_softmax.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2963](https://github.com/flashinfer-ai/flashinfer/pull/2963) test: xfail cuDNN FP8 prefill on Blackwell with CUDA <= 12.9 | 2026-04-03 | attention_kv, benchmark_test, arch_pipeline, gemm_quant | Known cuDNN bug causes test_cudnn_prefill_fp8 to fail on Blackwell (SM100) systems with CUDA 12.9. Mark as expected failure until the cuDNN fix is available. 📌 Description 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please m... | test: `tests/attention/test_cudnn_prefill.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2618](https://github.com/flashinfer-ai/flashinfer/pull/2618) perf(gdn): optimize MTP kernel with ILP rows and SMEM v caching | 2026-03-07 | attention_kv, benchmark_test | Improve GDN MTP (Multi-Token Processing) kernel performance by: - Add instruction-level parallelism (ILP) with 1/2/4/8-row processing - Enable shared memory caching for V tensor tiles - Dynamic dispatch based on batch size and sequence length - Change default: disable_state_update=False (h always up... | kernel: `flashinfer/gdn_decode.py`, `flashinfer/gdn_kernels/__init__.py`, `flashinfer/gdn_kernels/gdn_decode_mtp.py`, `flashinfer/gdn_kernels/gdn_decode_nontranspose.py`<br>benchmark: `benchmarks/bench_gdn_decode.py`<br>test: `tests/gdn/test_decode_delta_rule.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2908](https://github.com/flashinfer-ai/flashinfer/pull/2908) feat(gdn): state checkpointing in chunk_gated_delta_rule | 2026-04-08 | attention_kv, arch_pipeline | 📌 Description Implement prefill part of the feature request 2873 🔍 Related Issues 2873 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Checks - [x] I have installed `pre-commit` by... | kernel: `csrc/gdn_prefill_launcher.cu`, `csrc/prefill_kernel_delta_rule_sm90.cu`, `flashinfer/gdn_prefill.py`, `flashinfer/jit/gdn.py`<br>test: `tests/gdn/test_prefill_delta_rule.py`<br>other: `csrc/flat_prefill_kernel_delta_rule_sm90_extern.inc`, `csrc/gdn_prefill_sm90_kernel_inst.jinja` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2677](https://github.com/flashinfer-ai/flashinfer/pull/2677) feat: add support for more MLA head dimensions | 2026-03-05 | attention_kv, benchmark_test | 📌 Description Primary Goal Adds TRTLLM Gen support for MLA heads with nope head dimension `128`, compressed dimension `256` and RoPE dimension `64`. Changes - Updating the hash for TRTLLM Gen FMHA artifacts to point to a set of generated kernels that supports the new dimensions - Adjusted assertions... | kernel: `csrc/trtllm_fmha_kernel_launcher.cu`, `flashinfer/artifacts.py`, `flashinfer/mla.py`, `flashinfer/prefill.py`<br>benchmark: `benchmarks/routines/attention.py`<br>test: `tests/attention/test_trtllm_gen_attention.py`, `tests/attention/test_trtllm_gen_mla.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#3259](https://github.com/flashinfer-ai/flashinfer/pull/3259) Add dynamic tokens-per-page TRTLLM-GEN GQA kernels | 2026-05-11 | attention_kv, arch_pipeline, benchmark_test | - update TRTLLM-GEN FMHA artifact path and checksum to the newly published cubins - select dynamic tokens-per-page kernels for paged GQA decode and prefill when page size is at least 128 - keep MLA decode kernel selection on dense-mask keys and add Blackwell-only dynamic page-size coverage - relax f... | kernel: `flashinfer/artifacts.py`, `include/flashinfer/trtllm/fmha/fmhaKernels.cuh`, `include/flashinfer/trtllm/fmha/fmhaRunnerParams.h`, `include/flashinfer/trtllm/fmha/kernelParams.h`<br>test: `tests/attention/test_attention_sink_blackwell.py`, `tests/attention/test_trtllm_gen_attention.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2679](https://github.com/flashinfer-ai/flashinfer/pull/2679) feat(gdn): add BF16 state kernel with MTP support beyond T>4 with intermediate caching. | 2026-04-02 | attention_kv, benchmark_test | Add a high-performance CuTe DSL kernel for GDN decode with BF16 hidden state storage. Provides both T=1 (single token) and MTP (multi-token prediction) variants using a cooperative row approach. Key design: - Each warp processes one V-row at a time (4 warps = 4 V-rows/iter) - cp.async pipeline with... | kernel: `flashinfer/gdn_decode.py`, `flashinfer/gdn_kernels/__init__.py`, `flashinfer/gdn_kernels/gdn_decode_bf16_state.py`<br>benchmark: `benchmarks/bench_gdn_decode.py`<br>test: `tests/gdn/test_decode_delta_rule.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2988](https://github.com/flashinfer-ai/flashinfer/pull/2988) [Fmha] support nvfp4 output keepsMmaAb generation kernels | 2026-04-08 | attention_kv, gemm_quant, benchmark_test | **Same as https://github.com/flashinfer-ai/flashinfer/pull/2795. It is recreated because the original branch was force-pushed.** - Update cubin artifact path/checksum to new build with nvfp4 output support - Fix kernel selection: remove E2M1 output dtype condition from mixed-precision path, allowing... | kernel: `include/flashinfer/trtllm/fmha/fmhaKernels.cuh`<br>test: `tests/attention/test_trtllm_gen_attention.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2237](https://github.com/flashinfer-ai/flashinfer/pull/2237) [feat] Integrate SGLang concat_mla_k kernel into flashinfer | 2025-12-18 | attention_kv, benchmark_test | 📌 Description - Integrate SGLang optimized concat_mla_k kernel - Add test as well as benchmark 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Checks - [x] I have i... | kernel: `csrc/concat_mla.cu`, `flashinfer/concat_ops.py`, `flashinfer/dsv3_ops/__init__.py`, `flashinfer/jit/dsv3_optimizations.py`<br>benchmark: `benchmarks/bench_concat_mla.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#3154](https://github.com/flashinfer-ai/flashinfer/pull/3154) fix: pass skip_softmax_threshold_scale_factor to prefill wrapper in test | 2026-04-23 | attention_kv, benchmark_test, arch_pipeline | 📌 Description The wrapper consistency check in _test_trtllm_batch_prefill was calling wrapper_trtllm_gen.run() without skip_softmax_threshold_scale_factor, causing it to default to None (standard attention kernel) while the raw API used 1e-30 (skipsSoftmax kernel variant). Different cubin kernels pr... | test: `tests/attention/test_trtllm_gen_attention.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2689](https://github.com/flashinfer-ai/flashinfer/pull/2689) Fix 10 bugs in BF16 XQA MLA kernel for SM120/SM121 | 2026-05-07 | attention_kv, arch_pipeline | Fixes 10 bugs in PR 2675 (BF16 XQA MLA on SM120) that cause the kernel to produce 100% NaN output. Validated on SM121a (DGX Spark GB10) — all configurations now produce correct results with max_diff < 11 microunits vs PyTorch reference. This PR builds on 2675's foundation and adds the fixes needed t... | kernel: `csrc/xqa/defines.h`, `csrc/xqa/mla_sm120.cu`, `csrc/xqa/tensorMap.cpp`, `flashinfer/jit/xqa.py`<br>test: `tests/attention/test_xqa_mla_bf16.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#3155](https://github.com/flashinfer-ai/flashinfer/pull/3155) fix(gdn): use physical SM count for SM100 persistent prefill kernel | 2026-04-25 | attention_kv, arch_pipeline, scheduler_autotune | 📌 Description Fixes the `num_sm` issue CodeRabbit flagged on 3001 but which was not applied before merge: https://github.com/flashinfer-ai/flashinfer/pull/3001discussion_r3048905545 The raw `HardwareInfo().get_max_active_clusters(1)` call returns 0 / stale values in spawned subprocesses (e.g. vLLM's... | kernel: `flashinfer/gdn_kernels/blackwell/gdn_prefill.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2779](https://github.com/flashinfer-ai/flashinfer/pull/2779) feat: FP8 output support for CUTLASS MLA paged attention | 2026-05-11 | attention_kv, gemm_quant | Closes 2778 Wires `output_scale` and separate `ElementOut` type through the CUTLASS MLA paged attention kernel stack to enable native FP8 output, eliminating a separate bf16→FP8 quant kernel launch. - Template `MlaSm100` on `ElementOut` separately from `Element` (input type) - Pass `output_scale` th... | kernel: `csrc/cutlass_mla.cu`, `csrc/flashinfer_mla_binding.cu`, `flashinfer/mla/_core.py`, `include/flashinfer/attention/cutlass_mla.cuh`<br>test: `tests/attention/test_cutlass_mla_fp8_output.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2901](https://github.com/flashinfer-ai/flashinfer/pull/2901) feat: add pdl support for cute dsl mla decode kernel support | 2026-03-29 | attention_kv, compiler_runtime | 📌 Description Add pdl support for cute dsl mla decode kernels, Add pdl before, <img width="1371" height="337" alt="image" src="https://github.com/user-attachments/assets/9282d98e-46b7-4c17-8456-f4dcf908956a" /> Add pdl after, <img width="1403" height="375" alt="image" src="https://github.com/user-at... | kernel: `flashinfer/mla/_core.py`, `flashinfer/mla/cute_dsl/mla_decode.py`, `flashinfer/mla/cute_dsl/mla_decode_fp16.py`, `flashinfer/mla/cute_dsl/mla_decode_fp8.py`<br>test: `tests/attention/test_cute_dsl_mla_decode.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#3132](https://github.com/flashinfer-ai/flashinfer/pull/3132) [CuTe DSL] Fix FP8 MLA persistent perf regression and ProxyKind cu13 wheel breakage | 2026-04-22 | attention_kv, compiler_runtime | Two functional fixes plus two defensive refactors on the modular CuTe DSL attention kernels added in 2805: 1. **`perf(cute_dsl/mla)`** — recover a ~6.4 % geomean perf regression on FP8 MLA decode persistent (`is_var_seq=False`) versus trtllm-gen on B200, by unblocking the cute-DSL JIT's constant fol... | kernel: `flashinfer/cute_dsl/attention/mla_decode.py`, `flashinfer/cute_dsl/attention/mla_decode_fp8.py`, `flashinfer/cute_dsl/attention/prefill.py`, `flashinfer/cute_dsl/attention/roles/correction.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2255](https://github.com/flashinfer-ai/flashinfer/pull/2255) fix: support int64 IdType for RoPE part argument in `rope_quantize_fp8_append_paged_kv_cache` | 2025-12-24 | attention_kv, gemm_quant | 📌 Description `rope_quantize_fp8_append_paged_kv_cache` is a merged API of `rope_quantize` and `append_paged_kv_cache`(2037). However, `typename IdType` from `RopeQuantize` and `AppendPagedKVCache` should not be merged into the same one since they could be in different dtype. `AppendPagedKVCache`'s... | kernel: `csrc/rope.cu`, `flashinfer/rope.py`, `include/flashinfer/pos_enc.cuh`<br>test: `tests/attention/test_rope.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2325](https://github.com/flashinfer-ai/flashinfer/pull/2325) bugfix: fix multi-cta top-k implementation when k value is different for different row | 2026-01-13 | attention_kv, moe_routing, benchmark_test | 📌 Description This PR fixes a bug in the multi-CTA radix top-k kernel where histogram data corruption occurs when processing batches with mixed k values (some k >= vocab_size, others k < vocab_size) in persistent loop mode. Root Cause: When a row has k >= vocab_size, the radix select phase is skippe... | kernel: `include/flashinfer/topk.cuh`<br>test: `tests/utils/test_sampling.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2560](https://github.com/flashinfer-ai/flashinfer/pull/2560) fix: guard CUTLASS FMHA against SM12x and fix fmha_v2 SM121a check | 2026-03-13 | attention_kv, arch_pipeline, compiler_runtime | - **Remove SM12x from CUTLASS FMHA support**: `get_fmha_module()` and `gen_fmha_cutlass_sm100a_module()` incorrectly included SM12x GPUs (RTX 5090, DGX Spark) in their support checks. SM12x lacks the `tcgen05` MMA instructions required by the CUTLASS FMHA SM100 kernel (`SM100_MMA_F16BF16_SS/TS`, `SM... | kernel: `flashinfer/jit/attention/modules.py`, `flashinfer/prefill.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2630](https://github.com/flashinfer-ai/flashinfer/pull/2630) Add parallel attention | 2026-04-20 | attention_kv, scheduler_autotune | 📌 Description Add a `parallel_attention` module to FlashInfer that enables distributed attention computation using **Ulysses** (all-to-all head parallelism) and **Ring** (P2P KV exchange with online softmax merging) strategies, or a combination of both. New files - **`parallel_attention.py`** — `Par... | kernel: `flashinfer/parallel_attention/__init__.py`, `flashinfer/parallel_attention/attention_ops.py`, `flashinfer/parallel_attention/parallel_attention.py`, `flashinfer/parallel_attention/parallel_config.py`<br>test: `tests/attention/test_parallel_attention.py`<br>other: `scripts/task_test_multi_gpu_comm_kernels.sh` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2711](https://github.com/flashinfer-ai/flashinfer/pull/2711) feat: Add DiT-oriented kernels where Qk (Bmm1) type can be reinterpreted into Int8 or BFloat16 | 2026-05-01 | attention_kv, gemm_quant | 📌 Description This PR adds support for DiT-oriented TRTLLM kernels with 3 variants: - Qk in BFloat16 → Bmm1 in BFloat16 + V in E4m3 → Bmm2 in E4m3 - Qk in Int8 with SageAttention scaling factors → Bmm1 in Int8 + V in E4m3 → Bmm2 in E4m3 - Qk in E4m3 with SageAttention scaling factors → Bmm1 in E4m3... | kernel: `csrc/trtllm_fmha_kernel_launcher.cu`, `flashinfer/artifacts.py`, `flashinfer/prefill.py`, `include/flashinfer/trtllm/fmha/fmhaKernels.cuh`<br>test: `tests/attention/test_trtllm_ragged_dit.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2855](https://github.com/flashinfer-ai/flashinfer/pull/2855) [fix] bugfix 1044: Auto-inject well-known JIT additional tensor buffers in prefill and decode run() APIs | 2026-04-10 | attention_kv, compiler_runtime | 📌 Description This PR fixes https://github.com/flashinfer-ai/flashinfer/issues/1044. When using JIT attention variants with well-known additional tensor names (maybe_custom_mask, maybe_mask_indptr, maybe_alibi_slopes), the run() method's JIT path only forwarded user-provided *args, ignoring internal... | kernel: `flashinfer/decode.py`, `flashinfer/prefill.py`, `flashinfer/utils.py`<br>test: `tests/utils/test_jit_example.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#3020](https://github.com/flashinfer-ai/flashinfer/pull/3020) trtllm non causal support | 2026-05-01 | attention_kv, benchmark_test | 📌 Description Non-causal (dense-mask) support to trtllm_batch_context_with_kv_cache NOTE TO REVIEWER: the new "casual" input being inserted in the middle of the public API could cause API regressions to users using positional arguments. I think the current ordering next to window_left makes more sen... | kernel: `csrc/trtllm_fmha_kernel_launcher.cu`, `flashinfer/prefill.py`<br>benchmark: `benchmarks/routines/attention.py`<br>test: `tests/attention/test_trtllm_gen_attention.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2405](https://github.com/flashinfer-ai/flashinfer/pull/2405) perf: improve gdn decode cute-dsl kernels | 2026-02-03 | attention_kv, benchmark_test | 📌 Description Follow up of 2370 , this PR improves the benchmark scripts and add comparison with baselines: * benchmark using cupti with l2 flush * compare with sglang's `fused_sigmoid_gating_delta_rule_update` function (with tile size optimization mentioned by @ vadiklyutiy). this PR also implement... | kernel: `flashinfer/gdn_decode.py`<br>benchmark: `benchmarks/bench_gdn_decode.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2991](https://github.com/flashinfer-ai/flashinfer/pull/2991) Add SM 103 as one of supported capabilities for mm_M1_16_K7168_N256 | 2026-04-08 | attention_kv, benchmark_test, gemm_quant | add SM 103 support for mm_M1_16_K7168_N256. cc: @b8zhong, @Fridge003, @leejnau 🔍 Related Issues * https://github.com/sgl-project/sglang/pull/17707 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete.... | kernel: `flashinfer/gemm/routergemm.py`<br>test: `tests/model_optimizations/test_dsv3_router_gemm.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2750](https://github.com/flashinfer-ai/flashinfer/pull/2750) [Spark unit test debugging] Fix for tests/attention/test_trtllm_gen_mla.py | 2026-03-17 | attention_kv, benchmark_test | 📌 Description Problem: `test_trtllm_gen_mla.py` fails with `cudaErrorIllegalAddress` (254 failures) on the DGX Spark nightly CI when running XQA MLA tests with `layer_dimensions1` (64 query heads). Root cause: `xqa_mla()` in `flashinfer/xqa.py` hardcoded `head_group_ratio = 128` regardless of the ac... | kernel: `flashinfer/xqa.py`<br>test: `tests/attention/test_trtllm_gen_mla.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2138](https://github.com/flashinfer-ai/flashinfer/pull/2138) feat: add trtllm-gen per-tensor sparseMla kernels. | 2025-11-25 | attention_kv | 📌 Description This MR adds trtllm-gen per-tensor sparseMla kernels. 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Checks - [x] I have installed `pre-commit` by ru... | kernel: `csrc/fmhaReduction.cu`, `csrc/trtllm_fmha_kernel_launcher.cu`, `flashinfer/artifacts.py`, `flashinfer/decode.py`<br>test: `tests/attention/test_trtllm_gen_attention.py`, `tests/attention/test_trtllm_gen_mla.py`, `tests/comm/test_trtllm_mnnvl_allreduce_custom_comm.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2477](https://github.com/flashinfer-ai/flashinfer/pull/2477) feat: Add TRTLLM-Gen Skip-Softmax kernels for prefill and decode | 2026-02-11 | attention_kv | 📌 Description This PR adds initial Blackwell support for Skip-Softmax attention sparsity. See: - NVIDIA Blog post - BLASST Paper on arXiv The skip-softmax threshold is added as an optional parameter. If it is `None` then 'normal' attention is used, otherwise we will use skip-softmax with the specifi... | kernel: `csrc/trtllm_fmha_kernel_launcher.cu`, `flashinfer/artifacts.py`, `flashinfer/decode.py`, `flashinfer/mla.py`<br>test: `tests/attention/test_trtllm_gen_attention.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2619](https://github.com/flashinfer-ai/flashinfer/pull/2619) feat: add pool+indices support to gated_delta_rule_decode_pretranspose (bf16 path)  | 2026-03-04 | attention_kv, memory_primitives | - Adds `initial_state=[pool, HV, V, K]` and `initial_state_indices=[B]` parameters to `gated_delta_rule_decode_pretranspose`, allowing callers to pass an SSM state pool + per-batch indices directly instead of doing gather/scatter around the kernel. - The kernel reads and writes the pool in-place, so... | kernel: `flashinfer/gdn_decode.py`, `flashinfer/gdn_kernels/gdn_decode_bf16_state.py`<br>test: `tests/gdn/test_decode_delta_rule.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2770](https://github.com/flashinfer-ai/flashinfer/pull/2770) feat: Expose TRT-LLM FMHA style paged KV Cache and page table layout | 2026-03-23 | attention_kv | 📌 Description We received a request to expose TRT-LLM's paged KV Cache layout and page table style, in order to ease the process of integrating FlashInfer into TRT-LLM. This pull request exposes this feature via adding `uses_shared_paged_kv_idx` as an option to the TRT-LLM Gen FMHA kernels, updates... | kernel: `csrc/trtllm_fmha_kernel_launcher.cu`, `flashinfer/decode.py`, `flashinfer/mla.py`, `flashinfer/prefill.py`<br>test: `tests/attention/test_trtllm_gen_attention.py`, `tests/attention/test_trtllm_gen_mla.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#3147](https://github.com/flashinfer-ai/flashinfer/pull/3147) Ameyn/wide vec t1 | 2026-05-06 | attention_kv, benchmark_test | Replaces the legacy `gdn_decode_bf16state_cooprow_kernel` and the `gdn_decode_bf16state_mtp_kernel` (ILP=8) with a new **`gdn_wide_vec_kernel`** (LDG.E.128 / STG.E.128 fast path) plus a small-batch `mtp_ilp4` fallback. Drops ~1900 LOC of dead/unused code, adds split-pool support (2905-compatible) to... | kernel: `flashinfer/gdn_decode.py`, `flashinfer/gdn_kernels/gdn_decode_bf16_state.py`<br>benchmark: `benchmarks/bench_gdn_decode.py`<br>test: `tests/gdn/test_decode_delta_rule.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2536](https://github.com/flashinfer-ai/flashinfer/pull/2536) fallback to fa2 (instead of fa3) for unsupported configuration (bf16 Q, Fp8 KV) | 2026-02-12 | attention_kv, gemm_quant, scheduler_autotune | 📌 Description When the KV cache dtype is FP8, Q dtype is BF16, GPU is hopper, and the backend is set to "auto", we should set the backend as "fa2" instead of "fa3". 🔍 Related Issues https://github.com/flashinfer-ai/flashinfer/issues/2348 🚀 Pull Request Checklist Thank you for contributing to FlashIn... | kernel: `flashinfer/utils.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#3156](https://github.com/flashinfer-ai/flashinfer/pull/3156) [fix] fix blackwell gdn accuracy issue | 2026-04-24 | attention_kv, arch_pipeline | 📌 Description Fixed the accuracy issue in blackwell gdn kernel found by @bestzsq. The root cause is that the legacy `max_coord` is not the actual last coord of the `sCumprod`. We change to the last coord instead. It's a deeply hidden bug that we hadn't discovered previously. Thanks to @bestzsq. Repr... | kernel: `flashinfer/gdn_kernels/blackwell/gated_delta_net_chunked.py`<br>test: `tests/gdn/test_prefill_delta_rule.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#3290](https://github.com/flashinfer-ai/flashinfer/pull/3290) Fix [Spark unit test CI]: defer torch._dynamo.disable to avoid import-time crash in CI | 2026-05-12 | attention_kv, compiler_runtime, benchmark_test | 📌 Description - 3271 (feat(moe): add SM120 W4A16 b12x kernels) added a @torch._dynamo.disable decorator to current_cuda_stream() in cute_dsl/utils.py. This eagerly imports torch._dynamo at module load time, which triggers getpass.getuser() during cache-dir initialization. This crashes in CI containe... | kernel: `flashinfer/cute_dsl/utils.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2254](https://github.com/flashinfer-ai/flashinfer/pull/2254) feat: support non-contiguous query for trtllm-gen attention backend | 2025-12-22 | attention_kv, benchmark_test | 📌 Description As requested by @nandor , this pr implements non-contiguous query for trtllm-gen attention backend (by passing the stride to tma descriptor constructor). We can also add similar supports to xqa as well, but in this PR we only make change to trtllm-gen backend. 🔍 Related Issues 🚀 Pull R... | kernel: `csrc/trtllm_fmha_kernel_launcher.cu`, `include/flashinfer/trtllm/fmha/fmhaRunnerParams.h`, `include/flashinfer/trtllm/fmha/kernelParams.h`<br>test: `tests/attention/test_trtllm_gen_attention.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2489](https://github.com/flashinfer-ai/flashinfer/pull/2489) [bugfix]Correct chunk_end calculation in multi-CTA collaboration when max_len > length | 2026-02-20 | attention_kv, benchmark_test, moe_routing | 📌 Description bugfix to https://github.com/flashinfer-ai/flashinfer/issues/2486 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Checks - [ ] I have installed `pre-c... | kernel: `include/flashinfer/topk.cuh`<br>test: `tests/utils/test_topk.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2842](https://github.com/flashinfer-ai/flashinfer/pull/2842) perf: Optimize GDN MTP decode kernel (v15) — eliminate ilp=1 fallback… | 2026-04-02 | attention_kv, benchmark_test | …, add inline kernel for small batches Replace the three-kernel dispatch (original + optimized + smem) with a two-kernel design: an inline kernel for BS<=2 and a warp-specialized kernel for BS>=3. Key changes: - BS=8-16: Use tile_v=32 with ilp=4 for ALL T values, removing the ilp=1 fallback at T>=4... | kernel: `flashinfer/gdn_decode.py`, `flashinfer/gdn_kernels/__init__.py`, `flashinfer/gdn_kernels/gdn_decode_mtp.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#3022](https://github.com/flashinfer-ai/flashinfer/pull/3022) [Fix] Fix XQA V tile reading from wrong page when nbVItersPerXIter > 1 | 2026-05-13 | attention_kv, arch_pipeline | 📌 Description Fix incorrect XQA attention results on architectures with `cacheVTileSeqLen = 32` (SM120/SM121) when `head_dim = 256` and `page_size < 64`. Bug On SM120 with `head_dim=256` and `page_size` of 16 or 32, the XQA `mha.cu` kernel produces incorrect attention outputs. All `page_size=64` cas... | kernel: `csrc/xqa/mha.cu`<br>test: `tests/attention/test_trtllm_gen_attention.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#3064](https://github.com/flashinfer-ai/flashinfer/pull/3064) Loosened trtllm_ragged_attention_deepseek shape assertion | 2026-05-07 | attention_kv, arch_pipeline | 📌 Description `trtllm_ragged_attention_deepseek` kernel is available for GLM-5 but is currently blocked by the assertion. Loosened assertion and added unit test to verify. Cutlass does **not** support this shape, so a naive implementation is used to test correctness. Also added missing checks in cut... | kernel: `csrc/fmha_cutlass_sm100.cu`, `flashinfer/prefill.py`, `include/flashinfer/attention/blackwell/fmha_cutlass_sm100.cuh`<br>test: `tests/attention/test_trtllm_gen_attention.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2456](https://github.com/flashinfer-ai/flashinfer/pull/2456) fix: fix illegal memory access for NaN input in sampling kernels | 2026-03-13 | attention_kv, benchmark_test | 📌 Description Fix illegal memory access when input probabilities contain NaN values. Added `valid` output tensor so callers can distinguish failed sampling from legitimately sampling token 0. Also added missing `@register_fake_op` for `min_p_sampling_from_probs` to support `torch.compile`. Changes A... | kernel: `csrc/flashinfer_sampling_binding.cu`, `csrc/sampling.cu`, `flashinfer/sampling.py`, `include/flashinfer/sampling.cuh`<br>test: `tests/utils/test_sampling.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2547](https://github.com/flashinfer-ai/flashinfer/pull/2547) feat: Enable TRTLLM-Gen Skip-Softmax attention for MLA | 2026-02-19 | attention_kv | 📌 Description This PR is a follow-up to 2477, expanding support to MLA. It also modifies the runner slightly to 'short-circuit' to normal attention kernels if threshold is zero, to reduce overhead. Tests updated to use a very tiny threshold instead, so we still get the same result as normal attentio... | kernel: `csrc/trtllm_fmha_kernel_launcher.cu`, `flashinfer/mla.py`, `flashinfer/prefill.py`<br>test: `tests/attention/test_trtllm_gen_attention.py`, `tests/attention/test_trtllm_gen_mla.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2588](https://github.com/flashinfer-ai/flashinfer/pull/2588) Perf: Optimize GDN decode pretranspose kernel for all batch sizes | 2026-02-26 | attention_kv, benchmark_test | Two optimizations: 1. Early gate reads: Move gate GMEM reads (r_A_log, r_a, r_dt_bias, r_b) to immediately before the barrier, hiding memory latency during sync. 2. Always use 8-CTA architecture: Remove batch size conditional that switched to big_batch (1 CTA) for B > 32. The 8-CTA small_batch kerne... | kernel: `flashinfer/gdn_decode.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#3058](https://github.com/flashinfer-ai/flashinfer/pull/3058) Support lse in trtllm paged attn kernels | 2026-04-15 | attention_kv | 📌 Description 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Checks - [x] I have installed `pre-commit` by running `pip install pre-commit` (or used your preferred... | kernel: `csrc/trtllm_fmha_kernel_launcher.cu`, `flashinfer/decode.py`, `flashinfer/mla/_core.py`, `flashinfer/prefill.py`<br>test: `tests/attention/test_trtllm_gen_attention.py`, `tests/attention/test_trtllm_gen_mla.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2053](https://github.com/flashinfer-ai/flashinfer/pull/2053) feat: add xqa mla backend | 2025-11-10 | attention_kv | 📌 Description add xqa mla backend and corresponding unittests 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Checks - [ ] I have installed `pre-commit` by running... | kernel: `csrc/xqa/mla_sm120.cu`, `flashinfer/decode.py`<br>test: `tests/attention/test_trtllm_gen_mla.py`, `tests/attention/test_xqa_mla_batch_decode.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2281](https://github.com/flashinfer-ai/flashinfer/pull/2281) feat: IdType indices in sampling kernels | 2026-01-03 | attention_kv, benchmark_test | 📌 Description Based on this comment in https://github.com/flashinfer-ai/flashinfer/pull/2127, we can add support for Int64 indices as well. I decided to do this using `IdType` like it is done in other files. 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before w... | kernel: `csrc/sampling.cu`, `csrc/tvm_ffi_utils.h`, `flashinfer/sampling.py`<br>test: `tests/utils/test_sampling.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2857](https://github.com/flashinfer-ai/flashinfer/pull/2857) [fix] bugfix 541: Make single_prefill/decode compatible with torch.compile CUDA graphs | 2026-04-10 | attention_kv | 📌 Description This PR fixes https://github.com/flashinfer-ai/flashinfer/issues/541. single_prefill_with_kv_cache and single_decode_with_kv_cache fail with RuntimeError: live storage data ptrs in the cudagraph pool but not accounted for when used with torch.compile(mode="reduce-overhead"). The root c... | kernel: `flashinfer/decode.py`, `flashinfer/prefill.py`, `flashinfer/utils.py`<br>test: `tests/attention/test_batch_decode_kernels.py`, `tests/attention/test_batch_prefill_kernels.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2125](https://github.com/flashinfer-ai/flashinfer/pull/2125) feat: support variable sequence length in decode kernel of trtllm-gen attention | 2025-12-20 | attention_kv | 📌 Description Fix 1832 This PR adds two extra parameters `max_q_len` and `cum_seq_lens_q` to `flashinfer.decode.trtllm_batch_decode_with_kv_cache` kernel to support variable sequence lengths for request queries. Benchmark result: <img width="1233" height="728" alt="image" src="https://github.com/use... | kernel: `csrc/trtllm_fmha_kernel_launcher.cu`, `flashinfer/decode.py`, `flashinfer/mla.py`<br>test: `tests/attention/test_trtllm_gen_attention.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2799](https://github.com/flashinfer-ai/flashinfer/pull/2799) [fmha-v2] Support HND and NHD paged KV cache layouts with conditional stride handling | 2026-05-01 | attention_kv | Previously, the fmha_v2 paged kv kernels only supported the HND layout. When users passed in an NHD layout, transpose(-3, -2).contiguous() is called, which introduced significant overhead. This pull request adds native support for the NHD layout to the fmha_v2 kernels by allowing users to pass in cu... | kernel: `csrc/fmha_v2/fmha/gmem_tile_qkv_packed.h`, `csrc/fmha_v2/fmha/warpspec/dma.h`, `csrc/fmha_v2/fused_multihead_attention.h`, `csrc/fmha_v2/fused_multihead_attention_demo_bert_params.h` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2959](https://github.com/flashinfer-ai/flashinfer/pull/2959) [Fmha] Add head_dim=512 support for trtllm attention kernels | 2026-04-22 | attention_kv | Add support for `head_dim=512` in the trtllm FMHA kernel selection. Changes - Add SDPA-based reference implementation for `head_dim > 256` in tests (FlashInfer FA2/FA3 kernels don't support `head_dim > 256`) - Add `test_trtllm_batch_prefill_head_dim_512` and `test_trtllm_batch_decode_head_dim_512` c... | kernel: `csrc/fmhaReduction.cu`, `flashinfer/artifacts.py`, `include/flashinfer/trtllm/fmha/fmhaKernels.cuh`, `include/flashinfer/trtllm/fmha/kernelParams.h`<br>test: `tests/attention/test_trtllm_gen_attention.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2132](https://github.com/flashinfer-ai/flashinfer/pull/2132) feat: add seed offset args to sampler to allow cuda graph support | 2025-11-26 | attention_kv, benchmark_test | 📌 Description This PR adds optional seed/offset args to all the sampler functions to prevent calling the `get_seed_and_offset` function. If that function is not called, we can potentially make the sampler forward call as part of CUDAGraph and use that to replay it. We can directly compute the Seed/o... | kernel: `flashinfer/sampling.py`<br>test: `tests/utils/test_sampling.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2521](https://github.com/flashinfer-ai/flashinfer/pull/2521) Feat/gdn decode pooled | 2026-03-09 | attention_kv | 📌 Description This PR adds pool-indexed (indirect) state access to the GDN decode kernel, enabling zero-copy integration with SGLang's state pool architecture. Background: SGLang's State Pool Architecture In SGLang, when serving linear attention models (like Qwen3-Next using Gated Delta Rule), we ma... | kernel: `flashinfer/gdn_decode.py`, `flashinfer/gdn_kernels/gdn_decode_pretranspose.py`<br>test: `tests/gdn/test_decode_delta_rule.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#3136](https://github.com/flashinfer-ai/flashinfer/pull/3136) fix(mla): widen page index to int64_t to avoid 32-bit overflow | 2026-05-06 | attention_kv | 📌 Description In the MLA decode/prefill KV load path, `indices[q] * ckv_stride_page` was computed in 32-bit because `IdType` is `int32_t` and `*_stride_page` is `uint32_t`; the product wraps modulo 2^32 before any widening to `int64_t` (Hopper) or pointer arithmetic (FA2). For large page pools (e.g.... | kernel: `include/flashinfer/attention/mla.cuh`, `include/flashinfer/attention/mla_hopper.cuh`<br>test: `tests/attention/test_mla_decode_kernel.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#3230](https://github.com/flashinfer-ai/flashinfer/pull/3230) fix(gdn_decode): widen pool indices to Int64 to prevent int32 element-offset overflow | 2026-05-11 | attention_kv | 📌 Description Fix `CUDA error: an illegal memory access was encountered` in `flashinfer.gdn_decode.gated_delta_rule_decode_pretranspose` when the pool+indices API is used with sufficiently large pool indices. **Root cause.** The CuTe-DSL kernels compute the per-slot element offset (`pool_idx * strid... | kernel: `flashinfer/gdn_kernels/gdn_decode_bf16_state.py`, `flashinfer/gdn_kernels/gdn_decode_pretranspose.py`<br>test: `tests/gdn/test_decode_pretranspose_noncontiguous_pool.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2148](https://github.com/flashinfer-ai/flashinfer/pull/2148) Enable Hopper FA3 FP8 attention in decode.py | 2026-01-06 | attention_kv | 📌 Description https://github.com/flashinfer-ai/flashinfer/pull/2111 already enabled Hopper FA3 FP8 attention in `prefill.py`. This is just a follow-up PR to make the same change in `decode.py` because `decode.py` actually uses prefill kernels. 🔍 Related Issues 🚀 Pull Request Checklist Thank you for... | kernel: `flashinfer/decode.py`, `flashinfer/jit/attention/modules.py`, `flashinfer/prefill.py`<br>test: `tests/attention/test_hopper_fp8_attention.py`, `tests/utils/test_jit_example.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2836](https://github.com/flashinfer-ai/flashinfer/pull/2836) [Fmha] Sparse MLA decode kernel selection heuristics | 2026-03-20 | attention_kv, scheduler_autotune | 📌 Description Improve kernel-selection heuristic in FlashInfer's trtllm-gen FMHA runner (SM100/SM103 sparse MLA decode), and cleans up the kernel selection loop in `run()`. Changes **`selectSparseMlaGenerationKernel()` (new function)** Separates sparse MLA selection from the generic generation path... | kernel: `flashinfer/artifacts.py`, `include/flashinfer/trtllm/fmha/fmhaKernels.cuh`, `include/flashinfer/trtllm/fmha/kernelParams.h` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2905](https://github.com/flashinfer-ai/flashinfer/pull/2905) feat(gdn): separate input and output pool indices | 2026-04-17 | attention_kv | 📌 Description Introduce separate output indices parameter for gated_delta_rule_decode_pretranspose. This addresses decoded part of feature request in 2873 🔍 Related Issues 2873 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the... | kernel: `flashinfer/gdn_decode.py`, `flashinfer/gdn_kernels/gdn_decode_bf16_state.py`, `flashinfer/gdn_kernels/gdn_decode_pretranspose.py`<br>test: `tests/gdn/test_decode_delta_rule.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2727](https://github.com/flashinfer-ai/flashinfer/pull/2727) [gdn] support non-contiguous state for decoding | 2026-03-25 | attention_kv | 📌 Description vllm uses non-contiguous state for gdn. Make flashinfer also support it 🔍 Related Issues https://github.com/flashinfer-ai/flashinfer/pull/2521 https://github.com/flashinfer-ai/flashinfer/issues/2687 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review you... | kernel: `flashinfer/gdn_decode.py`, `flashinfer/gdn_kernels/gdn_decode_pretranspose.py`<br>test: `tests/gdn/test_decode_pretranspose_noncontiguous_pool.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#3173](https://github.com/flashinfer-ai/flashinfer/pull/3173) fix: add sm_121 to TMEM column fallback map | 2026-05-05 | attention_kv, compiler_runtime | 📌 Description Adds `sm_121` to `_TMEM_MAX_ALLOC_COLUMNS_MAP` in `flashinfer/cute_dsl/attention/compat.py`. SM120 and SM121 share the same 12.x compute capability spec, so the value (512) matches `sm_120`. Without this entry, `get_max_tmem_alloc_cols` raises `ValueError` on SM121 devices when `cute.a... | kernel: `flashinfer/cute_dsl/attention/compat.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2810](https://github.com/flashinfer-ai/flashinfer/pull/2810) feat(gdn): add padding index guard for bf16 decode kernel | 2026-03-22 | attention_kv | Clamp negative slot indices to 0 before passing to the bf16 fast-path kernel to prevent out-of-bounds memory access when padding indices (-1) are present in initial_state_indices. 📌 Description 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your... | kernel: `flashinfer/gdn_kernels/gdn_decode_bf16_state.py`<br>test: `tests/gdn/test_decode_delta_rule.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#3251](https://github.com/flashinfer-ai/flashinfer/pull/3251) Issue #3047: Handle empty KV in MLA chunked-prefill | 2026-05-07 | attention_kv | 📌 Description Moves empty-KV handling for MLA chunked-prefill into the kernels. The generic MLA path now short-circuits zero-KV tiles before invalid negative tile access and writes the empty-attention identity directly (`out = 0`, `lse = -inf`). The empty writer also handles partial outputs with the... | kernel: `include/flashinfer/attention/mla.cuh`, `include/flashinfer/attention/mla_hopper.cuh`<br>test: `tests/attention/test_deepseek_mla.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2307](https://github.com/flashinfer-ai/flashinfer/pull/2307) Added the device version checks | 2026-01-08 | attention_kv, benchmark_test | 📌 Description 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Checks - [ x] I have installed `pre-commit` by running `pip install pre-commit` (or used your preferre... | test: `tests/attention/test_cudnn_prefill.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2945](https://github.com/flashinfer-ai/flashinfer/pull/2945) fix: use float instead of double in sampling binary search to avoid FP64 bottleneck on SM103 | 2026-04-04 | attention_kv, arch_pipeline | 📌 Description - Replace `double` with `float` for binary search variables (`low`, `high`, `pivot_0`, `pivot_1`) in three sampling kernels: `TopKSamplingFromProbKernel`, `TopPSamplingFromProbKernel`, `TopKTopPSamplingFromProbKernel` - Use inline PTX IEEE-754 arithmetic (`mul.rn.f32`, `add.rn.f32`, `d... | kernel: `include/flashinfer/sampling.cuh` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#3163](https://github.com/flashinfer-ai/flashinfer/pull/3163) fix: remove over-strict K%4 assert in get_shuffle_matrix_sf_a_row_indices | 2026-05-12 | attention_kv, gemm_quant | Removes the `assert K % 4 == 0` guard from `get_shuffle_matrix_sf_a_row_indices` in `flashinfer/utils.py`. The assertion rejects valid inputs (e.g. `K=90` for GPT-OSS-120B MXFP4 weights), even though the downstream kernel handles them correctly. **Why it's safe to remove:** - `get_shuffle_matrix_sf_... | kernel: `flashinfer/utils.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2415](https://github.com/flashinfer-ai/flashinfer/pull/2415) Remove cudaMalloc/Free in GDN prefill kernel | 2026-01-25 | attention_kv | 📌 Description In GDN prefill kernel, this line of code will cause redundant cudaMalloc/cudaFree in kernel execution, which harms runtime performance. This workspace buffer is used for TMA store output. https://github.com/flashinfer-ai/flashinfer/blob/a49b45336e56e4615eae102cf29d5110293d9130/csrc/fla... | kernel: `csrc/flat/prefill/prefill_kernel.hpp`, `csrc/flat/prefill/prefill_kernel_delta_rule_sm90.cu`, `csrc/flat/prefill/prefill_kernel_delta_rule_sm90.cuh`, `csrc/gdn_prefill_launcher.cu` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2509](https://github.com/flashinfer-ai/flashinfer/pull/2509) perf: cache cudaGetDeviceProperties in gdn_prefill to avoid per-call overhead | 2026-02-09 | attention_kv | - Cache `device_properties.major` and `device_properties.multiProcessorCount` using `static thread_local` variables in `gdn_prefill_launcher()` and `gdn_prefill()` respectively, instead of calling `cudaGetDeviceProperties` on every invocation. - Each `cudaGetDeviceProperties` call adds ~1ms of synch... | kernel: `csrc/gdn_prefill_launcher.cu` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#3267](https://github.com/flashinfer-ai/flashinfer/pull/3267) [chore] Add guard to blackwell GDN prefill | 2026-05-12 | attention_kv | 📌 Description Raise not implemented error when trying to use blackwell GDN prefill kernel but cuda version earlier than 13 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-... | kernel: `flashinfer/gdn_prefill.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#3293](https://github.com/flashinfer-ai/flashinfer/pull/3293) fix(gdn): allow importing gdn_decode without a CUDA device | 2026-05-14 | attention_kv | 📌 Description - Defer `torch.cuda.get_device_properties()` and `torch.cuda.get_device_capability()` in `gdn_decode_bf16_state.py `from module scope to lazy `@functools.cache` helpers that derive the device from the input tensor (q.device), matching the convention used by the CuTe DSL MLA and MoE bac... | kernel: `flashinfer/gdn_decode.py`, `flashinfer/gdn_kernels/gdn_decode_bf16_state.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2295](https://github.com/flashinfer-ai/flashinfer/pull/2295) bugfix: use torch cached default generators | 2026-01-07 | attention_kv, memory_primitives | 📌 Description Fixed the 2284. In the past, before 1641, the flashinfer used torch default generator `at::cuda::detail::getDefaultCUDAGenerator()` while 1641 will create one new generator instance at a time. This PR recovers the default generator from torch. 🔍 Related Issues 🚀 Pull Request Checklist... | kernel: `flashinfer/sampling.py`, `flashinfer/utils.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2610](https://github.com/flashinfer-ai/flashinfer/pull/2610) Ameyn/gdn bf16 tolerance parallel reduction | 2026-02-23 | attention_kv | 📌 Description 1. fma2 not supported for hopper, fix for that for bf16 h state version of gdn decode. 2. Increase atol_kv from 0.005 to 0.016 to accommodate 1 ULP differences in BF16 that arise from parallel warp-level reductions vs sequential reference implementation. This fixes seed-specific test f... | kernel: `flashinfer/gdn_kernels/gdn_decode_bf16_state.py`<br>test: `tests/gdn/test_decode_delta_rule.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2801](https://github.com/flashinfer-ai/flashinfer/pull/2801) [fix] bugfix 1419: Add batch size shape validation in decode and prefill run() APIs | 2026-03-23 | attention_kv | 📌 Description This PR fixes https://github.com/flashinfer-ai/flashinfer/issues/1419, adding shape checks in several run() APIs to validate q.shape[0] before launching kernels, preventing silent OOB reads that cause CUDA crashes. 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to... | kernel: `flashinfer/decode.py`, `flashinfer/prefill.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2844](https://github.com/flashinfer-ai/flashinfer/pull/2844) read real strides for kv and block scale | 2026-04-01 | attention_kv | Acked-by: Shiyang Chen <shiychen@nvidia.com> 📌 Description I realize that current kv block scale factor's stride is derived from kv cache instead of reading from the actual tensor. This put a lot of unnecessary assumption/constraint on the scale factor. 🔍 Related Issues 🚀 Pull Request Checklist Than... | kernel: `csrc/trtllm_fmha_kernel_launcher.cu`, `flashinfer/decode.py`, `flashinfer/prefill.py`, `include/flashinfer/trtllm/fmha/fmhaRunnerParams.h` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#3165](https://github.com/flashinfer-ai/flashinfer/pull/3165) fix(gdn): address remaining CodeRabbit feedback from #3001 | 2026-04-27 | attention_kv | 📌 Description Addresses the two remaining CodeRabbit findings on 3001 that weren't applied before merge: * **Normalize `scale=0.0` to the default `1/sqrt(d_k)`** before backend dispatch so the same call gives matching numerics on SM90 and SM100. The SM90 C++ kernel treats `0.0` as a sentinel for "us... | kernel: `flashinfer/gdn_prefill.py` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2432](https://github.com/flashinfer-ai/flashinfer/pull/2432) fix: Sampling: CUDA Graph fix | 2026-02-13 | attention_kv | 📌 Description 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Checks - [x] I have installed `pre-commit` by running `pip install pre-commit` (or used your preferred... | kernel: `csrc/flashinfer_sampling_binding.cu`, `csrc/sampling.cu`, `flashinfer/sampling.py`, `include/flashinfer/sampling.cuh` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#1727](https://github.com/flashinfer-ai/flashinfer/pull/1727) fix: put sampling kernel launch into macro | 2025-09-19 | attention_kv | The macro DISPATCH_COMPUTE_CAP_NUM_THREADS is used for compatibility with kernels less than sm80, but some kernel launches are not included in it, which breaks compatibility with sm75. 1697 📌 Description 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we re... | kernel: `include/flashinfer/sampling.cuh` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |
| [#2438](https://github.com/flashinfer-ai/flashinfer/pull/2438) fix: improve numerical stability of Gumbel sampling | 2026-01-30 | attention_kv | 📌 Description Without this change, `flashinfer.sampling.sampling_from_logits` too often samples extremely unlikely tokens. 🔍 Related Issues <https://nvbugs/5791242> 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following i... | kernel: `include/flashinfer/sampling.cuh` | Preserve page/KV/layout and plan-run contracts; profile memory traffic separately from math utilization. |

### MoE / Routing

Use this section for: Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea.
NCU first look: Tensor pipe %, SM imbalance, branch divergence, L2 traffic, permutation traffic, and synchronization stalls.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#2966](https://github.com/flashinfer-ai/flashinfer/pull/2966) Fused moe all-reduce routed scaling factor + quant support | 2026-04-13 | moe_routing, memory_primitives, gemm_quant, attention_kv, benchmark_test | …rnel 📌 Description In some cases like kimi k2.5, glm, we require an extra scale factor applied over all routed experts. While we can add a constant multiplier over the experted scaling factors, it is more efficient to fuse this multiplication inside the kernel. e.g. https://huggingface.co/moonshota... | kernel: `csrc/trtllm_moe_allreduce_fusion.cu`, `flashinfer/comm/trtllm_ar.py`, `include/flashinfer/comm/trtllm_moe_allreduce_fusion.cuh`<br>test: `tests/comm/test_trtllm_moe_allreduce_fusion_finalize.py` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#3227](https://github.com/flashinfer-ai/flashinfer/pull/3227) [Bugfix] Fix fused MoE autotuning correctness issues by filtering clusterDimZ | 2026-05-06 | moe_routing, scheduler_autotune, gemm_quant, arch_pipeline, attention_kv | 📌 Description There has been correctness issues observed using fused MoE autotuning. Investigating shows tactics using `clusterDimZ >= 1` produces incorrect outputs. See https://github.com/flashinfer-ai/flashinfer/issues/3197 This PR changes the following: - only allow tactics with `clusterDimZ == 1... | kernel: `csrc/trtllm_batched_gemm_runner.cu`, `flashinfer/fused_moe/core.py`<br>test: `tests/moe/test_trtllm_gen_moe_autotune_tactics.py` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#3115](https://github.com/flashinfer-ai/flashinfer/pull/3115) perf(autotuner): replace power-of-2 token buckets with hybrid spacing & fix missing routing_replay_out arg | 2026-04-24 | moe_routing, benchmark_test, gemm_quant, scheduler_autotune, attention_kv | 📌 Description This PR includes two improvements: 1. **perf(autotuner): Replace power-of-2 token buckets with hybrid spacing** — Pure power-of-2 spacing creates huge gaps at large values (e.g. a jump from 1024 to 2048), forcing the autotuner to pick a kernel optimised for a very different workload si... | kernel: `flashinfer/fused_moe/core.py`, `flashinfer/fused_moe/cute_dsl/tuner.py`, `flashinfer/fused_moe/utils.py`, `flashinfer/gemm/gemm_base.py` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#2916](https://github.com/flashinfer-ai/flashinfer/pull/2916) fix: Fix autotuner crash on meta-device tensor in trtllm_fp4_block_scale_routed_moe | 2026-04-01 | moe_routing, scheduler_autotune, gemm_quant, benchmark_test, attention_kv | 📌 Description - Fixes `RuntimeError: Cannot pack tensors on meta when trtllm_fp4_block_scale_routed_moe` is called with autotuning enabled - Ensures the autotuner profiles the correct kernel code path (no-routing) when routing is pre-computed Root Cause When `trtllm_fp4_block_scale_routed_moe` is ca... | kernel: `flashinfer/fused_moe/core.py`<br>test: `tests/autotuner/test_trtllm_fused_moe_autotuner_integration.py` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#3171](https://github.com/flashinfer-ai/flashinfer/pull/3171) fix(cute_dsl/moe): correct tile_size=256 gemm2 tactic enumeration | 2026-04-30 | moe_routing, compiler_runtime, gemm_quant, attention_kv, benchmark_test | 📌 Description `get_gemm2_valid_tactics(tile_size)` in `flashinfer/fused_moe/cute_dsl/tuner.py` ignored its `tile_size` argument and unconditionally returned 1-CTA tactics (`mma_m=128, cluster=(1, X)`). At `tile_size=256` the upstream gemm1 kernel emits a 2-CTA `mma_m=256` layout that the 1-CTA gemm2... | kernel: `flashinfer/fused_moe/cute_dsl/tuner.py`<br>test: `tests/moe/test_cute_dsl_fused_moe.py` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#3191](https://github.com/flashinfer-ai/flashinfer/pull/3191) fix(sm12x): fix micro-kernel workspace sizing when routed_rows > num_local_experts | 2026-05-07 | moe_routing, compiler_runtime, arch_pipeline, attention_kv, benchmark_test | Two bugs in the `b12x_fused_moe` micro-kernel path (SM120/SM121, triggered when `routed_rows <= micro_cutover`, typically 20–40): - **`allocate_sm120_static_workspace`** (`moe_dispatch.py`): `compact_topk_ids` was sized `state_E` (num local experts), but the micro-kernel fills it with `flat_ids` of... | kernel: `flashinfer/fused_moe/cute_dsl/blackwell_sm12x/moe_dispatch.py`, `flashinfer/fused_moe/cute_dsl/blackwell_sm12x/triton_compact.py`<br>test: `tests/moe/test_b12x_fused_moe.py` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#3304](https://github.com/flashinfer-ai/flashinfer/pull/3304) fix: MNNVL Allreduce uses bitwise sentinel checking to avoid subnormal value issue (#3053) | 2026-05-13 | moe_routing, memory_primitives, norm_elementwise, attention_kv, benchmark_test | 📌 Description This PR fixed an inconsistency when polling communication buffer and checking for sentinel value. The old code does FP comparison with 0 and checks the sign bit to determine whether the polled value is -0.0; The caveat is that when the valid input contains a negative subnormal value, t... | kernel: `include/flashinfer/comm/trtllm_mnnvl_allreduce.cuh`<br>test: `tests/comm/test_trtllm_mnnvl_allreduce.py` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#3193](https://github.com/flashinfer-ai/flashinfer/pull/3193) perf(moe): optimize SM120 b12x MoE short decode | 2026-05-07 | moe_routing, arch_pipeline, attention_kv, benchmark_test, compiler_runtime | Synchronize the SM120 b12x MoE implementation from the upstream b12x kernels, including the short-decode dispatch and micro-kernel fixes. 📌 Description Synchronizes the SM120 B12x MoE implementation with the upstream b12x kernel changes. This PR updates the short-decode path for B12x fused MoE, incl... | kernel: `flashinfer/fused_moe/cute_dsl/blackwell_sm12x/moe_dispatch.py`, `flashinfer/fused_moe/cute_dsl/blackwell_sm12x/moe_micro_kernel.py` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#2557](https://github.com/flashinfer-ai/flashinfer/pull/2557) [Bugfix][comm] Fix FP4 one-shot launch config instability in trtllm_allreduce_fusion | 2026-02-17 | moe_routing, memory_primitives, gemm_quant, scheduler_autotune, attention_kv | 📌 Description Fix unstable FP4 one-shot launch configuration in `trtllm_allreduce_fusion`. On SM100/B300 with FP4 fusion, launcher could pick non power-of-two `cluster_size` (e.g. hidden_dim=7168 -> block_size=128 -> cluster_size=7). This can lead to systematic numerical mismatch (observed ~41.4% mi... | kernel: `include/flashinfer/comm/trtllm_allreduce_fusion.cuh` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#3226](https://github.com/flashinfer-ai/flashinfer/pull/3226) cute_dsl/moe: drop redundant Python-side moe_sort buffer init | 2026-05-06 | moe_routing, compiler_runtime, memory_primitives, attention_kv, benchmark_test | 📌 Description Aligns flashinfer's `moe_sort` wrapper with the trt-llm thop allocation pattern (`torch::empty(...)` with no Python-side init). Removed from the preallocated-buffer (CUDA-graph) path: - `expanded_idx_to_permuted_idx.fill_(-1)` (and the stale "kernel expects this" comment) - `permuted_i... | kernel: `flashinfer/fused_moe/cute_dsl/moe_utils.py`<br>test: `tests/moe/test_cute_dsl_fused_moe.py` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#2864](https://github.com/flashinfer-ai/flashinfer/pull/2864) Add support for Relu2 in BF16 fused MoE | 2026-04-13 | moe_routing, attention_kv, gemm_quant, benchmark_test | 📌 Description * Added support for Relu2 non-gated activation in BF16 Fused MoE by adding `activation_type` to external API: * `trtllm_bf16_moe` * `trtllm_bf16_routed_moe` * `Bf16MoeLauncher::init` * Updated trtllm-gen batched GEMM kernels * Updated `tests/moe/test_trtllm_gen_fused_moe.py::test_deeps... | kernel: `csrc/trtllm_batched_gemm_runner.cu`, `csrc/trtllm_fused_moe_kernel_launcher.cu`, `flashinfer/artifacts.py`, `flashinfer/fused_moe/__init__.py`<br>test: `tests/moe/test_trtllm_gen_fused_moe.py`, `tests/moe/utils.py` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#2821](https://github.com/flashinfer-ai/flashinfer/pull/2821) fix: Autotuner _find_nearest_profile non-power-of-2 num_tokens, create launchers for all supported tileN in trtllm fused MoE | 2026-03-20 | moe_routing, scheduler_autotune, benchmark_test, attention_kv | 📌 Description It fixes two autotuner related bugs: 1. Revert back the autotuner fix that was reverted in https://github.com/flashinfer-ai/flashinfer/pull/2697 2. Fix the issue that https://github.com/flashinfer-ai/flashinfer/pull/2697 revealed, which is trtllm fused MoE kernel launcher crash when it... | kernel: `csrc/trtllm_fused_moe_kernel_launcher.cu`, `flashinfer/autotuner.py`<br>test: `tests/autotuner/test_autotuner_core.py`, `tests/autotuner/test_trtllm_fused_moe_autotuner_integration.py`, `tests/autotuner/utils.py` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#2984](https://github.com/flashinfer-ai/flashinfer/pull/2984) fix: restore SM120 CUTLASS MoE tile candidate removed by #2927 (test_trtllm_cutlass_fused_moe.py) | 2026-04-07 | moe_routing, arch_pipeline, benchmark_test, gemm_quant, scheduler_autotune | Fix SM120 CUTLASS MoE test failures caused by 2927 removing viable tile candidates. Root cause Overestimated SMEM usage (e.g. 128KB instead of actual 48-64KB for `128x128x128B`) and removed all viable tiles for FP8×FP4 (MXFP8×MXFP4). Changes - `cutlass_heuristic.cpp`: Restore `128x128x128B` as SM120... | kernel: `csrc/nv_internal/tensorrt_llm/kernels/cutlass_kernels/cutlass_heuristic.cpp` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#2564](https://github.com/flashinfer-ai/flashinfer/pull/2564) fix: W4A8 autotune crash in cutlass_fused_moe profiler workspace | 2026-02-18 | moe_routing, benchmark_test, gemm_quant, scheduler_autotune | Description **Problem** `cutlass_fused_moe` crashes with `RuntimeError: Assertion failed: quant_1 && quant_2` when called inside `autotune()` with W4A8 quantization. Other quant modes (FP8 block scaling, BF16+MXFP4) are unaffected. **Root Cause** `getProfilerWorkspaces()` in `cutlass_fused_moe_kerne... | kernel: `csrc/fused_moe/cutlass_backend/cutlass_fused_moe_kernels.cuh`<br>test: `tests/moe/test_trtllm_cutlass_fused_moe.py` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#2629](https://github.com/flashinfer-ai/flashinfer/pull/2629) fix: cute dsl nvfp4 moe routing index error | 2026-02-25 | moe_routing, compiler_runtime, gemm_quant | 📌 Description To fix the following bug: When the CuteDSL MoE kernels were ported from TensorRT-LLM to FlashInfer, the mPtrPermutedIdxToExpandedIdx field was accidentally dropped from the routing kernel's DataBase struct in RoutingKernel.h. TRT-LLM's routing kernel produces three reverse-mapping outp... | kernel: `csrc/moe_utils_binding.cu`, `csrc/trtllm_fused_moe_routing_deepseek.cu`, `csrc/trtllm_fused_moe_routing_llama4.cu`, `csrc/trtllm_fused_moe_routing_renormalize.cu`<br>test: `tests/moe/test_cute_dsl_fused_moe.py` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#3198](https://github.com/flashinfer-ai/flashinfer/pull/3198) fix(cute_dsl/moe): correct off-by-one in get_max_num_tiles to match TRT-LLM | 2026-05-04 | moe_routing, compiler_runtime, attention_kv, benchmark_test | 📌 Description Replace `get_max_num_tiles` in `flashinfer/fused_moe/cute_dsl/moe_utils.py` with TRT-LLM's compact closed-form. The previous formula over-allocated by 1 tile whenever `(E - L) % T != 0`, where `E = num_tokens * top_k`, `L = num_local_experts`, `T = tile_size`. Fix code block Provably e... | kernel: `flashinfer/fused_moe/cute_dsl/moe_utils.py`<br>test: `tests/moe/test_cute_dsl_fused_moe.py` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#2993](https://github.com/flashinfer-ai/flashinfer/pull/2993) Second part of refactoring the routing part | 2026-04-13 | moe_routing, attention_kv, scheduler_autotune | …PdlOverlapWithNext;Remove DeepSeekV3 float32 logits constraint from kernel launchers 📌 Description 1. Add dynamic block kernel (`routingIndicesDynBlockKernel`) comes from the TensorRT-LLM. https://github.com/NVIDIA/TensorRT-LLM/pull/12456/ . Made related modification by refactoring `LAUNCH_ROUTING_... | kernel: `csrc/fused_moe/trtllm_backend/trtllm_fused_moe_routing_common.cu`, `csrc/fused_moe/trtllm_backend/trtllm_fused_moe_routing_custom.cu`, `csrc/fused_moe/trtllm_backend/trtllm_fused_moe_routing_deepseek.cu`, `csrc/fused_moe/trtllm_backend/trtllm_fused_moe_routing_llama4.cu`<br>test: `tests/autotuner/test_trtllm_fused_moe_autotuner_integration.py`, `tests/moe/test_trtllm_gen_fused_moe.py` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#2311](https://github.com/flashinfer-ai/flashinfer/pull/2311) refactor: decorate all operators with @flashinfer_api | 2026-01-09 | moe_routing, attention_kv, gemm_quant | Add `@flashinfer_api` decorator to the remaining public API functions: - flashinfer/topk.py: can_implement_filtered_topk() - flashinfer/topk.py: top_k() After comprehensive search through all Python API files, these were the only two public operator APIs missing the decorator. All other modules alre... | kernel: `flashinfer/api_logging.py`, `flashinfer/comm/allreduce.py`, `flashinfer/cute_dsl/blockscaled_gemm.py`, `flashinfer/fused_moe/fused_routing_dsv3.py` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#2744](https://github.com/flashinfer-ai/flashinfer/pull/2744) [feat] Add 2048 experts and 32 Top K  | 2026-03-17 | moe_routing | 📌 Description - Integrate https://github.com/NVIDIA/TensorRT-LLM/pull/11510 to support 2048 num of experts and 32 TopK in renormalize - Refactor MOE cu files 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the f... | kernel: `csrc/fused_moe/trtllm_backend/routingDeepSeek/RoutingDeepSeekCommon.cuh`, `csrc/fused_moe/trtllm_backend/routingDeepSeek/launchClusterKernel.cu`, `csrc/fused_moe/trtllm_backend/routingDeepSeek/launchCoopKernel.cu`, `csrc/fused_moe/trtllm_backend/routingDeepSeek/launchHistogramKernel.cu`<br>test: `tests/moe/test_trtllm_gen_fused_moe.py` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#3025](https://github.com/flashinfer-ai/flashinfer/pull/3025) Prevent MoE autotuner buffer overflow on large token buckets | 2026-04-10 | moe_routing, scheduler_autotune, compiler_runtime, attention_kv | 📌 Description CuteDslMoEWrapper pre-allocates intermediate buffers sized for max_num_tokens, but the autotuner can probe buckets larger than that (e.g. 8192 tokens vs 2048 max). The GEMM kernels then write past buffer bounds, silently corrupting model weights and eventually triggering cudaErrorIlleg... | kernel: `flashinfer/fused_moe/cute_dsl/fused_moe.py`, `flashinfer/fused_moe/cute_dsl/tuner.py` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#3014](https://github.com/flashinfer-ai/flashinfer/pull/3014) perf: Optimize CUTLASS MoE helper kernels for small-batch decode workloads | 2026-04-10 | moe_routing, benchmark_test, attention_kv | 📌 Description Ported optimizations from TRTLLM and expanded a bit: - Remove unnecessary N-dim SF padding loops from `expandInputRowsKernel` and `doActivationKernel` — the CUTLASS grouped GEMM never reads scale factors beyond `tokens_to_expert` - Add M=0 early exit in `computeStridesTmaWarpSpecialize... | kernel: `csrc/fused_moe/cutlass_backend/cutlass_fused_moe_kernels.cuh`<br>test: `tests/moe/test_trtllm_cutlass_fused_moe.py` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#2803](https://github.com/flashinfer-ai/flashinfer/pull/2803) Refactor the routing part | 2026-04-05 | moe_routing | 📌 Description Refactor the routing part of the trtllm-gen. Execution Flow Each routing method follows the same high-level pattern: 1. **TopK selection** — Compute top-K experts per token from routing scores. 2. **Histogram** — Count how many tokens are assigned to each expert. 3. **Offsets + Permuta... | kernel: `csrc/fused_moe/trtllm_backend/routingDeepSeek/RoutingDeepSeekCommon.cuh`, `csrc/fused_moe/trtllm_backend/routingDeepSeek/launchClusterKernel.cu`, `csrc/fused_moe/trtllm_backend/routingDeepSeek/launchCoopKernel.cu`, `csrc/fused_moe/trtllm_backend/routingDeepSeek/launchHistogramKernel.cu` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#3133](https://github.com/flashinfer-ai/flashinfer/pull/3133) feat: Add `row_starts` and `dsa_graph_safe` to topk | 2026-04-24 | moe_routing, attention_kv, benchmark_test | 📌 Description @humansand Parent PR: https://github.com/flashinfer-ai/flashinfer/pull/3095 SGLang PR: https://github.com/sgl-project/sglang/pull/22851 Add `row_starts` and `dsa_graph_safe` for SGLang DSA integration. 🔍 Related Issues https://github.com/sgl-project/sglang/pull/22851issuecomment-428626... | kernel: `csrc/flashinfer_topk_binding.cu`, `csrc/topk.cu`, `flashinfer/topk.py`, `include/flashinfer/topk.cuh`<br>test: `tests/utils/test_topk.py` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#3210](https://github.com/flashinfer-ai/flashinfer/pull/3210) fix(dcp_alltoall): require MNNVL workspace, drop broken plain-memory path | 2026-05-05 | moe_routing, attention_kv, benchmark_test | Follow-up to 2951. The merged DCP A2A code shipped with two latent foot-guns that this PR cleans up: 1. The `mapping=None` branch in `decode_cp_a2a_allocate_workspace` returns a per-rank `torch.zeros` tensor — this deadlocks at runtime on any real multi-GPU setup. 2. The MNNVL workspace tensor's lif... | kernel: `flashinfer/comm/__init__.py`, `flashinfer/comm/dcp_alltoall.py`<br>test: `tests/comm/test_dcp_alltoall.py`, `tests/comm/test_mnnvl_dcp_alltoall.py` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#2308](https://github.com/flashinfer-ai/flashinfer/pull/2308) Fix: FilteredTopKUnifiedKernel read value out of length | 2026-01-09 | moe_routing, attention_kv, benchmark_test | 📌 Description I found an issue in the implementation of `top_k_ragged_transform` where elements beyond the specified `lengths` range can be read. This leads to out-of-bounds indices in the result. Minimal repro: code block Output: code block In this example, `lengths = [5]`, so indices in `idxs` sho... | kernel: `include/flashinfer/topk.cuh`<br>test: `tests/utils/test_topk.py` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#2605](https://github.com/flashinfer-ai/flashinfer/pull/2605) [bugfix] Fix FilteredTopK overflow correctness | 2026-02-25 | moe_routing, attention_kv, benchmark_test | 📌 Description This PR fixes issue 2604: FilteredTopK could produce incorrect results when the refine candidate buffer overflows in long-sequence, tie-heavy workloads. Root Cause FilteredTopK uses a fixed-size refine candidate buffer (SMEM_INPUT_SIZE=16K), which can overflow when the threshold bin is... | kernel: `include/flashinfer/topk.cuh`<br>test: `tests/utils/test_topk.py` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#2099](https://github.com/flashinfer-ai/flashinfer/pull/2099) [DSV3] Optimized routing kernels dsv3 | 2025-11-19 | moe_routing, benchmark_test | 📌 Description 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Checks - [ ] I have installed `pre-commit` by running `pip install pre-commit` (or used your preferred... | kernel: `csrc/fused_moe/moeTopKFuncs.cuh`, `csrc/fused_moe/noAuxTcKernels.cu`, `flashinfer/dsv3_ops/__init__.py`, `flashinfer/fused_moe/__init__.py`<br>test: `tests/model_optimizations/test_dsv3_fused_routing.py` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#2181](https://github.com/flashinfer-ai/flashinfer/pull/2181) Rename noauxtc to fused_topk_deepseek | 2025-12-06 | moe_routing, attention_kv, benchmark_test | 📌 Description 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Checks - [ ] I have installed `pre-commit` by running `pip install pre-commit` (or used your preferred... | kernel: `flashinfer/dsv3_ops/__init__.py`, `flashinfer/fused_moe/__init__.py`, `flashinfer/fused_moe/fused_routing_dsv3.py`<br>test: `tests/model_optimizations/test_dsv3_fused_routing.py` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#2594](https://github.com/flashinfer-ai/flashinfer/pull/2594) Bf16 routed moe | 2026-03-03 | moe_routing, attention_kv, benchmark_test | 📌 Description Add `trtllm_bf16_routed_moe` api 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Checks - [x] I have installed `pre-commit` by running `pip install pr... | kernel: `csrc/trtllm_fused_moe_kernel_launcher.cu`, `flashinfer/__init__.py`, `flashinfer/fused_moe/__init__.py`, `flashinfer/fused_moe/core.py`<br>test: `tests/moe/test_trtllm_gen_routed_fused_moe.py` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#2642](https://github.com/flashinfer-ai/flashinfer/pull/2642) [fp8_blockwise]Fix int32 overflow in TRTLLM fused MoE activation kernel | 2026-03-05 | moe_routing, gemm_quant, norm_elementwise | 📌 Description Fix CUDA Illegal Memory Access (IMA) caused by int32 overflow in activationKernel and activationDeepSeekKernel in the TRTLLM fused MoE pipeline. Root cause: The index computation `permutedIdx * params.innerDim + hiddenIdx` uses int32 arithmetic. With large MoE configurations (e.g. 256... | kernel: `csrc/trtllm_fused_moe_dev_kernel.cu` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#2853](https://github.com/flashinfer-ai/flashinfer/pull/2853) fix: int32 overflow in `trtllm_fp4_block_scale_moe` causing "Unsupported hidden state scale shape" for EP32+ configs | 2026-03-24 | moe_routing, gemm_quant, scheduler_autotune | 📌 Description Fix `int32` overflow in `trtllm_fp4_block_scale_moe` that causes a misleading `NotImplementedError: Unsupported hidden state scale shape` when deploying large Expert Parallel configurations (e.g., EP32 with `DeepSeek-R1 NVFP4`). **Step 1, NVFP4 activation quantization (per EP rank)** E... | kernel: `csrc/trtllm_fused_moe_kernel_launcher.cu` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#2982](https://github.com/flashinfer-ai/flashinfer/pull/2982) feat(comm): add MOE Finalize/Reduction patterns to unified allreduce_fusion API | 2026-04-13 | moe_routing, memory_primitives | Add MOE-specific patterns to the unified `allreduce_fusion()` API, enabling fused MoE Finalize/Reduction + AllReduce + ResidualAdd + RMSNorm through a single entry point. Previously, users had to call `trtllm_moe_allreduce_fusion()` and `trtllm_moe_finalize_allreduce_fusion()` directly. Now they can... | kernel: `flashinfer/comm/allreduce.py`, `flashinfer/comm/trtllm_ar.py`<br>test: `tests/comm/test_allreduce_fusion_moe_unified_api.py`<br>docs: `docs/api/comm.rst` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#2190](https://github.com/flashinfer-ai/flashinfer/pull/2190) Fix for moe on sm110 | 2025-12-12 | moe_routing, scheduler_autotune, gemm_quant | 📌 Description SM100 configs are generated for SM110 devices, but of the valid MOE kernels for SM110, only TMA Epilogue Scheduling is supported. In the generated configs, `EpilogueScheduleType::AUTO` is not handled well in the `getDispatchFunctionForSM100` dispatch function (it is never resolved to a... | kernel: `csrc/nv_internal/tensorrt_llm/kernels/cutlass_kernels/cutlass_heuristic.cpp`, `csrc/nv_internal/tensorrt_llm/kernels/cutlass_kernels/moe_gemm/moe_gemm_template_dispatch.h` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#2548](https://github.com/flashinfer-ai/flashinfer/pull/2548) chore/feat: Add do_finalize to trtllm-gen fp8/f16 MoE APIs | 2026-02-23 | moe_routing, gemm_quant, attention_kv | 📌 Description This PR aims to add the `do_finalize` to the trtllm-gen fp8/f16 MoE APIs to align with fp4 APIs. This also allows flexible post processing of MoE. Additionally, fix the bug that the output tensors are allocated twice for bf16/fp8 MoE. The API changes include: - A new argument `do_final... | kernel: `csrc/trtllm_fused_moe_kernel_launcher.cu`, `flashinfer/fused_moe/core.py` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#3021](https://github.com/flashinfer-ai/flashinfer/pull/3021) fix: extend moe alltoall top-k specializations | 2026-04-13 | moe_routing, benchmark_test | - extend moe alltoall dispatch launch specialization to `top_k` values 6, 10, 16, and 22 - add explicit combine reduction trees for `top_k` values 6, 10, 16, and 22 while keeping the generic fallback for other valid cases - align combine regression coverage with representative MoE model parameter se... | kernel: `csrc/nv_internal/tensorrt_llm/kernels/communicationKernels/moeAlltoAllKernels.cu`, `csrc/nv_internal/tensorrt_llm/kernels/communicationKernels/moeAlltoAllKernels.h`<br>test: `tests/comm/test_trtllm_moe_alltoall.py` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#3024](https://github.com/flashinfer-ai/flashinfer/pull/3024) [feat] Add routing_replay_out support to MoE kernels and Python API | 2026-04-14 | moe_routing | 📌 Description Add an optional `routing_replay_out` parameter to all MoE kernel entry points. When provided (int16 tensor of shape `[N, top_k]`), the routing kernel writes selected expert IDs per token during MoE routing — inside the same fused kernel that computes the MoE output. When `None` (defaul... | kernel: `csrc/fused_moe/noAuxTcKernels.cu`, `csrc/fused_moe/trtllm_backend/trtllm_fused_moe_routing_deepseek.cu`, `csrc/trtllm_fused_moe_kernel_launcher.cu`, `csrc/trtllm_fused_moe_runner.cu`<br>test: `tests/model_optimizations/test_dsv3_fused_routing.py`, `tests/moe/test_trtllm_gen_routed_fused_moe.py`<br>docs: `docs/vllm_routing_replay_integration.md` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#2678](https://github.com/flashinfer-ai/flashinfer/pull/2678) [chore] bench_moe_deepseek.py allows adjusting expert distribution | 2026-03-05 | moe_routing, benchmark_test | 📌 Description - Adds `--routing-bias-scale` to indirectly affect expert balance for benchmark data initialization - Adds `Active expert`, `Tokens/slot` stats, `TB/s` to allow for more informed decision code block After smoothing out expert histogram, the best implementation changes. CuteDSL MoE is f... | benchmark: `benchmarks/bench_moe_deepseek.py` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#2869](https://github.com/flashinfer-ai/flashinfer/pull/2869) Support Sigmoid (sigmoid+topk) routing function | 2026-05-06 | moe_routing | 📌 Description Depends on 2803 . This PR adds `RoutingMethodType.Sigmoid` to support a routing function that applies sigmoid before topk (without renormalization) to be used by MoE layers that use this routing function. 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfe... | kernel: `csrc/trtllm_fused_moe_kernel_launcher.cu`, `csrc/trtllm_fused_moe_runner.cu`, `flashinfer/fused_moe/core.py`, `flashinfer/tllm_enums.py`<br>test: `tests/moe/test_trtllm_gen_fused_moe.py` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#3056](https://github.com/flashinfer-ai/flashinfer/pull/3056) PR #2772 might have introduced a device side compilation regression | 2026-04-14 | moe_routing, attention_kv, memory_primitives | 📌 Description 🔍 Related Issues https://github.com/flashinfer-ai/flashinfer/pull/2772 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Checks - [ ] I have installed `pre-commit` by ru... | kernel: `include/flashinfer/comm/trtllm_allreduce_fusion.cuh`, `include/flashinfer/comm/trtllm_moe_allreduce_fusion.cuh` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#3166](https://github.com/flashinfer-ai/flashinfer/pull/3166) Refactor Part 3- Add block-per-token feature in the customized routing method | 2026-05-04 | moe_routing | 📌 Description Adds `routingIndicesBlockScoresKernel` — a block-per-token top-K kernel modeled on TRT-LLM's `deepseek_v3_topk_kernel` (no-groups path) — to `routingCustom`, and wires it into the dispatcher alongside the fused cluster / warp-per-token paths. Also removes a stale `num_experts % 4 == 0`... | kernel: `csrc/fused_moe/trtllm_backend/trtllm_fused_moe_routing_custom.cu`, `csrc/fused_moe/trtllm_backend/trtllm_fused_moe_routing_deepseek.cu`, `csrc/fused_moe/trtllm_backend/trtllm_fused_moe_routing_llama4.cu`, `include/flashinfer/trtllm/fused_moe/RoutingCustomPolicy.cuh`<br>test: `tests/moe/test_trtllm_gen_fused_moe.py`<br>other: `scripts/task_test_jit_cache_package_build_import.sh` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#2535](https://github.com/flashinfer-ai/flashinfer/pull/2535) Add sm90 guard to fence.acquire | 2026-02-12 | moe_routing, arch_pipeline | 📌 Description 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Checks - [x] I have installed `pre-commit` by running `pip install pre-commit` (or used your preferred... | kernel: `csrc/nv_internal/tensorrt_llm/kernels/communicationKernels/moeAlltoAllKernels.cu` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#2439](https://github.com/flashinfer-ai/flashinfer/pull/2439) Add sm90 guard to fence ptx | 2026-02-03 | moe_routing, arch_pipeline | 📌 Description 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Checks - [ ] I have installed `pre-commit` by running `pip install pre-commit` (or used your preferred... | kernel: `csrc/nv_internal/tensorrt_llm/kernels/communicationKernels/moeAlltoAllKernels.cu` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#2882](https://github.com/flashinfer-ai/flashinfer/pull/2882) Fix silent bug with FP8 per tensor non-gated MoE | 2026-04-14 | moe_routing, gemm_quant | 📌 Description This PR fixed a **silent bug** that forces fallback tactic with _TRTLLM MoE FP8 and non-gated models_ (Nemotron). code block There is no crash, only a performance bug (fallback tactic will always be selected). The error raised from the C++ code seems to be hidden by this try-except in... | kernel: `csrc/trtllm_fused_moe_kernel_launcher.cu` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#2592](https://github.com/flashinfer-ai/flashinfer/pull/2592) [BugFix] guard against uint32 underflow in multi-CTA TopK chunk calculation | 2026-03-13 | moe_routing, benchmark_test | - Fix unsigned integer underflow in `RadixTopKKernel` when `chunk_start >= length` in multi-CTA mode with variable-length rows. - Add regression test for ragged transform mode. Problem In multi-CTA mode, `chunk_size` and `ctas_per_group` are derived from `max_len` (the input tensor stride). In ragge... | test: `tests/utils/test_topk.py` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#2602](https://github.com/flashinfer-ai/flashinfer/pull/2602) fix: get tensors by const ref to not rely on deleted move constructor for `TensorView` | 2026-02-22 | moe_routing, attention_kv | 📌 Description Getting a `Tensor` our of an `Array` as a `TensorView` attempts to call the deleted move-constructor `TensorView(Tensor&&)`. We can instead get a const ref to the tensors out of the array. Error messages before these changes: code block No errors after the fix. by CodeRabbit * **Refact... | kernel: `csrc/fused_moe/cutlass_backend/flashinfer_cutlass_fused_moe_binding.cu` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#2502](https://github.com/flashinfer-ai/flashinfer/pull/2502) Allow non-DeepSeekV3 routing with one group | 2026-02-05 | moe_routing, attention_kv | 📌 Description This PR allows running any routing method with one group. Previously, all routing methods except for `DeepSeekV3` required the number of groups to be unset or set to zero. However, Mistral Large 3 defines it to be one and uses `Renormalize` as routing. This worked only by using a worka... | kernel: `csrc/trtllm_fused_moe_kernel_launcher.cu` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |
| [#2762](https://github.com/flashinfer-ai/flashinfer/pull/2762) fix: fix OOB issue for vLLM | 2026-04-24 | moe_routing | 📌 Description Fix https://github.com/vllm-project/vllm/issues/35706, by adding back boundary check 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Checks - [x] I ha... | kernel: `csrc/fused_moe/cutlass_backend/cutlass_fused_moe_kernels.cuh` | Separate routing, permutation, top-k, and expert GEMM costs; keep tail-token and expert-layout tests attached to the idea. |

### Norm / Elementwise / Epilogue

Use this section for: Look for fusion, vectorization, dtype conversion, and store-traffic reductions before changing math.
NCU first look: DRAM %, L2 bytes, global load/store sectors, eligible warps, and long scoreboard.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#2965](https://github.com/flashinfer-ai/flashinfer/pull/2965) Add flashinfer.fused_rmsnorm_silu() with native kernel backend | 2026-04-08 | norm_elementwise, benchmark_test, attention_kv, compiler_runtime | 📌 Description Originally, this was kernel open sourced into CuDNN OSS and integrated here: https://github.com/flashinfer-ai/flashinfer/pull/2691. However, CuDNN OSS does not have native support for on-disk cache or precompiled PyPI wheels. This limits end-to-end perf since this would not support dyn... | kernel: `csrc/flashinfer_rmsnorm_silu_binding.cu`, `csrc/rmsnorm_silu.cu`, `flashinfer/__init__.py`, `flashinfer/aot.py`<br>benchmark: `benchmarks/routines/flashinfer_benchmark_utils.py`, `benchmarks/routines/norm.py`, `benchmarks/samples/sample_testlist.txt`<br>test: `tests/norm/test_fused_rmsnorm_silu.py`<br>docs: `docs/api/norm.rst` | Look for fusion, vectorization, dtype conversion, and store-traffic reductions before changing math. |
| [#3157](https://github.com/flashinfer-ai/flashinfer/pull/3157) feat: DiT layer norm fusions for WAN: flashinfer.diffusion_ops | 2026-05-01 | norm_elementwise, benchmark_test, attention_kv, gemm_quant | 📌 Description Add fused DIT (Diffusion Transformer) LayerNorm CUDA kernels for video generation self-attention blocks (WAN 2.2 and similar architectures). Three fused operation modes, each combining multiple steps into a single kernel launch: \| API \| Operations Fused \| \|-----\|-----------------\| \| `f... | kernel: `csrc/flashinfer_norm_binding.cu`, `csrc/norm.cu`, `flashinfer/diffusion_ops/__init__.py`, `flashinfer/norm/__init__.py`<br>benchmark: `benchmarks/bench_fused_dit_layernorm.py`, `benchmarks/routines/flashinfer_benchmark_utils.py`, `benchmarks/routines/norm.py`<br>test: `tests/norm/test_fused_dit_layernorm.py` | Look for fusion, vectorization, dtype conversion, and store-traffic reductions before changing math. |
| [#2928](https://github.com/flashinfer-ai/flashinfer/pull/2928) fix: clamp enable_pdl=True to False on SM < 90 to prevent PDL PTX on Ampere | 2026-04-01 | norm_elementwise, scheduler_autotune, attention_kv, compiler_runtime, gemm_quant | 📌 Description In 0.6.7, the norm kernels switched from the CUDA JIT path to the CuTe DSL path by default. The CuTe DSL kernels emit `griddepcontrol.wait` / `griddepcontrol.launch_dependents` PTX when `enable_pdl=True`, which requires `.target sm_90` or higher. Fix: Add ensures a clamp such that `ena... | kernel: `flashinfer/norm/__init__.py` | Look for fusion, vectorization, dtype conversion, and store-traffic reductions before changing math. |
| [#2777](https://github.com/flashinfer-ai/flashinfer/pull/2777) perf: Performance tune cute dsl RMSNorm variants | 2026-03-17 | norm_elementwise, compiler_runtime, benchmark_test | 📌 Description Rewrites all CuTe-DSL RMSNorm kernel variants (`rmsnorm`, `gemma_rmsnorm`, `fused_add_rmsnorm`, `gemma_fused_add_rmsnorm`, `rmsnorm_quant`, `fused_add_rmsnorm_quant`, `qk_rmsnorm`, `gemma_qk_rmsnorm`) **Key changes:** * Multi-row blocks with async global-to-shared copy (cpasync): Each... | kernel: `flashinfer/norm/kernels/fused_add_rmsnorm.py`, `flashinfer/norm/kernels/rmsnorm.py`, `flashinfer/norm/utils.py` | Look for fusion, vectorization, dtype conversion, and store-traffic reductions before changing math. |
| [#2926](https://github.com/flashinfer-ai/flashinfer/pull/2926) feat: add Relu2 (squared ReLU) activation support in CUTLASS MoE backend | 2026-04-01 | norm_elementwise, moe_routing, gemm_quant | Add **Relu2 (squared ReLU) activation** support for the CUTLASS MoE GEMM path, enabling models that use Relu2 as their MoE gate activation (e.g. Nemotron-H MTP draft model) to run through the FlashInfer CUTLASS backend instead of throwing `InvalidType` at runtime. Problem Two pieces were missing: 1.... | kernel: `csrc/nv_internal/tensorrt_llm/cutlass_extensions/include/cutlass_extensions/epilogue_helpers.h`, `csrc/nv_internal/tensorrt_llm/kernels/cutlass_kernels/moe_gemm/moe_gemm_template_dispatch.h` | Look for fusion, vectorization, dtype conversion, and store-traffic reductions before changing math. |
| [#3007](https://github.com/flashinfer-ai/flashinfer/pull/3007) fix: use sym_int64 for strides in rmsnorm CuTe DSL kernels to prevent int32 overflow | 2026-04-08 | norm_elementwise, compiler_runtime | 📌 Description **Summary:** - Fix integer overflow in rmsnorm CuTe DSL kernels when tensor strides exceed INT32_MAX (~2.1B) - Change `cute.sym_int()` → `cute.sym_int64()` for all stride symbols across 5 compiled kernel functions in `rmsnorm.py` and `fused_add_rmsnorm.py` - Add 5 regression tests usin... | kernel: `flashinfer/norm/kernels/fused_add_rmsnorm.py`, `flashinfer/norm/kernels/rmsnorm.py`<br>test: `tests/utils/test_norm.py` | Look for fusion, vectorization, dtype conversion, and store-traffic reductions before changing math. |

### Memory / Primitives

Use this section for: Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract.
NCU first look: DRAM throughput, L2 hit rate, memory sectors, shared-memory conflicts, and synchronization overhead.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#2563](https://github.com/flashinfer-ai/flashinfer/pull/2563) Add support for the combinations of allreduce, allgather, and reducescatter | 2026-04-23 | memory_primitives, benchmark_test, moe_routing | 📌 Description This PR adds support for the combinations of allreduce,allgather, and reducescatter: - allreduce - allgather - reducescatter - allreduce + allgather - reducescatter + allreduce The last two communication patterns occur when TP and DP are both enabled in the attention part. Besides comb... | kernel: `csrc/mixed_comm.cu`, `flashinfer/comm/mixed_comm.py`, `flashinfer/jit/comm.py`, `include/flashinfer/comm/mixed_comm.cuh`<br>benchmark: `benchmarks/flashinfer_benchmark.py`, `benchmarks/routines/flashinfer_benchmark_utils.py`, `benchmarks/routines/mixed_comm.py`<br>test: `flashinfer/testing/utils.py`, `tests/comm/conftest.py`, `tests/comm/test_mixed_comm.py`<br>other: `csrc/mixed_comm_kernel_inst.jinja` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |
| [#3091](https://github.com/flashinfer-ai/flashinfer/pull/3091) Vendor CCCL v3.3.2 from GitHub instead of relying on CTK-bundled copy | 2026-04-22 | memory_primitives, attention_kv, moe_routing, compiler_runtime | 📌 Description Pin CCCL (CUB, libcudacxx, Thrust) to a specific release tag as a git submodule under `3rdparty/cccl`, replacing the implicit dependency on whatever version ships with the user's CUDA Toolkit. This enables the CCCL team to land improvements (e.g., TopK, DeviceTransform, fast_mod_div) i... | kernel: `csrc/fused_moe/trtllm_backend/trtllm_fused_moe_dev_kernel.cu`, `flashinfer/jit/cpp_ext.py`, `flashinfer/jit/env.py`, `include/flashinfer/fastdiv.cuh`<br>wrapper: `build_backend.py`, `scripts/modal_runner.py`<br>other: `.gitmodules`, `3rdparty/cccl`, `pyproject.toml` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |
| [#3089](https://github.com/flashinfer-ai/flashinfer/pull/3089) [Fmha] update trtllm-gen FMHA cubins and sync headers for context SWA fix | 2026-04-23 | memory_primitives, attention_kv | 📌 Description The branch has 2 commits: 1. Update trtllm-gen FMHA cubins to fix context SWA page-skip — updates artifacts.py path + checksum 2. Sync trtllm FMHA headers with latest trtllm-gen (from PR 2711) — cherry-picks header changes to match the new cubin MetaInfo struct 🔍 Related Issues 🚀 Pull... | kernel: `flashinfer/artifacts.py`, `include/flashinfer/trtllm/fmha/fmhaKernels.cuh` | Transfer primitive policy carefully: coalescing, shared-memory staging, partial tiles, and determinism are part of the contract. |

### Scheduler / Autotune

Use this section for: Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing.
NCU first look: SM occupancy, waves/SM, active cycles, tail effects, and load imbalance.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#3216](https://github.com/flashinfer-ai/flashinfer/pull/3216) fix(cute_dsl/moe): make autotuner bucket configuration adapt to runtime input | 2026-05-06 | scheduler_autotune, moe_routing, compiler_runtime, attention_kv, benchmark_test | 📌 Description The autotuner's `DynamicTensorSpec` in `flashinfer/fused_moe/cute_dsl/tuner.py` declared `gen_tuning_buckets` as the pre-computed tuple `get_hybrid_num_tokens_buckets(8192)` and `map_to_tuning_buckets` as `lambda x: map_to_hybrid_bucket(x,8192)`. The hardcoded 8192 cap silently clamped... | kernel: `flashinfer/fused_moe/cute_dsl/tuner.py`<br>test: `tests/moe/test_cute_dsl_fused_moe.py` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#2554](https://github.com/flashinfer-ai/flashinfer/pull/2554) feat: Add autotuner config caching, thread safety, and documentation | 2026-03-14 | scheduler_autotune, benchmark_test | 📌 Description **Summary** This PR adds the ability to persist and reload autotuner configs via a JSON cache file, making it easy for users and inference frameworks to tune once and reuse optimal kernel configurations across runs. It also adds thread safety to the `AutoTuner` singleton and atomic fil... | kernel: `flashinfer/autotuner.py`<br>benchmark: `benchmarks/flashinfer_benchmark.py`, `benchmarks/routines/gemm.py`, `benchmarks/routines/moe.py`<br>test: `tests/autotuner/test_autotuner_configs.py`<br>docs: `docs/autotuning.rst`, `docs/index.rst` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#3126](https://github.com/flashinfer-ai/flashinfer/pull/3126) autotuner: check cache before synthesizing profile input tensors | 2026-04-23 | scheduler_autotune, benchmark_test, memory_primitives, attention_kv | 📌 Description AutoTuner.choose_one's tuning-mode loop calls _prepare_input_tensors(p, inputs) before checking the cache. On a cache hit the synthesized tensors are thrown away, but their torch.rand / torch.randint kernel launches already happened on the device. For any caller that runs choose_one re... | kernel: `flashinfer/autotuner.py` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |
| [#2927](https://github.com/flashinfer-ai/flashinfer/pull/2927) feat: SM121 (GB10) tile filtering and autotuner robustness | 2026-04-03 | scheduler_autotune, arch_pipeline, attention_kv | - **Unified SMEM tile filter** — extracted `tile_fits_smem(sm, m, n, k_elem, config, stages=2)` helper that encodes the per-architecture SMEM limit (102,400 B for SM 8.6/8.9/12.x, per CUDA Programming Guide) and is shared by both the SM12x TMA WS path and the Ampere mainloop path. - **SM12x FP4 tile... | kernel: `csrc/nv_internal/tensorrt_llm/kernels/cutlass_kernels/cutlass_heuristic.cpp`, `flashinfer/autotuner.py`, `flashinfer/compilation_context.py` | Treat scheduler/autotune configs as shape-specific evidence; replay benchmark shapes before generalizing. |

### Architecture / Pipeline

Use this section for: Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages.
NCU first look: Tensor pipe %, memory pipe utilization, barrier stalls, wait groups, and occupancy.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#2725](https://github.com/flashinfer-ai/flashinfer/pull/2725) fix: Add SM120 (RTX Blackwell desktop) support for NVFP4 MoE kernels | 2026-03-20 | arch_pipeline, gemm_quant, moe_routing, compiler_runtime, attention_kv | SM120 desktop Blackwell GPUs (RTX PRO 6000, RTX 5090) are blocked from NVFP4 MoE grouped GEMM due to hardcoded SM100-only checks. **Changes:** - `jit/fused_moe.py`: Add major version 12 to `supported_major_versions` - `csrc/trtllm_fused_moe_kernel_launcher.cu`: `ICHECK_EQ(major, 10)` -> `ICHECK_GE(m... | kernel: `csrc/trtllm_fused_moe_kernel_launcher.cu`, `flashinfer/compilation_context.py`, `flashinfer/jit/fused_moe.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2780](https://github.com/flashinfer-ai/flashinfer/pull/2780) fix(jit): enable GDC for CUTLASS GEMM PDL — SM100 flag only | 2026-03-20 | arch_pipeline, gemm_quant, compiler_runtime, scheduler_autotune, attention_kv | Re-applies 2716 (reverted in 2737) with the fix for the AOT build failure. **Only `-DCUTLASS_ENABLE_GDC_FOR_SM100=1`** is added. The `-DCUTLASS_ENABLE_GDC_FOR_SM90=1` flag that broke AOT builds is intentionally omitted. Why the original PR broke AOT `sm90_gemm_tma_warpspecialized_cooperative.hpp:794... | kernel: `flashinfer/jit/gemm/core.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#3276](https://github.com/flashinfer-ai/flashinfer/pull/3276) fix(fmha_v2): fix FP8 V-scratch pipeline and varlen scheduler on SM90 | 2026-05-14 | arch_pipeline, attention_kv, scheduler_autotune, gemm_quant | Forked and modified from @bobboli's Original PR: https://github.com/flashinfer-ai/flashinfer/pull/2957 Root cause On Hopper (SM90), the warp-specialized FP8 FMHAv2 kernel runs a dedicated DMA warpgroup that explicitly transposes the V tile (via STSM) from `smem_v_scratch` into `smem_v` before BMM2,... | kernel: `csrc/fmha_v2/fmha/warpspec/circular_buffer.h`, `csrc/fmha_v2/fmha/warpspec/dma.h`, `csrc/fmha_v2/fmha/warpspec/kernel_traits.h`, `flashinfer/jit/attention/fmha_v2/fmha_library.py`<br>test: `tests/attention/test_fmha_v2_prefill.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#3032](https://github.com/flashinfer-ai/flashinfer/pull/3032) fused_moe: pre-filter SM89 tactics with zero occupancy on SM120 Blackwell (fix review feedback on #2764) | 2026-04-14 | arch_pipeline, moe_routing, gemm_quant, attention_kv | Supersedes 2764 (by @dryu-nv). This PR carries the original fix forward and addresses all review feedback raised by CodeRabbit and Gemini Code Assist. Closes 3031. Original fix (2764) On SM120 (Blackwell), pure FP8 MoE falls back to `cutlass::arch::Sm89` kernels. Some SM89 tile configs exceed Blackw... | kernel: `csrc/fused_moe/cutlass_backend/flashinfer_cutlass_fused_moe_binding.cu`, `csrc/nv_internal/tensorrt_llm/kernels/cutlass_kernels/include/moe_gemm_kernels.h`, `csrc/nv_internal/tensorrt_llm/kernels/cutlass_kernels/include/moe_kernels.h`, `csrc/nv_internal/tensorrt_llm/kernels/cutlass_kernels/moe_gemm/moe_gemm_template_dispatch.h` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2387](https://github.com/flashinfer-ai/flashinfer/pull/2387) A Blackwell-optimized version of selective_state_update (mamba) | 2026-01-22 | arch_pipeline, compiler_runtime, attention_kv | 📌 Description This contributes a `selective_state_update` kernel that has been manually optimized for sm100. <img width="3000" height="3000" alt="runtime_vs_batch_size_NVIDIA_B200" src="https://github.com/user-attachments/assets/2718d992-1066-4385-b120-d22eccd3a941" /> Technically, the code in the s... | kernel: `flashinfer/aot.py`, `flashinfer/jit/mamba/__init__.py`, `flashinfer/jit/mamba/selective_state_update.py`, `flashinfer/mamba/selective_state_update.py`<br>other: `uv.lock` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2798](https://github.com/flashinfer-ai/flashinfer/pull/2798) Upgrade cutlass 4.2.1 -> 4.4.2 | 2026-03-19 | arch_pipeline, gemm_quant, moe_routing | 📌 Description Upgrade cutlass 4.2.1 -> 4.4.1, also add "CUTLASS_ENABLE_GDC_" to cutlass compilation flags. Addresses this issue raised on slack: "Hi team, we're seeing CUTLASS TMA descriptor crashes on DGX Spark ... the crash happens in tma_warp_specialized_generic_moe_gemm_kernelLauncher<Sm120, fp4... | kernel: `csrc/nv_internal/tensorrt_llm/kernels/cutlass_kernels/fpA_intB_gemm/fpA_intB_gemm_template.h`, `csrc/nv_internal/tensorrt_llm/kernels/cutlass_kernels/moe_gemm/moe_gemm_template_dispatch.h`<br>other: `3rdparty/cutlass`, `csrc/nv_internal/tensorrt_llm/cutlass_extensions/include/cutlass_extensions/gemm/collective/builders/sm90_gmma_builder_mixed_input.inl`, `csrc/nv_internal/tensorrt_llm/kernels/cutlass_kernels/fpA_intB_gemm/launchers/fpA_intB_launcher_sm90.inl`, `csrc/nv_internal/tensorrt_llm/kernels/cutlass_kernels/moe_gemm/launchers/moe_gemm_tma_ws_mixed_input_launcher.inl` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |
| [#2083](https://github.com/flashinfer-ai/flashinfer/pull/2083) test: Change incorrect inputs in test_hopper.py | 2025-11-13 | arch_pipeline, benchmark_test, attention_kv | 📌 Description Brings in some changes to `test_hopper.py` to pass more unit tests * `test_deepseek_prefill` --> Raise tolerance for bf16 inputs * Others: The code block is an incorrect API and results in invalid input errors. Change it to: `token_pos_in_items_len=token_pos_in_items_len,` so that it m... | test: `tests/attention/test_hopper.py` | Extract the architecture assumption first: SM target, TMA/WGMMA/tcgen path, cluster use, and pipeline stages. |

### Compiler / Runtime

Use this section for: Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests.
NCU first look: Generated kernel shape, launch overhead, occupancy, register pressure, and compile-time-selected schedule.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#2709](https://github.com/flashinfer-ai/flashinfer/pull/2709) Mamba2 SSD Combined Forward Pass (Blackwell CuTe DSL Kernel) | 2026-03-23 | compiler_runtime, benchmark_test, arch_pipeline, memory_primitives, scheduler_autotune | 📌 Description This PR adds a high-performance **Mamba2 Structured State-Space Duality (SSD) combined forward kernel** targeting Blackwell (SM100+) GPUs, implemented using NVIDIA's CuTe DSL. The kernel fuses the entire SSD chunk-scan forward pass — including cumulative sum preprocessing, chunk state... | kernel: `csrc/seq_chunk_cumsum.cu`, `csrc/seq_chunk_cumsum_jit_binding.cu`, `flashinfer/jit/mamba/__init__.py`, `flashinfer/jit/mamba/seq_chunk_cumsum.py`<br>benchmark: `benchmarks/bench_mamba_ssd_combined.py`<br>test: `tests/mamba/test_chunk_scan_combined.py`, `tests/mamba/test_seq_chunk_cumsum.py`, `tests/mamba/triton_reference/ssd_bmm.py`, `tests/mamba/triton_reference/ssd_chunk_scan.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2398](https://github.com/flashinfer-ai/flashinfer/pull/2398) feat: cuteDSL fp4 moe for better DSR1 performance. | 2026-02-07 | compiler_runtime, benchmark_test, gemm_quant, moe_routing | 📌 Description cuteDSL fp4 moe from TRTLLM with fusion. issue 2259 The collective fusion performance improvement collected from TRTLLM PR 8880, PR 9288, PR 9618,. Introducing two new API for moe `cute_dsl_fused_moe_nvfp4`, `CuteDslMoEWrapper`. The two API are functionally equivalent. The `cute_dsl_fu... | kernel: `csrc/moe_utils_binding.cu`, `csrc/nv_internal/include/tensorrt_llm/common/config.h`, `csrc/nv_internal/tensorrt_llm/kernels/cuteDslKernels/moeUtils.cu`, `csrc/nv_internal/tensorrt_llm/kernels/cuteDslKernels/moeUtils.h`<br>benchmark: `benchmarks/bench_moe_deepseek.py`<br>test: `tests/moe/__init__.py`, `tests/moe/test_cute_dsl_fused_moe.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#3066](https://github.com/flashinfer-ai/flashinfer/pull/3066) feat: Add b12x CuTe DSL fused MoE for SM120 | 2026-04-15 | compiler_runtime, arch_pipeline, benchmark_test, moe_routing, gemm_quant | 📌 Description - Extend `cute_dsl_fused_moe_nvfp4` and `CuteDslMoEWrapper` to support SM120/SM121 GPUs - Port the b12x fused MoE kernels (static + dynamic) using CuTe DSL - Add workspace cache for the functional API to avoid per-call allocation overhead Architecture The SM120 MoE kernel fuses the ent... | kernel: `flashinfer/cute_dsl/fp4_common.py`, `flashinfer/fused_moe/cute_dsl/blackwell_sm12x/__init__.py`, `flashinfer/fused_moe/cute_dsl/blackwell_sm12x/moe_dispatch.py`, `flashinfer/fused_moe/cute_dsl/blackwell_sm12x/moe_dynamic_kernel.py`<br>benchmark: `benchmarks/routines/flashinfer_benchmark_utils.py`, `benchmarks/routines/moe.py`<br>test: `tests/moe/test_cute_dsl_fused_moe.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#3252](https://github.com/flashinfer-ai/flashinfer/pull/3252) fix(cute_dsl/moe): unbias autotuner profiling for tile_size enumeration | 2026-05-11 | compiler_runtime, moe_routing, scheduler_autotune, benchmark_test, attention_kv | 📌 Description `CuteDslMoEWrapper.__init__` pre-allocates `_gemm1_output`, `_gemm1_output_scale`, and `_moe_sort_buffers` sized for `self.tile_size` only. The `use_prealloc` gate in `_forward_with_tactic` (`fused_moe.py`) is `tile_size == self.tile_size and self.use_cuda_graph and num_tokens <= self.... | kernel: `flashinfer/autotuner.py`, `flashinfer/fused_moe/cute_dsl/fused_moe.py`, `flashinfer/fused_moe/cute_dsl/tuner.py`<br>test: `tests/moe/test_cute_dsl_fused_moe.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2645](https://github.com/flashinfer-ai/flashinfer/pull/2645) int16 Block-Scaled State and Stochastic Rounding for SSU (mamba) | 2026-03-04 | compiler_runtime, attention_kv, scheduler_autotune, benchmark_test | Motivation The `selective_state_update` kernels (single-token STP and multi-token MTP) store SSM state in memory between steps. This PR adds two complementary features for reducing state memory bandwidth and improving numerical quality: **int16 block-scaled quantization** for 2× memory footprint red... | kernel: `csrc/flashinfer_mamba_binding.cu`, `csrc/selective_state_update.cu`, `csrc/selective_state_update_kernel_inst.cu`, `flashinfer/aot.py`<br>test: `tests/mamba/test_philox_rounding.py`, `tests/mamba/test_selective_state_update_mtp.py`, `tests/mamba/test_selective_state_update_stp.py`, `tests/mamba/triton_reference/selective_state_update.py`<br>other: `.gitignore`, `csrc/selective_state_update_customize_config.jinja` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2944](https://github.com/flashinfer-ai/flashinfer/pull/2944) feat: Add CuTe DSL grouped-gemm + combine fusion support | 2026-05-14 | compiler_runtime, gemm_quant, moe_routing, benchmark_test, attention_kv | Enables https://github.com/sgl-project/sglang/pull/21877 - Add fused grouped-GEMM + combine operation to the CuTe DSL Blackwell masked GEMM kernel, enabling the GEMM epilogue to perform weighted scatter-reduce (combine) directly into multi-rank output buffers using `cp.reduce.async.bulk` PTX instruc... | kernel: `flashinfer/gemm/kernels/grouped_gemm_masked_blackwell.py`<br>test: `tests/gemm/test_multi_gpu_cute_dsl_blockscaled_gemm_fusion.py`<br>other: `scripts/task_test_multi_gpu_comm_kernels.sh` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2960](https://github.com/flashinfer-ai/flashinfer/pull/2960) Update NVSHMEM interface to use NVSHMEM4Py instead of custom bindings | 2026-04-06 | compiler_runtime, memory_primitives, attention_kv, moe_routing, benchmark_test | 📌 Description Replace FlashInfer's custom NVSHMEM C/TVM-FFI bindings (csrc/nvshmem_binding.cu) with NVSHMEM4Py (`nvshmem.core`) Python bindings. This eliminates the device linking step that caused `nvlink error: Undefined eference to 'nvshmemi_device_state_d'` during AOT builds, and removes ~200 lin... | kernel: `csrc/nvshmem_binding.cu`, `flashinfer/aot.py`, `flashinfer/comm/nvshmem.py`, `flashinfer/comm/nvshmem_allreduce.py`<br>test: `tests/comm/test_nvshmem.py`<br>other: `docker/install/install_python_packages.sh`, `flashinfer-jit-cache/pyproject.toml`, `scripts/task_run_unit_tests.sh`, `scripts/task_test_single_node_comm_kernels.sh` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2529](https://github.com/flashinfer-ai/flashinfer/pull/2529) Chore: Cute dsl moe update (TMA.RED implementation) | 2026-02-16 | compiler_runtime, moe_routing, arch_pipeline, gemm_quant, benchmark_test | 📌 Description The PR is follow up to PR 2398 To integration TRTLLM PR 10987. Use TMA.RED to improve effective memory bandwidth Perf data is (tested on GB200): Tokens \| CuteDSL (main) ms \| CuteDSL (TMA.RED) ms \| TRTLLM gen ms \| CUTLASS ms \| Winner \| CuteDSL Speedup (main/TMA.RED) -- \| -- \| -- \| -- \|... | kernel: `flashinfer/fused_moe/cute_dsl/blackwell/blockscaled_contiguous_grouped_gemm_finalize_fusion.py`, `flashinfer/fused_moe/cute_dsl/blackwell/utils.py`, `flashinfer/fused_moe/cute_dsl/blockscaled_contiguous_grouped_gemm_finalize_fusion.py`<br>test: `tests/moe/test_cute_dsl_fused_moe.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2654](https://github.com/flashinfer-ai/flashinfer/pull/2654) fix: Add fused MOE and GEMM AOT modules for SM121 | 2026-03-02 | compiler_runtime, gemm_quant, arch_pipeline, moe_routing, attention_kv | - Add `gen_cutlass_fused_moe_sm120_module()`, `gen_gemm_sm120_module()`, and `gen_gemm_sm120_module_cutlass_fp4()` to the `if has_sm121:` block in `aot.py` - SM121 (DGX Spark / GB10) AOT builds now include fused MOE and GEMM modules Problem SM121 was missing fused_moe, gemm, and fp4_gemm_cutlass mod... | kernel: `flashinfer/aot.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2735](https://github.com/flashinfer-ai/flashinfer/pull/2735) fix: Fix cute dsl moe failure with nvidia-cutlass-dsl >= 4.4.0 | 2026-03-16 | compiler_runtime, moe_routing, gemm_quant, attention_kv | 📌 Description Fix issue 2693 Remove the patch when cute dsl version is order than 4.4.0 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Checks - [x] I have installe... | kernel: `flashinfer/fused_moe/cute_dsl/blackwell/blockscaled_contiguous_gather_grouped_gemm_swiglu_fusion.py`, `flashinfer/fused_moe/cute_dsl/blackwell/blockscaled_contiguous_grouped_gemm_finalize_fusion.py`, `flashinfer/fused_moe/cute_dsl/blackwell/custom_pipeline.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2533](https://github.com/flashinfer-ai/flashinfer/pull/2533) fix: include fp8_blockscale_gemm_90 in AOT jit-cache | 2026-02-13 | compiler_runtime, gemm_quant, memory_primitives, attention_kv | - Add fp8_blockscale_gemm_90 (gen_fp8_blockscale_gemm_sm90_module) to the AOT build list when SM90 is enabled. - Avoid runtime JIT compilation for fp8_blockscale_gemm_sm90 in environments without CUDA dev headers, which can fail with cublasLt.h not found. Changes - flashinfer/aot.py: append gen_fp8_... | kernel: `flashinfer/aot.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2301](https://github.com/flashinfer-ai/flashinfer/pull/2301) Selective State Update kernel (mamba) | 2026-01-13 | compiler_runtime, attention_kv, benchmark_test | 📌 Description This PR implements `selective_state_update` kernels for the Mamba layer (e.g. in the Nemotron model). This implementation is necessary as the default Triton implementation in TRT-LLM is inefficient. Specifically, the PR implements two kernels: - a simple pre-Hopper kernel with vectoriz... | kernel: `csrc/flashinfer_mamba_binding.cu`, `csrc/selective_state_update.cu`, `flashinfer/__init__.py`, `flashinfer/aot.py`<br>test: `tests/mamba/__init__.py`, `tests/mamba/selective_state_update_triton.py`, `tests/mamba/test_selective_state_update.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2885](https://github.com/flashinfer-ai/flashinfer/pull/2885) feat: add SM120 fmha_v2 kernels to AOT pip wheel builds | 2026-05-11 | compiler_runtime, arch_pipeline, attention_kv, gemm_quant | `gen_trtllm_fmha_v2_sm120_module()` exists in `jit/attention/modules.py` and the JIT runtime path (`generate_kernels.py`) already dispatches to it correctly. However, `aot.py`'s `gen_all_modules()` — which drives the pip wheel AOT build — was missing it from the `has_sm120 or has_sm121` section. Thi... | kernel: `flashinfer/aot.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#3278](https://github.com/flashinfer-ai/flashinfer/pull/3278) fix(jit): propagate -DNDEBUG to host-side cflags | 2026-05-11 | compiler_runtime, attention_kv, benchmark_test | 📌 Description `gen_jit_spec` adds `-DNDEBUG` only to `extra_cuda_cflags` (consumed by `nvcc` for `.cu` files), not to `extra_cflags` (consumed by `g++` for host-side `.cpp`). Several host-only translation units are part of MoE/GEMM JIT specs — most notably `csrc/nv_internal/cpp/common/logger.cpp` —... | kernel: `flashinfer/jit/core.py`<br>test: `tests/test_jit_cpp_ext.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#3158](https://github.com/flashinfer-ai/flashinfer/pull/3158) CICD bug fix: ensure data/ symlinks exist before jit-cache AOT compilation | 2026-04-24 | compiler_runtime, memory_primitives, attention_kv | 📌 Description The jit-cache wheel build (`scripts/build_flashinfer_jit_cache_whl.sh`) runs `python -m build --wheel` without first installing the main flashinfer package. The AOT compilation imports flashinfer directly from the source tree, but the `flashinfer/data/` symlinks are never created becau... | kernel: `flashinfer-jit-cache/build_backend.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2870](https://github.com/flashinfer-ai/flashinfer/pull/2870) fix: fix cute dsl swap_ab tactic failure | 2026-03-24 | compiler_runtime, gemm_quant, attention_kv | 📌 Description Fix swap_ab tactic error in cute dsl blockscaled dense gemm 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Checks - [ ] I have installed `pre-commit`... | kernel: `flashinfer/gemm/gemm_base.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2543](https://github.com/flashinfer-ai/flashinfer/pull/2543) misc: point triton blackwell-ptxas to local cuda ptxas | 2026-02-12 | compiler_runtime, arch_pipeline, attention_kv | 📌 Description Previously, SM110 was not being recognized by the CUDA 12.8 ptxas packaged with Triton 3.6.0: `ptxas-blackwell fatal : Value 'sm_110' is not defined for option 'gpu-name'` This change checks if the current environment has CUDA 13.0+ ptxas and points internal triton variable to that, wh... | kernel: `flashinfer/triton/__init__.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2872](https://github.com/flashinfer-ai/flashinfer/pull/2872) fix: add cute dsl moe utils to AOT | 2026-03-24 | compiler_runtime, moe_routing | 📌 Description Add cute dsl moe utils to AOT package 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Checks - [ ] I have installed `pre-commit` by running `pip insta... | kernel: `flashinfer/aot.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |
| [#2339](https://github.com/flashinfer-ai/flashinfer/pull/2339) bugfix: Ninja race condition fix | 2026-01-14 | compiler_runtime, attention_kv | 📌 Description **Fix race condition in JIT compilation for multi-GPU/multi-process environments** This PR resolves a race condition in multi-process environments where concurrent Ninja builds contended for a shared `.ninja_log` file (`ninja: error: opening build log: No such file or directory`), caus... | kernel: `flashinfer/jit/core.py`, `flashinfer/jit/cpp_ext.py` | Treat compiler/runtime integration as part of the kernel: launch wrapper, cache/JIT behavior, generated code, and build flags need tests. |

### Benchmark / Test Evidence

Use this section for: Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code.
NCU first look: Use the PR's benchmark/profile command as the first replay target.

| PR | Merged | Signals | What changed | Evidence paths | Transfer note |
| --- | --- | --- | --- | --- | --- |
| [#3080](https://github.com/flashinfer-ai/flashinfer/pull/3080) feat: Add b12x_fused_moe / B12xMoEWrapper SM120 APIs with micro kernel and ReLU2 | 2026-04-20 | benchmark_test, arch_pipeline, moe_routing, compiler_runtime, gemm_quant | 📌 Description New SM120/SM121 MoE APIs (`b12x_fused_moe`, `B12xMoEWrapper`) with: - **Micro kernel** for tiny decode batches (≤20-40 routed rows) on SM120/SM121, with Triton routing compaction pre-pass and MAC tuning ladder - **ReLU2 activation** (`max(0,x)²`) for non-gated MoE (Nemotron-Super) acro... | kernel: `flashinfer/__init__.py`, `flashinfer/cute_dsl/fp4_common.py`, `flashinfer/cute_dsl/utils.py`, `flashinfer/fused_moe/__init__.py`<br>benchmark: `benchmarks/routines/moe.py`, `benchmarks/routines/moe_utils.py`<br>test: `tests/moe/test_b12x_fused_moe.py`, `tests/moe/test_cute_dsl_fused_moe.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2119](https://github.com/flashinfer-ai/flashinfer/pull/2119) perf: bunch of features and optimizations for top-k (sampling + sparse attention) | 2025-12-12 | benchmark_test, attention_kv, compiler_runtime, moe_routing, norm_elementwise | 📌 Description This PR implements several features and optimizations for top-k: Multi-CTA optimization Followup of 2044 , this PR optimizes the top-k/top-p filter using multi-cta optimizations, more specifically: * split the vocabulary into chunks and let each cta handles one chunk * make sure the lo... | kernel: `csrc/flashinfer_sampling_binding.cu`, `csrc/flashinfer_topk_binding.cu`, `csrc/renorm.cu`, `csrc/topk.cu`<br>test: `tests/utils/test_logits_processor.py`, `tests/utils/test_sampling.py`, `tests/utils/test_topk.py`<br>other: `scripts/task_jit_run_tests_part3.sh` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#3009](https://github.com/flashinfer-ai/flashinfer/pull/3009) [feat] Faster topk algorithm | 2026-04-20 | benchmark_test, moe_routing, arch_pipeline, attention_kv, compiler_runtime | 📌 Description This PR implements a faster topk algorithm that uses sm90+ CTA clusters feature. This is a non-deterministic algorithm, but does not drop indices and instead overflows to global memory. Benchmark results show that it's faster than both the multi-cta topk algorithm and the filtering alg... | kernel: `csrc/flashinfer_fast_topk_clusters_binding.cu`, `flashinfer/jit/topk.py`, `flashinfer/topk.py`, `flashinfer/utils.py`<br>benchmark: `benchmarks/bench_topk.py`<br>test: `flashinfer/testing/utils.py`, `tests/utils/test_topk.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#3128](https://github.com/flashinfer-ai/flashinfer/pull/3128) chore: Address non-blocking review feedback for #3051 / #3080 | 2026-04-24 | benchmark_test, gemm_quant, arch_pipeline, compiler_runtime, moe_routing | 📌 Description Follow-up to 3051 (`backend="b12x"` for `mm_fp4` on SM120) and 3080 (`b12x_fused_moe` / `B12xMoEWrapper` SM120 APIs) addressing four reviewer comments that landed after merge. No public API changes; no kernel behavior changes. - **Copyright**: bump `tests/moe/test_b12x_fused_moe.py` to... | kernel: `flashinfer/fused_moe/cute_dsl/blackwell_sm12x/moe_dynamic_kernel.py`, `flashinfer/fused_moe/cute_dsl/blackwell_sm12x/moe_micro_kernel.py`, `flashinfer/fused_moe/cute_dsl/blackwell_sm12x/moe_static_kernel.py`, `flashinfer/gemm/__init__.py`<br>benchmark: `benchmarks/routines/flashinfer_benchmark_utils.py`, `benchmarks/routines/moe.py`, `benchmarks/samples/sample_testlist.txt`<br>test: `tests/moe/test_b12x_fused_moe.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#3095](https://github.com/flashinfer-ai/flashinfer/pull/3095) feat: implement configurable `tie_break` for filtered topk | 2026-04-22 | benchmark_test, moe_routing, scheduler_autotune, attention_kv | 📌 Description @humansand Implement configurable `tie_break` for filtered topk. This automatically achieves determinism. 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-com... | kernel: `csrc/flashinfer_topk_binding.cu`, `csrc/topk.cu`, `flashinfer/__init__.py`, `flashinfer/topk.py`<br>benchmark: `benchmarks/bench_topk.py`<br>test: `tests/utils/test_topk.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2644](https://github.com/flashinfer-ai/flashinfer/pull/2644) feat: FP32 dtype output for BF16 matmuls (CUTLASS & cuDNN) | 2026-03-18 | benchmark_test, gemm_quant, attention_kv, compiler_runtime | 📌 Description Adds support for FP32 dtype output for `mm_bf16` and `bmm_bf16` for the CUTLASS and cuDNN backends. I'm not familiar enough with the TGV kernel to know if / how to support it for that backend. 🔍 Related Issues https://github.com/flashinfer-ai/flashinfer/issues/2624 🚀 Pull Request Check... | kernel: `csrc/bf16_gemm_cutlass.cu`, `flashinfer/gemm/gemm_base.py`, `flashinfer/jit/gemm/core.py`<br>benchmark: `benchmarks/routines/gemm.py`<br>test: `tests/gemm/test_bmm_bf16.py`, `tests/gemm/test_mm_bf16.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#3203](https://github.com/flashinfer-ai/flashinfer/pull/3203) Include TinyGEMM into BF16 autotuner | 2026-05-05 | benchmark_test, gemm_quant, scheduler_autotune, attention_kv | Adds TinyGEMM as a supported `mm_bf16` backend and includes it in `backend="auto"` candidate selection when BF16 output is requested. This lets `mm_bf16(..., backend="auto", pdl=True)` consider TinyGEMM instead of excluding it from the BF16 autotuned path. TinyGEMM remains restricted to BF16 output,... | kernel: `flashinfer/gemm/gemm_base.py`<br>benchmark: `benchmarks/routines/gemm.py`<br>test: `tests/gemm/test_mm_bf16.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2362](https://github.com/flashinfer-ai/flashinfer/pull/2362) benchmarks: Add norm and quantization routines to microbenchmark harness. | 2026-01-16 | benchmark_test, gemm_quant, norm_elementwise | 📌 Description **No changes to library code** Extends the `flashinfer_benchmark.py` microbenchmark harness to support normalization and quantization routines. This enables performance benchmarking for RMSNorm variants (including fused residual-add and FP8/FP4 quantization) and standalone quantization... | benchmark: `benchmarks/README.md`, `benchmarks/flashinfer_benchmark.py`, `benchmarks/routines/flashinfer_benchmark_utils.py`, `benchmarks/routines/norm.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2591](https://github.com/flashinfer-ai/flashinfer/pull/2591) Mamba SSU: better automatic kernel selection + algorithm selection optionally exposed to the user. | 2026-02-22 | benchmark_test, compiler_runtime | 📌 Description This PR does several things: - Improves automatic kernel selection based on the arch, state_dtype, and the batch size (see image below). - Slightly improves performance at small batch sizes by launching several CTAs per tile. - Adds jinja templates for the `selective_state_update` func... | kernel: `csrc/flashinfer_mamba_binding.cu`, `csrc/selective_state_update.cu`, `csrc/selective_state_update_kernel_inst.cu`, `flashinfer/aot.py`<br>benchmark: `benchmarks/routines/mamba.py`<br>test: `tests/mamba/test_selective_state_update_mtp.py`, `tests/mamba/test_selective_state_update_stp.py`, `tests/mamba/triton_reference/__init__.py`, `tests/mamba/triton_reference/selective_state_update.py`<br>other: `csrc/selective_state_update_customize_config.jinja`, `csrc/selective_state_update_dtype_inst.jinja` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2215](https://github.com/flashinfer-ai/flashinfer/pull/2215) feat: further optimize top-k and add fused top-k page construction kernels for DSA | 2025-12-19 | benchmark_test, attention_kv, moe_routing | 📌 Description Follow up of 2119 , this PR implements `top_k_page_table_transform` and `top_k_ragged_transform` function required for dsa in sglang. (fp16/bf16/fp32 are supported), as requested in 2221 . This PR also adds the more top-k algorithm to choose from: 1. filtered top-k (used in tilelang an... | kernel: `csrc/flashinfer_topk_binding.cu`, `csrc/topk.cu`, `flashinfer/__init__.py`, `flashinfer/topk.py`<br>benchmark: `benchmarks/bench_topk.py`<br>test: `tests/utils/test_topk.py`<br>docs: `docs/api/sampling.rst`, `docs/api/topk.rst`, `docs/index.rst` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2942](https://github.com/flashinfer-ai/flashinfer/pull/2942) [Perf] Refactor MoE autotuning to set valid topk ids in routed MoE tuning | 2026-04-07 | benchmark_test, moe_routing, scheduler_autotune | 📌 Description This PR fixes various issues in Trtllm MoE autotuning: - Currently trtllm MoE uses torch.empty-initialized topk ids for routed MoE autotuning, this results in misrepresented MoE expert distribution, and leads to suboptimal tuned kernel performance. This PR initializes topk ids to be wi... | kernel: `flashinfer/autotuner.py`, `flashinfer/fused_moe/core.py`<br>benchmark: `benchmarks/bench_trtllm_gen_fused_moe_autotuner.py`<br>test: `tests/autotuner/test_trtllm_fused_moe_autotuner_integration.py`<br>other: `flashinfer-jit-cache/pyproject.toml` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2661](https://github.com/flashinfer-ai/flashinfer/pull/2661) feat: implement deterministic topk | 2026-04-01 | benchmark_test, moe_routing, attention_kv | 📌 Description > Part of the FilteredTopK implementation refers to or is adapted from @Linda-Stadter's work in 2759 Deterministic Mode for Top-K Kernels FilteredTopK Kernel FilteredTopKKernel implements deterministic mode as follows: 1. Build a coarse histogram. - Build a coarse histogram on the top... | kernel: `csrc/flashinfer_topk_binding.cu`, `csrc/topk.cu`, `flashinfer/topk.py`, `include/flashinfer/topk.cuh`<br>benchmark: `benchmarks/bench_topk.py`<br>test: `tests/utils/test_topk.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2962](https://github.com/flashinfer-ai/flashinfer/pull/2962) Improved `simple` mamba SSU kernel  | 2026-05-13 | benchmark_test, compiler_runtime | 📌 Description This PR upgrades the SSU MTP "simple" kernel with **cp.async state prefetching**, vectorized loads, and a consolidated state write path, delivering drastic performance improvements — particularly in the latency spectrum. The `async_horizontal` kernel was a temporary development vehicle... | kernel: `flashinfer/aot.py`, `flashinfer/jit/mamba/selective_state_update.py`, `flashinfer/mamba/selective_state_update.py`, `include/flashinfer/mamba/common.cuh`<br>benchmark: `benchmarks/bench_ssu_sweep_mtp.py`, `benchmarks/bench_ssu_sweep_sol.py`<br>test: `tests/mamba/test_selective_state_update_mtp.py`, `tests/mamba/test_selective_state_update_varlen.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2865](https://github.com/flashinfer-ai/flashinfer/pull/2865) Mamba SSU: horizontal MTP kernel (+ DSTATE=96 support) | 2026-04-04 | benchmark_test | 📌 Description This PR adds a high-performance **horizontal MTP kernel** for `selective_state_update` and extends DSTATE support to **non-power-of-2 values** (e.g. DSTATE=96). It also includes the full history of the vertical MTP kernel, int16 block-scaled state, stochastic rounding, and test infrast... | kernel: `flashinfer/jit/core.py`, `flashinfer/jit/mamba/__init__.py`, `flashinfer/jit/mamba/selective_state_update.py`, `flashinfer/mamba/selective_state_update.py`<br>benchmark: `benchmarks/bench_ssu_sweep_mtp.py`<br>test: `tests/mamba/test_selective_state_update_mtp.py`, `tests/mamba/test_selective_state_update_stp.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#3151](https://github.com/flashinfer-ai/flashinfer/pull/3151) perf: Add no-bias path for tinygemm_bf16 | 2026-04-24 | benchmark_test, gemm_quant, attention_kv | 📌 Description This PR updates tinygemm_bf16 to use a dedicated no-bias kernel path when `bias=None`, **avoiding the previous zero-bias materialization in the Python wrapper**. It also adds shared benchmark-harness support and benchmark smoke coverage for `tinygemm_bf16`, while keeping the branch sco... | kernel: `csrc/tinygemm2.cu`, `flashinfer/gemm/routergemm.py`<br>benchmark: `benchmarks/routines/flashinfer_benchmark_utils.py`, `benchmarks/routines/gemm.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2752](https://github.com/flashinfer-ai/flashinfer/pull/2752) [feat] Add air top-p algorithm | 2026-03-16 | benchmark_test, attention_kv, norm_elementwise | 📌 Description The existing FlashInfer top-p renormalization uses a ternary-search-based algorithm that requires ~30 iterative passes over the full vocabulary, with each pass reading from global memory. It also only uses one thread block per batch row, severely limiting parallelism. As shown in the b... | kernel: `csrc/flashinfer_sampling_binding.cu`, `csrc/renorm.cu`, `flashinfer/logits_processor/operators.py`, `flashinfer/sampling.py`<br>test: `tests/utils/test_logits_processor.py`, `tests/utils/test_sampling.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2696](https://github.com/flashinfer-ai/flashinfer/pull/2696) [benchmark] Add All Reduce benchmark | 2026-03-10 | benchmark_test, memory_primitives | 📌 Description Add all reduce benchmark to benchmark trtllm and trtllm_mnnvl_ar perf **Example** code block 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Checks -... | benchmark: `benchmarks/README.md`, `benchmarks/flashinfer_benchmark.py`, `benchmarks/routines/allreduce_comm.py`, `benchmarks/routines/flashinfer_benchmark_utils.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#3301](https://github.com/flashinfer-ai/flashinfer/pull/3301) bench(moe_deepseek): scope autotune(True) to pre-warm only | 2026-05-12 | benchmark_test, moe_routing, scheduler_autotune | 📌 Description Scopes `autotune(True)` to wrap only the per-backend pre-warm pass instead of the entire benchmark loop, so the measurement iterations themselves run with `is_tuning_mode=False`. Verified on B200 in NGC rc14 across all four measurement modes (graphs × CUPTI, on × off) at `--num-tokens... | benchmark: `benchmarks/bench_moe_deepseek.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2479](https://github.com/flashinfer-ai/flashinfer/pull/2479) fix: Fix memory bandwidth calculation in MLA benchmarks | 2026-02-04 | benchmark_test, attention_kv | 📌 Description * Fixed incorrect memory bandwidth calculation in `testBatchMLAPagedAttentionWrapper` that was using full tensor allocations instead of actual bytes accessed based on sequence lengths * Updated `bench_trtllm_gen_mla.py` to use the unified `bench_gpu_time()` utility with CUPTI for consi... | benchmark: `benchmarks/bench_trtllm_gen_mla.py`, `benchmarks/routines/attention.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2341](https://github.com/flashinfer-ai/flashinfer/pull/2341) chore: MoE benchmark effective BW fix for trtllm_block_scale_moe | 2026-01-14 | benchmark_test, moe_routing | 📌 Description The MoE benchmark script overestimates the num bytes loaded by assuming all experts are active. I saw effective BW exceeds 3x the peak BW of some system as a result. The fix is to calculate the routed experts (topk_ids) on the host side and count the unique number of experts, the same... | benchmark: `benchmarks/routines/moe.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2461](https://github.com/flashinfer-ai/flashinfer/pull/2461) add sgl_kernel.fast_topk_v2 to top_k benchmark | 2026-02-03 | benchmark_test, moe_routing | 📌 Description add sgl_kernel.fast_topk_v2 to top_k benchmark 🔍 Related Issues 🚀 Pull Request Checklist Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete. ✅ Pre-commit Checks - [x] I have installed `pre-commit` by running `... | benchmark: `benchmarks/bench_topk.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#3292](https://github.com/flashinfer-ai/flashinfer/pull/3292) bench(moe_deepseek): fix moe benchmark (supersedes #2886) | 2026-05-11 | benchmark_test, moe_routing | 📌 Description This refreshes `benchmarks/bench_moe_deepseek.py` so it runs cleanly on current main. It rebases Yunzhe Qiu's bench rewrite (`5677a080` from 2886, which restructures the bench so autotune runs inside the `bench_gpu_time` measurement region) onto post-3252 main, plus a small follow-up t... | benchmark: `benchmarks/bench_moe_deepseek.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2512](https://github.com/flashinfer-ai/flashinfer/pull/2512) benchmarks: Add microbenchmark support for Mamba selective_state_update | 2026-02-16 | benchmark_test | 📌 Description * Adds benchmarking support for the Mamba selective_state_update kernel to `flashinfer_benchmark.py`, covering both single-token prediction (STP) and multi-token prediction (MTP) modes. * Supports two backends: `flashinfer` (architecture-specific CUDA kernels for base/SM90/SM100+) and... | benchmark: `benchmarks/README.md`, `benchmarks/flashinfer_benchmark.py`, `benchmarks/routines/flashinfer_benchmark_utils.py`, `benchmarks/routines/mamba.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#923](https://github.com/flashinfer-ai/flashinfer/pull/923) feat: improve sampling algorithm robustness | 2025-03-09 | benchmark_test, attention_kv | 1. add a parameter `indices` to sampling APIs to allow multiple samples from the same probability distribution. 2. address the abnormal case of sum(probs) < u, where our earlier implementation might return an impossible value. 3. add unittest for comparing the sampled result distribution (with a lot... | kernel: `csrc/flashinfer_ops.cu`, `csrc/flashinfer_sampling_ops.cu`, `csrc/sampling.cu`, `flashinfer/sampling.py`<br>test: `tests/test_sampling.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#3184](https://github.com/flashinfer-ai/flashinfer/pull/3184) Fix trace-bmm-fp8 test: B should be K-major for subword types | 2026-05-01 | benchmark_test, gemm_quant | 📌 Description Closes 3188 Issue: Upstream change has introduced a failing CI test case: `tests/trace/test_reference_correctness.py::test_bmm_fp8_reference_correctness` Cause: `flashinfer.bmm_bf16`, `flashinfer.bmm_fp8` (any sub-32 dtypes) expect K-major inputs. The `cutlass` backend checks for this... | test: `tests/trace/test_reference_correctness.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#3215](https://github.com/flashinfer-ai/flashinfer/pull/3215) Fix bf16 cudnn override-shape test call signature | 2026-05-05 | benchmark_test, gemm_quant | 📌 Description The build_/execute_ functions gained `bias_is_not_none` and `bias` parameters, but `test_bf16_override_shape_dynamic_m` wasn't updated — pass `False`/`None` so it exercises the no-bias path. 🔍 Related Issues Should fix tests/gemm/test_cudnn_override_shape.py which is failing when testi... | test: `tests/gemm/test_cudnn_override_shape.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2058](https://github.com/flashinfer-ai/flashinfer/pull/2058) perf: Optimize helper max/minmax function in sampling.cuh | 2025-11-07 | benchmark_test, attention_kv | 📌 Description Apply optimizations similar to 2044 to max/min functions. `top_p_renorm_probs`, `top_k_renorm_probs`, and `top_k_mask_logits` see 14%, 7%, and 19% speedups on B200 (logs from `bench_sampling.py`: bench_sampling_before.txt and bench_sampling_after.txt ) 🔍 Related Issues 2044 🚀 Pull Requ... | kernel: `include/flashinfer/sampling.cuh` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2042](https://github.com/flashinfer-ai/flashinfer/pull/2042) test: Fix test_sampling.py on Spark | 2025-11-06 | benchmark_test | 📌 Description Current PR fixes `test_sampling.py::test_softmax` on Spark by inserting a `torch.cuda.synchronize()` before calling the softmax function. tl; dr why it works: PDL is enabled in these tests. Investigation shows that when PDL is enabled, `logits.view(-1).index_fill_(0, inf_idx, float("-i... | test: `tests/utils/test_sampling.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |
| [#2377](https://github.com/flashinfer-ai/flashinfer/pull/2377) chore: add script to run unittests/benchmarks on Modal GPU runners | 2026-01-20 | benchmark_test | 📌 Description Modal provides serverless GPU (B200/H100/etc) execution environments for running functions remotely. This PR implements the script to run flashinfer unittests/benchmarks on Modal, it's helpful for development environment without GPUs (agent/CI/etc). Example Usage code block 🚀 Pull Requ... | wrapper: `scripts/modal_runner.py` | Mine shape sets, tolerance rules, warmup logic, and profile commands before mutating code. |

## Per-PR Ledger Fields

When using an idea from this page, add one row to `artifacts/source-idea-ledger.md` with:

| Field | Value to record |
| --- | --- |
| Source key | `<repo>#<pr-number>` |
| Code evidence | Kernel, wrapper, benchmark, and test paths opened from the PR diff |
| Hypothesis | The concrete optimization idea derived from the PR |
| First experiment | Candidate version and benchmark shape set |
| Result | Correctness, geomean, best/worst cases, and NCU digest path |
| Do-not-reread key | Same as source key unless a single PR yields multiple independent ideas |

## How To Use This Page

- During the initial knowledge pass, read the category matching the target kernel and copy PR URL, changed paths, and hypothesis into the source idea ledger.
- During plateau expansion, choose PRs not already present in ledger do-not-reread keys; inspect the diff, linked issue, changed tests, and benchmark files before using the idea.
- Treat PR code as baseline/prior art unless the task and license allow copying or adapting it. When copied, record exact PR, commit, files, notice, and first delta.

diff --git a/flashinfer/xqa.py b/flashinfer/xqa.py
index 3a62592af1..fc34ef6c0b 100644
--- a/flashinfer/xqa.py
+++ b/flashinfer/xqa.py
@@ -25,6 +25,7 @@
 from .utils import (
     register_custom_op,
     register_fake_op,
+    get_compute_capability,
 )
 
 xqa_nvcc_flags = [
@@ -185,6 +186,8 @@ def xqa(
     semaphores: torch.Tensor,
     scratch: torch.Tensor,
 ) -> None:
+    if get_compute_capability(torch.device(device="cuda"))[0] != 9:
+        raise RuntimeError("XQA is only supported on SM90 GPUs")
     xqa_module = get_xqa_module(
         use_fp16, token_per_page, head_size, head_grp_size, use_sliding_window
     )
diff --git a/tests/test_xqa.py b/tests/test_xqa.py
index cc83a41227..b8fec2d31c 100644
--- a/tests/test_xqa.py
+++ b/tests/test_xqa.py
@@ -5,6 +5,7 @@
 import torch
 
 from flashinfer import xqa
+from flashinfer.utils import get_compute_capability
 
 
 def set_random_seed(seed=42):
@@ -147,6 +148,10 @@ def ref_attention(
     return out
 
 
+@pytest.mark.skipif(
+    get_compute_capability(torch.device(device="cuda"))[0] != 9,
+    reason="XQA is only supported on SM90 GPUs",
+)
 @pytest.mark.parametrize("use_sliding_window", [True, False])
 @pytest.mark.parametrize("use_fp16", [True, False])
 @pytest.mark.parametrize("use_attention_sinks", [True, False])

diff --git a/.clang-tidy b/.clang-tidy
index db0c1b1918f..e4c12c91f43 100644
--- a/.clang-tidy
+++ b/.clang-tidy
@@ -28,6 +28,7 @@ Checks:
   - '-modernize-use-nodiscard'
   - '-modernize-return-braced-init-list'
   - '-modernize-use-auto'
+  - 'clang-diagnostic-*'
   # END REMOVE ME
   # TODO(jfaibussowit):
   #
diff --git a/cmake/run_clang_tidy.sh.in b/cmake/run_clang_tidy.sh.in
index 3a28c83365b..d5b167d2bec 100755
--- a/cmake/run_clang_tidy.sh.in
+++ b/cmake/run_clang_tidy.sh.in
@@ -1,11 +1,13 @@
 #!/usr/bin/env bash
-set -xeou pipefail
+set -eou pipefail
 
 clang_tidy_args=()
 if test -n "${CCCL_CLANG_TIDY_ARGS:+x}"; then
     mapfile -t clang_tidy_args < <(echo "${CCCL_CLANG_TIDY_ARGS}" | tr ' ' '\n' | sort -u)
 fi
 
+set -x
+
 "@CCCL_CLANG_TIDY@" \
   --use-color \
   --quiet \
diff --git a/cub/benchmarks/bench/scan/exclusive/sum.warpspeed.cu b/cub/benchmarks/bench/scan/exclusive/sum.warpspeed.cu
index 3cd70b5af62..071878eb7c6 100644
--- a/cub/benchmarks/bench/scan/exclusive/sum.warpspeed.cu
+++ b/cub/benchmarks/bench/scan/exclusive/sum.warpspeed.cu
@@ -12,7 +12,10 @@
 #else // _CCCL_PP_COUNT(__CUDA_ARCH_LIST__) != 1
 
 #  if __CUDA_ARCH_LIST__ < 1000
-#    warning "Warpspeed scan requires at least sm_100. Disabling it."
+// We don't care if clang-tidy can't parse this
+#    ifndef _CCCL_CLANG_TIDY_INVOKED
+#      warning "Warpspeed scan requires at least sm_100. Disabling it."
+#    endif // !defined _CCCL_CLANG_TIDY_INVOKED
 #  else // __CUDA_ARCH_LIST__ < 1000
 
 #    if __cccl_ptx_isa < 860
